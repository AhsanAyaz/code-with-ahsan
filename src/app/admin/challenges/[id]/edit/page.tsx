import ChallengeEditScreen from "@/components/admin/ChallengeEditScreen";

/**
 * Admin Page to edit an existing challenge.
 * Relies on a client component (ChallengeEditScreen) to fetch data
 * and manage the edit state.
 */
export default function EditChallengePage() {
  return <ChallengeEditScreen />;
}
