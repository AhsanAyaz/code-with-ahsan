---
phase: 05-dashboard-leaderboard-offboarding-alumni
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/lib/ambassador/leaderboard.ts
  - src/lib/ambassador/leaderboard.test.ts
  - scripts/ambassador-leaderboard-snapshot.ts
  - .github/workflows/ambassador-activity-checks.yml
autonomous: true
requirements:
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "buildLeaderboardSnapshot(cohortId) returns top-3 lists per category and a per-uid ranks map for both cumulative and this-month windows"
    - "scripts/ambassador-leaderboard-snapshot.ts writes one doc per cohort to leaderboard_snapshots/{cohortId} on every run"
    - "Cron is idempotent: running twice in a row produces identical snapshot content (other than updatedAt timestamp)"
    - "Grace period math is correct — graceEndDate equals cohort.startDate + 28 days"
    - "this-month window uses UTC month boundary (recommendation from Open Question 1)"
    - "cron has --dry-run flag that skips Firestore writes but still prints planned writes"
  artifacts:
    - path: "src/lib/ambassador/leaderboard.ts"
      provides: "buildLeaderboardSnapshot full implementation; pure helpers for sort/rank"
      contains: "buildLeaderboardSnapshot"
    - path: "src/lib/ambassador/leaderboard.test.ts"
      provides: "DASH-05 top-3 shape test, DASH-06 grace math test, ranking determinism test"
      contains: "describe"
    - path: "scripts/ambassador-leaderboard-snapshot.ts"
      provides: "Hourly cron writer for leaderboard_snapshots/{cohortId}"
      contains: "main"
    - path: ".github/workflows/ambassador-activity-checks.yml"
      provides: "ambassador-leaderboard-snapshot job + hourly schedule + workflow_dispatch option"
      contains: "ambassador-leaderboard-snapshot"
  key_links:
    - from: "scripts/ambassador-leaderboard-snapshot.ts"
      to: "leaderboard_snapshots/{cohortId} Firestore doc"
      via: "db.collection(LEADERBOARD_SNAPSHOTS_COLLECTION).doc(cohortId).set(snapshot)"
      pattern: "LEADERBOARD_SNAPSHOTS_COLLECTION"
    - from: ".github/workflows/ambassador-activity-checks.yml ambassador-leaderboard-snapshot job"
      to: "scripts/ambassador-leaderboard-snapshot.ts"
      via: "npx tsx scripts/ambassador-leaderboard-snapshot.ts"
      pattern: "ambassador-leaderboard-snapshot"
    - from: "src/lib/ambassador/leaderboard.ts"
      to: "src/app/api/ambassador/dashboard/leaderboard/route.ts (Plan 03)"
      via: "shared LeaderboardSnapshot type contract"
      pattern: "LeaderboardSnapshot"
---

<objective>
Implement the hourly leaderboard snapshot pipeline: full `buildLeaderboardSnapshot` builder + the cron script that writes the snapshot doc + the GitHub Actions job that runs it hourly.

Purpose: DASH-07 requires hourly snapshots; DASH-03/04/05/06 require the data shape this builder produces. Plan 03 (dashboard API) reads what this plan writes — the cron and API must agree on collection name and doc shape.

Output: A cron-runnable script that produces deterministic, top-3-only-per-category snapshot docs in `leaderboard_snapshots/{cohortId}` for the current cohort, and a unit test suite that locks the contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-RESEARCH.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-PATTERNS.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-VALIDATION.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-01-SUMMARY.md

<interfaces>
<!-- Locked by Plan 01: -->

From src/lib/ambassador/leaderboard.ts (Plan 01):
```typescript
export interface LeaderboardEntry { uid: string; displayName: string; photoURL: string; count: number; }
export interface LeaderboardCategoryRanks { referralsRank: number; eventsRank: number; reportsRank: number; }
export interface LeaderboardWindow {
  referrals: LeaderboardEntry[];
  events: LeaderboardEntry[];
  reportsOnTime: LeaderboardEntry[];
  ambassadorRanks: Record<string, LeaderboardCategoryRanks>;
}
export interface LeaderboardSnapshot {
  cohortId: string;
  updatedAt: string;
  graceEndDate: string;
  cumulative: LeaderboardWindow;
  thisMonth: LeaderboardWindow & { month: string };
}
```

From src/lib/ambassador/constants.ts (Plan 01):
```typescript
export const LEADERBOARD_SNAPSHOTS_COLLECTION = "leaderboard_snapshots" as const;
export const LEADERBOARD_GRACE_PERIOD_MS = 28 * 24 * 60 * 60 * 1000;
```

From src/types/ambassador.ts (existing):
```typescript
export const PUBLIC_AMBASSADORS_COLLECTION = "public_ambassadors";
export const REFERRALS_COLLECTION = "referrals" as const;
export const AMBASSADOR_EVENTS_COLLECTION = "ambassador_events" as const;
export const MONTHLY_REPORTS_COLLECTION = "monthly_reports" as const;
export const AMBASSADOR_COHORTS_COLLECTION = "cohorts" as const;
// (Note: REFERRALS / EVENTS / REPORTS / COHORTS collections are exported from
//  src/lib/ambassador/constants.ts; PUBLIC_AMBASSADORS is exported from src/types/ambassador.ts.)
```

From src/lib/ambassador/currentCohort.ts (existing):
```typescript
export async function getCurrentCohortId(): Promise<string | null>;
```

From an existing analog cron — scripts/ambassador-report-flag.ts header pattern (lines 1-34):
```typescript
#!/usr/bin/env npx tsx
import { config } from "dotenv";
config({ path: ".env.local" });
import { db } from "../src/lib/firebaseAdmin";
import { FieldValue } from "firebase-admin/firestore";
const DRY_RUN = process.argv.includes("--dry-run");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement buildLeaderboardSnapshot + lock the contract with unit tests (DASH-03/04/05/06)</name>
  <files>src/lib/ambassador/leaderboard.ts, src/lib/ambassador/leaderboard.test.ts</files>
  <read_first>
    - src/lib/ambassador/leaderboard.ts (the stub from Plan 01 — replace the body of `buildLeaderboardSnapshot`, keep the existing exported interface contracts unchanged)
    - src/lib/ambassador/acceptance.ts (module structure to mirror — exports first, helpers below)
    - src/types/ambassador.ts §PublicAmbassadorDoc (lines 188-209)
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-RESEARCH.md §"Pattern 2: Leaderboard Snapshot Document", §"Pitfall 7: CollectionGroup query for leaderboard aggregation", and §"Code Examples"
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-VALIDATION.md §"Wave 0 Requirements" — this task creates the leaderboard.test.ts file
    - scripts/ambassador-report-flag.ts (full file — to copy header + import path style for cron)
  </read_first>
  <behavior>
    - `buildLeaderboardSnapshot(cohortId)` enumerates active ambassadors via `public_ambassadors` (active==true) — Pitfall 7 — NOT via collectionGroup
    - For each ambassador, computes three counts (referrals, non-hidden events, monthly reports) for the cumulative window AND for the this-month window (UTC YYYY-MM)
    - Sorts each category descending; emits ONLY the top-3 in `referrals`/`events`/`reportsOnTime` arrays
    - Builds `ambassadorRanks: Record<uid, { referralsRank, eventsRank, reportsRank }>` covering ALL active ambassadors (1-indexed; ties get the same rank, next rank skipped — standard competition ranking)
    - `graceEndDate` = `cohort.startDate.toDate() + LEADERBOARD_GRACE_PERIOD_MS`, ISO string (Pitfall 2 — must call `.toDate()` first)
    - `thisMonth.month` is the current UTC month formatted as `YYYY-MM`
    - Test 1 (DASH-05): a snapshot built with 5 mock ambassadors returns ARRAYS of length ≤ 3 in `referrals`/`events`/`reportsOnTime`, and `ambassadorRanks` has an entry for ALL 5 uids
    - Test 2 (DASH-05): when only 1 ambassador has a positive count, the top-3 array has length 1 (no zero-count padding)
    - Test 3 (DASH-06): given cohort.startDate = "2026-01-01T00:00:00Z", `graceEndDate` is exactly "2026-01-29T00:00:00.000Z" (ISO)
    - Test 4 (DASH-04): cumulative and thisMonth are computed independently — same fixture with thisMonth referrals filtered to only the current UTC month
    - Test 5 (ranking determinism): two ambassadors tied at 5 referrals both get rank 1; the next gets rank 3
  </behavior>
  <action>
    Replace the body of `src/lib/ambassador/leaderboard.ts` (keep the interface declarations from Plan 01 unchanged). Add the full implementation:

    ```typescript
    import { db } from "@/lib/firebaseAdmin";
    import {
      AMBASSADOR_EVENTS_COLLECTION,
      MONTHLY_REPORTS_COLLECTION,
      REFERRALS_COLLECTION,
    } from "@/lib/ambassador/constants";
    import { LEADERBOARD_GRACE_PERIOD_MS } from "@/lib/ambassador/constants";
    import {
      AMBASSADOR_COHORTS_COLLECTION,
      PUBLIC_AMBASSADORS_COLLECTION,
      type PublicAmbassadorDoc,
    } from "@/types/ambassador";

    /**
     * Standard competition ranking ("1224"):
     *   counts [10, 8, 8, 5] → ranks [1, 2, 2, 4].
     * Pure function (no Firestore) — testable in isolation.
     */
    export function rankByCount<T extends { uid: string; count: number }>(
      entries: T[],
    ): Map<string, number> {
      const sorted = [...entries].sort((a, b) => b.count - a.count);
      const ranks = new Map<string, number>();
      let lastCount: number | null = null;
      let lastRank = 0;
      sorted.forEach((entry, idx) => {
        const position = idx + 1;
        if (lastCount !== null && entry.count === lastCount) {
          ranks.set(entry.uid, lastRank);
        } else {
          ranks.set(entry.uid, position);
          lastRank = position;
          lastCount = entry.count;
        }
      });
      return ranks;
    }

    /** Returns "YYYY-MM" for the current UTC month — used by the this-month window. */
    export function currentUtcMonth(now: Date = new Date()): string {
      const y = now.getUTCFullYear();
      const m = String(now.getUTCMonth() + 1).padStart(2, "0");
      return `${y}-${m}`;
    }

    /** Returns the UTC start-of-month Date for a "YYYY-MM" string. */
    export function utcMonthStart(month: string): Date {
      const [y, m] = month.split("-").map(Number);
      return new Date(Date.UTC(y, m - 1, 1, 0, 0, 0, 0));
    }

    export interface AmbassadorCounts {
      uid: string;
      displayName: string;
      photoURL: string;
      cumulative: { referrals: number; events: number; reports: number };
      thisMonth: { referrals: number; events: number; reports: number };
    }

    /**
     * Compute counts for one ambassador. Public for cron testability;
     * NOT exported from the route handlers.
     */
    export async function computeAmbassadorCounts(
      uid: string,
      monthStart: Date,
      displayName: string,
      photoURL: string,
    ): Promise<AmbassadorCounts> {
      const [
        cumReferrals,
        cumEvents,
        cumReports,
        thisMonthReferrals,
        thisMonthEvents,
        thisMonthReports,
      ] = await Promise.all([
        db.collection(REFERRALS_COLLECTION).where("ambassadorId", "==", uid).count().get(),
        db
          .collection(AMBASSADOR_EVENTS_COLLECTION)
          .where("ambassadorId", "==", uid)
          .where("hidden", "==", false)
          .count()
          .get(),
        db.collection(MONTHLY_REPORTS_COLLECTION).where("ambassadorId", "==", uid).count().get(),
        db
          .collection(REFERRALS_COLLECTION)
          .where("ambassadorId", "==", uid)
          .where("convertedAt", ">=", monthStart)
          .count()
          .get(),
        db
          .collection(AMBASSADOR_EVENTS_COLLECTION)
          .where("ambassadorId", "==", uid)
          .where("hidden", "==", false)
          .where("date", ">=", monthStart)
          .count()
          .get(),
        db
          .collection(MONTHLY_REPORTS_COLLECTION)
          .where("ambassadorId", "==", uid)
          .where("submittedAt", ">=", monthStart)
          .count()
          .get(),
      ]);
      return {
        uid,
        displayName,
        photoURL,
        cumulative: {
          referrals: cumReferrals.data().count ?? 0,
          events: cumEvents.data().count ?? 0,
          reports: cumReports.data().count ?? 0,
        },
        thisMonth: {
          referrals: thisMonthReferrals.data().count ?? 0,
          events: thisMonthEvents.data().count ?? 0,
          reports: thisMonthReports.data().count ?? 0,
        },
      };
    }

    /** Build top-3 entries + complete rank map for one category. */
    function buildWindowCategory(
      entries: Array<{ uid: string; displayName: string; photoURL: string; count: number }>,
    ): { top3: LeaderboardEntry[]; ranks: Map<string, number> } {
      const ranks = rankByCount(entries);
      const top3 = [...entries]
        .sort((a, b) => b.count - a.count)
        .filter((e) => e.count > 0)
        .slice(0, 3);
      return { top3, ranks };
    }

    /**
     * Build the leaderboard snapshot for a cohort.
     *
     * Reads:
     *   - cohorts/{cohortId} (for startDate)
     *   - public_ambassadors where active==true && cohortId==cohortId (Pitfall 7)
     *   - per-ambassador: referrals + events + monthly_reports counts (cumulative + this-month)
     *
     * Returns a fully-shaped LeaderboardSnapshot — NOT written to Firestore here.
     * The cron script invokes this and persists the result.
     */
    export async function buildLeaderboardSnapshot(
      cohortId: string,
    ): Promise<LeaderboardSnapshot> {
      const cohortSnap = await db
        .collection(AMBASSADOR_COHORTS_COLLECTION)
        .doc(cohortId)
        .get();
      if (!cohortSnap.exists) {
        throw new Error(`Cohort not found: ${cohortId}`);
      }
      const cohort = cohortSnap.data() as { startDate?: FirebaseFirestore.Timestamp };
      if (!cohort.startDate) {
        throw new Error(`Cohort ${cohortId} missing startDate`);
      }
      const startMs = cohort.startDate.toDate().getTime();
      const graceEndDate = new Date(startMs + LEADERBOARD_GRACE_PERIOD_MS).toISOString();

      const month = currentUtcMonth();
      const monthStart = utcMonthStart(month);

      // Pitfall 7: enumerate via public_ambassadors (flat collection), not collectionGroup
      const ambassadorsSnap = await db
        .collection(PUBLIC_AMBASSADORS_COLLECTION)
        .where("active", "==", true)
        .where("cohortId", "==", cohortId)
        .get();

      const ambassadors = ambassadorsSnap.docs.map((d) => d.data() as PublicAmbassadorDoc);

      const counts = await Promise.all(
        ambassadors.map((a) =>
          computeAmbassadorCounts(a.uid, monthStart, a.displayName, a.photoURL),
        ),
      );

      const cumReferrals = counts.map((c) => ({ uid: c.uid, displayName: c.displayName, photoURL: c.photoURL, count: c.cumulative.referrals }));
      const cumEvents = counts.map((c) => ({ uid: c.uid, displayName: c.displayName, photoURL: c.photoURL, count: c.cumulative.events }));
      const cumReports = counts.map((c) => ({ uid: c.uid, displayName: c.displayName, photoURL: c.photoURL, count: c.cumulative.reports }));

      const tmReferrals = counts.map((c) => ({ uid: c.uid, displayName: c.displayName, photoURL: c.photoURL, count: c.thisMonth.referrals }));
      const tmEvents = counts.map((c) => ({ uid: c.uid, displayName: c.displayName, photoURL: c.photoURL, count: c.thisMonth.events }));
      const tmReports = counts.map((c) => ({ uid: c.uid, displayName: c.displayName, photoURL: c.photoURL, count: c.thisMonth.reports }));

      const cumRefBuilt = buildWindowCategory(cumReferrals);
      const cumEvtBuilt = buildWindowCategory(cumEvents);
      const cumRptBuilt = buildWindowCategory(cumReports);
      const tmRefBuilt = buildWindowCategory(tmReferrals);
      const tmEvtBuilt = buildWindowCategory(tmEvents);
      const tmRptBuilt = buildWindowCategory(tmReports);

      const cumulativeRanks: Record<string, LeaderboardCategoryRanks> = {};
      const thisMonthRanks: Record<string, LeaderboardCategoryRanks> = {};
      for (const a of ambassadors) {
        cumulativeRanks[a.uid] = {
          referralsRank: cumRefBuilt.ranks.get(a.uid) ?? 0,
          eventsRank: cumEvtBuilt.ranks.get(a.uid) ?? 0,
          reportsRank: cumRptBuilt.ranks.get(a.uid) ?? 0,
        };
        thisMonthRanks[a.uid] = {
          referralsRank: tmRefBuilt.ranks.get(a.uid) ?? 0,
          eventsRank: tmEvtBuilt.ranks.get(a.uid) ?? 0,
          reportsRank: tmRptBuilt.ranks.get(a.uid) ?? 0,
        };
      }

      return {
        cohortId,
        updatedAt: new Date().toISOString(),
        graceEndDate,
        cumulative: {
          referrals: cumRefBuilt.top3,
          events: cumEvtBuilt.top3,
          reportsOnTime: cumRptBuilt.top3,
          ambassadorRanks: cumulativeRanks,
        },
        thisMonth: {
          month,
          referrals: tmRefBuilt.top3,
          events: tmEvtBuilt.top3,
          reportsOnTime: tmRptBuilt.top3,
          ambassadorRanks: thisMonthRanks,
        },
      };
    }
    ```

    Then create `src/lib/ambassador/leaderboard.test.ts` with the following pure-function tests (no Firestore mocking needed for the helpers):

    ```typescript
    import { describe, it, expect } from "vitest";
    import {
      rankByCount,
      currentUtcMonth,
      utcMonthStart,
      LEADERBOARD_GRACE_PERIOD_MS as _GRACE,
    } from "./leaderboard";
    import { LEADERBOARD_GRACE_PERIOD_MS } from "./constants";

    describe("rankByCount (DASH-05 ranking determinism)", () => {
      it("assigns standard competition ranks (1224)", () => {
        const ranks = rankByCount([
          { uid: "a", count: 10 },
          { uid: "b", count: 8 },
          { uid: "c", count: 8 },
          { uid: "d", count: 5 },
        ]);
        expect(ranks.get("a")).toBe(1);
        expect(ranks.get("b")).toBe(2);
        expect(ranks.get("c")).toBe(2);
        expect(ranks.get("d")).toBe(4);
      });

      it("handles all-zero counts", () => {
        const ranks = rankByCount([
          { uid: "a", count: 0 },
          { uid: "b", count: 0 },
        ]);
        expect(ranks.get("a")).toBe(1);
        expect(ranks.get("b")).toBe(1);
      });

      it("returns empty map for empty input", () => {
        expect(rankByCount([]).size).toBe(0);
      });
    });

    describe("DASH-06 grace period math", () => {
      it("LEADERBOARD_GRACE_PERIOD_MS equals exactly 28 days in milliseconds", () => {
        expect(LEADERBOARD_GRACE_PERIOD_MS).toBe(28 * 24 * 60 * 60 * 1000);
      });

      it("startDate + grace = expected ISO", () => {
        const start = new Date("2026-01-01T00:00:00.000Z");
        const graceEnd = new Date(start.getTime() + LEADERBOARD_GRACE_PERIOD_MS);
        expect(graceEnd.toISOString()).toBe("2026-01-29T00:00:00.000Z");
      });
    });

    describe("currentUtcMonth / utcMonthStart (DASH-04 month boundary)", () => {
      it("currentUtcMonth returns YYYY-MM in UTC", () => {
        const result = currentUtcMonth(new Date("2026-03-15T12:34:56.000Z"));
        expect(result).toBe("2026-03");
      });

      it("utcMonthStart returns first ms of month at UTC", () => {
        const start = utcMonthStart("2026-03");
        expect(start.toISOString()).toBe("2026-03-01T00:00:00.000Z");
      });
    });
    ```

    Note: do NOT add a unit test that calls `buildLeaderboardSnapshot` directly — it requires a live Firestore mock. The helper functions above lock the contract; integration validation happens via the cron's manual `--dry-run` invocation in Task 2.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/ambassador/leaderboard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function buildLeaderboardSnapshot" src/lib/ambassador/leaderboard.ts` exits 0
    - `grep -q "export function rankByCount" src/lib/ambassador/leaderboard.ts` exits 0
    - `grep -q "export function currentUtcMonth" src/lib/ambassador/leaderboard.ts` exits 0
    - `grep -q "PUBLIC_AMBASSADORS_COLLECTION" src/lib/ambassador/leaderboard.ts` exits 0 (Pitfall 7 enumeration source)
    - `grep -vq "collectionGroup" src/lib/ambassador/leaderboard.ts` (must NOT use collectionGroup)
    - `grep -q "LEADERBOARD_GRACE_PERIOD_MS" src/lib/ambassador/leaderboard.ts` exits 0
    - `test -f src/lib/ambassador/leaderboard.test.ts` succeeds
    - `npm test -- --run src/lib/ambassador/leaderboard` passes (rankByCount 1224 ordering, grace math, currentUtcMonth)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>buildLeaderboardSnapshot fully implemented; pure helpers (rankByCount, currentUtcMonth, utcMonthStart) testable; tests cover DASH-05 ranking, DASH-06 grace math, DASH-04 month boundary. No collectionGroup query (Pitfall 7).</done>
</task>

<task type="auto">
  <name>Task 2: Cron writer + GitHub Actions wiring (DASH-07)</name>
  <files>scripts/ambassador-leaderboard-snapshot.ts, .github/workflows/ambassador-activity-checks.yml</files>
  <read_first>
    - scripts/ambassador-report-flag.ts (full file — header + dotenv pattern at lines 1-34, DRY_RUN at line 35, main() footer pattern at lines 147-160)
    - scripts/ambassador-discord-reconciliation.ts (full file — alternative analog; same structure)
    - .github/workflows/ambassador-activity-checks.yml (full file — extending it, NOT creating a new workflow per RESEARCH §"Pattern 8")
    - src/lib/ambassador/leaderboard.ts (Task 1 — buildLeaderboardSnapshot signature)
    - src/lib/ambassador/currentCohort.ts (getCurrentCohortId — handles null when no cohort active)
  </read_first>
  <action>
    1. Create `scripts/ambassador-leaderboard-snapshot.ts`:

    ```typescript
    #!/usr/bin/env npx tsx
    /**
     * Phase 5 (DASH-07): Hourly leaderboard snapshot writer.
     *
     * Triggered by .github/workflows/ambassador-activity-checks.yml
     * (cron '0 * * * *' or workflow_dispatch).
     *
     * Reads:  cohorts (current), public_ambassadors (active==true), referrals,
     *         ambassador_events (hidden==false), monthly_reports
     * Writes: leaderboard_snapshots/{cohortId} (single doc, set/merge=false)
     *
     * Idempotent: re-running produces the same content (other than updatedAt).
     * Dry-run flag (--dry-run) skips the Firestore write but still computes + logs.
     */
    import { config } from "dotenv";
    config({ path: ".env.local" });

    import { db } from "../src/lib/firebaseAdmin";
    import { FieldValue } from "firebase-admin/firestore";
    import { LEADERBOARD_SNAPSHOTS_COLLECTION } from "../src/lib/ambassador/constants";
    import { buildLeaderboardSnapshot } from "../src/lib/ambassador/leaderboard";
    import { getCurrentCohortId } from "../src/lib/ambassador/currentCohort";

    const DRY_RUN = process.argv.includes("--dry-run");

    async function main() {
      console.log(
        `[ambassador-leaderboard-snapshot] starting at ${new Date().toISOString()} (dry-run=${DRY_RUN})`,
      );

      const cohortId = await getCurrentCohortId();
      if (!cohortId) {
        console.log(
          "[ambassador-leaderboard-snapshot] no current cohort — nothing to snapshot. Exiting cleanly.",
        );
        return;
      }
      console.log(`[ambassador-leaderboard-snapshot] cohortId=${cohortId}`);

      const snapshot = await buildLeaderboardSnapshot(cohortId);

      console.log(
        `[ambassador-leaderboard-snapshot] built — ` +
          `cumulative ranks: ${Object.keys(snapshot.cumulative.ambassadorRanks).length} ambassadors, ` +
          `top3 referrals: ${snapshot.cumulative.referrals.length}, ` +
          `top3 events: ${snapshot.cumulative.events.length}, ` +
          `top3 reportsOnTime: ${snapshot.cumulative.reportsOnTime.length}, ` +
          `month=${snapshot.thisMonth.month}, graceEnd=${snapshot.graceEndDate}`,
      );

      if (DRY_RUN) {
        console.log("[ambassador-leaderboard-snapshot] DRY-RUN — no Firestore write.");
        return;
      }

      // Replace serializable updatedAt with FieldValue.serverTimestamp on write.
      // (The buildLeaderboardSnapshot return ships a string; we overwrite for storage.)
      const docRef = db.collection(LEADERBOARD_SNAPSHOTS_COLLECTION).doc(cohortId);
      await docRef.set(
        {
          ...snapshot,
          updatedAt: FieldValue.serverTimestamp(),
        },
        { merge: false },
      );

      console.log(
        `[ambassador-leaderboard-snapshot] wrote ${LEADERBOARD_SNAPSHOTS_COLLECTION}/${cohortId} successfully.`,
      );
    }

    main().catch((e) => {
      console.error("[ambassador-leaderboard-snapshot] FATAL", e);
      process.exit(1);
    });
    ```

    2. Edit `.github/workflows/ambassador-activity-checks.yml`. Three modifications:

    a) On line 6 (within the existing `schedule:` block) ADD a third schedule entry — keep ALL three lines:

    ```yaml
        # Daily at 08:00 UTC — missing-report flag + REPORT-05 DM reminders
        - cron: '0 8 * * *'
        # Weekly Monday at 09:00 UTC — Discord Ambassador role reconciliation (DISC-04)
        - cron: '0 9 * * 1'
        # Phase 5 DASH-07 — Hourly leaderboard snapshot
        - cron: '0 * * * *'
    ```

    b) In the `workflow_dispatch.inputs.job.options` block (around line 17), ADD `- leaderboard-snapshot` to the list (preserve existing options):

    ```yaml
            options:
              - report-flag
              - discord-reconciliation
              - both
              - leaderboard-snapshot
    ```

    c) After the existing `ambassador-discord-reconciliation:` job (after line 84 — the script invocation block ending the file), APPEND:

    ```yaml

      ambassador-leaderboard-snapshot:
        name: Hourly leaderboard snapshot (DASH-07)
        runs-on: ubuntu-latest
        if: |
          (github.event_name == 'schedule' && github.event.schedule == '0 * * * *') ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'leaderboard-snapshot')
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '22.x'
              cache: 'npm'
          - run: npm ci
          - name: Run leaderboard-snapshot cron
            env:
              NODE_ENV: production
              FIREBASE_SERVICE_ACCOUNT_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
              NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
            run: |
              if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
                npx tsx scripts/ambassador-leaderboard-snapshot.ts --dry-run
              else
                npx tsx scripts/ambassador-leaderboard-snapshot.ts
              fi
    ```

    Note: the leaderboard cron does NOT need the Discord secrets — only Firebase. Match the env block to ONLY those two secrets that the script actually uses.
  </action>
  <verify>
    <automated>test -f scripts/ambassador-leaderboard-snapshot.ts && grep -q "ambassador-leaderboard-snapshot:" .github/workflows/ambassador-activity-checks.yml && grep -q "0 \* \* \* \*" .github/workflows/ambassador-activity-checks.yml && grep -q "leaderboard-snapshot" .github/workflows/ambassador-activity-checks.yml && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/ambassador-leaderboard-snapshot.ts` succeeds (file exists; shebang present)
    - `head -1 scripts/ambassador-leaderboard-snapshot.ts` outputs `#!/usr/bin/env npx tsx`
    - `grep -q "import { config } from \"dotenv\"" scripts/ambassador-leaderboard-snapshot.ts` exits 0
    - `grep -q "config({ path: \".env.local\" })" scripts/ambassador-leaderboard-snapshot.ts` exits 0
    - `grep -q "DRY_RUN = process.argv.includes" scripts/ambassador-leaderboard-snapshot.ts` exits 0
    - `grep -q "buildLeaderboardSnapshot(cohortId)" scripts/ambassador-leaderboard-snapshot.ts` exits 0
    - `grep -q "FieldValue.serverTimestamp()" scripts/ambassador-leaderboard-snapshot.ts` exits 0
    - `grep -q "ambassador-leaderboard-snapshot:" .github/workflows/ambassador-activity-checks.yml` exits 0
    - `grep -q "cron: '0 \* \* \* \*'" .github/workflows/ambassador-activity-checks.yml` exits 0
    - `grep -q "leaderboard-snapshot" .github/workflows/ambassador-activity-checks.yml` matches at least 3 times (option, cron job name, dispatch matcher)
    - Existing `ambassador-report-flag` and `ambassador-discord-reconciliation` jobs are still present (no regression)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Cron script created with dotenv + DRY_RUN + buildLeaderboardSnapshot + Firestore write; GitHub Actions workflow extended with hourly schedule, new workflow_dispatch option, and new job; existing two jobs untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Firestore Admin SDK | Service account credential in repo secrets; Admin SDK bypasses rules |
| Cron output (snapshot doc) → Public ambassador read paths | Public ambassadors collection is publicly readable; leaderboard snapshot is NOT (admin-only via Firestore rules — gated through API) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | `leaderboard_snapshots/{cohortId}` doc contains `ambassadorRanks` map (every ambassador's exact rank) | mitigate | Firestore rules MUST deny client reads of this collection (admin/server-only). API extracts only `ambassadorRanks[ctx.uid]` per Pitfall 7. (Firestore rules update is in Plan 03's threat model — flagged here for cross-plan handoff.) |
| T-05-02-02 | Tampering | Cron writes wrong cohort's snapshot if `getCurrentCohortId()` returns stale value | accept | `getCurrentCohortId()` is the single source of truth used by all phase-2/3/4 routes; staleness is bounded to 1h (next cron run); low impact (leaderboard is hourly anyway) |
| T-05-02-03 | Denial of Service | Cron does N+1 reads × 3 windows × 6 categories | mitigate | `Promise.all` parallelizes the per-ambassador reads; budget ≤25 ambassadors × 6 count() queries = 150 reads/hour. Firestore count() aggregation is cheap (1 doc-read price each). Acceptable. |
| T-05-02-04 | Elevation of Privilege | Cron credential leak from repo secrets | accept | Standard GitHub Actions secret handling; no code changes here. Firebase service account key already used by `ambassador-report-flag` and `ambassador-discord-reconciliation` (operational footprint identical) |
| T-05-02-05 | Repudiation | No record of past snapshots — only current | accept | Snapshot doc is overwritten hourly; historical leaderboard is out of scope for v1 (UI only shows current). If audit needs arise, an append-only log can be added later. |
</threat_model>

<verification>
- `npm test -- --run src/lib/ambassador/leaderboard` green (helper tests pass)
- `npx tsc --noEmit` green for both src/ and scripts/
- Cron script's relative imports resolve at tsx runtime (`../src/lib/firebaseAdmin`, etc.)
- Workflow YAML still parses (`actionlint` if available; otherwise visual inspection that all three jobs and three schedule entries are present)
- Manual smoke test (post-deploy, not part of executor's automated verify): trigger `workflow_dispatch` with `dry_run: true` and confirm logs include the "DRY-RUN — no Firestore write" message
</verification>

<success_criteria>
- buildLeaderboardSnapshot returns a fully-shaped LeaderboardSnapshot from live Firestore reads
- Top-3 arrays never exceed 3 entries; zero-count entries are excluded
- ambassadorRanks map covers every active ambassador in the cohort
- Grace end date is start + 28 days, accurate to the millisecond
- Cron writes one doc per cohort to `leaderboard_snapshots/{cohortId}` with FieldValue.serverTimestamp for updatedAt
- Cron is idempotent (rerunning overwrites cleanly via merge:false)
- Hourly schedule + workflow_dispatch trigger both wired in GH Actions
- Existing two cron jobs remain functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-02-SUMMARY.md`
</output>
