---
phase: 05-dashboard-leaderboard-offboarding-alumni
plan: 05
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
  - "05-04"
files_modified:
  - src/app/ambassadors/dashboard/page.tsx
  - src/app/ambassadors/dashboard/DashboardClient.tsx
  - src/app/ambassadors/dashboard/PersonalStatsPanel.tsx
  - src/app/ambassadors/dashboard/OnboardingChecklist.tsx
  - src/app/ambassadors/dashboard/LeaderboardPanel.tsx
  - src/app/ambassadors/dashboard/AmbassadorOfMonthBanner.tsx
  - src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx
  - src/app/admin/ambassadors/members/[uid]/AlumniTransitionButton.tsx
  - src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-08
  - DASH-09
  - ALUMNI-03

must_haves:
  truths:
    - "Authenticated ambassador visiting /ambassadors/dashboard sees their name, cohort, personal stats (referrals/events/reports/strikes/next-report-due), onboarding checklist (5 items), leaderboard panel (or grace banner), and Ambassador-of-the-Month banner if set"
    - "Non-ambassador visiting /ambassadors/dashboard is redirected to /profile via client-side useEffect role-gate (DASH-01)"
    - "Onboarding checklist self-mark items (joinedDiscord, sharedReferralLink) PATCH /api/ambassador/profile and update locally without refetch; auto-derived items (setBio, uploadedVideo, loggedFirstEvent) reflect API response"
    - "Leaderboard panel shows top-3 per category + own rank row; during grace period it shows the alert-info banner from UI-SPEC instead of a table; supports cumulative/this_month tab toggle"
    - "Admin clicking 'Offboard ambassador' opens OffboardConfirmModal (replaces window.confirm); on confirm POSTs to /api/ambassador/members/[uid]/offboard, shows toast on success, persistent inline error banner with retry button on Discord failure"
    - "Admin AlumniTransitionButton renders only when cohort.endDate has passed; on confirm POSTs /api/ambassador/members/[uid]/alumni and refreshes member detail"
  artifacts:
    - path: "src/app/ambassadors/dashboard/page.tsx"
      provides: "Server shell for the dashboard (force-dynamic)"
      contains: "export const dynamic = \"force-dynamic\""
    - path: "src/app/ambassadors/dashboard/DashboardClient.tsx"
      provides: "Client root: role-gate redirect, dashboard fetch, child component composition"
      contains: "useMentorship"
    - path: "src/app/ambassadors/dashboard/PersonalStatsPanel.tsx"
      provides: "DaisyUI stats panel with referrals/events/reports/strikes/next-report-due (DASH-02)"
      contains: "stats stats-vertical lg:stats-horizontal"
    - path: "src/app/ambassadors/dashboard/OnboardingChecklist.tsx"
      provides: "5-item onboarding checklist (DASH-08)"
      contains: "role=\"list\""
    - path: "src/app/ambassadors/dashboard/LeaderboardPanel.tsx"
      provides: "Tab-toggle leaderboard with grace banner branch (DASH-03..06)"
      contains: "tabs tabs-boxed"
    - path: "src/app/ambassadors/dashboard/AmbassadorOfMonthBanner.tsx"
      provides: "Conditional banner (DASH-09)"
      contains: "Ambassador of the Month"
    - path: "src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx"
      provides: "Replaces window.confirm in MemberDetailClient with accessible modal"
      contains: "modal modal-open"
    - path: "src/app/admin/ambassadors/members/[uid]/AlumniTransitionButton.tsx"
      provides: "Term-completion alumni button gated by cohort.endDate"
      contains: "Mark as Alumni"
    - path: "src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx"
      provides: "Wires OffboardConfirmModal + AlumniTransitionButton + Discord retry banner"
      contains: "OffboardConfirmModal"
  key_links:
    - from: "src/app/ambassadors/dashboard/DashboardClient.tsx"
      to: "/api/ambassador/dashboard/me (Plan 03)"
      via: "authFetch in useEffect"
      pattern: "authFetch.*ambassador/dashboard/me"
    - from: "src/app/ambassadors/dashboard/LeaderboardPanel.tsx"
      to: "/api/ambassador/dashboard/leaderboard?view=cumulative|this_month (Plan 03)"
      via: "authFetch on mount + tab change"
      pattern: "authFetch.*ambassador/dashboard/leaderboard"
    - from: "src/app/ambassadors/dashboard/OnboardingChecklist.tsx"
      to: "/api/ambassador/profile (existing PATCH endpoint)"
      via: "authFetch PATCH for joinedDiscord + sharedReferralLink"
      pattern: "authFetch.*ambassador/profile"
    - from: "src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx"
      to: "/api/ambassador/members/[uid]/offboard + /alumni (Plan 04)"
      via: "fetch with x-admin-token"
      pattern: "members/\\$\\{uid\\}/(offboard|alumni)"
---

<objective>
Build the Phase 5 UI surfaces: ambassador dashboard at /ambassadors/dashboard (server shell + client root + 5 child components) and the admin lifecycle UI extensions on /admin/ambassadors/members/[uid] (OffboardConfirmModal, AlumniTransitionButton, Discord-retry banner). All components consume Plan 03/04 endpoints and follow the verbatim copy + accessibility rules in 05-UI-SPEC.md.

Purpose: Surface the data and lifecycle actions that Plans 02-04 build. Without this plan the dashboard endpoint and offboard/alumni endpoints exist but no human can reach them, and the existing `window.confirm` offboard path remains (Phase 4 flagged this as a deprecated pattern).

Output:
- Ambassador dashboard at /ambassadors/dashboard (page.tsx + 5 components)
- Admin OffboardConfirmModal + AlumniTransitionButton wired into MemberDetailClient
- Discord-removal retry banner persisted inline on offboard failure
- Final UI checkpoint to confirm visual + interaction quality before phase ships
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-RESEARCH.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-PATTERNS.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-UI-SPEC.md

# Existing components / patterns the executor MUST mirror exactly:
@src/app/ambassadors/report/page.tsx
@src/app/ambassadors/report/ReportPageClient.tsx
@src/app/admin/ambassadors/members/[uid]/ActivitySummaryPanel.tsx
@src/app/admin/ambassadors/members/[uid]/CronFlagsPanel.tsx
@src/app/admin/ambassadors/members/[uid]/StrikeConfirmModal.tsx
@src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx
@src/app/admin/ambassadors/[applicationId]/DiscordBanner.tsx

# Endpoint contracts produced by upstream plans:
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-03-PLAN.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-04-PLAN.md

<interfaces>
<!-- Endpoint response shapes the executor must consume verbatim. -->
<!-- These come from Plans 03 and 04 (same wave 2, depends_on satisfied before this plan runs). -->

GET /api/ambassador/dashboard/me (Plan 03):
```typescript
type DashboardMeResponse = {
  profile: { uid: string; displayName: string; email: string };
  stats: {
    referralsCount: number;
    eventsCount: number;
    reportsCount: number;
    reportsOnTime: number;
    strikes: number;
    nextReportDue: string | null; // ISO date or null
  };
  cohort: {
    cohortId: string;
    name: string;
    startDate: string; // ISO
    endDate: string;   // ISO
    ambassadorOfTheMonth: { uid: string; displayName: string } | null;
  } | null;
  onboarding: {
    joinedDiscord: boolean;       // persisted on subdoc.onboarding
    setBio: boolean;              // auto-derived (Pitfall 6)
    uploadedVideo: boolean;       // auto-derived
    sharedReferralLink: boolean;  // persisted on subdoc.onboarding
    loggedFirstEvent: boolean;    // auto-derived
  };
};
```

GET /api/ambassador/dashboard/leaderboard?view=cumulative|this_month (Plan 03):
```typescript
type LeaderboardResponse = {
  view: "cumulative" | "this_month";
  cohortId: string | null;       // null when no active cohort
  graceActive: boolean;
  graceEndDate: string | null;   // ISO
  month: string | null;          // YYYY-MM (this_month view only)
  top3: {
    referrals: Array<{ uid: string; displayName: string; photoURL: string; count: number; rank: number }>;
    events:    Array<{ uid: string; displayName: string; photoURL: string; count: number; rank: number }>;
    reportsOnTime: Array<{ uid: string; displayName: string; photoURL: string; count: number; rank: number }>;
  };
  ownRank: { referrals: number | null; events: number | null; reportsOnTime: number | null };
  updatedAt: string | null;      // ISO
} | { snapshot: null };          // when cron has not yet run
```

POST /api/ambassador/members/[uid]/offboard (Plan 04):
```typescript
// Request body: none. Auth: x-admin-token header.
type OffboardResponse =
  | { success: true; discordRemoved: boolean; emailSent: boolean }
  | { error: string }; // 401, 403, 404, 409 (already inactive), 500
```

POST /api/ambassador/members/[uid]/alumni (Plan 04):
```typescript
// Request body: none. Auth: x-admin-token header.
type AlumniResponse =
  | { success: true }
  | { error: string }; // 401, 403, 404, 409 (already inactive)
```

PATCH /api/ambassador/profile (existing — see Phase 1):
```typescript
// Body: Partial<{ onboarding: { joinedDiscord?: boolean; sharedReferralLink?: boolean } }>
// Auth: ambassador role claim required.
// Response: { success: true } or { error: string }.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build /ambassadors/dashboard page + DashboardClient + 5 child components</name>

  <files>
    src/app/ambassadors/dashboard/page.tsx,
    src/app/ambassadors/dashboard/DashboardClient.tsx,
    src/app/ambassadors/dashboard/PersonalStatsPanel.tsx,
    src/app/ambassadors/dashboard/OnboardingChecklist.tsx,
    src/app/ambassadors/dashboard/LeaderboardPanel.tsx,
    src/app/ambassadors/dashboard/AmbassadorOfMonthBanner.tsx
  </files>

  <read_first>
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-UI-SPEC.md (entire file — copy strings, color rules, hierarchy, accessibility)
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-PATTERNS.md sections for `page.tsx`, `DashboardClient.tsx`, `PersonalStatsPanel.tsx`, `LeaderboardPanel.tsx`, `AmbassadorOfMonthBanner.tsx`, `OnboardingChecklist.tsx` ("No Analog Found")
    - src/app/ambassadors/report/page.tsx (server shell — exact analog for `dynamic = "force-dynamic"`)
    - src/app/ambassadors/report/ReportPageClient.tsx (cancel-safe useEffect fetch, useMentorship + role gate)
    - src/app/admin/ambassadors/members/[uid]/ActivitySummaryPanel.tsx (DaisyUI `stats stats-vertical lg:stats-horizontal shadow` + `text-error` when strikes ≥ 2)
    - src/app/admin/ambassadors/members/[uid]/CronFlagsPanel.tsx (list-with-actions skeleton — closest structural match for the checklist)
    - src/lib/permissions.ts (`hasRole` for client gate)
    - src/contexts/MentorshipContext (provides `useMentorship` returning { profile, isLoading })
    - src/lib/apiClient.ts (`authFetch`)
    - src/contexts/ToastContext (`useToast`)
  </read_first>

  <behavior>
    - DashboardClient renders the page heading + subheading from UI-SPEC, then in order: AmbassadorOfMonthBanner → OnboardingChecklist → PersonalStatsPanel → LeaderboardPanel.
    - When `useMentorship()` resolves and `hasRole(profile, "ambassador")` is false, `router.replace("/profile")` fires; nothing else renders.
    - Dashboard `me` fetch is cancel-safe (sets `cancelled = true` on cleanup); on error, the page renders the UI-SPEC error alert string (`Could not load your dashboard. Check your connection and try again.`) and nothing else.
    - PersonalStatsPanel shows referrals/events/reportsOnTime/strikes/nextReportDue. Strikes ≥ 2 renders `text-error` value AND the `alert alert-error` warning string from UI-SPEC. `nextReportDue === null` renders `—`.
    - OnboardingChecklist renders 5 items in the order specified by UI-SPEC. `joinedDiscord` and `sharedReferralLink` items expose a `<button type="button">` ("Mark as done" / "Copy referral link") that PATCHes /api/ambassador/profile with the appropriate `onboarding.<flag>: true` and updates local state on success. The other three items render their CTA as a Next.js `<Link>` to `/profile` or `/ambassadors/report`. Completed items show `line-through text-base-content/50`. When all 5 are true, render the "You're all set!" copy in a single card.
    - LeaderboardPanel: when `graceActive === true`, render only the alert-info grace banner with weeks-remaining math (`Math.ceil((Date.parse(graceEndDate) - Date.now()) / (7*24*60*60*1000))`). Otherwise render the tabs + 3-column `<table>` (referrals | events | reportsOnTime) showing top-3, then a full-width `<tr>` with `bg-primary/10` for "Your rank: #N" per category. `aria-live="polite"` text shows `Updated N minutes ago`. Refresh button refetches.
    - AmbassadorOfMonthBanner returns `null` when `ambassadorOfTheMonth === null`. Otherwise renders the alert with primary border + displayName.
    - When `/api/ambassador/dashboard/leaderboard` returns `{ snapshot: null }`, render the UI-SPEC "Leaderboard data is being computed" copy in an `alert alert-info`.
  </behavior>

  <action>
1. Create `src/app/ambassadors/dashboard/page.tsx` — verbatim from PATTERNS.md "page.tsx" section. Server component. Imports `DashboardClient` from "./DashboardClient". Exports `const dynamic = "force-dynamic"`. Returns `<main className="page-padding mx-auto max-w-4xl space-y-8 py-8"><DashboardClient /></main>`.

2. Create `src/app/ambassadors/dashboard/DashboardClient.tsx`:
   - `"use client"`. Imports: `useEffect, useState` from react, `useRouter` from next/navigation, `authFetch` from `@/lib/apiClient`, `useMentorship` from `@/contexts/MentorshipContext`, `hasRole` from `@/lib/permissions`, plus the 5 child components from this same directory.
   - Define a local `DashboardData` type matching the `DashboardMeResponse` shape in <interfaces>.
   - `const { profile, isLoading: profileLoading } = useMentorship();` + `const router = useRouter();`.
   - useEffect role gate: `if (!profileLoading && profile && !hasRole(profile, "ambassador")) router.replace("/profile");` — deps `[profile, profileLoading, router]`.
   - useEffect data fetch: cancel-safe pattern from ReportPageClient. On non-OK response, set `error` state to the UI-SPEC string `"Could not load your dashboard. Check your connection and try again."`. Do NOT throw.
   - Loading state (no data, no error): render `<div className="py-12 text-center"><span className="loading loading-spinner loading-md" aria-label="Loading dashboard" /></div>`.
   - Error state: render `<div role="alert" className="alert alert-error"><span>{error}</span></div>`.
   - Success state — render in this order, all wrapped in `<div className="space-y-8">`:
     1. `<header><h1 className="text-2xl font-bold">Ambassador Dashboard</h1><p className="text-base-content/70">Your personal impact hub. Stats update hourly.</p></header>`
     2. `<AmbassadorOfMonthBanner ambassadorOfTheMonth={data.cohort?.ambassadorOfTheMonth ?? null} />`
     3. `<OnboardingChecklist initial={data.onboarding} />`
     4. `<PersonalStatsPanel {...data.stats} />`
     5. `<LeaderboardPanel cohortId={data.cohort?.cohortId ?? null} />`

3. Create `src/app/ambassadors/dashboard/PersonalStatsPanel.tsx`:
   - `"use client"`. Pure presentational component.
   - Props: `{ referralsCount: number; eventsCount: number; reportsOnTime: number; strikes: number; nextReportDue: string | null }` (the spec calls the panel "reportsCount" but the actual UI shows "Reports On Time" — bind to `reportsOnTime` from the response, not raw `reportsCount`).
   - Render exactly per UI-SPEC §Personal Stats Panel: section heading "Your Impact This Cohort" inside a `card bg-base-100 shadow-xl`. The DaisyUI `stats stats-vertical lg:stats-horizontal shadow` block with the 5 stats. `nextReportDue` value: if null render `—`, else `new Date(nextReportDue).toLocaleDateString()`. Strikes value uses `stat-value text-error` when `strikes >= 2`. Below the stats block, when `strikes >= 2`, render `<div role="alert" className="alert alert-error mt-4"><span>You have 2 confirmed strikes. Reach out to the program admin if you have questions.</span></div>`.

4. Create `src/app/ambassadors/dashboard/OnboardingChecklist.tsx`:
   - `"use client"`. Imports: `useState` from react, `Link` from next/link, `authFetch` from `@/lib/apiClient`, `useToast` from `@/contexts/ToastContext`.
   - Props: `{ initial: { joinedDiscord: boolean; setBio: boolean; uploadedVideo: boolean; sharedReferralLink: boolean; loggedFirstEvent: boolean } }`.
   - Maintain local state `flags` initialized to `initial`. After mount, optimistic flips on success, rollback + toast.error on failure.
   - Define an item config array with the 5 items in UI-SPEC order. For each item: `{ id, label, completed, action }`. Two distinct action shapes:
     - `selfMark` for `joinedDiscord` and `sharedReferralLink` — renders a `<button type="button" className="btn btn-sm btn-primary">` with the UI-SPEC CTA copy. On click: optimistically flip flag, PATCH `/api/ambassador/profile` with body `{ onboarding: { [id]: true } }`. For `sharedReferralLink`, the click handler ALSO copies the user's referral link to the clipboard via `navigator.clipboard.writeText(referralUrl)` BEFORE the PATCH (acceptable to read referralCode from `document.cookie`-free source — pull it from a new optional `referralCode` prop and pass through from DashboardClient via `data.profile`/extend the dashboard endpoint contract if needed; if not available, just open `/profile` via Link).
     - `link` for `setBio`, `uploadedVideo`, `loggedFirstEvent` — renders `<Link>` with the UI-SPEC CTA copy and `href="/profile"` or `href="/ambassadors/report"` per UI-SPEC.
   - Wrapper card: `<section className="card bg-base-100 shadow-xl"><div className="card-body">…</div></section>`.
   - Card body: `<h2 className="card-title">Getting Started</h2><p className="text-sm text-base-content/70">Complete these steps to hit the ground running.</p>` followed by `<ul role="list" className="space-y-2">` with each item as `<li className="flex items-start gap-3">`. Show a checkmark icon (or `<span aria-hidden="true">✔</span>`) when `completed`, and apply `line-through text-base-content/50` to the label `<span>`. CTA only renders when `!completed`.
   - When all 5 flags are true, swap the entire `<ul>` for a single `<div className="alert alert-success" role="status"><span>You're all set! Keep up the great work.</span></div>`.
   - Use `useToast()` for failure feedback: `toast.error("Could not save your progress. Please try again.")`.

5. Create `src/app/ambassadors/dashboard/LeaderboardPanel.tsx`:
   - `"use client"`. Imports: `useCallback, useEffect, useState` from react, `authFetch` from `@/lib/apiClient`.
   - Props: `{ cohortId: string | null }`.
   - Local state: `view: "cumulative" | "this_month"`, `data: LeaderboardResponse | null`, `loading: boolean`, `error: string | null`.
   - `fetchSnapshot(viewArg)`: cancel-safe `authFetch(\`/api/ambassador/dashboard/leaderboard?view=${viewArg}\`)`. On non-OK, set `error` to UI-SPEC error string. Trigger initial fetch in useEffect when `cohortId` changes; when no cohortId, render the no-cohort empty state alert.
   - Branch 1 (no `cohortId`): single alert-info with the UI-SPEC empty state copy.
   - Branch 2 (`data === null` while loading): `<span className="loading loading-spinner loading-md" />`.
   - Branch 3 (`data === { snapshot: null }`): `alert alert-info` with `"Leaderboard data is being computed. Check back in a few minutes."`.
   - Branch 4 (`graceActive === true`): heading + `<div role="alert" className="alert alert-info">` with `Leaderboard unlocks in {weeksRemaining} week(s). Build momentum — your activity is already being tracked.` where `weeksRemaining = Math.max(1, Math.ceil((Date.parse(graceEndDate) - Date.now()) / (7*24*60*60*1000)))`.
   - Branch 5 (active leaderboard):
     - `<div className="flex items-center justify-between gap-4 flex-wrap">` with title `Cohort Leaderboard` and the tab toggle (`<div role="tablist" className="tabs tabs-boxed">`) for cumulative vs this_month per UI-SPEC copy. Switching tabs calls `fetchSnapshot(newView)`.
     - `<table className="table">` with thead `<tr><th>Referrals</th><th>Events Hosted</th><th>Reports On Time</th></tr>` and three `<tr>` rows for ranks 1-3 across all three categories (each cell shows `#{rank} {displayName} ({count})`; if a category has fewer than 3 entries, fill missing cells with em-dash `—`).
     - Below the table, render up to 3 `<tr>`s (one per category) ONLY IF `ownRank.<category> !== null`, each as a full-width row spanning all columns: `<tr className="bg-primary/10"><td colSpan={3} aria-label={\`Your rank in the \${category} category\`}>Your rank: #{ownRank}</td></tr>`. The own-rank rows can live inside a separate `<tfoot>` for semantics.
     - Below the table: `<p aria-live="polite" className="text-sm text-base-content/60">Updated {minutesAgo} minutes ago</p>` where `minutesAgo = Math.max(0, Math.round((Date.now() - Date.parse(updatedAt)) / 60000))`.
     - Refresh button: `<button type="button" className="btn btn-ghost btn-sm" onClick={() => fetchSnapshot(view)} disabled={loading}>{loading ? "Refreshing…" : "Refresh"}</button>`.
   - Error fallback: `<div role="alert" className="alert alert-error"><span>Could not load leaderboard. Check your connection and try again.</span></div>`.

6. Create `src/app/ambassadors/dashboard/AmbassadorOfMonthBanner.tsx`:
   - `"use client"`. Pure presentational.
   - Props: `{ ambassadorOfTheMonth: { uid: string; displayName: string } | null }`.
   - Return `null` when `ambassadorOfTheMonth === null` (per UI-SPEC: never render a blank banner).
   - Otherwise: `<div className="alert border border-primary"><span className="font-bold text-primary">Ambassador of the Month</span><span>{ambassadorOfTheMonth.displayName}</span></div>`.
  </action>

  <verify>
    <automated>
      cd /Users/amu1o5/personal/code-with-ahsan &&
      ls src/app/ambassadors/dashboard/page.tsx src/app/ambassadors/dashboard/DashboardClient.tsx src/app/ambassadors/dashboard/PersonalStatsPanel.tsx src/app/ambassadors/dashboard/OnboardingChecklist.tsx src/app/ambassadors/dashboard/LeaderboardPanel.tsx src/app/ambassadors/dashboard/AmbassadorOfMonthBanner.tsx &&
      grep -q "force-dynamic" src/app/ambassadors/dashboard/page.tsx &&
      grep -q "router.replace(\"/profile\")" src/app/ambassadors/dashboard/DashboardClient.tsx &&
      grep -q "stats stats-vertical lg:stats-horizontal" src/app/ambassadors/dashboard/PersonalStatsPanel.tsx &&
      grep -q "role=\"list\"" src/app/ambassadors/dashboard/OnboardingChecklist.tsx &&
      grep -q "tabs tabs-boxed" src/app/ambassadors/dashboard/LeaderboardPanel.tsx &&
      grep -q "Ambassador of the Month" src/app/ambassadors/dashboard/AmbassadorOfMonthBanner.tsx &&
      grep -q "if (!ambassadorOfTheMonth)" src/app/ambassadors/dashboard/AmbassadorOfMonthBanner.tsx &&
      grep -q "bg-primary/10" src/app/ambassadors/dashboard/LeaderboardPanel.tsx &&
      grep -q "aria-live=\"polite\"" src/app/ambassadors/dashboard/LeaderboardPanel.tsx &&
      npx tsc --noEmit
    </automated>
  </verify>

  <acceptance_criteria>
    - All 6 component files exist and pass `tsc --noEmit`.
    - `page.tsx` exports `dynamic = "force-dynamic"` (regex match required).
    - `DashboardClient.tsx` calls `router.replace("/profile")` inside a `useEffect` that depends on `profile`, `isLoading`, and `router` (no missing deps warning when running `npx eslint src/app/ambassadors/dashboard/DashboardClient.tsx`).
    - `OnboardingChecklist.tsx` does NOT use `<input type="checkbox">` for self-mark items — UI-SPEC §Accessibility mandates `<button type="button">`. Verify: `grep -E "<input[^>]*type=\"checkbox\"" src/app/ambassadors/dashboard/OnboardingChecklist.tsx | wc -l` returns `0`.
    - `LeaderboardPanel.tsx` does NOT receive or render the full `ambassadorRanks` map — it only consumes `top3` and `ownRank` from the response. Verify: `grep -E "ambassadorRanks" src/app/ambassadors/dashboard/LeaderboardPanel.tsx | wc -l` returns `0`.
    - `AmbassadorOfMonthBanner.tsx` returns `null` when prop is null. Verify: file contains the literal `if (!ambassadorOfTheMonth) return null;`.
    - `PersonalStatsPanel.tsx` applies `text-error` to the strikes stat-value only when `strikes >= 2` (regex match for the conditional className required).
    - The dashboard fetch in `DashboardClient.tsx` is cancel-safe — file contains both `let cancelled = false` and `cancelled = true` (cleanup).
    - All UI-SPEC verbatim copy strings present in their respective files (grep at minimum: "Your Impact This Cohort", "Getting Started", "Cohort Leaderboard", "Ambassador Dashboard", "Your personal impact hub").
  </acceptance_criteria>

  <done>
    - `npx tsc --noEmit` passes from project root.
    - All 6 dashboard files exist at the listed paths.
    - All grep-based acceptance criteria above pass.
    - `npx eslint src/app/ambassadors/dashboard/` reports 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire OffboardConfirmModal + AlumniTransitionButton + Discord retry banner into MemberDetailClient</name>

  <files>
    src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx,
    src/app/admin/ambassadors/members/[uid]/AlumniTransitionButton.tsx,
    src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx
  </files>

  <read_first>
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-UI-SPEC.md §"Offboarding Flow (Admin)", §"Alumni Transition (Admin)", §"Visual Hierarchy Notes - /admin/ambassadors/members/[uid]"
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-PATTERNS.md §"Modal Pattern (admin destructive actions)", §"Admin Token Header", §"Toast Feedback Pattern"
    - src/app/admin/ambassadors/members/[uid]/StrikeConfirmModal.tsx (full file — exact pattern for `<dialog className="modal modal-open">` + `aria-labelledby` + admin token header)
    - src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx (current `handleRemove` callback uses `window.confirm` at line 53 — this MUST be replaced)
    - src/app/admin/ambassadors/[applicationId]/DiscordBanner.tsx (alert-error retry banner pattern)
    - src/contexts/ToastContext (`useToast`)
    - src/components/admin/AdminAuthGate (`ADMIN_TOKEN_KEY`)
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-04-PLAN.md (offboard + alumni endpoint contracts)
  </read_first>

  <behavior>
    - Admin clicking the existing "Remove from program" button NO LONGER triggers `window.confirm` — it opens `OffboardConfirmModal` (UI-SPEC: `Offboard ambassador` is the new label, but keep the existing button position; rename the label string).
    - On modal confirm: POST `/api/ambassador/members/[uid]/offboard` with `x-admin-token` header. On `success: true` and `discordRemoved: true && emailSent: true`, close modal, show success toast `"{displayName} has been offboarded."`, and refresh member detail (the response leaves `subdoc.active === false`, so the activity panel reflects the new state).
    - When `success: true` but `discordRemoved: false`, modal closes, success toast still fires, AND a persistent `alert alert-error` banner renders inline below the page header (NOT a toast — admin must see it persistently per UI-SPEC §Interaction States) with copy `"Discord role removal failed — retry below. The Firestore record has already been updated."` and a `btn btn-outline btn-sm btn-error` "Retry Discord removal" button. The retry button POSTs `/api/ambassador/members/[uid]/offboard` again (the endpoint is idempotent — Plan 04 returns 409 on already-inactive, so the executor MUST handle that branch by treating 409 as "subdoc already inactive — only attempt Discord removal"; OR add a separate retry endpoint/path. SIMPLER: dedicate the retry to a stand-alone DELETE call to `/api/discord/role/{memberId}/{roleId}` if it exists; check the codebase. If no such endpoint exists, the retry button calls the same /offboard endpoint and surfaces the 409 + new `discordRemoved` status). Implementation MUST handle 409 gracefully — show toast "Discord role retried." on a successful retry whether the response was 200 or 409 with `discordRemoved: true`.
    - When `success: true && emailSent: false`, fire `toast.error("Offboarding email could not be sent. Follow up manually.")` (non-blocking).
    - Failure (non-2xx, non-409): show `toast.error("Could not complete offboarding. Check your connection and try again.")`. Modal stays open with submit re-enabled.
    - Already-inactive 409 from offboard: modal shows the inline-error from StrikeConfirmModal pattern with copy `"This ambassador is already inactive. Refresh and try again."` and disables the confirm button.
    - `AlumniTransitionButton` ONLY renders when `cohort.endDate` has passed (Date.parse(endDate) < Date.now()). When the cohort is still active, the entire button (and its inline confirmation prompt) is hidden.
    - On `AlumniTransitionButton` click: render an inline confirmation prompt (UI-SPEC: NOT a modal — less consequential than offboarding) with the UI-SPEC body copy `Mark {displayName} as Alumni Ambassador? This will swap their role and remove them from the active cohort listing.`, role="alert", and two buttons: "Yes, mark as alumni" + "Cancel". Confirm POSTs `/api/ambassador/members/[uid]/alumni`. Success → toast + reload. 409 → toast.error("This ambassador is already inactive."). Other failure → toast.error from UI-SPEC.
    - Section order on MemberDetailClient (UI-SPEC §Visual Hierarchy Notes): ActivitySummaryPanel → CronFlagsPanel → StrikePanel → divider → AlumniTransitionButton (or null) → Offboard button → Report History → Event History.
  </behavior>

  <action>
1. Determine the existing Discord role-removal endpoint surface. Run `grep -rn "discord/role" src/app/api/ 2>/dev/null` (or similar) to confirm whether a dedicated retry route exists. If NONE exists, the retry button calls `/api/ambassador/members/[uid]/offboard` again — Plan 04's endpoint must handle the already-inactive case by returning 409 with the body shape `{ error: "already inactive", discordRemoved: <bool>, emailSent: false }`. **Coordinate with Plan 04 if necessary** — surface the contract gap as a checkpoint/decision finding rather than silently changing scope. Otherwise route the retry to the existing endpoint.

2. Create `src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx`:
   - `"use client"`. Imports: `useState` from react, `useToast` from `@/contexts/ToastContext`, `ADMIN_TOKEN_KEY` from `@/components/admin/AdminAuthGate`.
   - Props: `{ uid: string; displayName: string; isOpen: boolean; onClose: () => void; onCompleted: (result: { discordRemoved: boolean; emailSent: boolean }) => void }`.
   - Local state: `submitting: boolean`, `inlineError: string | null`.
   - Render structure (verbatim from PATTERNS.md §Modal Pattern, with UI-SPEC copy):
     - `<dialog className="modal modal-open" aria-labelledby="offboard-confirm-heading">`
     - `<div className="modal-box">`
     - `<h3 id="offboard-confirm-heading" className="text-xl font-bold">Offboard {displayName}?</h3>`
     - `<p className="py-4">This will revoke {displayName}'s ambassador role, remove their Discord Ambassador role, mark their cohort membership as ended, and send an offboarding email. This action cannot be undone. They will not receive the Alumni Ambassador status.</p>`
     - When `inlineError`: `<div role="alert" className="alert alert-error"><span>{inlineError}</span></div>`.
     - `<div className="modal-action">` with two buttons:
       - `<button type="button" className="btn btn-ghost" onClick={onClose} disabled={submitting}>Go back</button>`
       - `<button type="button" className="btn btn-error" onClick={handleConfirm} disabled={submitting}>{submitting && <span className="loading loading-spinner loading-sm" />} Yes, offboard {displayName}</button>`
     - Form backdrop close (same pattern as StrikeConfirmModal).
   - `handleConfirm`:
     - `setSubmitting(true)`. POST `/api/ambassador/members/[uid]/offboard` with header `x-admin-token: <localStorage[ADMIN_TOKEN_KEY]>`.
     - On 409: `setInlineError("This ambassador is already inactive. Refresh and try again.")`.
     - On other non-2xx: `setInlineError("Offboarding failed. Try again.")`.
     - On 200: parse JSON `{ success, discordRemoved, emailSent }`, call `onCompleted({ discordRemoved, emailSent })`, then `onClose()`.
     - On `emailSent === false`: `toast.error("Offboarding email could not be sent. Follow up manually.")`.
     - finally → `setSubmitting(false)`.

3. Create `src/app/admin/ambassadors/members/[uid]/AlumniTransitionButton.tsx`:
   - `"use client"`. Imports: `useState` from react, `useToast`, `ADMIN_TOKEN_KEY`.
   - Props: `{ uid: string; displayName: string; cohortEndDate: string | null; subdocActive: boolean; onCompleted: () => void }`.
   - **Visibility gate**: if `cohortEndDate === null || Date.parse(cohortEndDate) > Date.now() || !subdocActive` → return `null`. UI-SPEC: button only renders when cohort.endDate has passed AND subdoc is still active.
   - Local state: `confirming: boolean` (toggles inline prompt), `submitting: boolean`.
   - Default render: `<button type="button" className="btn btn-outline btn-sm" onClick={() => setConfirming(true)}>Mark as Alumni</button>`.
   - When `confirming`:
     - `<div role="alert" className="alert mt-2">` containing the body copy + two buttons "Yes, mark as alumni" (`btn btn-primary btn-sm`) and "Cancel" (`btn btn-ghost btn-sm`).
   - On confirm: POST `/api/ambassador/members/[uid]/alumni` with `x-admin-token`. 200 → toast.success(`{displayName} is now an Alumni Ambassador.`) + onCompleted(). 409 → toast.error("This ambassador is already inactive."). Other → toast.error("Could not complete the alumni transition. Check your connection and try again.").

4. Modify `src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx`:
   - Add to the `MemberDetail` type: `cohort: { cohortId: string; endDate: string | null } | null` if not already present. The existing `subdoc.cohortId` is enough to look up but the endDate must come from the `/api/ambassador/members/[uid]` GET response — verify in Phase 2/3 SUMMARY whether that route returns `cohort.endDate`. If it does NOT, surface a small contract gap: extend the GET handler to include `cohort: { cohortId, endDate, name }` derived from `db.collection(AMBASSADOR_COHORTS_COLLECTION).doc(subdoc.cohortId).get()`. This change MUST be made in this same plan (lives within `files_modified` scope — adding to MemberDetailClient AND a one-line GET handler change is acceptable IF it's a backend-source plan or surface as a checkpoint blocker otherwise).
   - **Decision**: do NOT modify the GET handler in this plan (out of `files_modified` scope and would touch a Wave-3 file owned by no other plan). Instead, the AlumniTransitionButton receives `cohortEndDate` as a NEW prop and fetches it lazily on first interaction OR the executor adds the GET handler change to this plan's `files_modified`. **Default: surface this as a checkpoint:decision blocker** so the user can choose between (a) extending GET in Plan 03 (revision), (b) adding to this plan's scope, or (c) deferring AlumniTransitionButton to a follow-up.

   For the actual MemberDetailClient changes:
   - Replace `handleRemove` (currently `window.confirm` + DELETE call) with: open `OffboardConfirmModal` instead. Remove the `confirm(...)` line and the DELETE fetch. Keep `setRemoving` for button-disabled state.
   - Add state: `offboardOpen: boolean`, `discordRetryNeeded: { displayName: string } | null`, `retryingDiscord: boolean`.
   - Replace the existing button:
     - Old: `<button className="btn btn-outline btn-error btn-sm" onClick={handleRemove} ...>Remove from program</button>`
     - New: `<button type="button" className="btn btn-outline btn-error btn-sm" onClick={() => setOffboardOpen(true)} disabled={!detail.subdoc.active}>Offboard ambassador</button>`. Disabled when subdoc.active is false (offboarded already).
   - Render `<OffboardConfirmModal>` when `offboardOpen`. `onCompleted={(result) => { if (!result.discordRemoved) setDiscordRetryNeeded({ displayName }); load(); }}`. `onClose={() => setOffboardOpen(false)}`.
   - Inline retry banner (when `discordRetryNeeded !== null`): `<div role="alert" className="alert alert-error"><div><h3>Discord role removal failed</h3><span>Discord role removal failed — retry below. The Firestore record has already been updated.</span></div><button type="button" className="btn btn-outline btn-sm btn-error" onClick={retryDiscord} disabled={retryingDiscord}>{retryingDiscord ? "Retrying…" : "Retry Discord removal"}</button></div>`. Place this banner directly under the `<header>` of the page.
   - `retryDiscord`: POST `/api/ambassador/members/[uid]/offboard` with admin header. Treat 409 as success-with-retry (parse `discordRemoved` from response, if present and true → clear banner). On 200 with `discordRemoved: true` → clear banner + toast.success("Discord role removed."). Other → toast.error("Discord role removal failed again. Try again later.").
   - Render `<AlumniTransitionButton uid={uid} displayName={displayName} cohortEndDate={null /* TODO from GET handler */} subdocActive={detail.subdoc.active ?? false} onCompleted={load} />` wrapped in `<div className="divider" />` separator before the offboard button section.
   - Page heading button placement (UI-SPEC §Visual Hierarchy): keep the offboard button in the existing header location. Place `<AlumniTransitionButton>` BELOW StrikePanel, separated by a divider.
  </action>

  <verify>
    <automated>
      cd /Users/amu1o5/personal/code-with-ahsan &&
      ls "src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx" "src/app/admin/ambassadors/members/[uid]/AlumniTransitionButton.tsx" &&
      grep -q "modal modal-open" "src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx" &&
      grep -q "aria-labelledby=\"offboard-confirm-heading\"" "src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx" &&
      grep -q "Yes, offboard" "src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx" &&
      grep -q "Mark as Alumni" "src/app/admin/ambassadors/members/[uid]/AlumniTransitionButton.tsx" &&
      grep -q "Date.parse(cohortEndDate)" "src/app/admin/ambassadors/members/[uid]/AlumniTransitionButton.tsx" &&
      ! grep -q "window.confirm\|if (!confirm(" "src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx" &&
      grep -q "OffboardConfirmModal" "src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx" &&
      grep -q "AlumniTransitionButton" "src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx" &&
      grep -q "Discord role removal failed" "src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx" &&
      npx tsc --noEmit
    </automated>
  </verify>

  <acceptance_criteria>
    - `OffboardConfirmModal.tsx` and `AlumniTransitionButton.tsx` exist and pass `tsc --noEmit`.
    - `MemberDetailClient.tsx` no longer contains `window.confirm` OR the literal `confirm("Remove this ambassador...")`. Verify negative grep.
    - `MemberDetailClient.tsx` imports and renders both new components.
    - The Discord retry banner copy `"Discord role removal failed — retry below. The Firestore record has already been updated."` appears verbatim in `MemberDetailClient.tsx`.
    - `OffboardConfirmModal.tsx` uses `<dialog className="modal modal-open">` (matches StrikeConfirmModal pattern) and `aria-labelledby` references the modal heading id.
    - `AlumniTransitionButton.tsx` returns `null` when `Date.parse(cohortEndDate) > Date.now()`. Verify file contains `Date.parse(cohortEndDate)` AND `return null`.
    - `OffboardConfirmModal.tsx` confirm button uses `btn btn-error` (destructive — UI-SPEC §Color rule).
    - `AlumniTransitionButton.tsx` confirm/default button uses `btn btn-outline` (NOT `btn-primary` — UI-SPEC explicitly forbids primary for this admin action).
    - `npx eslint src/app/admin/ambassadors/members/[uid]/` reports 0 errors.
  </acceptance_criteria>

  <done>
    - `npx tsc --noEmit` passes.
    - All 3 files exist (or modified) at the listed paths.
    - All grep-based acceptance criteria above pass.
    - Negative grep for `window.confirm` in MemberDetailClient passes (returns 0 matches).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of dashboard + admin lifecycle UI</name>

  <files>
    No new files. This task verifies the artifacts produced by Task 1 and Task 2 in a real browser.
  </files>

  <what-built>
    - Ambassador dashboard at /ambassadors/dashboard with personal stats, onboarding checklist, leaderboard panel (or grace banner), Ambassador-of-the-Month banner.
    - Admin OffboardConfirmModal replaces the deprecated window.confirm flow on /admin/ambassadors/members/[uid].
    - Admin AlumniTransitionButton renders only when the cohort end date has passed.
    - Discord-removal failure shows an inline persistent retry banner.
  </what-built>

  <action>
    PAUSE for human verification. Do NOT proceed to /gsd-checker until the user types "approved" (or provides revision feedback). Run `npm run dev` so the user can perform the verification steps documented in <how-to-verify>. Capture the user's response verbatim in the plan SUMMARY.
  </action>

  <how-to-verify>
    1. Run the dev server: `npm run dev`. Sign in as an ambassador user.
    2. Visit http://localhost:3000/ambassadors/dashboard. Confirm:
       - Page heading reads "Ambassador Dashboard" with subheading "Your personal impact hub. Stats update hourly."
       - PersonalStatsPanel shows 5 stats: Referrals / Events Hosted / Reports On Time / Confirmed Strikes / Next Report Due.
       - OnboardingChecklist shows 5 items in the UI-SPEC order. Click "Mark as done" on "Join the #ambassadors channel on Discord" — the item line-throughs and the change persists on refresh (PATCHes /api/ambassador/profile).
       - LeaderboardPanel: if the cron has run and grace period elapsed, shows the 3-column table + "Your rank: #N" row. If grace period is active, shows the "Leaderboard unlocks in N week(s)" banner. Toggle between "All time" and "This month" tabs.
    3. Sign out and sign in as a non-ambassador user. Visit /ambassadors/dashboard — confirm immediate redirect to /profile.
    4. Sign in as an admin. Visit /admin/ambassadors/members/{some-uid}. Click "Offboard ambassador":
       - Modal opens with heading "Offboard {displayName}?" and the full UI-SPEC body copy.
       - Click "Go back" — modal closes, no API call fired (verify via Network tab).
       - Click "Yes, offboard {displayName}" again — POST to /api/ambassador/members/{uid}/offboard fires, modal closes on success, success toast appears.
       - Reload the page — the offboard button is now disabled (subdoc.active is false).
    5. Find an ambassador whose cohort.endDate has passed. Verify the "Mark as Alumni" button renders. Click it → inline confirmation prompt appears → confirm → POST to /alumni fires → success toast.
    6. Find an ambassador whose cohort is still active. Verify the "Mark as Alumni" button is hidden (NOT just disabled).
    7. Accessibility spot-checks: tab through the dashboard with keyboard only — focus order matches the UI-SPEC visual hierarchy. Open the offboard modal and confirm focus is trapped inside the dialog.
    8. Mobile viewport (devtools Pixel 7 / iPhone): confirm the stats grid collapses to vertical, the leaderboard table is horizontally scrollable (if needed), and the modal is readable.
  </how-to-verify>

  <resume-signal>Type "approved" if all steps above match. Otherwise describe the failure and the executor will revise via /gsd-execute-phase --revise.</resume-signal>

  <verify>
    Human verification only — the user runs the steps in <how-to-verify> and types the resume-signal. Automated checks (tsc/eslint/grep) already passed in Task 1 and Task 2.
  </verify>

  <done>
    User responds with "approved" (or equivalent affirmative) after completing every step in <how-to-verify>. Any reported failures are surfaced to the orchestrator for revision before this plan is marked complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /ambassadors/dashboard server route | Untrusted user — server gates via `isAmbassadorProgramEnabled()` (existing layout) and the client also redirects via `hasRole`. |
| Browser → /api/ambassador/dashboard/me & /leaderboard | Untrusted user — auth handled by Plan 03 endpoints (verifyAuth + hasRoleClaim). |
| Browser → /api/ambassador/profile (PATCH from OnboardingChecklist) | Untrusted user — existing endpoint enforces verifyAuth + hasRoleClaim and validates the payload against a Zod schema. |
| Browser → /api/ambassador/members/[uid]/offboard & /alumni | Untrusted admin token — auth handled by Plan 04 endpoints (requireAdmin). |
| Discord retry button → /api/ambassador/members/[uid]/offboard | Same as above; idempotent on already-inactive subdoc (Plan 04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Spoofing | DashboardClient role gate | mitigate | Client redirect is defense-in-depth; primary gate is the existing `/ambassadors` layout server check + Plan 03 API auth. The client redirect MUST be wrapped in `useEffect` so it cannot be DOM-suppressed before a render. |
| T-05-05-02 | Tampering | OnboardingChecklist self-mark PATCH body | mitigate | The PATCH body is shaped `{ onboarding: { joinedDiscord?: true; sharedReferralLink?: true } }`. The existing /api/ambassador/profile route validates with Zod and rejects unknown keys (verify in Phase 1 SUMMARY). No client-supplied unrelated keys reach Firestore. |
| T-05-05-03 | Information Disclosure | LeaderboardPanel rendering | mitigate | Client receives only `top3` + `ownRank` (acceptance criterion: zero references to `ambassadorRanks` in the file). No way for a malicious ambassador to enumerate other ambassadors' rank numbers via the rendered tree. |
| T-05-05-04 | Information Disclosure | OffboardConfirmModal displayName surface | accept | DisplayName is already public on `public_ambassadors` and the admin already views it via the parent page. No new disclosure. |
| T-05-05-05 | Repudiation | Admin offboard / alumni actions | mitigate | Plan 04 server logs include `admin.uid` and target `uid` on every offboard/alumni call (server-side logger, not in this plan). UI does not weaken this — it surfaces toasts + reload. |
| T-05-05-06 | Denial of Service | LeaderboardPanel manual refresh | mitigate | Refresh button uses `disabled={loading}` + tab-switch debouncing (no spam fetch). Leaderboard endpoint reads a single Firestore doc — cheap. |
| T-05-05-07 | Elevation of Privilege | OffboardConfirmModal admin token | mitigate | Same `x-admin-token: localStorage[ADMIN_TOKEN_KEY]` pattern as StrikeConfirmModal; no new auth surface. If localStorage is empty, the request fires with empty header → server returns 401 (Plan 04 behavior). |
| T-05-05-08 | Elevation of Privilege | AlumniTransitionButton end-date check | accept | Client-side endDate check is UX (hides the button); the alumni endpoint (Plan 04) does NOT enforce endDate at all (admin-initiated transition with no time gate). Misuse risk is zero — only admins can call the endpoint, and the program intentionally allows early alumni transitions (Q4 resolution: admin-initiated only). |
| T-05-05-09 | Information Disclosure | Discord retry banner copy | accept | The banner reveals that Firestore was updated but Discord was not — surface useful operational info to the admin. No PII beyond displayName. |
| T-05-05-10 | Tampering | OnboardingChecklist optimistic UI | accept | A user could spoof the local UI by editing JS, but the persisted state is server-controlled (next page load corrects). No security impact. |
</threat_model>

<verification>
- All UI-SPEC verbatim copy strings appear in their target files (grep checks in acceptance criteria).
- All UI-SPEC §Accessibility constraints implemented:
  - `<dialog className="modal modal-open">` with `aria-labelledby` (OffboardConfirmModal)
  - `<table>` semantics for leaderboard
  - `<ul role="list">` + `<button type="button">` for self-mark checklist items
  - `aria-live="polite"` on the staleness display
  - `role="alert"` on the alumni inline confirmation
- `npx tsc --noEmit` passes from project root.
- `npx eslint src/app/ambassadors/dashboard/ src/app/admin/ambassadors/members/[uid]/` reports 0 errors.
- Negative grep: `window.confirm` removed from MemberDetailClient.
- Negative grep: `ambassadorRanks` does not appear in LeaderboardPanel.tsx.
- Human checkpoint task confirms the full flow works in a real browser including the keyboard-only and mobile viewport spot-checks.
</verification>

<success_criteria>
- `/ambassadors/dashboard` is reachable for ambassadors and redirects non-ambassadors to `/profile`.
- All 5 dashboard child components render in the UI-SPEC visual order with verbatim copy.
- Onboarding checklist self-mark items persist via PATCH `/api/ambassador/profile`.
- Leaderboard renders the grace banner OR the top-3 table + own-rank row, with a working tab toggle.
- Admin offboard flow uses `OffboardConfirmModal` (not `window.confirm`); Discord retry banner appears inline on Discord-removal failure.
- AlumniTransitionButton renders only when the cohort end date has passed AND the subdoc is still active.
- Phase 5 ships: every requirement DASH-01..09 has a UI surface a human can reach, and ALUMNI-03 is preserved (alumni badge surfaces unchanged from Phase 3 — not modified by this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-05-SUMMARY.md` documenting:
- Files created (6 dashboard files + 2 admin files) and modified (1 admin file).
- Whether the cohort.endDate gap was resolved (Plan 03 GET handler extended? deferred? local-only fetch added in AlumniTransitionButton?).
- Any deviations from UI-SPEC copy strings (should be zero).
- Lighthouse / a11y spot-check notes from Task 3.
</output>
