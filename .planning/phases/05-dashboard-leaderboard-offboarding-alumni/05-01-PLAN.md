---
phase: 05-dashboard-leaderboard-offboarding-alumni
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ambassador/constants.ts
  - src/types/ambassador.ts
  - src/lib/discord.ts
  - src/lib/email.ts
  - src/lib/ambassador/leaderboard.ts
autonomous: true
requirements:
  - DASH-08
  - DASH-09
  - DISC-05
  - EMAIL-04

must_haves:
  truths:
    - "LEADERBOARD_SNAPSHOTS_COLLECTION constant is exported and equals 'leaderboard_snapshots'"
    - "AmbassadorSubdoc type has an optional onboarding object with joinedDiscord and sharedReferralLink booleans"
    - "CohortPatchSchema accepts an optional ambassadorOfTheMonth field (uid + displayName)"
    - "removeDiscordRole(memberIdOrUsername, roleId) is exported from discord.ts and treats 204 + 404 as success"
    - "sendAmbassadorOffboardingEmail(email, displayName, cohortName) is exported from email.ts"
    - "src/lib/ambassador/leaderboard.ts exports LeaderboardEntry and LeaderboardSnapshot interfaces"
  artifacts:
    - path: "src/lib/ambassador/constants.ts"
      provides: "LEADERBOARD_SNAPSHOTS_COLLECTION export"
      contains: "LEADERBOARD_SNAPSHOTS_COLLECTION"
    - path: "src/types/ambassador.ts"
      provides: "AmbassadorSubdoc.onboarding field, CohortPatchSchema.ambassadorOfTheMonth, AmbassadorOfTheMonth interface, CohortDoc.ambassadorOfTheMonth"
      contains: "onboarding"
    - path: "src/lib/discord.ts"
      provides: "removeDiscordRole exported function"
      contains: "export async function removeDiscordRole"
    - path: "src/lib/email.ts"
      provides: "sendAmbassadorOffboardingEmail exported function"
      contains: "export async function sendAmbassadorOffboardingEmail"
    - path: "src/lib/ambassador/leaderboard.ts"
      provides: "LeaderboardEntry + LeaderboardSnapshot interfaces; buildLeaderboardSnapshot stub signature"
      contains: "export interface LeaderboardSnapshot"
  key_links:
    - from: "src/lib/ambassador/constants.ts"
      to: "scripts/ambassador-leaderboard-snapshot.ts and src/app/api/ambassador/dashboard/leaderboard/route.ts"
      via: "shared LEADERBOARD_SNAPSHOTS_COLLECTION import (relative path in cron, @/ alias in API)"
      pattern: "LEADERBOARD_SNAPSHOTS_COLLECTION"
    - from: "src/lib/discord.ts removeDiscordRole"
      to: "src/app/api/ambassador/members/[uid]/offboard/route.ts"
      via: "import { removeDiscordRole } from @/lib/discord"
      pattern: "removeDiscordRole"
    - from: "src/lib/email.ts sendAmbassadorOffboardingEmail"
      to: "src/app/api/ambassador/members/[uid]/offboard/route.ts"
      via: "import from @/lib/email"
      pattern: "sendAmbassadorOffboardingEmail"
---

<objective>
Establish foundational types, constants, and helper functions consumed by all subsequent Phase 5 plans (leaderboard, dashboard, offboarding, alumni).

Purpose: Plans 02, 03, 04 all depend on these contracts (collection name, type shapes, discord role removal, offboarding email). Splitting them out into Wave 1 allows Plans 02–04 to run in parallel in Wave 2.

Output: Shared constants, extended types, two new helper functions (Discord + email), and a stub leaderboard module exposing only the interface contracts (full implementation lands in Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-RESEARCH.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-PATTERNS.md

<interfaces>
<!-- Existing exports the executor MUST integrate with — extracted from codebase. -->

From src/types/ambassador.ts (existing):
```typescript
export interface AmbassadorSubdoc {
  cohortId: string;
  joinedAt: Date;
  active: boolean;
  strikes: number;
  discordMemberId: string | null;
  endedAt?: Date;
  // Phase 3 fields (D-03)
  university?: string;
  city?: string;
  publicTagline?: string;
  twitterUrl?: string;
  githubUrl?: string;
  personalSiteUrl?: string;
  cohortPresentationVideoUrl?: string;
  cohortPresentationVideoEmbedType?: CohortPresentationVideoEmbedType;
  // Phase 4 fields
  referralCode?: string;
  timezone?: string;
  updatedAt?: Date;
}

export interface CohortDoc {
  cohortId: string;
  name: string;
  startDate: Date;
  endDate: Date;
  maxSize: number;
  acceptedCount: number;
  status: CohortStatus;
  applicationWindowOpen: boolean;
  createdAt: Date;
  updatedAt: Date;
}

export const CohortPatchSchema = z.object({
  name: z.string().trim().min(3).optional(),
  maxSize: z.number().int().min(1).max(500).optional(),
  status: CohortStatusSchema.optional(),
  applicationWindowOpen: z.boolean().optional(),
}).refine((d) => Object.keys(d).length > 0, { message: "At least one field required" });
```

From src/lib/discord.ts (existing):
```typescript
const DISCORD_API = "https://discord.com/api/v10";
function getHeaders(): HeadersInit;     // bot token Authorization header
function getGuildId(): string;          // throws if env missing
async function fetchWithRateLimit(url, init): Promise<Response>;
export async function lookupMemberByUsername(username): Promise<{ id: string } | null>;
export async function assignDiscordRole(memberIdOrUsername, roleId): Promise<boolean>;
export const DISCORD_AMBASSADOR_ROLE_ID: string;  // env-backed
```

From src/lib/email.ts (existing):
```typescript
function wrapEmailHtml(content: string, title: string): string;
async function sendEmail(to: string, subject: string, html: string): Promise<boolean>;
function getSiteUrlForAmbassadorEmails(): string;
export async function sendAmbassadorApplicationDeclinedEmail(
  recipientEmail: string,
  applicantName: string,
  cohortName: string,
  notes?: string
): Promise<boolean>;  // pattern to mirror at line ~668
```

From src/lib/ambassador/constants.ts (existing — pattern to follow):
```typescript
/** Phase 4: Top-level Firestore collection for referral attribution docs (REF-03). */
export const REFERRALS_COLLECTION = "referrals" as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LEADERBOARD_SNAPSHOTS_COLLECTION constant + extend types (DASH-07, DASH-08, DASH-09)</name>
  <files>src/lib/ambassador/constants.ts, src/types/ambassador.ts</files>
  <read_first>
    - src/lib/ambassador/constants.ts (full file — note `as const` pattern at lines 47, 50, 53, 57, 61, 65)
    - src/types/ambassador.ts lines 152-178 (AmbassadorSubdoc) and lines 263-279 (CohortDoc) and lines 342-351 (CohortPatchSchema)
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-RESEARCH.md §"Pattern 6: Onboarding Checklist State Storage" and §"Pattern 7: Cohort PATCH for Ambassador of the Month"
  </read_first>
  <behavior>
    - LEADERBOARD_SNAPSHOTS_COLLECTION exports the literal string "leaderboard_snapshots" with `as const`
    - AmbassadorSubdoc gains an optional `onboarding?: { joinedDiscord?: boolean; sharedReferralLink?: boolean }` field — only flags requiring explicit user action (DASH-08; auto-derived fields like uploadedVideo/loggedFirstEvent are computed in the dashboard API per Pitfall 6)
    - CohortDoc gains an optional `ambassadorOfTheMonth?: { uid: string; displayName: string }` field (DASH-09 — paired so dashboard avoids second Firestore read per Open Question 2 recommendation)
    - CohortPatchSchema accepts an optional `ambassadorOfTheMonth: z.object({ uid: z.string().min(1), displayName: z.string().min(1) }).nullable().optional()` (admin can set or clear via null)
    - Existing tests still pass (no breaking change to existing schemas / interfaces — all new fields are optional)
  </behavior>
  <action>
    Add to `src/lib/ambassador/constants.ts` after the existing EVENT_EDIT_WINDOW_MS export (line 75):

    ```typescript
    /** Phase 5: Top-level Firestore collection for hourly leaderboard snapshot docs (DASH-07).
     *  Doc id is cohortId. Written by scripts/ambassador-leaderboard-snapshot.ts;
     *  read by GET /api/ambassador/dashboard/leaderboard. */
    export const LEADERBOARD_SNAPSHOTS_COLLECTION = "leaderboard_snapshots" as const;

    /** Phase 5 (DASH-06): Leaderboard grace period in milliseconds — first 4 weeks of cohort. */
    export const LEADERBOARD_GRACE_PERIOD_MS = 28 * 24 * 60 * 60 * 1000;
    ```

    Edit `src/types/ambassador.ts` AmbassadorSubdoc interface (lines 152-178). Append AFTER the `updatedAt?: Date;` field at line 177 (and BEFORE the closing `}` at line 178):

    ```typescript
      // ─── Phase 5 fields ────────────────────────────────────────────────
      /** DASH-08: Onboarding checklist self-mark flags. Only flags requiring explicit
       *  user action are stored — `loggedFirstEvent` and `uploadedVideo` are derived
       *  from event count and cohortPresentationVideoUrl at API time (Pitfall 6). */
      onboarding?: {
        joinedDiscord?: boolean;
        sharedReferralLink?: boolean;
      };
      /** ALUMNI-01 / Phase 5 offboarding: timestamp of when the ambassador was offboarded
       *  via the 2-strike flow. Distinct from `endedAt` (term completion) per ALUMNI-02. */
      offboardedAt?: Date;
    ```

    Edit `src/types/ambassador.ts` CohortDoc interface (lines 266-279). Append AFTER the `updatedAt: Date;` field (line 278) and BEFORE the closing `}`:

    ```typescript
      /** DASH-09: Admin-curated "Ambassador of the Month" for the current cohort.
       *  Stored as { uid, displayName } pair so dashboard can render without a second
       *  Firestore read. Cleared by admin sending null in PATCH body. */
      ambassadorOfTheMonth?: { uid: string; displayName: string } | null;
    ```

    Edit `CohortPatchSchema` (lines 342-349). Add the new field BEFORE the `.refine(...)` call:

    ```typescript
    export const CohortPatchSchema = z
      .object({
        name: z.string().trim().min(3).optional(),
        maxSize: z.number().int().min(1).max(500).optional(),
        status: CohortStatusSchema.optional(),
        applicationWindowOpen: z.boolean().optional(),
        // DASH-09 — admin sets (or clears via null) the Ambassador of the Month.
        ambassadorOfTheMonth: z
          .object({
            uid: z.string().trim().min(1),
            displayName: z.string().trim().min(1),
          })
          .nullable()
          .optional(),
      })
      .refine((d) => Object.keys(d).length > 0, { message: "At least one field required" });
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "LEADERBOARD_SNAPSHOTS_COLLECTION" src/lib/ambassador/constants.ts && grep -q "onboarding?" src/types/ambassador.ts && grep -q "ambassadorOfTheMonth" src/types/ambassador.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'export const LEADERBOARD_SNAPSHOTS_COLLECTION = "leaderboard_snapshots" as const' src/lib/ambassador/constants.ts` exits 0
    - `grep -q 'export const LEADERBOARD_GRACE_PERIOD_MS = 28' src/lib/ambassador/constants.ts` exits 0
    - `grep -q 'onboarding?: {' src/types/ambassador.ts` exits 0
    - `grep -q 'joinedDiscord?: boolean' src/types/ambassador.ts` exits 0
    - `grep -q 'sharedReferralLink?: boolean' src/types/ambassador.ts` exits 0
    - `grep -q 'offboardedAt?: Date' src/types/ambassador.ts` exits 0
    - `grep -q 'ambassadorOfTheMonth' src/types/ambassador.ts` matches at least 3 times (CohortDoc field, schema field, type comment)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors introduced)
  </acceptance_criteria>
  <done>Constants file declares LEADERBOARD_SNAPSHOTS_COLLECTION and LEADERBOARD_GRACE_PERIOD_MS; AmbassadorSubdoc carries optional onboarding map + offboardedAt; CohortDoc carries optional ambassadorOfTheMonth; CohortPatchSchema accepts that field; full TypeScript build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add removeDiscordRole(DISC-05) + sendAmbassadorOffboardingEmail (EMAIL-04) + leaderboard module skeleton</name>
  <files>src/lib/discord.ts, src/lib/email.ts, src/lib/ambassador/leaderboard.ts, src/lib/discord.test.ts</files>
  <read_first>
    - src/lib/discord.ts lines 1-50 (imports, helpers) and the full `assignDiscordRole` function — search for `export async function assignDiscordRole` and read the entire body so the symmetrical DELETE function follows the same shape
    - src/lib/email.ts lines 593-700 (the `getSiteUrlForAmbassadorEmails` helper at line 593, plus the three existing `sendAmbassadorApplication*Email` functions starting at lines 605, 634, 668 — copy the wrapper / structure verbatim)
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-PATTERNS.md §"Discord Role Removal (new function in discord.ts)" and §"src/lib/email.ts (modify — add sendAmbassadorOffboardingEmail)"
    - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-RESEARCH.md §"Pitfall 5: Discord role removal 404 is NOT an error"
  </read_first>
  <behavior>
    - `removeDiscordRole(memberIdOrUsername, roleId)` returns true on Discord 204 (success) AND 404 (member already gone — idempotent per Pitfall 5)
    - `removeDiscordRole` returns false on any other 4xx/5xx and logs the error; never throws to the caller
    - `removeDiscordRole` resolves a username via `lookupMemberByUsername` only when the input does NOT match `/^\d{17,19}$/` — same shape as `assignDiscordRole`
    - `sendAmbassadorOffboardingEmail(email, displayName, cohortName)` returns the boolean result of `sendEmail`, uses `wrapEmailHtml`, and follows the exact copy from 05-UI-SPEC §"Offboarding Email (EMAIL-04)"
    - `src/lib/ambassador/leaderboard.ts` exports `LeaderboardEntry`, `LeaderboardSnapshot` interfaces and a stub `buildLeaderboardSnapshot(cohortId)` signature (full implementation in Plan 02)
    - Unit test `src/lib/discord.test.ts` covers DISC-05: 204 → true, 404 → true, 500 → false (mock `fetchWithRateLimit` if exposed, else stub `global.fetch`)
  </behavior>
  <action>
    1. Append to `src/lib/discord.ts` (after the existing `assignDiscordRole` function — find its closing `}` and insert after it):

    ```typescript
    /**
     * DISC-05 — Phase 5 offboarding: remove the Discord Ambassador role.
     *
     * Idempotent semantics:
     *   - 204 (success) → true
     *   - 404 (member no longer in guild OR already lacks the role) → true (Pitfall 5)
     *   - any other 4xx/5xx → false (offboard route surfaces a retry banner)
     *
     * Mirrors `assignDiscordRole` exactly — same id/username resolution path,
     * same logger usage, same try/catch envelope.
     */
    export async function removeDiscordRole(
      discordMemberIdOrUsername: string,
      roleId: string,
    ): Promise<boolean> {
      log.debug(`Removing role ${roleId} from user ${discordMemberIdOrUsername}`);
      try {
        let memberId: string;
        if (/^\d{17,19}$/.test(discordMemberIdOrUsername)) {
          memberId = discordMemberIdOrUsername;
        } else {
          const member = await lookupMemberByUsername(discordMemberIdOrUsername);
          if (!member) {
            log.warn(`[Discord] Cannot remove role - user not found: ${discordMemberIdOrUsername}`);
            // Treat as success — the member is already absent from the guild,
            // which is the desired post-condition.
            return true;
          }
          memberId = member.id;
        }
        const guildId = getGuildId();
        const response = await fetchWithRateLimit(
          `${DISCORD_API}/guilds/${guildId}/members/${memberId}/roles/${roleId}`,
          { method: "DELETE", headers: getHeaders() },
        );
        if (response.status === 204 || response.status === 404) {
          log.debug(
            `Role ${roleId} removed from ${discordMemberIdOrUsername} (status ${response.status})`,
          );
          return true;
        }
        const errorText = await response.text();
        log.error(
          `[Discord] Failed to remove role ${roleId}: ${response.status} - ${errorText}`,
        );
        return false;
      } catch (error) {
        log.error(`[Discord] Error removing role ${roleId}:`, error);
        return false;
      }
    }
    ```

    Note: use whatever logger identifier the existing file uses (`log` vs `logger` vs `console`) — match `assignDiscordRole` verbatim. If the file imports a logger at the top, that same identifier MUST be reused.

    2. Append to `src/lib/email.ts` AFTER `sendAmbassadorApplicationDeclinedEmail` (search for its function body's closing `}` near line ~700):

    ```typescript
    /**
     * EMAIL-04 — Phase 5 offboarding email on 2-strike removal.
     *
     * Copy taken verbatim from 05-UI-SPEC §"Offboarding Email (EMAIL-04)".
     * Tone: kind-but-firm, acknowledges contributions, points at future cohorts.
     */
    export async function sendAmbassadorOffboardingEmail(
      recipientEmail: string,
      displayName: string,
      cohortName: string,
    ): Promise<boolean> {
      const subject = "Your Ambassador Status — Important Update";
      const siteUrl = getSiteUrlForAmbassadorEmails();
      const content = `
        <h2>Ambassador Program Update</h2>
        <p>Hi ${displayName},</p>
        <p>After two confirmed accountability checks, your participation in the <strong>${cohortName}</strong> ambassador cohort has ended.</p>
        <div class="highlight warning">
          <p>Your Discord Ambassador role has been removed. Your contributions during your time in the program are genuinely appreciated.</p>
        </div>
        <p>Future cohorts remain open. We hope to see you again when the timing is better.<br/>— Ahsan</p>
        <a href="${siteUrl}/ambassadors" class="button">Learn about future cohorts</a>
      `;
      return sendEmail(recipientEmail, subject, wrapEmailHtml(content, subject));
    }
    ```

    3. Create `src/lib/ambassador/leaderboard.ts`:

    ```typescript
    /**
     * Phase 5 (DASH-03..07) — Leaderboard snapshot module.
     *
     * Module structure mirrors src/lib/ambassador/acceptance.ts:
     *   - Exported interfaces at the top (consumed by both cron and API).
     *   - `buildLeaderboardSnapshot` helper exported for the cron writer.
     *   - Pure functions; no Firestore writes here — the cron script invokes
     *     this module and writes the resulting doc itself.
     *
     * Full implementation lands in Plan 02; this file ships the type
     * contract first so Plan 03 (dashboard API) can compile against it.
     */

    export interface LeaderboardEntry {
      uid: string;
      displayName: string;
      photoURL: string;
      count: number;
    }

    export interface LeaderboardCategoryRanks {
      referralsRank: number;
      eventsRank: number;
      reportsRank: number;
    }

    export interface LeaderboardWindow {
      referrals: LeaderboardEntry[]; // top-3 only
      events: LeaderboardEntry[];
      reportsOnTime: LeaderboardEntry[];
      ambassadorRanks: Record<string, LeaderboardCategoryRanks>;
    }

    export interface LeaderboardSnapshot {
      cohortId: string;
      /** ISO string at write time. Stored as Firestore Timestamp; route handlers
       *  normalize via toDate().toISOString() before returning to the client. */
      updatedAt: string;
      /** Cohort.startDate + 28 days, ISO string. UI compares to now to gate DASH-06. */
      graceEndDate: string;
      cumulative: LeaderboardWindow;
      thisMonth: LeaderboardWindow & { month: string }; // YYYY-MM (UTC) per Open Question 1 recommendation
    }

    /**
     * Build the leaderboard snapshot for a cohort.
     *
     * Plan 02 fills in the body. The signature is locked here so Plan 02 can
     * develop in parallel with Plan 03 / Plan 04 (which import only the types).
     */
    export async function buildLeaderboardSnapshot(
      cohortId: string,
    ): Promise<LeaderboardSnapshot> {
      // Full implementation in Plan 02 (Wave 2).
      void cohortId;
      throw new Error("buildLeaderboardSnapshot: not implemented (Plan 02)");
    }
    ```

    4. Create `src/lib/discord.test.ts` (or extend existing if present):

    ```typescript
    /**
     * Phase 5 (DISC-05): unit coverage for removeDiscordRole.
     * Test boundary: mock global.fetch (fetchWithRateLimit ultimately calls it).
     */
    import { describe, it, expect, beforeEach, vi } from "vitest";
    import { removeDiscordRole } from "./discord";

    describe("removeDiscordRole (DISC-05)", () => {
      beforeEach(() => {
        process.env.DISCORD_BOT_TOKEN = "test-token";
        process.env.DISCORD_GUILD_ID = "1234567890";
        vi.restoreAllMocks();
      });

      it("returns true on Discord 204", async () => {
        vi.spyOn(global, "fetch").mockResolvedValue(
          new Response(null, { status: 204 }),
        );
        const result = await removeDiscordRole("123456789012345678", "999");
        expect(result).toBe(true);
      });

      it("returns true on Discord 404 (idempotent — Pitfall 5)", async () => {
        vi.spyOn(global, "fetch").mockResolvedValue(
          new Response("Not found", { status: 404 }),
        );
        const result = await removeDiscordRole("123456789012345678", "999");
        expect(result).toBe(true);
      });

      it("returns false on Discord 500", async () => {
        vi.spyOn(global, "fetch").mockResolvedValue(
          new Response("Server error", { status: 500 }),
        );
        const result = await removeDiscordRole("123456789012345678", "999");
        expect(result).toBe(false);
      });

      it("returns false on Discord 403", async () => {
        vi.spyOn(global, "fetch").mockResolvedValue(
          new Response("Forbidden", { status: 403 }),
        );
        const result = await removeDiscordRole("123456789012345678", "999");
        expect(result).toBe(false);
      });
    });
    ```

    Note: if `src/lib/discord.test.ts` already exists, append the `describe("removeDiscordRole...")` block instead of recreating the file. Do NOT remove existing tests.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/discord</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function removeDiscordRole" src/lib/discord.ts` exits 0
    - `grep -q "response.status === 204 || response.status === 404" src/lib/discord.ts` exits 0
    - `grep -q "export async function sendAmbassadorOffboardingEmail" src/lib/email.ts` exits 0
    - `grep -q "Your Ambassador Status — Important Update" src/lib/email.ts` exits 0
    - `test -f src/lib/ambassador/leaderboard.ts` succeeds
    - `grep -q "export interface LeaderboardSnapshot" src/lib/ambassador/leaderboard.ts` exits 0
    - `grep -q "export interface LeaderboardEntry" src/lib/ambassador/leaderboard.ts` exits 0
    - `grep -q "ambassadorRanks" src/lib/ambassador/leaderboard.ts` exits 0
    - `npm test -- --run src/lib/discord` reports all `removeDiscordRole` cases pass
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>removeDiscordRole exported with 204+404 idempotency; sendAmbassadorOffboardingEmail exported with verbatim UI-SPEC copy; leaderboard.ts skeleton exposes interfaces and stub builder; discord.test.ts covers DISC-05 success/idempotent/error branches; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord REST API | External service — bot token in env, payload trusted only as far as response codes |
| Mailgun API | External service — API key in env; recipient email validated upstream by application/account flow |
| Firestore client | All writes via Admin SDK from server-side only (no rules dependency in this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | `removeDiscordRole` log lines | mitigate | Use existing `log.debug`/`log.warn`/`log.error` (no `console.log` of bot token); same pattern as `assignDiscordRole` (already approved) |
| T-05-01-02 | Tampering | `sendAmbassadorOffboardingEmail` HTML interpolation | mitigate | `displayName` and `cohortName` originate from Firestore-controlled docs (not user request bodies); no email-injection surface; pattern matches `sendAmbassadorApplicationDeclinedEmail` which already ships in production |
| T-05-01-03 | Denial of Service | `removeDiscordRole` rate-limit | mitigate | Reuse existing `fetchWithRateLimit` (already implements Discord rate-limit semantics); no change needed |
| T-05-01-04 | Repudiation | Offboarding email send result | accept | `sendEmail` returns boolean; offboard route persists `emailSent` flag in API response; admin can manually re-trigger if needed (low risk — email is informational, not security-critical) |
| T-05-01-05 | Tampering | `CohortPatchSchema.ambassadorOfTheMonth` shape | mitigate | Zod validates `{ uid: min(1), displayName: min(1) }`; admin gate enforced upstream by existing PATCH route (Phase 2). No XSS — rendered as text in DaisyUI, not raw HTML. |
</threat_model>

<verification>
- TypeScript build green (`npx tsc --noEmit`)
- `npm test -- --run src/lib/discord` passes (DISC-05 cases)
- All five new exports are present in their respective files
- No existing exports are renamed or removed (binary-compatible additive change)
</verification>

<success_criteria>
- LEADERBOARD_SNAPSHOTS_COLLECTION constant + LEADERBOARD_GRACE_PERIOD_MS exist in constants.ts
- AmbassadorSubdoc carries optional `onboarding`, `offboardedAt` fields
- CohortDoc carries optional `ambassadorOfTheMonth` field; CohortPatchSchema accepts it
- removeDiscordRole exported with idempotent 204/404 success path; unit tests green
- sendAmbassadorOffboardingEmail exported with verbatim UI-SPEC copy
- src/lib/ambassador/leaderboard.ts exists with LeaderboardSnapshot / LeaderboardEntry / LeaderboardWindow / LeaderboardCategoryRanks interfaces and a stub buildLeaderboardSnapshot
- Plans 02, 03, 04 can compile against these interfaces in parallel
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-01-SUMMARY.md`
</output>
