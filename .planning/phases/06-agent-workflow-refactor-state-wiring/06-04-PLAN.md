---
phase: 06-agent-workflow-refactor-state-wiring
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - .planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SOAK-LOG.md
autonomous: false
requirements:
  - AGENT-TEST-01
  - AGENT-PAR-02

must_haves:
  truths:
    - "All three workstream pytest modules pass together (`pytest -q` is green) before deploy is attempted."
    - "Local `adk web` smoke confirms RESEARCH Open Question 2 (SequentialAgent welcome-last ordering surfaces warm welcome as user-visible reply) AND Open Question 3 (root LlmAgent's transfer_to_agent successfully routes to SequentialAgent targets — both onboarding_agent and external_knowledge_agent)."
    - "Local `adk web` smoke confirms the three-branch ParallelAgent fan-out is visible in the Events tab (RESEARCH §5.4) — interleaved gh/devto/so events rather than stacked sequentially."
    - "Cloud Run cwa-assistant-bot is redeployed atomically (single image push) and survives the existing healthcheck."
    - "24h prod soak on Cloud Logging shows P95 latency_ms on external_knowledge queries drops by ≥40% vs. baseline (~18933ms → ≤~11360ms), no regression on error rate, no `KeyError: 'devto_result'`-style template failures (RESEARCH §5.5)."
  artifacts:
    - path: ".planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SOAK-LOG.md"
      provides: "Soak-period observation log — baseline P95 numbers from before deploy, post-deploy P95 numbers at T+1h / T+6h / T+24h, error-rate comparison, decision to ship or roll back"
      contains: "P95 latency"
  key_links:
    - from: "local dev (adk web)"
      to: "RESEARCH Open Question 2 + 3 verification"
      via: "manual smoke before Cloud Run deploy"
      pattern: "adk web"
    - from: "deployed cwa-assistant-bot"
      to: "Cloud Logging latency_ms / query_topic / error metrics"
      via: "existing HMAC-hashed usage metrics dashboard (Phase 02.1)"
      pattern: "latency_ms.*external_knowledge"
---

<objective>
Final gate for Phase 6: validate the three-workstream refactor end-to-end against `adk web` locally, redeploy `cwa-assistant-bot` to Cloud Run, run a 24h soak on existing usage metrics, and ship-or-rollback based on AGENT-PAR-02's ≥40% P95 latency-drop target.

Purpose: Closes AGENT-TEST-01 (the smoke-and-deploy gate) and AGENT-PAR-02 (the latency target — measured here, not in any unit test per CONTEXT.md `## Implementation Decisions > Testing` line 47: "Performance assertion is best-effort. Latency target (≥40% P95 drop from ~19s baseline) is measured in prod soak (post-deploy), not in unit tests"). Also formally answers RESEARCH Open Questions 2 (SequentialAgent child ordering for onboarding) and 3 (does `transfer_to_agent` route to SequentialAgent? — affects both `onboarding_agent` AND `external_knowledge_agent`).

Output: An `adk web` smoke run that exercises all three workstreams in one Discord-style conversation, a Cloud Run redeploy, a 24h soak log captured in `06-04-SOAK-LOG.md`, and a ship/rollback decision based on the soak metrics. This plan does NOT modify production agent code (Plans 01-03 own that surface); it ONLY runs verification + deploy + observe.

**Dependency rationale (B-2 / depends_on includes 06-02):** Plan 02 Task 4 is the per-workstream `adk web` smoke gate for the onboarding SequentialAgent's child-ordering verdict (welcome-LAST vs. welcome-FIRST). Plan 04 Task 1 confirms Plan 02 Task 4's verdict at the FULL-tree level (with all three workstreams composed under the root coordinator + with the second SequentialAgent `external_knowledge_agent` also exercised). If Plan 02 Task 4 already resolved welcome-LAST and committed (Branch A — the default), Plan 04 Task 1 is a re-confirmation only — the second `adk web` run shouldn't reveal anything new. If Plan 02 Task 4 surfaced welcome-FIRST and Plan 02 Task 5 Branch B fired, the committed ordering is welcome-FIRST and Plan 04 Task 1 simply confirms the same at the full-tree level. Plan 04 Task 1 is the LAST chance to catch a full-tree ordering regression before deploy.

This plan is NOT autonomous — it has multiple human checkpoints (smoke verdict, deploy approval, soak decision). User decides when to flip per CONTEXT.md `## Implementation Decisions > Deployment + soak` line 50: "Cloud Run redeploy required. Same path as Phase 02.1-03 redeploy. User decides when to flip."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-ROADMAP.md
@.planning/phases/06-agent-workflow-refactor-state-wiring/06-CONTEXT.md
@.planning/phases/06-agent-workflow-refactor-state-wiring/06-RESEARCH.md
@.planning/phases/06-agent-workflow-refactor-state-wiring/06-01-PLAN.md
@.planning/phases/06-agent-workflow-refactor-state-wiring/06-02-PLAN.md
@.planning/phases/06-agent-workflow-refactor-state-wiring/06-03-PLAN.md

@agent/community_assistant/agent.py
@agent/pyproject.toml
@.github/workflows/

<interfaces>
<!-- Deploy + soak observability surface. Refer to the Phase 02.1-03 redeploy procedure SUMMARY in `.planning/phases/02.1-adk-content-external-knowledge-sub-agents/` for the canonical Cloud Run image push + cwa-assistant-bot service update sequence. -->

**Cloud Run service:** `cwa-assistant-bot` — deployed from `agent/` build context. Region + project from existing CD setup (see `.github/workflows/` or Phase 02.1-03 SUMMARY).

**Cloud Logging dashboard (existing, Phase 02.1):** filters on the bot's structured stdout events. Key metrics:
- `latency_ms` (int, per-turn) — emitted by `agent/discord_bot/usage_metrics.py` after each agent response.
- `query_topic` (string) — bot's topic-classification label; the `external_knowledge` value is what AGENT-PAR-02 targets.
- error rate — count of turns where the agent returned `status="error"` or raised an unhandled exception.
- HMAC-hashed `user_id` — privacy-preserving identifier for cross-turn grouping. UNCHANGED by Phase 6.

**Baseline (from cfp-framing.md / v6.0 STATE.md, per RESEARCH §1.8 and the Phase 6 ROADMAP `**Goal:**`):** `latency_ms=18933` (P95-ish, single observed multi-source external_knowledge query). Plan 04 must pull a multi-day P95 from Cloud Logging BEFORE deploy to set a defensible baseline (a single observation is anecdotal; the 24h soak compares P95 to P95).

**adk web** — local-only dev UI shipped with `google-adk`. Started via `cd agent && adk web`. Provides Events tab (per-agent + per-tool fan-out visibility), State tab (`session.state` contents), and per-event LLM prompt inspection. This is the verification surface for RESEARCH Open Questions 2 and 3.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: CHECKPOINT — local `adk web` smoke across all three workstreams (RESEARCH Open Questions 2 + 3, §5.4 visibility)</name>
  <what-built>
After Plans 01-03 commit, the new agent topology is locally importable. This checkpoint exercises ALL three workstreams in a single multi-turn Discord-style conversation against `adk web` BEFORE pushing the image to Cloud Run. It formally resolves the two CRITICAL open questions from RESEARCH that no unit test can answer:

- **Open Question 2 (RESEARCH lines 826-830):** does the SequentialAgent surface the LAST child's response as the user-visible reply (recommended welcome-last ordering) or the FIRST child's? Plan 02 Task 4 already gated on this, but Plan 04 re-verifies because Plan 01 also relies on SequentialAgent surfacing — specifically the synthesizer's reply, not the leaf researchers' state-only output. If welcome-last works in Plan 02 Task 4, the synthesizer-last pattern in Plan 01 also works (same framework primitive).

- **Open Question 3 (RESEARCH lines 833-835):** does the root LlmAgent's `transfer_to_agent` route to a SequentialAgent target? In Phase 6, TWO of the root's `sub_agents` are now SequentialAgents (`onboarding_agent`, `external_knowledge_agent`). If `transfer_to_agent` only works for LlmAgent targets, BOTH workstreams break and the fallback per RESEARCH §Open Question 3 fires: wrap each SequentialAgent in a thin LlmAgent forwarder, OR defer the multi-output_key extraction.

Also exercises RESEARCH §5.4 (live-demo visibility): the Events tab should show the three external_knowledge branches as separate, interleaved events when a 3-source query fires — this is the talk-demo win (CFP framing).
  </what-built>
  <how-to-verify>
**Prereqs:**
1. Plans 01, 02, 03 all committed and on the working branch.
2. `cd agent && pytest -q` is green (entire test suite passes).
3. `cd agent && pip install -e .` (refresh editable install — the new `external_knowledge/` package needs to be re-registered per RESEARCH §Runtime State Inventory line 718).
4. `GOOGLE_API_KEY` set in env (existing dev setup). `GITHUB_TOKEN` set (boosts the GH rate limit from 60→5000/hr per `gh_researcher.py`; nice-to-have for smoke testing).

**Run `adk web`:**
```
cd agent && adk web
```
Open the printed URL (typically `http://localhost:8000`), select `community_assistant`.

**Workstream verification — single 5-turn conversation:**

**Turn 1 (onboarding + state-write verification):** Type "Hi, I just joined! I'm a junior dev focused on React and want to break into AI engineering. Where should I start?"

Expected in Events tab:
- Root coordinator emits `transfer_to_agent("onboarding_agent")`.
- ✅ Open Question 3 partially resolved: transfer routes successfully to the SequentialAgent target named `onboarding_agent`.
- Three child events fire in order: `onboarding_skill_level`, `onboarding_goals`, `onboarding_welcome` (per Plan 02 child-order recommendation).
- User-visible reply is the warm welcome from `onboarding_welcome`. NOT the single-word `"intermediate"` (skill_level output) or the 40-word goals summary.
  - ✅ Open Question 2 resolved (welcome-last ordering): mark in resume-signal.
  - ⚠ If user sees `"intermediate"` instead: Plan 02 Task 4's fallback fires — reorder to `[welcome_agent, skill_level_extractor, goals_extractor]`, flip test 6 of Plan 02 Task 1, re-run `pytest tests/test_state_propagation.py -x`, re-deploy locally, retry this checkpoint.

In State tab:
- `session.state["user_skill_level"]` is one of `{"beginner", "intermediate", "advanced", "unknown"}`.
- `session.state["user_goals"]` is a non-empty string referencing React / AI engineering / break-into-AI (or `"unknown"` if the extractor couldn't infer).

**Turn 2 (state-read verification for mentorship):** Type "Can you recommend mentors for me?"

Expected:
- `transfer_to_agent("mentorship_agent")`.
- In the Events tab, click the `mentorship_agent` event → inspect the resolved LLM prompt. The prompt should contain `Skill level: <whatever was extracted>` AND `Goals: <whatever was extracted>` (the `{user_skill_level?}` / `{user_goals?}` placeholders substituted from state).
- mentorship_agent's reply mentions the extracted skill level explicitly (per the instruction "If skill level is known, mention it explicitly when recommending mentors").

**Turn 3 (state-read verification for projects):** Type "What projects can I contribute to?"

Same expected as Turn 2 but with `transfer_to_agent("projects_agent")` and the projects_agent's resolved prompt contains the same substituted skill+goals.

**Turn 4 (external_knowledge fan-out — RESEARCH §5.4 visibility + Open Question 3 for the second SequentialAgent):** Type "Find me trending Angular repos on GitHub, any dev.to articles, and Stack Overflow questions about Angular signals."

Expected:
- `transfer_to_agent("external_knowledge_agent")`.
- ✅ Open Question 3 fully resolved: transfer routes successfully to the second SequentialAgent target. (If transfer FAILS here but worked in Turn 1, that's an LlmAgent-vs-SequentialAgent mismatch — surface to user, do not deploy.)
- In the Events tab, see TWO event groups under `external_knowledge_agent`:
  1. `ExternalFanOut` (ParallelAgent) with THREE child events: `gh_researcher`, `devto_researcher`, `so_researcher` — events should appear INTERLEAVED (timestamps tightly clustered), not strictly stacked one-after-the-other. This is the RESEARCH §5.4 talk-demo visibility win.
  2. `external_knowledge_synthesizer` (LlmAgent) — fires AFTER the three branches complete.
- User-visible reply is the synthesizer's unified output — cites repos / articles / questions with their URLs and source labels (e.g., "(via GitHub)", "(via dev.to)", "(via Stack Overflow)").

In State tab:
- `session.state["gh_result"]`, `session.state["devto_result"]`, `session.state["so_result"]` are each populated (string content from the respective branch).

**Turn 5 (AgentTool verification — Plan 03 surface):** Type "Do you have an AI guide?"

Expected:
- `transfer_to_agent("content_agent")`.
- In the Events tab, see `content_agent` → tool call event named `featured_resources_agent` (the AgentTool wrap). This is the RESEARCH §3.6 demo-ability win — featured-resource lookup is now visible as a tool call instead of a Python-side prepend.
- The wrapped agent internally calls `lookup_featured_resource(topic="...")` and returns the AI Guide entry.
- content_agent's final reply leads with the AI Guide title + URL.

**Failure modes + escalation:**

| Symptom | Diagnosis | Action |
|---------|-----------|--------|
| Turn 1 transfer to `onboarding_agent` fails with "agent not found" or routes elsewhere | Coordinator's instruction text + agent name mismatch — should not happen because both Plans 01/02 preserved the routable name | Verify `agent.py` import line; check `root_agent.sub_agents` list at runtime |
| Turn 1: user sees single-word "intermediate" or 40-word goals summary instead of warm welcome | RESEARCH Open Question 2 framework behavior is welcome-FIRST | Reorder Plan 02's onboarding_agent.sub_agents to `[welcome_agent, skill_level_extractor, goals_extractor]`; flip Plan 02 Task 1 test 6 assertion; re-run pytest; retry checkpoint |
| Turn 4 transfer to `external_knowledge_agent` fails OR routes but never produces a synthesizer reply | RESEARCH Open Question 3 fallback fires for SequentialAgent targets | STOP. Surface to user with Plan 01/02 rework options: (A) wrap both SequentialAgents in thin LlmAgent forwarders; (B) defer multi-output_key extraction to Phase 7/8. User decides. |
| Turn 4 events show 3 branches running strictly sequential (not interleaved) | ADK ParallelAgent isn't actually parallelizing — known unlikely per RESEARCH §1.5 + §5.4, but possible if framework version is unusual | Inspect timestamps; if confirmed sequential, the AGENT-PAR-02 latency target will not be met. Investigate ADK version, check for blocking sync I/O on httpx clients. May need to bump `google-adk` pin. |
| Turn 5 events show no `featured_resources_agent` tool call (the LLM never invokes it) | content_agent's instruction not prompting the LLM to call featured_resources_agent first | Verify Plan 03 Task 3's instruction update landed; the "SEARCH STRATEGY" block must explicitly say "call featured_resources_agent FIRST for flagship keywords" |
| State tab missing any of `user_skill_level` / `user_goals` / `gh_result` / `devto_result` / `so_result` after the relevant turn | output_key wiring broken in the implementation | Verify the leaf agent's `output_key="..."` attribute matches the expected key name exactly |

**Soft gates (nice-to-have but not blocking):**
- Turn 4's total wall-clock time on the synthesizer's final reply should feel snappier than baseline (~19s). This is anecdotal; the rigorous P95 measurement happens in Task 3's soak.
- The synthesizer should explicitly cite source labels per RESEARCH §1.6.
  </how-to-verify>
  <resume-signal>Type "smoke green" if all 5 turns produced the expected events and no failure-mode symptom appeared. Type "smoke green WITH order flip" if Plan 02 ordering needed to be welcome-FIRST and you applied the fix. Type "BLOCKED — Open Question 3 (SequentialAgent transfer)" or "BLOCKED — ParallelAgent not actually parallel" or "BLOCKED — featured_resources_agent never invoked" to surface specific failures with full context (Events-tab screenshot, ADK version, the resolved LLM prompt text) for triage before deploy.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CHECKPOINT — pull baseline P95 from Cloud Logging, then deploy cwa-assistant-bot</name>
  <what-built>
Before Task 3's 24h soak can measure AGENT-PAR-02's ≥40% P95 latency drop, we need a defensible baseline. The single observed `latency_ms=18933` from cfp-framing.md (RESEARCH §1.8) is anecdotal — a real P95 over the last 7 days of production traffic on `query_topic=external_knowledge` is the comparison number.

After capturing the baseline, deploy the refactored image to `cwa-assistant-bot`. Same procedure as Phase 02.1-03 redeploy.

**Why human-action and not auto:** the deploy step itself can be automated (gcloud / GitHub Actions). The PRE-deploy baseline pull and the DECISION-to-deploy ARE human judgment calls. User decides when to flip per CONTEXT.md `## Implementation Decisions > Deployment + soak` line 50.
  </what-built>
  <how-to-verify>
**Part A — pull baseline P95 from Cloud Logging:**

1. Open Cloud Logging in the project hosting `cwa-assistant-bot` (existing dashboard from Phase 02.1).
2. Filter on:
   - `resource.type="cloud_run_revision"`
   - `resource.labels.service_name="cwa-assistant-bot"`
   - `jsonPayload.query_topic="external_knowledge"`
   - Time range: last 7 days (or whatever interval gives a statistically meaningful sample — ≥50 turns ideally).
3. Compute P95 of `jsonPayload.latency_ms` over the filtered logs. Use Logs Explorer's Aggregations OR export to BigQuery + run `APPROX_QUANTILES(latency_ms, 100)[OFFSET(95)]`.
4. Record in `.planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SOAK-LOG.md`:
   ```markdown
   # Phase 6 Soak Log

   ## Baseline (pre-deploy)
   **Date pulled:** YYYY-MM-DD
   **Time range:** last 7 days
   **Sample size:** N turns on query_topic=external_knowledge
   **Baseline P95 latency_ms:** XXXX ms
   **P50 latency_ms:** YYYY ms (for context)
   **Error rate:** Z% (turns where status='error' or unhandled exception)

   **Target:** post-deploy P95 ≤ baseline_P95 × 0.6 (40% drop = AGENT-PAR-02 gate).
   ```
   If sample size is small (<20 turns), note that explicitly in the log — the comparison's confidence interval is wide. Don't fabricate a tight bound; document the uncertainty.

**Part B — deploy `cwa-assistant-bot` to Cloud Run:**

1. Verify image build succeeds locally: `cd agent && docker build -t cwa-assistant-bot-phase6 .` (or whatever the existing Dockerfile build command is). Check for any missing `agent/community_assistant/sub_agents/external_knowledge/` package — should be picked up automatically by hatchling per `agent/pyproject.toml:19-20` ("packages = ['community_assistant']").
2. Verify the running container can import the new package: `docker run --rm cwa-assistant-bot-phase6 python -c "from community_assistant.sub_agents.external_knowledge import external_knowledge_agent; from community_assistant.sub_agents.featured_resources import featured_resources_tool; print('imports OK')"` → prints `imports OK`. If not, fix the package install issue before pushing.
3. Push to Cloud Run via the existing CD path (refer to Phase 02.1-03 SUMMARY for the exact `gcloud run deploy` invocation or the GitHub Actions workflow that handles it). Same revision-suffix convention (e.g., `--revision-suffix=phase6`).
4. Watch the rollout: `gcloud run services describe cwa-assistant-bot --region=<existing-region> --format=json | jq '.status.latestReadyRevisionName, .status.traffic'` → confirm traffic moved to the new revision.
5. Verify the new revision is serving: send a single Discord message to the bot (or hit the bot's health endpoint if one exists per Phase 02.1) — expect a 200 / normal reply.

**Failure modes:**
- Build fails because hatchling doesn't pick up the new `external_knowledge/` subpackage → check that the new package has a proper `__init__.py` (Plan 01 Task 2 creates it) and that there's no `[tool.hatch.build.targets.wheel] only-include` directive missing it.
- Container starts but crashes on first request → check Cloud Run logs for `ImportError` or AttributeError; most likely the editable install on Docker didn't include the new package.
- Healthcheck fails → revert to previous revision via `gcloud run services update-traffic cwa-assistant-bot --to-revisions=<previous>=100`.

DO NOT proceed to Task 3 (soak) if any of the failure modes above fire. Surface to user with the exact error.
  </how-to-verify>
  <resume-signal>Type "baseline=XXXXms deployed=revision-NAME" with the exact baseline P95 and new revision name (e.g., "baseline=18420ms deployed=cwa-assistant-bot-phase6-00012"). Or type "BLOCKED — <failure mode>" with diagnostic details.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — 24h soak watch + ship/rollback decision (AGENT-PAR-02 gate)</name>
  <what-built>
After Task 2 deploys the new revision, observe Cloud Logging for 24h. AGENT-PAR-02 is satisfied IFF post-deploy P95 on `query_topic=external_knowledge` drops by ≥40% vs. baseline. CONTEXT.md `## Implementation Decisions > Deployment + soak` line 51: "24h soak gate. Phase doesn't ship until 24h on Cloud Run produces no regression on the HMAC-hashed usage metrics dashboard."

Also watch for the failure modes RESEARCH §5.5 flags:
- Error-rate REGRESSION: per-branch httpx failures are fine (each leaf swallows them per Plan 01's design — no synthesizer crash), but a `KeyError: 'devto_result'` style template failure would manifest as a 5xx-equivalent in Cloud Logging. Most likely indicates a missing `?` on a state ref slipped through — Plan 02 Task 1 test 10's regex was supposed to catch that, but if it shows up live, investigate.
- LLM-call-count regression beyond expected: baseline was ~1 LLM call per turn for `external_knowledge_agent` (the monolith); post-deploy expected is ~4 (3 leaves + 1 synthesizer for 3-source queries). For `onboarding_agent`, baseline was 1, post-deploy expected is 3 (welcome + 2 extractors). If actual is much higher, debug.
  </what-built>
  <how-to-verify>
**Observation cadence:** check Cloud Logging at T+1h, T+6h, T+24h after deploy. Record findings in `06-04-SOAK-LOG.md` under three section headers (`## Soak T+1h`, `## Soak T+6h`, `## Soak T+24h`). The literal phrase `T+24h` MUST appear as a section header — the Task 3 `<automated>` grep gate enforces this.

**At each checkpoint, capture:**
1. P95 `latency_ms` on `query_topic="external_knowledge"` since the new revision started serving. Filter:
   ```
   resource.type="cloud_run_revision"
   resource.labels.service_name="cwa-assistant-bot"
   resource.labels.revision_name="<new revision from Task 2>"
   jsonPayload.query_topic="external_knowledge"
   ```
2. P50 + sample count for context.
3. Error rate: count of turns where `jsonPayload.status="error"` OR the bot returned no response (proxy: count of Discord messages without a follow-up bot message within 30s — if such a metric exists; otherwise just the `status` field).
4. Compare to baseline. Compute `drop_pct = (baseline_p95 - current_p95) / baseline_p95 * 100`.

**Format for each checkpoint in `06-04-SOAK-LOG.md`:**
```markdown
## Soak T+Xh (YYYY-MM-DD HH:MM UTC)

| Metric | Baseline | Post-deploy | Delta |
|--------|----------|-------------|-------|
| P95 latency_ms (external_knowledge) | XXXX | YYYY | -ZZ% |
| P50 latency_ms | XXXX | YYYY | -ZZ% |
| Sample size | N | M | — |
| Error rate | A.A% | B.B% | +/- C.C pp |

**Observations:** <narrative — any template-failure messages? any unusual LLM-call-count spikes? any user-reported regressions in Discord?>
```

**Decision matrix at T+24h:**

| Drop % | Error-rate delta | Decision |
|--------|------------------|----------|
| ≥40% | ≤ baseline | ✅ SHIP — AGENT-PAR-02 met (GREEN). Mark Phase 6 complete. |
| 20-40% | ≤ baseline | ⚠ PARTIAL-SHIP (carry-over to next phase) — AGENT-PAR-02 is NOT met (DEFERRED, not GREEN). User decides whether to override + carry over. To proceed with PARTIAL-SHIP: (a) user explicitly authorizes the override in the resume-signal; (b) a new "Next Moves" entry is added to `.planning/STATE.md` pointing to a Phase 7/8 latency-investigation task (e.g., investigate per-branch tail latency, consider `skip_summarization=True` on featured_resources_tool per RESEARCH Assumption A6); (c) `06-04-SOAK-LOG.md`'s Final Decision section records the override rationale + handoff reference. Without all three, this path is NOT available — investigate or rollback instead. |
| <20% | ≤ baseline | ❌ INVESTIGATE-OR-ROLLBACK — drop is too small. Most likely diagnosis: ParallelAgent isn't actually parallelizing (RESEARCH §1.5 unlikely-but-possible — but Plan 01 test 12 should have caught this at unit-test time) OR the synthesizer LLM round-trip is dominating (RESEARCH §1.6 trade — synthesizer adds 1 LLM call vs. baseline). Default action: ROLLBACK + diagnose in a Plan 05 gap-closure. Only escalate to PARTIAL-SHIP (carry-over) if user explicitly accepts the override conditions (a)(b)(c) above. |
| Any | Significant regression (e.g., +5pp error rate) | ❌ ROLLBACK — `gcloud run services update-traffic cwa-assistant-bot --to-revisions=<previous>=100`. Diagnose with Cloud Logging error breakdown, fix in a Plan 05 (gap closure), redeploy. |

**Critical regression signals (immediate rollback regardless of latency):**
- `KeyError` or templating error in any synthesizer / downstream agent log (RESEARCH §5.5).
- HMAC-hashed user_id appearing as raw user_id in any log — privacy invariant broken (should not happen because Plan 6 doesn't touch usage_metrics.py per RESEARCH §5.3).
- Any unhandled Python exception propagating to Cloud Run's per-request error logs from `community_assistant/sub_agents/external_knowledge/`.

**At T+24h decision time, record in `06-04-SOAK-LOG.md`:**
```markdown
## Final Decision (T+24h, YYYY-MM-DD HH:MM UTC)

**P95 drop:** ZZ% (target: ≥40%)
**Error-rate delta:** +/- C.C pp (target: ≤ baseline)
**Decision:** SHIP / PARTIAL-SHIP (carry-over to next phase) / ROLLBACK
**Rationale:** <1-3 sentences>
**Next action:** <commit phase-complete tag / open Plan 05 gap-closure / rollback command run>
**(PARTIAL-SHIP only) AGENT-PAR-02 status:** DEFERRED (not GREEN) — carried over to Phase 7/8.
**(PARTIAL-SHIP only) STATE.md handoff entry:** <reference to the new "Next Moves" entry in `.planning/STATE.md` pointing to the Phase 7/8 latency-investigation task — must be committed in the same atomic commit as the SOAK-LOG.md final-decision update>
**(PARTIAL-SHIP only) User override resume-signal:** <verbatim from Task 3 resume-signal, must explicitly authorize the override>
```

If SHIP: this plan completes, Phase 6 is done. If anything else: surface to user for next steps.
  </how-to-verify>
  <resume-signal>Type "SHIP — P95 dropped ZZ% (XXXXms→YYYYms), no regression" or "ROLLBACK — <reason>" or "PARTIAL-SHIP (carry-over to next phase) — partial win at ZZ%, user override + handoff task tracked" — with the SOAK-LOG.md updated and committed.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SOAK-LOG.md && grep -q "Baseline P95" .planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SOAK-LOG.md && grep -q "T+24h" .planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SOAK-LOG.md && grep -q "Final Decision" .planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SOAK-LOG.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev `adk web` → in-memory ADK runtime | Same boundary as production but with InMemorySessionService and dev-only state. No prod data exposed. |
| `gcloud run deploy` → Cloud Run service | Authenticated deploy via existing CD credentials. Image push surface; no new credentials added by Phase 6. |
| Cloud Run → Cloud Logging | Existing log emission path from `discord_bot/usage_metrics.py`. UNCHANGED — Phase 6 adds no new logging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Elevation of Privilege | Deploy step accidentally pushing a broken image that bypasses agent-level safety (e.g., disables HMAC) | mitigate | Image build inherits unchanged `discord_bot/usage_metrics.py` (RESEARCH §5.3: HMAC privacy invariant is NOT in any phase-6-touched file). Pre-deploy import smoke (Task 2 Part B step 2) confirms the new packages import cleanly but does not run any agent logic — production smoke is via Task 1's `adk web` run. Cloud Run healthcheck (Task 2 Part B step 5) catches startup failures. Rollback path is `gcloud run services update-traffic` to the previous revision. |
| T-06-12 | Denial of Service | New refactor introduces an infinite loop / runaway LLM call burning quota during the 24h soak | mitigate | Each LlmAgent has model-level token caps (gemini-2.5-flash default). ADK Runner has per-turn timeouts. The synthesizer + extractors are bounded by their instruction constraints (≤40 words for goals_extractor; single word for skill_level_extractor; ≤5 items × 300 chars for synthesizer per the `_RESULT_LIMIT = 5` carried over from old monolith). Task 3's soak watches LLM-call count — if it spikes >10× baseline per turn, investigate immediately. |
| T-06-13 | Information Disclosure | The soak log itself accidentally including raw user content from Cloud Logging excerpts | mitigate | The Cloud Logging filter in Task 3 pulls aggregate metrics (P95 / P50 / counts), NOT individual log entries with `jsonPayload` content. If excerpts ARE included for narrative observation, scrub any `user_query` / `user_id` content before committing the log file — keep only HMAC-hashed identifiers and metric fields. Phase 6 doesn't add new fields to the log payload; the existing scrubbing convention is preserved. |
</threat_model>

<verification>
**Per-task verification:**
- Task 1: User signals `smoke green` or `smoke green WITH order flip` or `BLOCKED — <symptom>`. If green, proceed; if blocked, fix in Plan 01/02/03 and retry.
- Task 2: User signals baseline P95 numeric + deployed revision name. `06-04-SOAK-LOG.md` exists with the "Baseline" section filled in. Cloud Run shows traffic on the new revision (verify with `gcloud run services describe cwa-assistant-bot --format='value(status.traffic)'`).
- Task 3: User signals SHIP / ROLLBACK / DISCUSS with explicit drop% and error-rate delta. `06-04-SOAK-LOG.md` has 3 checkpoint sections (T+1h, T+6h, T+24h) + a Final Decision section.

**Plan-level verification:**
- Full `cd agent && pytest -q` still green (run once at the start of Task 1 to confirm Plans 01-03 actually landed cleanly).
- `06-04-SOAK-LOG.md` exists, is committed to git, and has both Baseline and Final Decision sections populated with concrete numbers.
- If decision is SHIP: Phase 6 ROADMAP entry marked complete. If ROLLBACK: a Plan 05 gap-closure stub is opened per `/gsd-plan-phase --gaps` (orchestrator decides; this plan just signals).
</verification>

<success_criteria>
- Local `adk web` smoke run (Task 1) completed across all 5 conversation turns; user resumed with `smoke green` (welcome-last works) OR `smoke green WITH order flip` (Plan 02 ordering was reversed + tests re-pinned).
- RESEARCH Open Question 2 (SequentialAgent surfaces welcome correctly) is formally resolved — final ordering documented in 06-04-SOAK-LOG.md and in Plan 02's SUMMARY.
- RESEARCH Open Question 3 (`transfer_to_agent` routes to SequentialAgent) is formally resolved YES for both `onboarding_agent` and `external_knowledge_agent`, unblocking Plan 01's SequentialAgent shape and Plan 02's SequentialAgent shape simultaneously.
- RESEARCH §5.4 visibility win confirmed: Events tab shows the three external_knowledge branches as separate, interleaved events (the talk-demo backbone).
- Plan 03's AgentTool visibility win confirmed: Events tab shows `featured_resources_agent` as a tool call for flagship-content queries.
- Baseline P95 captured from Cloud Logging over last 7 days BEFORE deploy (Task 2 Part A) and recorded in `06-04-SOAK-LOG.md`.
- `cwa-assistant-bot` deployed to Cloud Run with the refactored image (Task 2 Part B); traffic moved to the new revision; healthcheck green.
- 24h soak completed (Task 3) with 3 checkpoint observations (T+1h, T+6h, T+24h) and a Final Decision section in `06-04-SOAK-LOG.md`.
- **AGENT-PAR-02 gate (≥40% P95 drop):** AGENT-PAR-02 is GREEN only if T+24h P95 drop ≥40% on multi-source external_knowledge queries vs. baseline. A measured drop <40% requires explicit user override + an explicit handoff task tracked in `06-04-SOAK-LOG.md` AND a new entry in `.planning/STATE.md` "Next Moves" pointing to a Phase 7/8 latency-investigation task. The override does NOT auto-pass AGENT-PAR-02; it converts the phase to "PARTIAL-SHIP (carry-over to next phase)" — AGENT-PAR-02 is marked DEFERRED in the milestone tracker, not GREEN. Acceptable Task 3 outcomes: SHIP (≥40%), PARTIAL-SHIP (<40% with user override + STATE.md handoff entry committed), or ROLLBACK (regression). Bare "DISCUSS — accept the smaller win" without a STATE.md handoff entry is NOT a valid path.
- Privacy invariant preserved: no new logging added by Phase 6, HMAC-hashed user_id still emitted via `discord_bot/usage_metrics.py` unchanged (RESEARCH §5.3 + CONTEXT.md `## Implementation Decisions > Deployment + soak` line 52).
- No regression on the HMAC-hashed usage metrics dashboard.
- `06-04-SOAK-LOG.md` committed to git as the formal soak artifact (atomic commit: `docs(phase-06): record 24h soak — P95 dropped ZZ%, AGENT-PAR-02 met`).
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SUMMARY.md` per standard summary template. Include:
- The final `adk web` smoke verdict (welcome-last OR welcome-first), formally resolving RESEARCH Open Question 2.
- The resolution of RESEARCH Open Question 3 (`transfer_to_agent` routes to SequentialAgent — YES/NO; if NO, what fallback was applied).
- Baseline P95 (with sample size + time range) — preserved for Phase 7/8 to compare future regressions.
- Post-deploy P95 at T+24h — the AGENT-PAR-02 outcome number.
- Final SHIP / PARTIAL-SHIP / ROLLBACK decision and the underlying reasoning. If PARTIAL-SHIP, also surface: AGENT-PAR-02 marked DEFERRED, the STATE.md handoff entry path, and the user's override resume-signal verbatim.
- Cloud Run revision name that landed (e.g., `cwa-assistant-bot-phase6-00012`) — for traceability when Phase 7 starts attaching callbacks to the same tree.
- Reference back to the 3 prior plan SUMMARYs (Plans 01-03) — Phase 6's full surface is the union.
- Cross-phase contract handoff to Phase 7: the post-Phase-6 agent tree (with output_key writers + AgentTool wrap + ParallelAgent fan-out) is exactly the tree Phase 7's lifecycle callbacks will instrument. Note where Phase 7's `before_/after_agent_callback` will attach (per leaf researcher + per onboarding child + per synthesizer + content_agent + wrapped featured_resources_agent).
- Cross-phase contract handoff to Phase 8: `output_key` namespace lock from `.planning/milestones/v7.0-ROADMAP.md ## Cross-Phase Contracts` is now realized in production — `user_skill_level`, `user_goals`, `gh_result`, `devto_result`, `so_result` exist as session.state keys. Phase 8's `mentor_intake_clarifier` will write `user_timezone`, `user_focus_area`, `next_clarifying_question` on top of this base.
</output>
