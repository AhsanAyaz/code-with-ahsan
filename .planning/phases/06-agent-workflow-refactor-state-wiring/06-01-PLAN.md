---
phase: 06-agent-workflow-refactor-state-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/community_assistant/sub_agents/external_knowledge/__init__.py
  - agent/community_assistant/sub_agents/external_knowledge/_shapes.py
  - agent/community_assistant/sub_agents/external_knowledge/gh_researcher.py
  - agent/community_assistant/sub_agents/external_knowledge/devto_researcher.py
  - agent/community_assistant/sub_agents/external_knowledge/so_researcher.py
  - agent/community_assistant/sub_agents/external_knowledge/synthesizer.py
  - agent/community_assistant/sub_agents/external_knowledge_agent.py
  - agent/community_assistant/agent.py
  - agent/tests/test_external_knowledge_fan_out.py
  - agent/tests/test_external_knowledge_agent.py
autonomous: true
requirements:
  - AGENT-PAR-01
  - AGENT-PAR-02

must_haves:
  truths:
    - "The three upstream API calls (GitHub, dev.to, Stack Overflow) start concurrently rather than sequentially on a 3-source query."
    - "Each researcher branch writes its result to a distinct top-level session.state key (gh_result, devto_result, so_result)."
    - "An unhandled httpx error in one branch does NOT crash the other branches or the synthesizer — it surfaces as a status='error' payload that the synthesizer narrates."
    - "The synthesizer LlmAgent reads all three result keys via {gh_result?} / {devto_result?} / {so_result?} templating and produces one unified, cited reply."
    - "Root_agent still routes external/trending/dev.to/Stack Overflow queries to a sub_agent named 'external_knowledge_agent' — no coordinator-instruction text change."
  artifacts:
    - path: "agent/community_assistant/sub_agents/external_knowledge/__init__.py"
      provides: "Re-exports the composite external_knowledge_agent (SequentialAgent) for import by agent/community_assistant/agent.py"
      exports: ["external_knowledge_agent"]
    - path: "agent/community_assistant/sub_agents/external_knowledge/gh_researcher.py"
      provides: "gh_researcher LlmAgent + search_github_repos httpx tool + _GITHUB_CLIENT module singleton; output_key='gh_result'"
      exports: ["gh_researcher", "search_github_repos"]
    - path: "agent/community_assistant/sub_agents/external_knowledge/devto_researcher.py"
      provides: "devto_researcher LlmAgent + search_devto_articles httpx tool (with internal tag→top=7 fallback preserved); output_key='devto_result'"
      exports: ["devto_researcher", "search_devto_articles"]
    - path: "agent/community_assistant/sub_agents/external_knowledge/so_researcher.py"
      provides: "so_researcher LlmAgent + search_stackoverflow_questions httpx tool; output_key='so_result'"
      exports: ["so_researcher", "search_stackoverflow_questions"]
    - path: "agent/community_assistant/sub_agents/external_knowledge/synthesizer.py"
      provides: "external_knowledge_synthesizer LlmAgent that reads {gh_result?}/{devto_result?}/{so_result?} and emits ranked/deduped reply with source labels"
      exports: ["external_knowledge_synthesizer"]
    - path: "agent/community_assistant/sub_agents/external_knowledge/_shapes.py"
      provides: "Shared upstream-JSON → LLM-friendly dict shape helpers (_shape_github_repo, _shape_devto_article, _shape_stackoverflow_question) extracted from the old monolith"
      exports: ["_shape_github_repo", "_shape_devto_article", "_shape_stackoverflow_question"]
    - path: "agent/tests/test_external_knowledge_fan_out.py"
      provides: "pytest module: topology assertions + per-leaf tool shape tests + state-shape assertion after Runner-driven dispatch"
      contains: "test_topology_is_sequential_of_parallel_then_synthesizer"
  key_links:
    - from: "agent/community_assistant/agent.py"
      to: "agent/community_assistant/sub_agents/external_knowledge/__init__.py"
      via: "import (root_agent.sub_agents list)"
      pattern: "from \\.sub_agents\\.external_knowledge import external_knowledge_agent"
    - from: "agent/community_assistant/sub_agents/external_knowledge/__init__.py"
      to: "ParallelAgent[gh_researcher, devto_researcher, so_researcher]"
      via: "SequentialAgent.sub_agents[0]"
      pattern: "ParallelAgent\\(name=\"ExternalFanOut\""
    - from: "external_knowledge_synthesizer.instruction"
      to: "session.state keys gh_result / devto_result / so_result"
      via: "{gh_result?} / {devto_result?} / {so_result?} templating"
      pattern: "\\{gh_result\\?\\}.*\\{devto_result\\?\\}.*\\{so_result\\?\\}"
---

<objective>
Refactor the 289-line monolithic `external_knowledge_agent.py` (single `LlmAgent` with three sequential httpx tools) into a `SequentialAgent[ParallelAgent[gh_researcher, devto_researcher, so_researcher], external_knowledge_synthesizer]` composition that fans out the three upstream API calls concurrently and synthesizes their results.

Purpose: Closes AGENT-PAR-01 (the structural change) and unlocks AGENT-PAR-02 (the ~50% P95 latency drop measured in Plan 04's 24h soak). Each leaf researcher writes a distinct `output_key` (`gh_result`, `devto_result`, `so_result`) — the synthesizer reads all three via `{key?}` instruction templating to produce one unified reply.

Output: A new `external_knowledge` package (4 LlmAgents + 1 shapes helper + composite wiring), an updated `root_agent.sub_agents` import, the deprecated 289-line monolith deleted, and a new pytest file with topology + per-leaf tool tests. Mirrors the canonical ADK pattern from `/Users/amu1o5/personal/ai-agents-google-adk/3-multi-model/agent.py:61-89` (RESEARCH §1.2). No callbacks, no session-service changes — purely a topology refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-ROADMAP.md
@.planning/phases/06-agent-workflow-refactor-state-wiring/06-CONTEXT.md
@.planning/phases/06-agent-workflow-refactor-state-wiring/06-RESEARCH.md

@agent/community_assistant/agent.py
@agent/community_assistant/sub_agents/external_knowledge_agent.py
@agent/tests/test_external_knowledge_agent.py
@agent/tests/conftest.py

<interfaces>
<!-- Critical ADK API surface for this plan. Use the long-form import for AgentTool-adjacent imports (RESEARCH §3.1, §5.8) — even though Plan 01 doesn't use AgentTool, follow the same defensive import discipline across the milestone. -->

From `google.adk.agents`:
- `LlmAgent(name, model, description, instruction, tools=[...], output_key="...")` — leaf agent type. `output_key` writes the agent's final response text into `session.state[output_key]` (RESEARCH §2.1).
- `ParallelAgent(name, description, sub_agents=[...])` — runs all sub_agents concurrently via asyncio.gather (RESEARCH §1.5). No `model` / `instruction` / `tools` — workflow agent only.
- `SequentialAgent(name, description, sub_agents=[...])` — runs sub_agents in order. Same no-model/instruction/tools restriction.

From the existing `external_knowledge_agent.py` (lines 31-67) — the three shape helpers that move into `_shapes.py` verbatim:
- `_shape_github_repo(raw: dict) -> dict` (returns: name, url, description, stars, language, updated_at, topics)
- `_shape_devto_article(raw: dict) -> dict` (returns: title, url, description, tags, reactions, published_at, author)
- `_shape_stackoverflow_question(raw: dict) -> dict` (returns: title, url, score, answer_count, is_answered, tags, asked, asker — note `html.unescape` on title)

From `external_knowledge_agent.py:75-241` — the three tool function contracts that move 1-per-leaf:
- `search_github_repos(query: str) -> dict` — calls `_GITHUB_CLIENT.get("/search/repositories", params={"q","sort","order","per_page"})`, swallows `httpx.HTTPStatusError | httpx.RequestError`, returns `{status, query, count, repos}` on success and `{status:"error", message, repos:[]}` on failure. Reads `GITHUB_TOKEN` env var and adds `Authorization: Bearer …` header when present.
- `search_devto_articles(query: str) -> dict` — calls `_DEVTO_CLIENT.get("/api/articles", params={"per_page","tag"})`. Includes the **multi-word fallback** (lines 159-183 of current file): when the primary tag-search returns `[]` AND `" " in q`, calls a second time with `params={"per_page": 20, "top": "7"}` and client-side filters by substring on `title` / `tag_list`. The fallback exception path logs a warning (`_logger.warning`) and degrades to empty rather than flipping the primary success to error. **All of this stays inside `devto_researcher.py` — no separate fallback agent.**
- `search_stackoverflow_questions(query: str) -> dict` — calls `_STACKOVERFLOW_CLIENT.get("/2.3/search/advanced", params={"order","sort","q","site","pagesize","answers","filter"})`. Same error-swallow discipline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — write failing pytest module test_external_knowledge_fan_out.py</name>
  <files>agent/tests/test_external_knowledge_fan_out.py</files>
  <action>
Create `agent/tests/test_external_knowledge_fan_out.py` per the v6.0 RED/GREEN pattern (RESEARCH §4 Validation Architecture). Tests must FAIL on first run because the new `external_knowledge` package does not exist yet. Imports use the new path `from community_assistant.sub_agents.external_knowledge import external_knowledge_agent, ...`.

Required test cases (mirror existing pytest fixture patterns from `agent/tests/conftest.py` and `agent/tests/test_external_knowledge_agent.py`; do NOT delete the old test file in this task — Task 4 handles that):

1. `test_topology_is_sequential_of_parallel_then_synthesizer` — import `external_knowledge_agent` from the new package path, assert it is `SequentialAgent`, assert `len(external_knowledge_agent.sub_agents) == 2`, assert `external_knowledge_agent.sub_agents[0]` is `ParallelAgent` with `.name == "ExternalFanOut"`, assert that ParallelAgent has exactly 3 children with names `"gh_researcher"`, `"devto_researcher"`, `"so_researcher"`, assert `external_knowledge_agent.sub_agents[1].name == "external_knowledge_synthesizer"`, assert `external_knowledge_agent.name == "external_knowledge_agent"` (preserved routable name per RESEARCH §1.3).

2. `test_leaf_output_keys_are_distinct_and_correct` — import each leaf, assert `gh_researcher.output_key == "gh_result"`, `devto_researcher.output_key == "devto_result"`, `so_researcher.output_key == "so_result"` (RESEARCH §2.5 namespace lock).

3. `test_synthesizer_instruction_reads_all_three_state_keys` — read `external_knowledge_synthesizer.instruction` (string substring assertion), assert it contains literally `"{gh_result?}"` AND `"{devto_result?}"` AND `"{so_result?}"` (the `?` form is mandatory per RESEARCH §1.6 and §2.2). Reject the non-`?` form with the same assertion: `"{gh_result}"` standalone must NOT appear (i.e., every state ref must carry `?`).

4. `test_gh_shape_via_fixture` — reuse existing fixture `external_github.json` (already in `agent/tests/fixtures/`). Monkeypatch the new module's `_GITHUB_CLIENT.get` to return that fixture wrapped in `httpx.Response(200, json=...)`. Call `search_github_repos("angular")` from `community_assistant.sub_agents.external_knowledge.gh_researcher`. Assert `result["status"] == "success"` and every `repo["url"]` starts with `"https://github.com/"`. (Direct port of the existing happy-path test against the new module.)

5. `test_devto_shape_via_fixture` — same pattern with `external_devto.json` and `_DEVTO_CLIENT`. Assert `"dev.to" in article["url"]`.

6. `test_so_shape_via_fixture` — same pattern with `external_stackoverflow.json` and `_STACKOVERFLOW_CLIENT`. Assert `"stackoverflow.com" in question["url"]`.

7. `test_gh_swallows_httpx_error_returns_status_error` — monkeypatch `_GITHUB_CLIENT.get` to raise `httpx.ConnectError("dns failed")`. Call `search_github_repos("angular")`. Assert `result["status"] == "error"` and `result["repos"] == []`. (Critical: this is the AGENT-PAR-01 fail-fast pitfall guard per RESEARCH §1.5 — each leaf MUST swallow upstream errors so ParallelAgent does not crash on one branch failure.)

8. `test_devto_swallows_httpx_error_returns_status_error` — same as #7 for dev.to.

9. `test_so_swallows_httpx_error_returns_status_error` — same as #7 for SO.

10. `test_devto_multi_word_fallback_preserved` — port the existing `test_search_devto_articles_fallback_filters_top_articles` test from `test_external_knowledge_agent.py:198-222` against the new module path. The dev.to fallback (tag→top=7 with client-side substring filter) lives inside `devto_researcher.py` — no separate fallback agent (CONTEXT.md `## Implementation Decisions > Topology`: "One specialist per upstream API. … dev.to's tag→top=7 fallback stays internal to that specialist").

11. `test_gh_uses_github_token_when_set` — port the existing `test_search_github_repos_uses_auth_header_when_token_present` test (lines 134-146 of the old test file) verbatim against the new module path. `GITHUB_TOKEN` env-var read must move with the function (RESEARCH §Runtime State Inventory: "env var lookup is preserved verbatim").

12. `test_fan_out_executes_three_leaves_concurrently` — **CRITICAL: this is the ONLY test that delivers AGENT-TEST-01's "all 3 invoked in a single event loop tick" clause.** Without it, AGENT-TEST-01's concurrent-invocation requirement is silently relaxed to topology-only proof. Pattern per RESEARCH §4.4 lines 601-611 (the Side-effect ordering test, formalized):

    ```python
    import asyncio, time
    import pytest
    from google.adk.runners import Runner
    from google.adk.sessions import InMemorySessionService
    from google.genai import types
    from community_assistant.sub_agents.external_knowledge import external_knowledge_agent

    @pytest.mark.asyncio
    async def test_fan_out_executes_three_leaves_concurrently(monkeypatch):
        """Drives the SequentialAgent via Runner + InMemorySessionService.
        Monkeypatches each leaf's httpx client .get() with a timestamp-recording stub.
        Asserts (a) all 3 result keys appear in session.state AND
        (b) max(t1,t2,t3) - min(t1,t2,t3) < 100ms — proving parallel dispatch."""

        from community_assistant.sub_agents.external_knowledge import (
            gh_researcher as gh_mod, devto_researcher as devto_mod, so_researcher as so_mod,
        )

        timestamps: dict[str, float] = {}

        def _make_stub(label: str, payload: dict):
            def _stub(path, params=None, headers=None):
                # Record monotonic timestamp at the moment THIS branch fires its outbound call.
                timestamps[label] = time.monotonic()
                # Tiny sleep simulates I/O so a sequential dispatcher would space them apart > 100ms.
                time.sleep(0.05)
                # Return a minimal valid httpx.Response shape — adapt to _ok_response helper.
                return _ok_response(payload, "https://example.test")
            return _stub

        monkeypatch.setattr(gh_mod._GITHUB_CLIENT, "get", _make_stub("gh", {"items": []}))
        monkeypatch.setattr(devto_mod._DEVTO_CLIENT, "get", _make_stub("devto", []))
        monkeypatch.setattr(so_mod._STACKOVERFLOW_CLIENT, "get", _make_stub("so", {"items": []}))

        session_service = InMemorySessionService()
        runner = Runner(agent=external_knowledge_agent, app_name="test_app", session_service=session_service)
        session = await session_service.create_session(app_name="test_app", user_id="u1", session_id="s1")
        content = types.Content(role="user", parts=[types.Part(text="trending angular repos and articles")])

        async for _event in runner.run_async(user_id="u1", session_id="s1", new_message=content):
            pass  # drain the event stream

        # (a) all three branches populated session.state
        updated = await session_service.get_session(app_name="test_app", user_id="u1", session_id="s1")
        assert "gh_result" in updated.state
        assert "devto_result" in updated.state
        assert "so_result" in updated.state

        # (b) concurrent dispatch — the three stub calls were made within 100ms of each other.
        assert len(timestamps) == 3, f"expected 3 branch timestamps, got {sorted(timestamps)}"
        spread_ms = (max(timestamps.values()) - min(timestamps.values())) * 1000
        assert spread_ms < 100, f"branches dispatched {spread_ms:.1f}ms apart (>100ms = sequential, not parallel)"
    ```

    Pattern source: RESEARCH §4.4 lines 605-609 explicitly names this as approach (1) "side-effect ordering test … monkeypatch each leaf's tool with a function that records its call timestamp into a shared list, then run the SequentialAgent via Runner and assert the three timestamps are within e.g. 100ms of each other (`max - min < 0.1`)". RESEARCH §4.4 line 611 notes that "really did run in parallel" is also confirmed by the soak's latency drop — but the unit-level proof lives here in test 12. Test 12 is the ONLY automated assertion of AGENT-TEST-01's concurrent-invocation clause; topology test (test 1) is attribute introspection only and does NOT prove concurrency.

    Notes:
    - Uses the timestamp-spread approximation, not literal "same event-loop tick" introspection (which is not practically observable from userspace). The 100ms threshold is generous — sequential dispatch with `time.sleep(0.05)` per branch would space them ≥50ms apart, often >150ms with overhead; parallel dispatch via `asyncio.gather` clusters all three within ~5ms.
    - The `time.sleep(0.05)` inside `_stub` is deliberate: it forces a sequential dispatcher to fail the assertion. If the test passes with the sleep present, `ParallelAgent` is actually parallelizing as claimed.
    - If the `_ok_response` helper signature differs from what the stubs need, adapt — the assertion (timestamps spread + 3 state keys) is the contract.
    - Marked `@pytest.mark.asyncio` per `agent/pyproject.toml`'s `asyncio_mode="auto"` (RESEARCH §4.1) — the runner is async; the test must be too.

Use the `_ok_response(json_data, url)` helper pattern from the existing `test_external_knowledge_agent.py:29-32` (constructs an `httpx.Response` with a `request` attached so `raise_for_status()` works under monkeypatch). Copy that helper into the new file.

DO NOT modify any production code in this task. The point is that these tests fail at the import statement and then pass after Task 2-3 land. Commit before Task 2.

Per CONTEXT.md `## Implementation Decisions > Code organization`: prefer per-workstream atomic commits — the failing tests AND the implementation in Tasks 2-3 can land in the same commit if the executor finds it tighter, but the test-first ordering MUST be preserved (RED → GREEN; see RESEARCH §Validation Architecture > Wave 0 Gaps).
  </action>
  <verify>
    <automated>cd agent && pytest tests/test_external_knowledge_fan_out.py -x 2>&1 | grep -E "(ModuleNotFoundError|ImportError|collected|FAILED|PASSED)" | head -20</automated>
  </verify>
  <done>`agent/tests/test_external_knowledge_fan_out.py` exists with 12 test functions; running it produces import errors or collection failures (because the new package does not exist yet) — this is the RED state per RESEARCH §Validation Architecture. The 12 test names listed in the action are all present (verified by `grep -c "^def test_" agent/tests/test_external_knowledge_fan_out.py | grep 12`).</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — create the external_knowledge package (4 LlmAgents + shapes + composite)</name>
  <files>agent/community_assistant/sub_agents/external_knowledge/__init__.py, agent/community_assistant/sub_agents/external_knowledge/_shapes.py, agent/community_assistant/sub_agents/external_knowledge/gh_researcher.py, agent/community_assistant/sub_agents/external_knowledge/devto_researcher.py, agent/community_assistant/sub_agents/external_knowledge/so_researcher.py, agent/community_assistant/sub_agents/external_knowledge/synthesizer.py</files>
  <action>
Create the new `agent/community_assistant/sub_agents/external_knowledge/` Python package per the RECOMMENDED layout in RESEARCH §Code Examples (lines 668-678). The package layout was selected over the flat layout because it keeps 5 related modules co-located for one logical capability (RESEARCH §Code Examples: "Recommendation: the package layout").

**`_shapes.py`** — extract the three private shape helpers from the old `external_knowledge_agent.py:31-67` verbatim:
- `_shape_github_repo(raw: dict) -> dict` (lines 31-40 of old file)
- `_shape_devto_article(raw: dict) -> dict` (lines 43-53)
- `_shape_stackoverflow_question(raw: dict) -> dict` (lines 56-67) — keep the `html.unescape` import and call.
Module docstring: `"""Upstream-JSON → LLM-friendly dict shape helpers shared by the three external_knowledge researchers."""`. No agent code in this file.

**`gh_researcher.py`** — create:
- Module-level `_GITHUB_CLIENT = httpx.Client(base_url="https://api.github.com", timeout=10.0)` (verbatim from old file line 19).
- Module-level `_RESULT_LIMIT = 5` (verbatim from line 23).
- `search_github_repos(query: str) -> dict` tool function — copy verbatim from old file lines 75-124 with imports updated to use the new `_shapes` module (`from ._shapes import _shape_github_repo`). The `GITHUB_TOKEN` env-var read (lines 99-101) is preserved verbatim per RESEARCH §Runtime State Inventory: "preserve env var names verbatim across the move". Same docstring; same try/except for `httpx.HTTPStatusError, httpx.RequestError` returning `{"status": "error", ...}` — this is the AGENT-PAR-01 fail-fast pitfall guard (RESEARCH §1.5).
- LlmAgent instantiation:
  - `name="gh_researcher"`, `model="gemini-2.5-flash"`, `description="Searches GitHub for repos matching the user query."`, `tools=[search_github_repos]`, `output_key="gh_result"`.
  - Instruction: "Call search_github_repos with the user query as-is. Return a concise summary of the top results. ALWAYS cite the html_url of each repo. If the tool returns status='error', return a brief 'GitHub temporarily unavailable' acknowledgement so the synthesizer can narrate the partial result." (mirrors RESEARCH §1.4 example).

**`devto_researcher.py`** — same shape as `gh_researcher.py` but for dev.to:
- `_DEVTO_CLIENT = httpx.Client(base_url="https://dev.to", timeout=10.0)`.
- `_logger = logging.getLogger(__name__)` (the multi-word fallback logs warnings, see old file line 17).
- `search_devto_articles(query: str) -> dict` — copy verbatim from old file lines 127-191, INCLUDING the tag→top=7 multi-word fallback block (lines 159-183) AND the warning-log on fallback failure. CONTEXT.md `## Implementation Decisions > Topology` mandates this stays in the dev.to specialist: "One specialist per upstream API. … dev.to's tag→top=7 fallback stays internal to that specialist — no separate fallback agent."
- LlmAgent: `name="devto_researcher"`, `output_key="devto_result"`, same instruction shape as gh_researcher but referencing dev.to articles and citing the article URL.

**`so_researcher.py`** — same shape:
- `_STACKOVERFLOW_CLIENT = httpx.Client(base_url="https://api.stackexchange.com", timeout=10.0)`.
- `search_stackoverflow_questions(query: str) -> dict` — copy verbatim from old file lines 194-241.
- LlmAgent: `name="so_researcher"`, `output_key="so_result"`, instruction references Stack Overflow questions and cites the question URL.

**`synthesizer.py`** — RESEARCH §1.6 (lines 226-253) provides the canonical instruction template. Use it verbatim. LlmAgent fields:
- `name="external_knowledge_synthesizer"`, `model="gemini-2.5-flash"`.
- `description="Merges GitHub, dev.to, and Stack Overflow results into a unified, cited reply."`
- `instruction` (multi-line — copy RESEARCH §1.6 lines 235-249 verbatim; the only mandatory parts are the three `{gh_result?}` / `{devto_result?}` / `{so_result?}` substrings on separate lines under a "Sources" header, the per-branch error-handling rule, and the "ALWAYS cite the source URL" + per-source label rules). The `?` form is mandatory per RESEARCH §2.2 — every state reference in this instruction must carry `?`.
- NO `tools=[...]`, NO `output_key` (synthesizer's output is the user-facing reply; the SequentialAgent surfaces it as final output).

**`__init__.py`** — wires the composite per RESEARCH §1.3 (lines 159-183):
```
from google.adk.agents import ParallelAgent, SequentialAgent

from .gh_researcher import gh_researcher
from .devto_researcher import devto_researcher
from .so_researcher import so_researcher
from .synthesizer import external_knowledge_synthesizer

external_knowledge_fan_out = ParallelAgent(
    name="ExternalFanOut",
    description="Fans out a single user query across GitHub, dev.to, and Stack Overflow.",
    sub_agents=[gh_researcher, devto_researcher, so_researcher],
)

external_knowledge_agent = SequentialAgent(
    name="external_knowledge_agent",  # PRESERVED routable name (RESEARCH §1.3) so root_agent's coordinator instruction does not change.
    description=(
        "Surfaces third-party developer knowledge from GitHub (trending repos), dev.to "
        "(community articles), and Stack Overflow (answered questions). "
        "This is a FALLBACK agent — use mentorship_agent, projects_agent, roadmap_agent, "
        "and content_agent first when the query fits their domain."
    ),
    sub_agents=[external_knowledge_fan_out, external_knowledge_synthesizer],
)

__all__ = ["external_knowledge_agent"]
```

The description string is intentionally similar (not identical) to the old monolith's description (old file lines 251-257) — preserved enough that the coordinator-LLM's routing mental model continues to fire on the same triggers (CONTEXT.md `## Implementation Decisions > Topology`: "Description preserved so coordinator-LLM still routes 'GitHub-at-large / dev.to / Stack Overflow' queries here").

After implementation, all 12 tests from Task 1 must pass: `cd agent && pytest tests/test_external_knowledge_fan_out.py -x` exits 0. **Critical: test 12 (`test_fan_out_executes_three_leaves_concurrently`) is the AGENT-TEST-01 concurrent-invocation gate** — if it fails (timestamp spread >100ms), the ParallelAgent isn't actually parallelizing and the AGENT-PAR-02 P95 drop in Plan 04 will not materialize. Surface the failure to the user before continuing; do not relax the 100ms threshold to make it pass.

DO NOT touch `agent/community_assistant/agent.py` or delete the old monolith in this task — Task 3 handles import switch + delete atomically so the working tree never has two agents with the same name `external_knowledge_agent` registered in `root_agent.sub_agents`.

Atomic commit: `feat(agent): split external_knowledge into ParallelAgent fan-out with synthesizer`. May be folded with Task 1's RED commit per CONTEXT.md `## Implementation Decisions > Code organization` ("Whether to land the refactor in 1 commit per workstream or per-file. Default: per-workstream atomic commits") — executor's call as long as RED → GREEN ordering is preserved in history.
  </action>
  <verify>
    <automated>cd agent && pytest tests/test_external_knowledge_fan_out.py -x</automated>
  </verify>
  <done>`agent/community_assistant/sub_agents/external_knowledge/` package exists with 6 files (__init__.py, _shapes.py, gh_researcher.py, devto_researcher.py, so_researcher.py, synthesizer.py). All 12 tests in `test_external_knowledge_fan_out.py` pass (`pytest tests/test_external_knowledge_fan_out.py -x` exits 0), including test 12 which gates AGENT-TEST-01's concurrent-invocation clause. `python -c "from community_assistant.sub_agents.external_knowledge import external_knowledge_agent; print(type(external_knowledge_agent).__name__)"` prints `SequentialAgent`.</done>
</task>

<task type="auto">
  <name>Task 3: Switch root_agent import, delete old monolith, retire old test file</name>
  <files>agent/community_assistant/agent.py, agent/community_assistant/sub_agents/external_knowledge_agent.py, agent/tests/test_external_knowledge_agent.py</files>
  <action>
Atomic refactor — the working tree must not have two `external_knowledge_agent` definitions at any point.

**1. Update `agent/community_assistant/agent.py`:**
Change line 4 from `from .sub_agents.external_knowledge_agent import external_knowledge_agent` to `from .sub_agents.external_knowledge import external_knowledge_agent` (new package path).

Lines 53-69 (`root_agent = LlmAgent(...)`) remain UNCHANGED — `external_knowledge_agent` is still the name in `sub_agents=[...]`; only the implementation behind the name changes. ROOT_INSTRUCTION (lines 12-51) UNCHANGED per CONTEXT.md `## Implementation Decisions > Topology` ("Root_agent still delegates to this composite via the same `description` so coordinator routing logic doesn't change").

**2. Delete `agent/community_assistant/sub_agents/external_knowledge_agent.py`** — the entire 289-line monolith. CONTEXT.md `## Implementation Decisions > Code organization` mandates this: "Delete or deprecate the monolithic `external_knowledge_agent.py`. No dual-path support, no backwards-compat shim. Root agent's `sub_agents=[...]` list updates atomically with the refactor."

**3. Delete `agent/tests/test_external_knowledge_agent.py`** — the 260-line old test file. Every test case in it has been ported to `test_external_knowledge_fan_out.py` in Task 1 against the new module paths (RESEARCH §Wave 0 Gaps + §4.2 → "agent/tests/test_external_knowledge_fan_out.py — covers AGENT-PAR-01"). Keeping the old file would leave dead imports (`import community_assistant.sub_agents.external_knowledge_agent as mod` — module no longer exists) and break test collection.

**4. Verify nothing else imports the old path:**
```
grep -rn "from .sub_agents.external_knowledge_agent" agent/ || echo "no matches — clean"
grep -rn "community_assistant.sub_agents.external_knowledge_agent" agent/ || echo "no matches — clean"
```
Both must print "no matches — clean". If anything else still imports the old path, surface the file and fail loudly — do NOT silently leave a broken import.

**5. Run the full agent test suite to confirm no regressions on the OTHER agents** (this catches anything that quietly depended on a side-effect of importing the old monolith — e.g., another test importing one of the three shape helpers from the old path): `cd agent && pytest -q`. Should be green.

**6. Local-only smoke test** (NOT verified in CI but called out for the executor before commit, mirrors RESEARCH §Open Question 3 — defers full smoke to Plan 04):
```
cd agent && python -c "
from community_assistant.agent import root_agent
ek = next(a for a in root_agent.sub_agents if a.name == 'external_knowledge_agent')
print('root sub_agents has external_knowledge_agent:', type(ek).__name__)
assert type(ek).__name__ == 'SequentialAgent'
print('OK')
"
```
Expected output: `root sub_agents has external_knowledge_agent: SequentialAgent` + `OK`.

**Open Question 3 flag (RESEARCH §Open Questions): does `transfer_to_agent` from the root LlmAgent successfully route to a SequentialAgent target?** This is a CRITICAL gate but is verified in Plan 04's `adk web` smoke test (not unit-testable here). If Plan 04 finds `transfer_to_agent` does NOT route to SequentialAgent targets, the fallback per RESEARCH §Open Question 3 is to wrap the SequentialAgent in a thin LlmAgent forwarder. Plan 04 owns that decision and any rework.

Atomic commit: `refactor(agent): retire monolithic external_knowledge_agent in favor of ParallelAgent composite` — single commit that adds the import path change + deletes both the old `external_knowledge_agent.py` and `test_external_knowledge_agent.py`. The full test suite must be green at this commit (`pytest -q`).
  </action>
  <verify>
    <automated>cd agent && pytest -q 2>&1 | tail -20 && grep -rn "from .sub_agents.external_knowledge_agent" community_assistant/ || true</automated>
  </verify>
  <done>`agent/community_assistant/sub_agents/external_knowledge_agent.py` and `agent/tests/test_external_knowledge_agent.py` deleted (`ls agent/community_assistant/sub_agents/external_knowledge_agent.py 2>&1 | grep "No such file"` succeeds). `agent/community_assistant/agent.py:4` imports from new package path. Full agent test suite green (`pytest -q` exits 0). No grep matches for the old import path anywhere under `agent/`. Local smoke `python -c` snippet prints `SequentialAgent` + `OK`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → upstream API (httpx) | Each leaf researcher makes outbound httpx calls to github.com / dev.to / stackexchange.com. Untrusted-but-rate-limited public APIs. |
| Upstream API → LLM prompt | Upstream JSON (repo descriptions, article titles, SO question titles) gets shaped + concatenated into a state value the synthesizer LLM reads via templating. Tampering surface = upstream returning malicious markdown / prompt-injection. |
| Parallel branch → branch (state isolation) | Each ParallelAgent child writes to its own `output_key`; no shared writes. Race-condition pitfall (RESEARCH §5.1) avoided by design. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Synthesizer LlmAgent reading upstream-controlled strings via `{gh_result?}` / `{devto_result?}` / `{so_result?}` | mitigate | Existing shape helpers (`_shape_github_repo`, etc.) truncate `description`/`excerpt` to 300 chars (lines 35, 48 of old file). Preserved verbatim in `_shapes.py`. Synthesizer instruction explicitly states "Never invent a URL. Only echo what the upstream branches provided" (RESEARCH §1.6) — narrows prompt-injection blast radius. PII redaction at the model layer (`before_model_callback`) is Phase 7 territory per CONTEXT.md `## Deferred Ideas`; not in scope here. |
| T-06-02 | Denial of Service | A single ParallelAgent branch crashing (unhandled exception) propagating up and killing the synthesizer + the whole user turn (RESEARCH §1.5: "an unhandled exception in any child propagates up and stops the whole ParallelAgent") | mitigate | Each leaf tool's existing try/except for `httpx.HTTPStatusError, httpx.RequestError` returning `{"status": "error", ...}` (Task 2 preserves this verbatim). Tests 7, 8, 9 in Task 1 enforce the pattern under monkeypatched `httpx.ConnectError`. Synthesizer instruction explicitly handles per-branch error payloads ("If a branch returned an error, mention that source is temporarily unreachable and proceed with the others" — RESEARCH §1.6). |
| T-06-03 | Information Disclosure | `GITHUB_TOKEN` value leaking into LLM prompts or Cloud Logging if accidentally moved into instruction strings during the file split | accept | Mitigation pre-existing: the token is only read inside `search_github_repos` and only used to construct an `Authorization` header on the outbound httpx call (lines 99-101 of old file). Move is verbatim per RESEARCH §Runtime State Inventory. Tests 11 of Task 1 covers the header-set path; the token never appears in the function's return dict. No new disclosure surface added. |
| T-06-04 | Repudiation | A user claiming "the bot told me X repo was popular but it isn't" — bot has no audit log of which upstream JSON it served | accept | Pre-existing posture, unchanged by Phase 6 refactor. Cloud Logging emits `query_topic` + `latency_ms` (via `discord_bot/usage_metrics.py`), not full responses. Phase 7's lifecycle callbacks will add per-agent enter/exit logging; not in this phase. RESEARCH §Security Domain explicitly notes Phase 6 has "no new security surface". |
</threat_model>

<verification>
**Per-task verification:**
- Task 1: `cd agent && pytest tests/test_external_knowledge_fan_out.py -x` produces 12 collection items, all failing/erroring on import (RED state).
- Task 2: `cd agent && pytest tests/test_external_knowledge_fan_out.py -x` exits 0 (GREEN — all 12 tests pass, including the concurrent-dispatch assertion).
- Task 3: `cd agent && pytest -q` exits 0 with the full suite green; `ls agent/community_assistant/sub_agents/external_knowledge_agent.py` returns "No such file"; the local-import smoke snippet prints `SequentialAgent` + `OK`.

**Plan-level verification:**
```
cd agent && pytest tests/test_external_knowledge_fan_out.py tests/test_content_agent_blog.py tests/test_content_agent_youtube.py tests/test_mentorship_agent.py tests/test_projects_agent.py tests/test_roadmap_agent.py tests/test_relevance.py tests/test_featured_resources.py tests/test_platform_client.py tests/test_semantic_search_mentors.py tests/test_discord_bot_message_handler.py tests/test_discord_bot_session_mapping.py tests/test_usage_metrics.py -q
```
All green. No reference to `external_knowledge_agent.py` (the old module path) remains anywhere under `agent/`.
</verification>

<success_criteria>
- `agent/community_assistant/sub_agents/external_knowledge/` package exists with the 6 files listed in `files_modified` (`__init__.py`, `_shapes.py`, `gh_researcher.py`, `devto_researcher.py`, `so_researcher.py`, `synthesizer.py`).
- `agent/community_assistant/sub_agents/external_knowledge_agent.py` and `agent/tests/test_external_knowledge_agent.py` are DELETED (CONTEXT.md `## Implementation Decisions > Code organization` mandate).
- `agent/community_assistant/agent.py:4` imports `external_knowledge_agent` from the new package path; `root_agent.sub_agents` still contains an agent named `external_knowledge_agent` whose runtime type is `SequentialAgent`.
- `external_knowledge_agent.sub_agents[0]` is `ParallelAgent(name="ExternalFanOut", sub_agents=[gh_researcher, devto_researcher, so_researcher])`; `external_knowledge_agent.sub_agents[1]` is `external_knowledge_synthesizer` (LlmAgent).
- Each leaf has the correct `output_key`: `gh_researcher.output_key=="gh_result"`, `devto_researcher.output_key=="devto_result"`, `so_researcher.output_key=="so_result"`.
- `external_knowledge_synthesizer.instruction` references `{gh_result?}`, `{devto_result?}`, `{so_result?}` (the `?` form — non-`?` form is forbidden per RESEARCH §2.2).
- Each leaf tool function (`search_github_repos`, `search_devto_articles`, `search_stackoverflow_questions`) swallows `httpx.HTTPStatusError | httpx.RequestError` and returns `{"status": "error", ...}` — verified by tests 7, 8, 9 of Task 1 (AGENT-PAR-01 fail-fast pitfall guard, RESEARCH §1.5).
- dev.to multi-word tag→top=7 fallback lives inside `devto_researcher.py`, not in a separate fallback agent (CONTEXT.md `## Implementation Decisions > Topology`).
- The full `agent/` pytest suite is green (`cd agent && pytest -q` exits 0).
- HMAC privacy invariant NOT replicated in any new sub-agent file (RESEARCH §5.3: "Phase 6 does NOT need to replicate any HMAC pattern inside the new sub-agents — privacy is enforced by the bot's `emit_usage_event(...)` wrapping at the boundary").
- AGENT-TEST-01's concurrent-invocation clause ("all 3 invoked in a single event loop tick") is enforced by test 12 (`test_fan_out_executes_three_leaves_concurrently`) — the Runner-driven test with `time.sleep(0.05)` per stubbed branch + `max-min < 100ms` timestamp-spread assertion. Pattern per RESEARCH §4.4 lines 601-611.
- AGENT-PAR-02 (≥40% P95 latency drop) is NOT verified in this plan — it is measured in Plan 04's 24h prod soak. This plan only ships the structural change; the latency win is the downstream effect.
- Open Question 3 (does `transfer_to_agent` route to SequentialAgent?) is FLAGGED for Plan 04's `adk web` smoke test, not gated here.
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-workflow-refactor-state-wiring/06-01-SUMMARY.md` per the standard summary template. Include:
- Which leaf modules were created and their `output_key` values (the cross-phase contract for Phase 7/8).
- Whether the package layout (RESEARCH §Code Examples recommended) or flat layout was used.
- Whether all three error-path tests (7, 8, 9 in Task 1) passed without modification — confirming the AGENT-PAR-01 fail-fast pitfall guard is in place.
- The observed timestamp spread on test 12 (`test_fan_out_executes_three_leaves_concurrently`) — the actual `max-min` ms value, for Phase 7/8 traceability. A spread well under 100ms (e.g., <10ms) is the expected outcome on healthy ParallelAgent dispatch.
- Any deviations from the canonical reference at `/Users/amu1o5/personal/ai-agents-google-adk/3-multi-model/agent.py:61-89` and why.
- Confirmation that `agent.py:4` is the ONLY line in production code that changed under `community_assistant/` outside the new package — i.e., the root_agent's instruction and sub_agents list remain semantically identical (CONTEXT.md `## Implementation Decisions > Topology` "Description preserved").
</output>
