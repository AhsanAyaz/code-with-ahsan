---
phase: 16-homepage-redesign
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/home/SocialReachBar.tsx
  - src/components/home/FounderCredibility.tsx
  - src/app/page.tsx
autonomous: false
requirements: [HOME-04, HOME-05, HOME-06, HOME-07]

must_haves:
  truths:
    - "Visitor sees social reach bar with YouTube, Instagram, Facebook, LinkedIn, GitHub, X, Discord follower counts"
    - "Visitor sees Ahsan's founder credibility section with photo, GDE badge, and brief community-founder bio positioned below community content"
    - "Visitor sees a newsletter signup section on the homepage"
    - "Visitor sees a community FAQ section on the homepage"
    - "Full homepage renders top-to-bottom: banners, hero, pillars, stats, social reach, founder, newsletter, FAQ"
  artifacts:
    - path: "src/components/home/SocialReachBar.tsx"
      provides: "Social platform follower counts display"
      min_lines: 30
    - path: "src/components/home/FounderCredibility.tsx"
      provides: "Founder bio section with photo and GDE badge"
      min_lines: 40
    - path: "src/app/page.tsx"
      provides: "Assembled homepage with all new sections"
      min_lines: 40
  key_links:
    - from: "src/components/home/SocialReachBar.tsx"
      to: "/api/stats"
      via: "fetch social data from API or receive as prop"
      pattern: "fetch.*api/stats|socialReach"
    - from: "src/app/page.tsx"
      to: "src/components/home/*"
      via: "imports all new homepage components"
      pattern: "import.*from.*components/home"
    - from: "src/app/page.tsx"
      to: "src/components/HomeFAQ"
      via: "retains existing FAQ component"
      pattern: "import.*HomeFAQ"
    - from: "src/app/page.tsx"
      to: "src/components/NewsletterForm"
      via: "retains existing newsletter component"
      pattern: "import.*NewsletterForm"
---

<objective>
Create social reach bar and founder credibility components, then assemble the complete homepage by wiring all new and existing components into page.tsx.

Purpose: Complete the homepage redesign by adding social proof, founder context, and assembling all sections in the correct community-first order.
Output: Two new components plus a fully restructured page.tsx that replaces the old personal-branding homepage.
</objective>

<execution_context>
@/home/ahsan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-homepage-redesign/16-01-SUMMARY.md

@src/app/page.tsx
@src/components/HomeFAQ.tsx
@src/components/NewsletterForm.tsx
@src/components/HomeBanners.tsx
@src/data/siteMetadata.js
@src/data/socialReach.ts
@src/app/api/stats/route.ts

<interfaces>
<!-- Stats API response shape -->
```typescript
{
  community: { mentors, mentees, activeMentorships, completedMentorships, averageRating };
  social: Record<string, { label: string; count: number; url: string }>;
  // social keys: youtube, instagram, facebook, linkedin, github, x, discord
  cachedAt: string;
}
```

<!-- Social reach config (from src/data/socialReach.ts) -->
```typescript
export type SocialPlatform = { label: string; count: number; url: string };
export const socialReach: Record<string, SocialPlatform>;
// Keys: youtube, instagram, facebook, linkedin, github, x, discord
```

<!-- Existing components to retain -->
```typescript
// src/components/HomeFAQ.tsx - default export, no props
// src/components/NewsletterForm.tsx - default export, no props
// src/components/HomeBanners.tsx - default export, props: { banners: Banner[] }
```

<!-- Plan 01 components (will exist after 16-01 executes) -->
```typescript
// src/components/home/CommunityHero.tsx - default export
// src/components/home/PillarsGrid.tsx - default export
// src/components/home/CommunityStats.tsx - default export, fetches own data (community stats + Discord members)
```

<!-- Existing images -->
<!-- /static/images/avatar.jpeg - Ahsan's photo -->
<!-- /static/images/gde_logo_brackets.png - GDE badge logo -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SocialReachBar and FounderCredibility components</name>
  <files>src/components/home/SocialReachBar.tsx, src/components/home/FounderCredibility.tsx</files>
  <action>
Create `src/components/home/SocialReachBar.tsx` — a "use client" component:
- Fetches social data from `/api/stats` on mount (extract the `social` field from the response)
- Displays a horizontal bar of 7 social platforms (YouTube, Instagram, Facebook, LinkedIn, GitHub, X, Discord) — all entries from the social data
- NOTE: The actual numbers come from the API (which reads socialReach.ts config). The numbers shown depend on what the owner has configured. Display them formatted: e.g. 15000 -> "15k+", 2000 -> "2k+", 500 -> "500+"
- Each platform shows: platform name/icon and formatted follower count
- Each is a link to the platform URL (from API response)
- Use platform-appropriate colors or keep uniform with DaisyUI theme
- Layout: horizontal scrollable on mobile, flex-wrap row on desktop
- Style: compact bar, bg-base-200 or bg-base-100 with subtle border, centered content
- Show loading skeleton while fetching, hide on error

Create `src/components/home/FounderCredibility.tsx` — a component (can be server or client):
- Section positioned BELOW community content (pillars, stats, social) but ABOVE newsletter/FAQ
- Left side: Ahsan's photo using Next.js Image component, src="/static/images/avatar.jpeg", rounded
- Right side: GDE badge image (src="/static/images/gde_logo_brackets.png"), name "Muhammad Ahsan Ayaz", brief bio as community FOUNDER (not resume):
  - "Google Developer Expert in Angular & AI. Author, speaker, and the founder of Code With Ahsan — a community of 4,500+ developers learning and building together."
  - This is NOT a resume. Keep it 2-3 sentences focused on community founding.
- Link to /about page: "Learn more about Ahsan" or similar
- Layout: side-by-side on lg (photo left, text right), stacked on mobile (photo on top)
- Style: bg-base-100 section with subtle styling, DaisyUI theme classes

Both must pass `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <done>SocialReachBar displays 7 social platform counts (including Discord) from API. FounderCredibility shows Ahsan's photo, GDE badge, and community-founder bio. Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble complete homepage in page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Rewrite `src/app/page.tsx` to assemble the new community-first homepage. Section order top to bottom:

1. **HomeBanners** — keep existing banner section (conditional on NEXT_PUBLIC_SHOW_BANNERS)
2. **CommunityHero** — new community-first hero (replaces old Hero import)
3. **PillarsGrid** — new 5-pillar grid (replaces old Features import)
4. **CommunityStats** — new live stats section
5. **SocialReachBar** — new social follower counts
6. **FounderCredibility** — new founder section (below community content)
7. **Newsletter** — keep existing newsletter section with NewsletterForm (retain the current wrapper markup with heading "Join the Community" and description)
8. **HomeFAQ** — keep existing FAQ component

Remove imports for old `Hero` and `Features` components. Import new components from `@/components/home/`.

Keep the existing metadata export (title, description, openGraph, twitter) — update the title/description if needed to be more community-focused:
- Title: "Code With Ahsan | Developer Community for Mentorship, Projects & Learning"
- Description: "Join 4,500+ developers in a community built around mentorship, real-world projects, learning roadmaps, courses, and books."

Keep the `getHomeBanners()` server function and `async` page component pattern.

The page is a server component. Client components (CommunityHero, PillarsGrid, CommunityStats, SocialReachBar) handle their own "use client" declarations.

Must pass `npx tsc --noEmit` AND `npm run build` successfully.
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>page.tsx imports and renders all sections in correct order. Build succeeds. Old Hero and Features imports removed. Homepage shows community-first content flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete homepage</name>
  <action>
User visually verifies the complete homepage redesign. All automated work is done in Tasks 1-2.

What was built: Complete homepage redesign with community-first hero, 5 pillar cards, live stats from API (including Discord members), social reach bar, founder credibility section, newsletter, and FAQ — all assembled in page.tsx.

How to verify:
1. Run `npm run dev` and visit http://localhost:3000
2. Verify hero section shows community messaging (NOT "Build with Precision" or personal branding)
3. Verify hero has a "Join the Community" (or similar) CTA button
4. Scroll down — verify 5 pillar cards visible: Mentorship, Projects, Roadmaps, Courses, Books
5. Click each pillar card link — verify they navigate to correct pages
6. Verify live stats section shows numbers (mentors, mentees, mentorships, rating, Discord members)
7. Verify social reach bar shows platform names with follower counts (including Discord)
8. Verify founder section shows Ahsan's photo, GDE badge, and brief community-founder bio
9. Verify newsletter signup section is present
10. Verify FAQ section is present at the bottom
11. Test mobile view (resize browser to ~375px) — verify all sections stack properly

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>User confirms visual correctness of all homepage sections</verify>
  <done>User approves the homepage layout, content order, and visual appearance across desktop and mobile</done>
</task>

</tasks>

<verification>
Complete homepage renders all required sections:
```bash
npm run build
# Then verify the page loads at localhost:3000
```
</verification>

<success_criteria>
- Social reach bar displays 7 platform follower counts (including Discord) from /api/stats
- Founder section shows photo, GDE badge, community-founder bio (not resume)
- page.tsx assembles all sections in community-first order
- Newsletter and FAQ sections present on the page
- Build passes with zero errors
- Visual verification confirms community-first homepage
</success_criteria>

<output>
After completion, create `.planning/phases/16-homepage-redesign/16-02-SUMMARY.md`
</output>
