---
phase: 16-homepage-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/CommunityHero.tsx
  - src/components/home/PillarsGrid.tsx
  - src/components/home/CommunityStats.tsx
  - src/data/socialReach.ts
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "Visitor sees a community-named hero with tagline and join CTA — no personal branding in the hero"
    - "Visitor sees all five community pillars (Mentorship, Projects, Roadmaps, Courses, Books) with descriptions and working links"
    - "Visitor sees live community stats (mentor count, mentee count, active/completed mentorships, avg rating, Discord members) pulled from /api/stats"
  artifacts:
    - path: "src/components/home/CommunityHero.tsx"
      provides: "Community-first hero section"
      min_lines: 40
    - path: "src/components/home/PillarsGrid.tsx"
      provides: "Five-pillar navigation grid"
      min_lines: 50
    - path: "src/components/home/CommunityStats.tsx"
      provides: "Live stats display fetched from API"
      min_lines: 40
  key_links:
    - from: "src/components/home/CommunityStats.tsx"
      to: "/api/stats"
      via: "fetch in useEffect or SWR"
      pattern: "fetch.*api/stats"
    - from: "src/components/home/CommunityStats.tsx"
      to: "social.discord"
      via: "extract Discord member count from API social data"
      pattern: "social.*discord|discord.*count"
    - from: "src/components/home/PillarsGrid.tsx"
      to: "pillar pages"
      via: "Next.js Link hrefs"
      pattern: "href=.*/mentorship|/projects|/roadmaps|/courses|/books"
---

<objective>
Create the three core above-the-fold homepage components: community-first hero, five-pillar navigation grid, and live community stats display.

Purpose: Replace the personal-branding hero with a community-identity landing experience that shows activity and directs visitors to pillars.
Output: Three new components in src/components/home/ ready to be wired into page.tsx by Plan 02. Updated socialReach.ts with Discord entry.
</objective>

<execution_context>
@/home/ahsan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/api/stats/route.ts
@src/data/socialReach.ts
@src/components/Hero.tsx
@src/components/Features.tsx

<interfaces>
<!-- Stats API response shape (from src/app/api/stats/route.ts) -->
```typescript
// GET /api/stats returns:
{
  community: {
    mentors: number;
    mentees: number;
    activeMentorships: number;
    completedMentorships: number;
    averageRating: number;
  };
  social: Record<string, { label: string; count: number; url: string }>;
  // social keys currently: youtube, instagram, facebook, linkedin, github, x
  // Task 2 will add 'discord' key to socialReach.ts
  cachedAt: string;
}
```

<!-- Existing DaisyUI theme classes used across the site -->
<!-- bg-base-100, bg-base-200, bg-base-300, text-base-content, text-primary, text-secondary, text-accent, btn-primary, btn-outline, card, card-body -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CommunityHero and PillarsGrid components</name>
  <files>src/components/home/CommunityHero.tsx, src/components/home/PillarsGrid.tsx</files>
  <action>
Create `src/components/home/CommunityHero.tsx` — a "use client" component:
- Large heading: "Join 4,500+ Developers" (or similar community-scale messaging)
- Subheading: community name "Code With Ahsan" and a tagline about learning, mentorship, and building together
- Primary CTA button: "Join the Community" linking to /community or Discord
- Secondary CTA: "Explore Mentorship" linking to /mentorship
- Keep the existing DaisyUI theme (bg-base-100, text-base-content, btn-primary, etc.)
- Use subtle background effects similar to current Hero.tsx (grid pattern, gradient orbs) but NO personal branding, NO code block, NO "Build with Precision", NO Ahsan's name in the hero
- Mobile responsive: stack CTAs vertically on small screens
- Do NOT use lucide icons Terminal/Code/Cpu stat counters from old hero — those are personal branding

Create `src/components/home/PillarsGrid.tsx` — a component displaying 5 community pillars:
- Mentorship: "Get matched with experienced developers for 1-on-1 guidance" -> /mentorship
- Projects: "Collaborate on real-world projects with structured team support" -> /projects
- Roadmaps: "Follow curated learning paths from beginner to advanced" -> /roadmaps
- Courses: "In-depth video courses on Angular, React, and modern web dev" -> /courses
- Books: "Comprehensive guides and cookbooks for web development" -> /books
- Use DaisyUI card components with hover effects (similar style to current Features.tsx cards)
- Grid layout: 3 columns on lg (top row: 3, bottom row: 2 centered), 2 columns on md, 1 on mobile
- Each card has an icon (use lucide-react icons: Users, FolderGit2, Map, BookOpen, BookText), title, description, and arrow link indicator
- Section heading: "Community Pillars" or "What We Do" with brief subtitle

Both components must pass `npx tsc --noEmit` with zero errors.
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <done>CommunityHero.tsx renders community-first hero with no personal branding. PillarsGrid.tsx renders 5 pillar cards with descriptions and links. Both compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add Discord to socialReach config and create CommunityStats component with live API data</name>
  <files>src/data/socialReach.ts, src/components/home/CommunityStats.tsx</files>
  <action>
First, add a Discord entry to `src/data/socialReach.ts`:
- Add `discord: { label: "Discord", count: 500, url: "https://discord.gg/codewithahsan" }` to the socialReach record (use a reasonable placeholder count; the owner can update the number later without code changes). Place it after the `x` entry.

Then create `src/components/home/CommunityStats.tsx` — a "use client" component that:
- Fetches from `/api/stats` on mount using fetch + useEffect (or a simple useSWR-style pattern if SWR is already in the project, otherwise plain fetch with useState)
- Displays community stats in a visually appealing row/grid:
  - Mentors (community.mentors)
  - Mentees (community.mentees)
  - Active Mentorships (community.activeMentorships)
  - Completed Mentorships (community.completedMentorships)
  - Avg Rating (community.averageRating) — display as "X.X / 5" with a star icon
  - Discord Members (social.discord.count) — display with a Discord icon or "Discord Members" label. Pull this from the `social` field of the API response (the API returns `social: Record<string, { label, count, url }>`). If `social.discord` is missing, gracefully omit this stat.
- Use DaisyUI stat components or custom cards with large numbers and labels
- Show a loading skeleton (DaisyUI skeleton or simple pulse animation) while fetching
- Handle error state gracefully (hide section or show fallback)
- Section heading: "Community in Numbers" or "Live Community Stats"
- Layout: 6 stats in a row on lg, 3+3 on md, 2+2+2 on mobile
- Style: bg-base-200 section with stat numbers in text-primary or text-accent for emphasis

Must pass `npx tsc --noEmit` with zero errors.
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <done>socialReach.ts includes a Discord entry. CommunityStats.tsx fetches from /api/stats, displays 6 stats (5 community metrics + Discord members) with loading state, handles errors gracefully, compiles without TypeScript errors.</done>
</task>

</tasks>

<verification>
All three new components exist in src/components/home/ and compile cleanly:
```bash
npx tsc --noEmit
ls src/components/home/CommunityHero.tsx src/components/home/PillarsGrid.tsx src/components/home/CommunityStats.tsx
```
</verification>

<success_criteria>
- CommunityHero shows community identity (not personal branding) with join CTA
- PillarsGrid shows all 5 pillars with correct links
- CommunityStats fetches live data from /api/stats and displays community metrics plus Discord member count
- socialReach.ts includes a Discord entry so the stats API serves it
- All components use existing DaisyUI theme classes
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-homepage-redesign/16-01-SUMMARY.md`
</output>
