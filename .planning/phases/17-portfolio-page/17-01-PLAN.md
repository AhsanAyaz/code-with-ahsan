---
phase: 17-portfolio-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/workHistory.ts
  - src/data/testimonials.ts
  - src/data/openSourceProjects.ts
  - src/components/portfolio/PortfolioBio.tsx
  - src/components/portfolio/BooksSection.tsx
  - src/components/portfolio/WorkHistorySection.tsx
  - src/components/portfolio/TestimonialsSection.tsx
  - src/components/portfolio/ContactSection.tsx
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - PORT-05
  - PORT-06
  - PORT-07

must_haves:
  truths:
    - "Visitor sees Ahsan's professional bio with photo and GDE badge"
    - "Visitor sees published books with covers, descriptions, and purchase links"
    - "Visitor sees work history / professional experience"
    - "Visitor sees testimonials from mentees and students"
    - "Visitor sees contact/hire section with professional inquiry options"
  artifacts:
    - path: "src/data/workHistory.ts"
      provides: "Work history data array"
      exports: ["workHistory"]
    - path: "src/data/testimonials.ts"
      provides: "Testimonials data array"
      exports: ["testimonials"]
    - path: "src/data/openSourceProjects.ts"
      provides: "Open source projects data array"
      exports: ["openSourceProjects"]
    - path: "src/components/portfolio/PortfolioBio.tsx"
      provides: "Professional bio with photo and GDE badge"
      min_lines: 30
    - path: "src/components/portfolio/BooksSection.tsx"
      provides: "Books grid with covers and links"
      min_lines: 30
    - path: "src/components/portfolio/WorkHistorySection.tsx"
      provides: "Work history timeline"
      min_lines: 30
    - path: "src/components/portfolio/TestimonialsSection.tsx"
      provides: "Testimonials carousel/grid"
      min_lines: 20
    - path: "src/components/portfolio/ContactSection.tsx"
      provides: "Contact/hire section"
      min_lines: 20
  key_links:
    - from: "src/components/portfolio/BooksSection.tsx"
      to: "src/data/booksData.js"
      via: "import booksData"
      pattern: "import.*booksData"
    - from: "src/components/portfolio/WorkHistorySection.tsx"
      to: "src/data/workHistory.ts"
      via: "import workHistory"
      pattern: "import.*workHistory"
    - from: "src/components/portfolio/TestimonialsSection.tsx"
      to: "src/data/testimonials.ts"
      via: "import testimonials"
      pattern: "import.*testimonials"
---

<objective>
Create portfolio data files and static-data components for the recruiter-ready /about page redesign.

Purpose: Provide all data structures and the five components that rely on static/imported data, so Plan 02 can add dynamic sections and assemble the final page.
Output: 3 data files + 5 React components ready for page assembly.
</objective>

<execution_context>
@/home/ahsan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/data/booksData.js
@src/data/socialReach.ts
@src/components/home/FounderCredibility.tsx
@src/data/authors/ahsan_detailed.mdx
</context>

<interfaces>
<!-- Existing data the executor needs -->

From src/data/booksData.js:
```javascript
const booksData = [
  {
    id: number,
    title: string,
    description: string,
    amazonLink: string | null,
    imageUrl: string,
    author?: string,
    link?: string,
    btnText?: string,
    actionType: "link",
    edition?: string,
  },
  // ... 4 books total
];
export default booksData;
```

From src/components/home/FounderCredibility.tsx (pattern reference for bio):
- Uses Image from "next/image" for avatar and GDE badge
- Avatar at `/static/images/avatar.jpeg`
- GDE badge at `/static/images/gde_logo_brackets.png`
- Uses DaisyUI classes: `border-base-300`, `text-base-content`, `bg-base-100`
- Server component (no "use client")

From src/data/authors/ahsan_detailed.mdx frontmatter:
```yaml
name: Muhammad Ahsan Ayaz
avatar: /static/images/avatar.jpeg
occupation: Software Architect | GDE in AI & Angular | Author | Speaker
company: Scania Group
email: muhd.ahsanayaz@gmail.com
twitter: https://twitter.com/codewith_ahsan
linkedin: https://www.linkedin.com/in/ahsanayaz
github: https://github.com/ahsanayaz
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio data files</name>
  <files>src/data/workHistory.ts, src/data/testimonials.ts, src/data/openSourceProjects.ts</files>
  <action>
Create three typed data files following the pattern of existing data files like `socialReach.ts`.

**src/data/workHistory.ts** — Export a typed array `workHistory` with entries:
```typescript
export type WorkEntry = {
  company: string;
  role: string;
  period: string;       // e.g. "2021 - Present"
  location: string;
  description: string;  // 1-2 sentence summary
  current?: boolean;
};
```
Include these entries (sourced from the MDX frontmatter and LinkedIn references in the existing about page):
1. Scania Group — Software Architect — "2021 - Present" — Stockholm, Sweden — current: true — "Leading frontend architecture and developer experience for connected vehicle platforms."
2. Visionarea / Ciklum — Senior Software Engineer — "2019 - 2021" — Stockholm, Sweden — "Built enterprise Angular applications and mentored junior developers."
3. Creatix / Gaditek — Software Engineer — "2015 - 2019" — Karachi, Pakistan — "Full-stack development on multiple product lines serving millions of users."
4. 7Vals — Frontend Developer — "2013 - 2015" — Karachi, Pakistan — "Built responsive web applications and contributed to UI component libraries."

**src/data/testimonials.ts** — Export a typed array `testimonials`:
```typescript
export type Testimonial = {
  name: string;
  role: string;          // e.g. "Mentee", "Student", "Workshop attendee"
  text: string;          // The testimonial quote
  avatarInitials: string; // 2-letter initials for placeholder avatar
};
```
Include 4-5 placeholder testimonials with realistic but clearly placeholder content. Use names like "A.K.", "S.R.", etc. for initials. Mark with a code comment `// TODO: Replace with real testimonials from mentees/students`.

**src/data/openSourceProjects.ts** — Export a typed array `openSourceProjects`:
```typescript
export type OpenSourceProject = {
  name: string;
  description: string;
  url: string;
  stars?: string;       // e.g. "1.2k" — optional, can be added later
  tech: string[];       // tech tags
};
```
Include these projects (from Ahsan's GitHub):
1. ng-cookbook — "Code samples and recipes from the Angular Cookbook" — github.com/nickalchemist/ng-cookbook — tech: ["Angular", "TypeScript"]
2. ng-logicbuddy — "A Logic Buddy game built with Angular" — tech: ["Angular", "TypeScript"]
3. code-with-ahsan (this site) — "Community platform for mentorship, projects, and learning" — tech: ["Next.js", "TypeScript", "Firebase"]

Add a comment `// TODO: Update with actual GitHub URLs and star counts`.
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit src/data/workHistory.ts src/data/testimonials.ts src/data/openSourceProjects.ts 2>&1 | head -20</automated>
  </verify>
  <done>Three typed data files exist with exported arrays. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build portfolio bio, books, work history, testimonials, and contact components</name>
  <files>src/components/portfolio/PortfolioBio.tsx, src/components/portfolio/BooksSection.tsx, src/components/portfolio/WorkHistorySection.tsx, src/components/portfolio/TestimonialsSection.tsx, src/components/portfolio/ContactSection.tsx</files>
  <action>
Create 5 server components (no "use client") in `src/components/portfolio/`. Follow the DaisyUI styling patterns from FounderCredibility.tsx and the homepage components. Use `page-padding` for container widths. Each section uses `border-t border-base-300` as separator and alternates `bg-base-100` / `bg-base-200` backgrounds.

**PortfolioBio.tsx (PORT-01):**
- Full-width hero-style section at top of page
- Large avatar image (200x200) using next/image with `src="/static/images/avatar.jpeg"`
- GDE badge next to name (reuse pattern from FounderCredibility)
- Name: "Muhammad Ahsan Ayaz"
- Title: "Software Architect | Google Developer Expert in AI & Angular | Author | Speaker"
- Company: "Scania Group, Stockholm"
- A 2-3 sentence professional bio paragraph (more detailed than the homepage FounderCredibility — mention: 10+ years experience, 4 published books, GDE status, community founder, speaker at international conferences)
- Row of icon links: GitHub, LinkedIn, Twitter/X, Email — use the URLs from the MDX frontmatter data

**BooksSection.tsx (PORT-02):**
- Import `booksData` from `@/data/booksData.js`
- Section heading: "Published Books"
- Responsive grid: 1 col mobile, 2 cols md, 4 cols lg
- Each book card shows: cover image (use next/image with the book's imageUrl), title, edition if present, description, and a purchase CTA button
- Purchase button links to `book.link || book.amazonLink` with text `book.btnText || "Get on Amazon"`
- Cards use `bg-base-100 border border-base-300 rounded-xl shadow-sm`

**WorkHistorySection.tsx (PORT-05):**
- Import `workHistory` from `@/data/workHistory.ts`
- Section heading: "Professional Experience"
- Vertical timeline layout using a left border-line pattern
- Each entry shows: company name (bold), role, period, location, and description
- Current position gets a `badge badge-primary badge-sm` "Current" indicator
- Use `relative` positioning with a dot/circle on the timeline line

**TestimonialsSection.tsx (PORT-06):**
- Import `testimonials` from `@/data/testimonials.ts`
- Section heading: "What People Say"
- Grid layout: 1 col mobile, 2 cols md
- Each testimonial card: quote text in italics, avatar circle with initials (colored background using DaisyUI `bg-primary text-primary-content`), name, and role
- Cards use `bg-base-100 border border-base-300 rounded-xl p-6`

**ContactSection.tsx (PORT-07):**
- Section heading: "Work With Me"
- Brief intro paragraph: available for conference talks, consulting, mentorship, and collaborations
- Grid of 3-4 contact options as cards:
  1. Speaking — "Invite me to speak at your event" — link to email
  2. Consulting — "Need Angular/AI expertise?" — link to LinkedIn
  3. Mentorship — "Join the mentorship program" — link to /mentorship
  4. Collaboration — "Let's build something together" — link to email
- Each card: icon (use emoji or text icon), title, description, CTA link
- Cards use `bg-base-100 border border-base-300 rounded-xl hover:border-primary/40 hover:shadow-sm transition-all`
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit src/components/portfolio/PortfolioBio.tsx src/components/portfolio/BooksSection.tsx src/components/portfolio/WorkHistorySection.tsx src/components/portfolio/TestimonialsSection.tsx src/components/portfolio/ContactSection.tsx 2>&1 | head -30</automated>
  </verify>
  <done>All 5 components compile without TypeScript errors. Each component renders a self-contained section with DaisyUI styling, proper imports from data files, and next/image for images.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new files
- All data files export typed arrays
- All components are server components (no "use client" directive)
- BooksSection imports from existing booksData.js
- WorkHistorySection and TestimonialsSection import from new data files
- DaisyUI classes used throughout (no custom CSS)
</verification>

<success_criteria>
- 3 data files created with proper TypeScript types and exported arrays
- 5 portfolio components created, each rendering a distinct section
- All files compile without TypeScript errors
- Components follow established DaisyUI patterns from Phase 16 homepage components
</success_criteria>

<output>
After completion, create `.planning/phases/17-portfolio-page/17-01-SUMMARY.md`
</output>
