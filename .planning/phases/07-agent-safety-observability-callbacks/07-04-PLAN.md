---
phase: 07-agent-safety-observability-callbacks
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - .planning/phases/07-agent-safety-observability-callbacks/07-04-SOAK-LOG.md
autonomous: false
requirements:
  - AGENT-TEST-02
  - AGENT-CB-MODEL-01
  - AGENT-CB-TOOL-01
  - AGENT-CB-TOOL-02
  - AGENT-CB-AGENT-01
  - AGENT-CB-AGENT-02

must_haves:
  truths:
    - "All three callback layers (PII, tool cache, lifecycle) pass their pytest suites together (`pytest -q` is green; 41 callback tests (16 PII + 12 cache + 13 lifecycle) + full Phase 6 suite) BEFORE deploy is attempted."
    - "Local `adk web` 5-turn smoke confirms each callback layer fires in the live ADK runtime: (a) `[REDACTED PII]` appears for a card-number-like input (Plan 07-01); (b) `tool_cache_miss` then `tool_cache_hit` events appear in stdout (Events tab) for a repeated blog/youtube query (Plan 07-02); (c) `agent.enter` + `agent.exit` events fire for every LEAF LlmAgent traversal in a turn (Plan 07-03); (d) wrapper agents (SequentialAgent + ParallelAgent) emit NO lifecycle events — confirming wrapper-skip rule in production runtime."
    - "RESEARCH §14 Open Question 1 (AgentTool execution path callback firing) is formally resolved YES or NO with diagnostic evidence captured in 07-04-SOAK-LOG.md."
    - "RESEARCH §14 Open Question 3 (callback overhead at p99) is bounded with a measured number from the soak log — overhead per turn ≤ 50ms p99 (3 layers × ~12 leaves × ~1ms each, RESEARCH §1)."
    - "Cloud Run `cwa-assistant-bot` is redeployed atomically with `USAGE_HASH_SECRET` env var still wired (Phase 02.1 invariant) and `--cpu-throttling=false` annotation preserved (Phase 06-04 invariant); revision survives the existing healthcheck."
    - "24h prod soak on Cloud Logging shows (i) `jsonPayload.event_type='agent.exit'` events emitting for the expected leaf agents at the expected cadence (no missing branch), (ii) `tool_cache_hit` event firing for repeated queries (cache populated and reused across turns), (iii) `[REDACTED PII]` substitution observable for any matching pattern in synthetic test queries, (iv) no `cwa_callbacks WARNING` spike (indicates exception-swallow path firing — debug ASAP if seen)."
    - "Deferred AGENT-PAR-02 P95 gate (STATE.md Next Moves #1) is closed here using the new per-leaf `duration_ms` events as substrate. P95 of `jsonPayload.duration_ms` filtered by `jsonPayload.event_type='agent.exit' AND jsonPayload.agent IN ('gh_researcher','devto_researcher','so_researcher')` over 24h gives per-branch P95; max-branch P95 + synthesizer P95 ≈ external_knowledge end-to-end P95. Compare to Phase 6-04-SUMMARY baseline."
  artifacts:
    - path: ".planning/phases/07-agent-safety-observability-callbacks/07-04-SOAK-LOG.md"
      provides: "Soak-period observation log for Phase 7 — pre-deploy baseline (callback overhead measured locally), 5-turn smoke verdicts for each callback layer + AgentTool callback firing resolution (Open Question 1) + overhead bound (Open Question 3), post-deploy observations at T+1h / T+6h / T+24h covering cache hit ratio, lifecycle event volume, PII redaction matches, deferred AGENT-PAR-02 per-leaf P95 closure, error-rate delta, and SHIP / PARTIAL-SHIP / ROLLBACK decision."
      contains: "callback overhead"
  key_links:
    - from: "local dev (adk web)"
      to: "RESEARCH §14 Open Questions 1 + 3 verification"
      via: "manual 5-turn smoke before Cloud Run deploy"
      pattern: "adk web"
    - from: "deployed cwa-assistant-bot"
      to: "Cloud Logging structured events (agent.enter / agent.exit / tool_cache_hit / tool_cache_miss / [REDACTED PII])"
      via: "new Phase 7 jsonPayload fields"
      pattern: "jsonPayload.event_type"
    - from: "deployed cwa-assistant-bot (per-leaf duration_ms)"
      to: "AGENT-PAR-02 closure (Phase 6 carryover, STATE.md Next Moves #1)"
      via: "Cloud Logging P95 query on event_type='agent.exit' filtered by external_knowledge leaf names"
      pattern: "duration_ms.*gh_researcher|devto_researcher|so_researcher"
---

<objective>
Final gate for Phase 7: validate the three callback layers (PII redaction + tool-result cache + lifecycle logging) end-to-end against `adk web` locally, redeploy `cwa-assistant-bot` to Cloud Run, run a 24h soak on Cloud Logging, and ship-or-rollback based on the Phase 7 success criteria. This plan parallels Phase 6 Plan 04's structure but is LIGHTER — Phase 7 is purely additive (callbacks only; no agent-tree restructure), so the smoke focuses on callback firing + event emission rather than agent ordering / fan-out behavior. Cross-phase: this plan ALSO closes Phase 6's deferred AGENT-PAR-02 P95 gate (STATE.md Next Moves #1) using the new per-leaf `duration_ms` events as the measurement substrate.

Purpose: Closes AGENT-TEST-02 (the smoke-and-deploy gate for Phase 7) and provides the production verification that AGENT-CB-MODEL-01 / AGENT-CB-TOOL-01 / AGENT-CB-TOOL-02 / AGENT-CB-AGENT-01 / AGENT-CB-AGENT-02 actually fire in the deployed Cloud Run runtime (not just under pytest mocks). Resolves RESEARCH §14 Open Question 1 (does `before_/after_agent_callback` fire inside AgentTool execution for `featured_resources_agent`?) and Open Question 3 (callback overhead p99 bound). Open Question 2 was answered in Phase 6 Plan 04 (welcome-LAST ordering committed) — Phase 7 re-confirms only as smoke side effect.

Output: An `adk web` smoke run that exercises all three callback layers in one Discord-style conversation, a Cloud Run redeploy that preserves Phase 02.1 + Phase 06 deploy invariants, a 24h soak log captured in `07-04-SOAK-LOG.md`, and a ship/rollback decision based on the soak metrics + AGENT-PAR-02 closure verdict. This plan does NOT modify any production agent code (Plans 07-01/07-02/07-03 own that surface); it ONLY runs verification + deploy + observe.

**Dependency rationale (depends_on includes all three prior plans):** Plans 07-01 (PII), 07-02 (cache), and 07-03 (lifecycle) each commit independently with their own RED→GREEN pytest gates. Plan 07-04 is the FIRST point where all three layers run together in a live ADK runtime (the pytests use mock `CallbackContext`, not real ADK execution). The 5-turn smoke is the integration-test surface that no unit test can replicate.

This plan is NOT autonomous — three human checkpoints (smoke verdict, deploy approval, soak decision). User decides when to flip per the same convention as Phase 06-04 (CONTEXT.md `## Implementation Decisions > Deployment + soak`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-ROADMAP.md
@.planning/phases/07-agent-safety-observability-callbacks/07-RESEARCH.md
@.planning/phases/07-agent-safety-observability-callbacks/07-01-PLAN.md
@.planning/phases/07-agent-safety-observability-callbacks/07-02-PLAN.md
@.planning/phases/07-agent-safety-observability-callbacks/07-03-PLAN.md
@.planning/phases/06-agent-workflow-refactor-state-wiring/06-04-SUMMARY.md

@agent/community_assistant/agent.py
@agent/community_assistant/callbacks.py
@agent/pyproject.toml
@agent/Dockerfile
@.github/workflows/

<interfaces>
<!-- Deploy + soak observability surface. Phase 7 is purely additive; the deploy procedure inherits from Phase 02.1-03 (image push to Cloud Run) and Phase 06-04 (most recent rev: cwa-assistant-bot-00012-8x6). -->

**Cloud Run service:** `cwa-assistant-bot` — deployed from `agent/` build context. Region + project from existing CD setup. Current production revision per Phase 06-04-SUMMARY: `cwa-assistant-bot-00012-8x6` (Phase 6 ParallelAgent fan-out landed PARTIAL-SHIP with AGENT-PAR-02 deferred).

**Phase 06-04 deploy procedure deviations (DOCUMENTED — must replicate in Plan 07-04):**
1. **`--dockerfile` flag absent in gcloud 533.0.0** — the option does not exist in the installed `gcloud` version. Workaround: rely on auto-detection of the Dockerfile at `agent/Dockerfile` (Cloud Run Buildpacks picks it up from the build context root). Verify with `gcloud --version` before deploy; if version differs from 533.0.0, the `--dockerfile` option may be available — use whichever path is current.
2. **Pre-1980 mtime issue with `codewithahsan-revamp/` upload** — gcloud rejects files with mtime before 1980 epoch. Workaround: use `.gcloudignore` to restrict upload to ONLY `agent/` directory contents. The `.gcloudignore` file at the repo root MUST include `codewithahsan-revamp/` (the Next.js subapp doesn't need to be in the Cloud Run image). Verify via `gcloud meta list-files-for-upload --source=agent` does NOT include `codewithahsan-revamp/`.
3. **`--cpu-throttling=false` annotation preserved** — Phase 06-04 deploy used this to keep the container warm between Discord turns (avoids cold-start tax). Carry forward in Plan 07-04 deploy via `--no-cpu-throttling` (gcloud flag form).
4. **`USAGE_HASH_SECRET` env var via `--set-secrets`** — Phase 02.1 invariant. Plan 07-03's lifecycle events depend on this for the `session_id_hash` HMAC. If env var is missing post-deploy, lifecycle events still fire but with `session_id_hash=""` (degradation path tested in Plan 07-03 Test 5). NOT a deploy-blocker but worth verifying explicitly.

**Cloud Logging dashboard (existing, Phase 02.1 + augmented by Phase 7 jsonPayload fields):** filters on the bot's structured stdout events. Key Phase 7 metrics:
- `jsonPayload.event_type="agent.enter"` — emitted by Plan 07-03 lifecycle before-callback per leaf LlmAgent.
- `jsonPayload.event_type="agent.exit"` — emitted by Plan 07-03 lifecycle after-callback per leaf LlmAgent; carries `duration_ms` for P95 measurement.
- `jsonPayload.event_type="tool_cache_hit"` / `"tool_cache_miss"` — emitted by Plan 07-02 cache callbacks for `search_blog_posts` + `search_youtube_videos`.
- `[REDACTED PII]` substring in any log line — proxy for Plan 07-01 PII redaction firing (the PII layer mutates `llm_request.contents`, so the redacted text appears in the model-input trace AND in any downstream agent emission that echoes it; the literal placeholder string is the grep anchor).
- Existing Phase 02.1 `latency_ms` + `query_topic` fields preserved — Phase 7 does NOT modify `bot.py` usage_metrics emission.

**Per-leaf P95 closure for AGENT-PAR-02 (Phase 6 carryover, STATE.md Next Moves #1):**
- Phase 6 Plan 04 ROLLED-BACK then re-deployed as PARTIAL-SHIP — measured P95 drop did not hit the ≥40% target. The reason (per 06-04-SUMMARY) was that the soak-measurement substrate was end-to-end `latency_ms` (turn-level), which mixes orchestrator overhead + LLM round-trips + the three fan-out branches into one number — too coarse to attribute the savings.
- Plan 07-04 closes this gap by querying per-LEAF `duration_ms` from the new `agent.exit` events. Cloud Logging query:
  ```
  resource.labels.service_name="cwa-assistant-bot"
  resource.labels.revision_name="<phase-7-rev>"
  jsonPayload.event_type="agent.exit"
  jsonPayload.agent=("gh_researcher" OR "devto_researcher" OR "so_researcher")
  ```
  Aggregate `APPROX_QUANTILES(jsonPayload.duration_ms, 100)[OFFSET(95)]` per agent → 3 per-branch P95 numbers. The fan-out P95 ≈ `max(p95_gh, p95_devto, p95_so)`. Add synthesizer P95: `jsonPayload.agent="external_knowledge_synthesizer"` → `p95_synth`. Total `external_knowledge_agent` P95 ≈ `max(p95_gh, p95_devto, p95_so) + p95_synth`. Compare to Phase 6-04-SUMMARY's recorded baseline (the soak's pre-deploy P95).
- Decision in Plan 07-04 soak-log: if the new per-branch breakdown reveals the drop IS ≥40% (just was diluted by orchestrator overhead in the turn-level metric), mark AGENT-PAR-02 GREEN here (Phase 7 retroactively closes Phase 6's deferred gate). If the per-branch breakdown ALSO shows <40%, AGENT-PAR-02 remains DEFERRED, carry-over to Phase 8.

**adk web** — local-only dev UI. Same surface as Phase 06-04 Task 1. For Phase 7, the verification target is the Events tab AND the terminal stdout (which now carries Plan 07-03's structured JSON events as standalone lines next to ADK's own event prints).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: CHECKPOINT — local `adk web` 5-turn smoke across all three callback layers (resolves RESEARCH §14 Open Questions 1 + 3)</name>
  <what-built>
After Plans 07-01/07-02/07-03 commit, the new callback layers are locally importable + wired onto every agent in the tree. This checkpoint exercises ALL three layers in a single multi-turn Discord-style conversation against `adk web` BEFORE pushing to Cloud Run. It formally resolves the two remaining RESEARCH §14 Open Questions:

- **Open Question 1 (RESEARCH §14):** does `before_/after_agent_callback` fire inside the AgentTool execution path for the `featured_resources_agent`? Plan 07-03 wired the callback kwargs onto `featured_resources_agent` (the underlying LlmAgent), but ADK's AgentTool invocation may or may not route through the agent's lifecycle hooks. The 5-turn smoke is the FIRST live test — if the callback fires, the per-leaf P95 dashboard works for the wrapped agent too; if not, document the gap in the soak log + update Plan 07-04's success criteria to exclude `featured_resources_agent` from per-leaf observability.

- **Open Question 3 (RESEARCH §14):** what is the observed callback overhead at p99? Local smoke gives a coarse bound (single-turn wall-clock vs. baseline); production soak gives the rigorous number. Locally: 5 turns × ~12 leaves/turn × 2 callbacks/leaf = ~120 callback invocations. Wall-clock should be indistinguishable from baseline (no `adk web` UI lag).

Also exercises the talk-demo angle (RESEARCH §12): the Events tab + terminal stdout together demonstrate the observability win — every agent traversal visible as a structured event, cache hits visible as `tool_cache_hit` events, PII redaction visible as `[REDACTED PII]` in the model-input trace.
  </what-built>
  <how-to-verify>
**Prereqs:**
1. Plans 07-01, 07-02, 07-03 all committed and on the working branch (`git log --oneline | head -10` shows `feat(07-01)`, `feat(07-02)`, `feat(07-03)` commits).
2. `cd agent && .venv/bin/pytest -q` is green (full suite — 41 callback tests (16 PII + 12 cache + 13 lifecycle) + all Phase 6 + Phase 02.1 tests pass together).
3. `cd agent && pip install -e .` (refresh editable install; not strictly required since Plan 07 doesn't add new packages, but doesn't hurt).
4. Env vars set:
   - `GOOGLE_API_KEY` (existing dev setup).
   - `USAGE_HASH_SECRET="<any-deterministic-string>"` — REQUIRED for Plan 07-03 lifecycle events to emit non-empty `session_id_hash`. Local dev can use any value (e.g., `export USAGE_HASH_SECRET="local-smoke-secret"`).
   - `GITHUB_TOKEN` (existing; nice-to-have for GH rate limit).

**Run `adk web`:**
```
cd agent && adk web
```
Open the printed URL (typically `http://localhost:8000`), select `community_assistant`. **Keep the terminal visible side-by-side with the browser** — Plan 07-03 lifecycle events emit to stdout, not to the ADK Events tab; they appear in the terminal as single-line JSON.

**Workstream verification — single 5-turn conversation:**

**Turn 1 (PII redaction — Plan 07-01 verification + Open Question 3 baseline):** Type: `Hi, I just joined! My card 4111 1111 1111 1111 and my email is jane.doe@example.com — can you help me?`

Expected:
- In the terminal stdout (or the ADK Events tab → root agent's `before_model_callback` trace if visible), the model-input contents have the card number and email REPLACED with `[REDACTED PII]` (Plan 07-01 spec). The substitution should occur BEFORE the model call — verify by inspecting the resolved prompt in the Events tab → root_agent → model_request event.
- The bot's reply should NOT echo back the card number or email (because the model never saw them).
- ✅ **AGENT-CB-MODEL-01 verified in live runtime.**
- Wall-clock for Turn 1 reply should be indistinguishable from baseline. If a noticeable pause (>500ms added) is observed, capture timing for the soak log's Open Question 3 bound.

**Turn 2 (lifecycle logging — Plan 07-03 verification + wrapper-skip in live runtime):** Type: `What projects can I contribute to?`

Expected in terminal stdout (JSON events, one per line):
- `{"severity":"INFO","event_type":"agent.enter","agent":"community_assistant",...}` (root agent enter).
- `{"severity":"INFO","event_type":"agent.enter","agent":"projects_agent",...}` (after `transfer_to_agent`).
- `{"severity":"INFO","event_type":"agent.exit","agent":"projects_agent","duration_ms":<int>,...}`.
- `{"severity":"INFO","event_type":"agent.exit","agent":"community_assistant","duration_ms":<int>,...}`.
- ✅ **AGENT-CB-AGENT-01 + AGENT-CB-AGENT-02 verified in live runtime.**
- ✅ **Wrapper-skip live verification:** NO `agent.enter` or `agent.exit` event with `"agent":"onboarding_agent"` or `"external_knowledge_agent"` or `"ExternalFanOut"` should appear in the entire 5-turn conversation. If any wrapper emits a lifecycle event, the wrapper-skip rule failed at the runtime level even though the static grep gate (Plan 07-03 Step F) was clean — surface to user immediately.
- ✅ **Correlation:** the `invocation_id` should match across the enter/exit pair for `projects_agent` (and root).
- ✅ **Privacy gate (live):** none of the emitted lifecycle events should contain the substring `"card"`, `"4111"`, `"jane.doe"`, or any other raw turn-1 content. Test 9 of Plan 07-03 enforces this at unit-test level; live `adk web` is the regression verification surface.

**Turn 3 (tool cache MISS — Plan 07-02 verification, first call):** Type: `Find me Angular signals blog posts and YouTube tutorials.`

Expected:
- `transfer_to_agent("content_agent")`.
- In terminal stdout: a `tool_cache_miss` event with the BLOCKER 1 6-field shape — `{"severity":"INFO","event_type":"tool_cache_miss","agent":"content_agent","invocation_id":"<id>","session_id_hash":"<16 hex>","tool":"search_blog_posts"}`. Plan 07-02 emits on cache miss. NO `cache_key` field is part of the event schema — derivation is internal to the callback.
- Same `tool_cache_miss` event for `search_youtube_videos`.
- The actual tool calls execute (live API to blog/youtube backends).
- Lifecycle events fire as expected for `content_agent`.
- ✅ **AGENT-CB-TOOL-01 (cache miss path) verified.**

**Turn 4 (tool cache HIT — Plan 07-02 verification, repeat call):** Type: `Find me Angular signals blog posts and YouTube tutorials.` (EXACT same query as Turn 3.)

Expected:
- `transfer_to_agent("content_agent")`.
- In terminal stdout: a `tool_cache_hit` event with the BLOCKER 1 7-field shape — `{"severity":"INFO","event_type":"tool_cache_hit","agent":"content_agent","invocation_id":"<id>","session_id_hash":"<16 hex>","tool":"search_blog_posts","age_s":<int 0..600>}`. Plan 07-02 emits on cache hit. The tool body should NOT execute (verify by checking that no blog backend log/network call fires — `adk web` events tab may show the tool call but skipped, or the `before_tool_callback` short-circuits before the function body). NO `cache_key` field — only `age_s` distinguishes hit from miss.
- Same `tool_cache_hit` event for `search_youtube_videos`.
- Wall-clock for Turn 4 reply should be noticeably faster than Turn 3 (the cache hit short-circuits 2 tool calls).
- ✅ **AGENT-CB-TOOL-01 + AGENT-CB-TOOL-02 (cache hit + write paths) verified.**

**Turn 5 (AgentTool execution path — RESEARCH §14 Open Question 1 resolution):** Type: `Do you have an AI guide?`

Expected:
- `transfer_to_agent("content_agent")`.
- In the Events tab, see `content_agent` → tool call event named `featured_resources_agent` (the AgentTool wrap from Phase 6 Plan 03).
- ⚠ **CRITICAL OBSERVATION (Open Question 1):** in the terminal stdout, look for `{"event_type":"agent.enter","agent":"featured_resources_agent",...}` and the matching `agent.exit`. If the events appear → ✅ **Open Question 1 resolved YES** — AgentTool execution DOES route through the wrapped agent's lifecycle hooks. If the events do NOT appear → ✅ **Open Question 1 resolved NO** — AgentTool execution bypasses lifecycle hooks; document the gap.
- Record the verdict in the soak log: `Open Question 1: <YES|NO> — featured_resources_agent lifecycle event <observed|missing> at Turn 5.`

**Failure modes + escalation:**

| Symptom | Diagnosis | Action |
|---------|-----------|--------|
| Turn 1: card number / email echoed in bot's reply | PII redaction not firing — `before_model_callback` not registered on root_agent | Verify Plan 07-01 Task 2 Step C wiring landed in `agent/community_assistant/agent.py`. Re-run `pytest tests/test_callbacks_pii.py -x`; should be green. If callback registers but doesn't fire, check ADK version mismatch (require `google-adk>=1.29.0` per pyproject.toml). |
| Turn 2: no `agent.enter` / `agent.exit` events in terminal stdout | Lifecycle callback not wired OR stdout buffering swallowing events | Check `sys.stdout.flush()` call inside `lifecycle_before_agent` / `lifecycle_after_agent` body. Verify Plan 07-03 wiring count grep is exactly 12 (re-run `grep -c "before_agent_callback=lifecycle_before_agent" ...`). |
| Turn 2: wrapper agent (`onboarding_agent` / `external_knowledge_agent` / `ExternalFanOut`) emits a lifecycle event | Wrapper-skip rule failed at runtime (static grep was clean) | STOP. Surface to user with the offending event JSON. Likely cause: a SequentialAgent/ParallelAgent constructor accidentally got the lifecycle kwarg in a way the grep didn't catch (e.g., kwarg-by-variable). Inspect `onboarding_agent.py` and `external_knowledge/__init__.py` carefully. |
| Turn 2: `session_id_hash` in lifecycle events is `""` (empty string) | `USAGE_HASH_SECRET` not set in env at `adk web` startup time | `export USAGE_HASH_SECRET="local-smoke-secret"` and restart `adk web`. NOT a code bug — degradation path tested in Plan 07-03 Test 5. |
| Turn 4: `tool_cache_hit` event does NOT appear (cache miss again) | Cache key normalization or TTL bug — either key derivation differs between Turn 3 and Turn 4, or TTL is too short | The event schema does NOT expose `cache_key`. Diagnose by (a) confirming both turns ran inside `content_agent` (`agent` field) and emitted for the same `tool` name; (b) checking elapsed wall-clock between Turn 3 and Turn 4 is <600s; (c) running `cd agent && .venv/bin/python -c "from community_assistant.callbacks import _derive_cache_key; print(_derive_cache_key('search_blog_posts', {'query':'Angular signals'}))"` to confirm `q_norm` reproduces the expected `search_blog_posts__angular signals` key. If wall-clock and normalization both check out, dump `session.state['app_cache']` from the running adk web process. |
| Turn 4: cache hit fires but tool body STILL executes (visible network call) | `before_tool_callback` not short-circuiting properly — return value from callback isn't being used as the tool result | Check Plan 07-02 `before_tool_cache` returns the cached value (not `None`) on hit. Re-run `pytest tests/test_callbacks_tool_cache.py::test_cache_hit_short_circuits -x`. |
| Turn 5: NO `featured_resources_agent` lifecycle event in stdout | RESEARCH §14 Open Question 1 resolved NO — AgentTool bypasses lifecycle | Document in soak log. NOT a blocker — Plan 07-03 wiring is correct, the limitation is in ADK's AgentTool execution path. Update Phase 7 SUMMARY's "Known Limitations" section. Optionally, file a Phase 8 quick-task to instrument `lookup_featured_resource` (the Python tool function inside `featured_resources_agent`) with manual logging as a workaround. |
| Wall-clock pause on any turn >1s added vs. baseline | Callback overhead too high (RESEARCH §14 Open Question 3 fails) | Capture timing in soak log. Investigate: most likely culprit is the PII regex compile happening per-call instead of at module import (RESEARCH §11 says compile-at-import). Verify `PII_PATTERNS` is module-level. |

**Soft gates (nice-to-have but not blocking):**
- Turn 4's wall-clock should feel noticeably faster than Turn 3 (cache hit win — this is the talk-demo angle).
- Terminal stdout should look "clean" — JSON events interspersed with ADK's own event prints. If JSON events are garbled (interleaved with other prints mid-line), `sys.stdout` is being shared without locking — investigate but not a blocker (RESEARCH §9 Pitfall 5: per-line atomicity is OS-level, not Python-level guaranteed).
  </how-to-verify>
  <resume-signal>Type "smoke green — OQ1=<YES|NO>" (with the AgentTool Open Question 1 verdict). Or type "BLOCKED — <symptom>" with diagnostic details (terminal stdout excerpt, ADK version, env var state). Resume-signal MUST explicitly include the Open Question 1 verdict, even if all other gates pass — it determines whether Plan 07-04 soak log marks `featured_resources_agent` as observable or not.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CHECKPOINT — pre-deploy verification + Cloud Run redeploy (preserves Phase 02.1 + Phase 06-04 invariants)</name>
  <what-built>
After Task 1's smoke is green, deploy the Phase 7 image to `cwa-assistant-bot`. The deploy preserves the Phase 02.1 `USAGE_HASH_SECRET` env var (lifecycle events depend on it for `session_id_hash`) and the Phase 06-04 `--no-cpu-throttling` annotation (keeps container warm). Documents the Phase 06-04 deploy procedure deviations (`--dockerfile` flag absent, pre-1980 mtime workaround) inline so the procedure is self-contained for the operator.

**Why human-action and not auto:** the deploy step itself can be automated via gcloud, but the DECISION-to-deploy is a human judgment call. The pre-deploy verification (Dockerfile builds clean, `.gcloudignore` excludes `codewithahsan-revamp/`, env vars are wired) is a checklist the operator runs.
  </what-built>
  <how-to-verify>
**Part A — pre-deploy verification:**

1. **Verify all callback tests + full suite are green on the current commit:**
   ```
   cd /Users/amu1o5/personal/code-with-ahsan/agent && .venv/bin/pytest -q
   ```
   Expected: 41 callback tests (16 PII + 12 cache + 13 lifecycle) + all Phase 6 tests + Phase 02.1 tests pass together. NO skipped tests with `<reason>` warnings. NO `xfail`. If any failure, STOP — do not deploy.

2. **Verify Dockerfile builds the new callbacks.py + wiring cleanly:**
   ```
   cd /Users/amu1o5/personal/code-with-ahsan/agent && docker build -t cwa-assistant-bot-phase7:local .
   ```
   Expected: build succeeds. The new `agent/community_assistant/callbacks.py` should be picked up by hatchling (existing `[tool.hatch.build.targets.wheel] packages = ["community_assistant"]` directive in `agent/pyproject.toml` includes everything under `community_assistant/`, so no Dockerfile change needed). Also verify `freezegun` is in the installed deps (Plan 07-02 added it):
   ```
   docker run --rm cwa-assistant-bot-phase7:local python -c "import freezegun; print('freezegun OK')"
   ```

3. **Verify the runtime can import the new callback module + wiring:**
   ```
   docker run --rm cwa-assistant-bot-phase7:local python -c "from community_assistant.agent import root_agent; from community_assistant.callbacks import pii_sanitizer, before_tool_cache, after_tool_cache, lifecycle_before_agent, lifecycle_after_agent; assert root_agent.before_model_callback is pii_sanitizer; assert root_agent.before_agent_callback is lifecycle_before_agent; assert root_agent.after_agent_callback is lifecycle_after_agent; print('callbacks wired OK')"
   ```
   Expected: `callbacks wired OK` (verifies root_agent has all 3 Phase 7 kwargs wired). If `AssertionError`, the wiring landed but a callback identity check failed — investigate before deploy.

4. **Verify `.gcloudignore` workaround for pre-1980 mtime issue (Phase 06-04 carryover):**
   ```
   cat /Users/amu1o5/personal/code-with-ahsan/.gcloudignore 2>/dev/null | grep -E "^codewithahsan-revamp"
   ```
   Expected output: `codewithahsan-revamp/` line present. If missing, ADD it (single line) and commit BEFORE proceeding — gcloud upload will fail otherwise with `Cannot pack files with mtime before 1980` style error.

5. **Verify `USAGE_HASH_SECRET` is provisioned on Cloud Run (Phase 02.1 carryover):**
   ```
   gcloud run services describe cwa-assistant-bot --region=<existing-region> --format="value(spec.template.spec.containers[0].env)" 2>&1 | grep -E "USAGE_HASH_SECRET"
   ```
   Expected: a `--set-secrets` reference to a Secret Manager binding. If missing, add via `--set-secrets=USAGE_HASH_SECRET=USAGE_HASH_SECRET:latest` in the deploy command (Step 6 below).

6. **Verify gcloud version + `--dockerfile` flag availability (Phase 06-04 deviation context, NIT 3 — flag probe robustness):**
   ```
   gcloud --version | grep "Google Cloud SDK"
   gcloud run deploy --dockerfile=NOTEXIST 2>&1 | grep -E "Unknown flag|unrecognized arguments"
   ```
   The second command directly invokes `gcloud run deploy` with a nonexistent value for `--dockerfile`. If the flag is ABSENT in the installed gcloud (matches Phase 06-04's gcloud 533.0.0), gcloud's argparse emits `unrecognized arguments: --dockerfile=NOTEXIST` or `ERROR: Unknown flag: --dockerfile` BEFORE doing anything else — that is a positive match for "flag absent, use auto-detection (Dockerfile picked up from `agent/` build context root)". If the second command instead prints `Dockerfile NOTEXIST not found` or proceeds to a build error, the flag IS available and you can use it explicitly. This probe is more robust than `gcloud run deploy --help | grep -E "\s+--dockerfile"` because Google rewrites the help text frequently and a stale grep pattern can silently report wrong availability.

7. **Capture Phase 6 pre-deploy baseline (WARNING 5 — soak comparison substrate):** before redeploying, capture the existing `latency_ms` P95 distribution from Cloud Logging for the current production revision (Phase 6's `cwa-assistant-bot-00012-8x6`) over the prior 7 days. This is the baseline against which Plan 07-04's post-deploy P95 (Task 3) is compared. Query:
   ```
   resource.type="cloud_run_revision"
   resource.labels.service_name="cwa-assistant-bot"
   resource.labels.revision_name="cwa-assistant-bot-00012-8x6"
   jsonPayload.event_type="bot_message"
   ```
   Aggregate `APPROX_QUANTILES(jsonPayload.latency_ms, 100)[OFFSET(50)]` and `[OFFSET(95)]` and `[OFFSET(99)]` over the 7-day window. Record p50 / p95 / p99 numbers in `07-04-SOAK-LOG.md` under a new `## Baseline (pre-deploy)` section. Phase 7's post-deploy P95 (Task 3 Step 7 — error-rate delta + latency comparison) is judged against THIS number. Required SOAK-LOG.md template addition:
   ```markdown
   ## Baseline (pre-deploy)
   - Source revision: cwa-assistant-bot-00012-8x6 (Phase 6 last-good).
   - Sample window: <YYYY-MM-DD> to <YYYY-MM-DD> (7 days prior to deploy).
   - Sample size: N turns.
   - Phase 6 latency_ms p50: ___ ms
   - Phase 6 latency_ms p95: ___ ms
   - Phase 6 latency_ms p99: ___ ms
   - Phase 6 error rate: ___ %
   - Phase 6 external_knowledge end-to-end latency (Phase 6-04-SUMMARY recorded): ___ ms (this is the AGENT-PAR-02 baseline)
   ```
   The Final Decision section MUST compare these pre-deploy numbers to the post-deploy distribution and conclude whether callback overhead is within the RESEARCH §14 Open Question 3 bound (≤50ms p99 added).

**Part B — Cloud Run redeploy:**

1. **Deploy command** (matches Phase 06-04 invocation; adjust region / project per existing CD setup):
   ```
   gcloud run deploy cwa-assistant-bot \
     --source=agent \
     --region=<existing-region> \
     --project=<existing-project> \
     --no-cpu-throttling \
     --revision-suffix=phase7 \
     --update-secrets=USAGE_HASH_SECRET=USAGE_HASH_SECRET:latest \
     --quiet
   ```
   Note: `--source=agent` triggers Cloud Run's Buildpacks path which auto-detects `agent/Dockerfile`. The `.gcloudignore` from Part A Step 4 prevents `codewithahsan-revamp/` from being included in the upload.

2. **Watch the rollout:**
   ```
   gcloud run services describe cwa-assistant-bot --region=<existing-region> --format=json | jq '{latestRev: .status.latestReadyRevisionName, traffic: .status.traffic}'
   ```
   Expected: `latestRev` shows the new `cwa-assistant-bot-XXXXX-phase7` revision; `traffic` shows 100% to the new revision.

3. **Verify the new revision is serving end-to-end:**
   - Send a single Discord message to the production bot (or hit the health endpoint).
   - Expect a 200 / normal reply.
   - Check Cloud Logging immediately: `jsonPayload.event_type="agent.enter"` should appear within seconds of the Discord message. If no lifecycle events appear, the callback isn't firing in production runtime even though smoke was green locally — investigate before declaring Task 2 complete.

**Failure modes:**

| Symptom | Diagnosis | Action |
|---------|-----------|--------|
| `docker build` fails on the new `callbacks.py` | Module-level import error (typo, missing dep) | Inspect the error; most likely a missing `from google.genai import types` import for the return-type annotation. Fix in `callbacks.py` and re-run Step 1. |
| `gcloud run deploy` fails with "mtime before 1980" | `.gcloudignore` not excluding `codewithahsan-revamp/` | Add the line per Part A Step 4. Re-run deploy. |
| `gcloud run deploy` fails with "Dockerfile not found" | Build context confusion — `--source=agent` not interpreting correctly | Try `--source=.` + explicit `--dockerfile=agent/Dockerfile` if gcloud version supports it; otherwise `cd agent && gcloud run deploy ... --source=.` |
| Deploy succeeds but healthcheck fails | Container starts but immediately crashes — most likely an import error inside `callbacks.py` that didn't surface in `docker build` | Check Cloud Run logs for the startup `ImportError`. Rollback via `gcloud run services update-traffic cwa-assistant-bot --to-revisions=cwa-assistant-bot-00012-8x6=100` (Phase 06-04's last-good revision). |
| Deploy succeeds, healthcheck passes, but NO lifecycle events in Cloud Logging | `sys.stdout` buffering on Cloud Run (uncommon — Cloud Run has line-buffered stdout by default for Python) | Verify `sys.stdout.flush()` is called after each `sys.stdout.write(...)` in `lifecycle_before_agent` / `lifecycle_after_agent` / cache callbacks. If the flush call is present but events still missing, set `PYTHONUNBUFFERED=1` in the Cloud Run env vars (low-risk addition). |
| Deploy succeeds but lifecycle events show `session_id_hash=""` | `USAGE_HASH_SECRET` not bound on the new revision | Re-deploy with `--update-secrets=USAGE_HASH_SECRET=USAGE_HASH_SECRET:latest` explicitly. Phase 02.1 invariant requires this secret on every revision. |

DO NOT proceed to Task 3 (soak) if any failure mode above fires.
  </how-to-verify>
  <resume-signal>Type "deployed=cwa-assistant-bot-XXXXX-phase7" with the exact new revision name (e.g., "deployed=cwa-assistant-bot-00013-phase7"). Or type "BLOCKED — <failure mode>" with diagnostic details. The resume-signal MUST include the actual revision name — Task 3's Cloud Logging queries filter by `resource.labels.revision_name="<this value>"`.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — 24h soak watch + AGENT-PAR-02 closure + Phase 7 ship/rollback decision</name>
  <what-built>
After Task 2 deploys the new revision, observe Cloud Logging for 24h. Phase 7's ROADMAP success criteria (per the Phase 7 entry in `.planning/ROADMAP.md`):
1. PII redaction observable in production (any synthetic test query with a card-number / email pattern produces `[REDACTED PII]` in the model-input trace).
2. Tool cache hit ratio measurable on `search_blog_posts` + `search_youtube_videos` — at least one `tool_cache_hit` event over 24h proves the cache works for real traffic.
3. Lifecycle events emit for every leaf LlmAgent at the expected cadence — no missing branch (each of `gh_researcher`, `devto_researcher`, `so_researcher`, `external_knowledge_synthesizer`, `mentorship_agent`, `projects_agent`, `roadmap_agent`, `content_agent`, the 3 onboarding children, and root `community_assistant` should appear with non-zero event count).
4. v6.0 HMAC privacy invariant preserved — no raw user content in any lifecycle event payload (sample 20 random `agent.enter` events from the soak, scrub-check for substrings matching user IDs / queries — should find none).

**Also closes Phase 6's deferred AGENT-PAR-02 gate (STATE.md Next Moves #1)** using the new per-leaf `duration_ms` events as substrate.

Watch for the failure modes RESEARCH §9 flags:
- **Callback exception swallow firing in prod:** `cwa_callbacks WARNING` events appearing in Cloud Logging at non-zero cadence. Plan 07-01 Test 16 + Plan 07-02 Test 12 + Plan 07-03 Tests 10/11 confirmed the swallow path works under pytest; production exposure may surface a code path the tests didn't cover. Sev-2: investigate within the soak window, do NOT auto-rollback for low-volume warnings.
- **Cache poisoning:** a `tool_cache_hit` event with the WRONG result content (e.g., hit returns YouTube results when the query was a blog query). Plan 07-02 Test 4 (tool-name isolation) prevented this at unit-test level, but production cardinality could expose an edge case. Sev-1: immediate rollback.
- **Lifecycle event volume spike beyond expected:** baseline expectation is ~12 events/turn × ~100 turns/day = 1200/day. If actual is >10× that (e.g., 12K/day), a wrapper accidentally got lifecycle wiring OR an LlmAgent is being invoked recursively in a loop. Sev-2.
  </what-built>
  <how-to-verify>
**Observation cadence:** check Cloud Logging at T+1h, T+6h, T+24h after deploy. Record findings in `07-04-SOAK-LOG.md` under three section headers (`## Soak T+1h`, `## Soak T+6h`, `## Soak T+24h`). The literal phrase `T+24h` MUST appear as a section header — the Task 3 `<automated>` grep gate enforces this.

**At each checkpoint, capture (Phase 7 specific metrics):**

1. **Lifecycle event volume + per-leaf breakdown:**
   ```
   resource.type="cloud_run_revision"
   resource.labels.service_name="cwa-assistant-bot"
   resource.labels.revision_name="<from Task 2 resume-signal>"
   jsonPayload.event_type="agent.enter"
   ```
   Aggregate by `jsonPayload.agent` → table of (agent_name, event_count) over the interval since deploy. Expected: 12 distinct agent names, all with non-zero counts (assuming traffic touched all sub-agents — onboarding may be lower if no new users joined).

2. **Per-leaf `duration_ms` P95 (for AGENT-PAR-02 closure):**
   ```
   resource.labels.service_name="cwa-assistant-bot"
   resource.labels.revision_name="<from Task 2>"
   jsonPayload.event_type="agent.exit"
   jsonPayload.agent=("gh_researcher" OR "devto_researcher" OR "so_researcher" OR "external_knowledge_synthesizer")
   ```
   Aggregate `APPROX_QUANTILES(jsonPayload.duration_ms, 100)[OFFSET(95)]` per `jsonPayload.agent` → 4 per-branch P95 numbers. Compute:
   - `external_knowledge_branch_p95 = max(p95_gh, p95_devto, p95_so)` (parallel — slowest branch dominates).
   - `external_knowledge_total_p95 ≈ external_knowledge_branch_p95 + p95_synth`.
   - Compare to Phase 6-04-SUMMARY's recorded baseline.

3. **Tool cache hit ratio:**
   ```
   jsonPayload.event_type=("tool_cache_hit" OR "tool_cache_miss")
   ```
   Aggregate by event_type → `hit_ratio = hits / (hits + misses)`. Expected: non-zero hit ratio over 24h (community questions repeat — e.g., "what's the AI roadmap" recurs).

4. **PII redaction proof:**
   - Send a synthetic test message to the bot containing a fake card number: `4111 1111 1111 1111 test card`.
   - Wait 30s.
   - Query Cloud Logging for the model-input trace of that turn (revision_name filter + within 30s window). Verify the literal `4111 1111 1111 1111` does NOT appear in any log line for that turn; `[REDACTED PII]` does appear.

5. **Privacy gate (production sampling):**
   - Pull 20 random `agent.enter` events from the last 24h:
     ```
     jsonPayload.event_type="agent.enter"
     | sample 20
     ```
   - For each event, verify `jsonPayload` contains EXACTLY: `severity`, `event_type`, `agent`, `invocation_id`, `session_id_hash`, `enter_ts_ms`. No additional fields. No field contains a string that looks like a user query (length >50 chars, contains spaces or sentence punctuation) — those would indicate state-key leakage.

6. **Callback exception WARNING rate:**
   ```
   jsonPayload.message=~"lifecycle_(before|after)_agent error|pii_sanitizer error|(before|after)_tool_cache error"
   ```
   Expected: 0 warnings over 24h. If non-zero, debug each occurrence — the exception traceback is logged with `exc_info=True`.

7. **Error rate delta vs. baseline:** count of turns where `jsonPayload.status="error"` OR the bot returned no response. Compare to Phase 6-04-SUMMARY's post-deploy error rate.

**Format for each checkpoint in `07-04-SOAK-LOG.md`:**
```markdown
## Soak T+Xh (YYYY-MM-DD HH:MM UTC)

### Lifecycle events
| Agent | event_count (since deploy) | p50 duration_ms | p95 duration_ms |
|-------|---------------------------|-----------------|-----------------|
| community_assistant (root) | N | X | Y |
| onboarding_skill_level | N | X | Y |
| ... (12 rows total) ... |
| featured_resources_agent | N (or "MISSING — OQ1=NO") | X | Y |

### Tool cache
| event_type | count | ratio |
|------------|-------|-------|
| tool_cache_hit | H | H/(H+M) |
| tool_cache_miss | M | — |

### PII redaction
- Synthetic test message sent at <UTC time>: card number `4111-...`. Result: ✅ `[REDACTED PII]` observed in model-input trace; raw card number absent. OR ❌ raw card number leaked — Sev-1 rollback.

### Privacy gate sampling
- 20 random `agent.enter` events sampled. Field set conformance: ✅ all 20 / ❌ N out of 20 had extra fields.
- Content scrub: ✅ no field contains a string >50 chars / ❌ N events had suspicious string content.

### Callback exception warnings
- `cwa_callbacks WARNING` count over interval: N (expected 0).
- If non-zero: list the exc_info tracebacks here (scrubbed of any user content).

### AGENT-PAR-02 closure (Phase 6 carryover)
| Metric | Baseline (06-04-SUMMARY) | Phase 7 measured | Delta |
|--------|--------------------------|------------------|-------|
| external_knowledge p95 (end-to-end) | XXXX ms | YYYY ms | -ZZ% |
| gh_researcher p95 | — (new metric) | A ms | — |
| devto_researcher p95 | — | B ms | — |
| so_researcher p95 | — | C ms | — |
| external_knowledge_synthesizer p95 | — | D ms | — |
| Branch-max p95 = max(A,B,C) | — | E ms | — |
| Synthesized total = E + D | — | F ms | — |

### Observations
<narrative — any unexpected pattern in the per-leaf data? did one branch dominate? did the cache hit ratio surprise us?>
```

**Decision matrix at T+24h:**

| Phase 7 SC1-4 verdict | AGENT-PAR-02 verdict | Error-rate delta | Decision |
|-----------------------|----------------------|------------------|----------|
| All 4 green | ≥40% drop (newly measurable) | ≤ baseline | ✅ SHIP — Phase 7 complete AND Phase 6's deferred AGENT-PAR-02 retroactively GREEN. Mark both in STATE.md + ROADMAP.md. |
| All 4 green | <40% drop (still deferred) | ≤ baseline | ⚠ PARTIAL-SHIP for Phase 7 (SC1-4 green) — Phase 7 itself is GREEN. AGENT-PAR-02 remains DEFERRED, carry over to Phase 8 with a NEW STATE.md "Next Moves" entry pointing to a Phase 8 investigation (e.g., synthesizer streaming, gemini-2.5-flash-lite for non-flagship branches). |
| ≥1 of SC1-4 red | Any | Any | ❌ INVESTIGATE — identify which SC failed: PII not firing (Plan 07-01 production bug), cache not hitting (Plan 07-02 cardinality issue), lifecycle missing leaf (Plan 07-03 wiring missed in prod despite static grep), or privacy gate failed (Sev-1 rollback). Open a Plan 07-05 gap-closure. |
| Any | Any | Significant regression (e.g., +5pp error rate, OR `cwa_callbacks WARNING` count >50 in 24h, OR `KeyError`-style template failure visible) | ❌ ROLLBACK — `gcloud run services update-traffic cwa-assistant-bot --to-revisions=cwa-assistant-bot-00012-8x6=100`. Diagnose with Cloud Logging error breakdown, fix in a Plan 07-05 (gap closure), redeploy. |

**Critical regression signals (immediate rollback regardless of latency):**
- Raw card number / email / SSN visible in any log line for a turn where the user sent one (PII regression — Plan 07-01 layer broken in prod).
- `tool_cache_hit` returning content from a DIFFERENT tool than requested (cache poisoning — Plan 07-02 isolation broken).
- HMAC-hashed `user_id` appearing as raw `user_id` in any log — privacy invariant broken (should not happen because Phase 7 doesn't touch `usage_metrics.py`).
- Any unhandled Python exception propagating to Cloud Run's per-request error logs from `community_assistant/callbacks.py`.

**At T+24h decision time, record in `07-04-SOAK-LOG.md`:**
```markdown
## Final Decision (T+24h, YYYY-MM-DD HH:MM UTC)

**Phase 7 Success Criteria status:**
| SC | Description | Status |
|----|-------------|--------|
| SC1 | PII redaction observable | ✅/❌ |
| SC2 | Tool cache hit ratio measurable | ✅/❌ |
| SC3 | Lifecycle events for every leaf | ✅/❌ |
| SC4 | v6.0 HMAC privacy preserved | ✅/❌ |

**RESEARCH §14 Open Questions resolutions:**
- OQ1 (AgentTool callback firing): ✅ YES / ❌ NO — featured_resources_agent lifecycle event <observed | missing> in Cloud Logging.
- OQ3 (callback overhead p99): <bounded at N ms based on T+24h soak; method: compare avg latency_ms pre-/post-Phase-7 since architecture is purely additive>.

**AGENT-PAR-02 status (Phase 6 carryover):** GREEN (≥40% drop measured via per-leaf events) / DEFERRED (still <40%, carry to Phase 8) / N/A (insufficient traffic for statistical P95).

**Phase 7 Decision:** SHIP / PARTIAL-SHIP / ROLLBACK
**Rationale:** <1-3 sentences>
**Next action:** <commit phase-7-complete tag / open Plan 07-05 gap-closure / rollback command run>
**(If PARTIAL-SHIP) STATE.md handoff entry:** <reference to the new "Next Moves" entry — e.g., "Phase 8 AGENT-PAR-02 investigation: synthesizer streaming">
```

If SHIP: this plan completes, Phase 7 is done, AGENT-PAR-02 may close retroactively. If anything else: surface to user for next steps.
  </how-to-verify>
  <resume-signal>Type "SHIP — SC1-4 green, AGENT-PAR-02 <GREEN|DEFERRED>, OQ1=<YES|NO>" or "PARTIAL-SHIP — SC1-4 green but AGENT-PAR-02 still DEFERRED (carry to Phase 8)" or "ROLLBACK — <reason>" — with the SOAK-LOG.md updated and committed.</resume-signal>
  <verify>
    <automated>test -f /Users/amu1o5/personal/code-with-ahsan/.planning/phases/07-agent-safety-observability-callbacks/07-04-SOAK-LOG.md && grep -q "callback overhead" /Users/amu1o5/personal/code-with-ahsan/.planning/phases/07-agent-safety-observability-callbacks/07-04-SOAK-LOG.md && grep -q "T+24h" /Users/amu1o5/personal/code-with-ahsan/.planning/phases/07-agent-safety-observability-callbacks/07-04-SOAK-LOG.md && grep -q "Final Decision" /Users/amu1o5/personal/code-with-ahsan/.planning/phases/07-agent-safety-observability-callbacks/07-04-SOAK-LOG.md && grep -qE "OQ1.*(YES|NO)" /Users/amu1o5/personal/code-with-ahsan/.planning/phases/07-agent-safety-observability-callbacks/07-04-SOAK-LOG.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev `adk web` → in-memory ADK runtime | Same boundary as Phase 06-04 — InMemorySessionService, dev-only state. New Phase 7 callbacks fire in this surface FIRST. |
| `gcloud run deploy` → Cloud Run service | Authenticated deploy via existing CD credentials. Image push surface unchanged; Phase 7 adds no new build-time dependencies (freezegun is a dev-only dep, not packaged). |
| Cloud Run → Cloud Logging (Phase 7-augmented) | Existing structured-stdout path now carries new `jsonPayload.event_type` values (`agent.enter`, `agent.exit`, `tool_cache_hit`, `tool_cache_miss`) plus the literal `[REDACTED PII]` substring substituted by Plan 07-01. The Cloud Logging dashboard queries (Task 3) consume these fields. |
| Synthetic PII test query → Plan 07-01 PII layer | Task 3 Step 4 sends a card-number-like message to verify production redaction. The test message is fake (`4111 1111 1111 1111` is a known test value, not a real card). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-25 | Information Disclosure | Soak log accidentally includes raw user content from Cloud Logging excerpts | mitigate | Cloud Logging filters in Task 3 pull AGGREGATE metrics (counts, ratios, percentiles), NOT individual log entries with `jsonPayload` content. The privacy-gate sampling (Step 5) inspects FIELD NAMES + FIELD-VALUE LENGTHS, not field values. Excerpt of any raw user content into the soak log is explicitly forbidden — only HMAC'd identifiers and metric fields. |
| T-07-26 | Elevation of Privilege | Deploy pushes a broken image that bypasses PII redaction (e.g., callback wiring lost in prod) | mitigate | Part A Step 3 docker-runtime import + identity check confirms `root_agent.before_model_callback is pii_sanitizer` BEFORE deploy. Task 3 Step 4 sends a synthetic PII test message in production within 1h of deploy — if redaction fails, immediate rollback path. |
| T-07-27 | Denial of Service | New callback layer introduces per-call overhead that compounds at scale, causing latency regression | mitigate | RESEARCH §11 mandates regex compile-at-import (PII patterns are module-level). Plan 07-01 / 07-02 / 07-03 each have unit tests asserting callbacks return quickly (no I/O). Task 3's Open Question 3 bound (p99 callback overhead measured from soak) gives the production number. If >50ms, investigate — most likely culprit is regex backtracking on adversarial input (e.g., a 10KB user message hitting the PII patterns); mitigate by limiting `re.search` to first N chars of each content part. |
| T-07-28 | Tampering | Cloud Logging quota saturation from lifecycle event volume causes other logs to be dropped | accept | RESEARCH §9 Pitfall 4 calculation: ~12 events/turn × ~100 turns/day = 1200 events/day. Negligible against the project's existing Cloud Logging budget (Phase 02.1 baseline is ~3000 usage_metrics events/day; Phase 7 adds ~5x that). Stays well under 50 GiB/month free tier. If unexpected spike (>50K/day), Plan 07-05 gap-closure to downgrade lifecycle events to DEBUG severity + add Cloud Logging exclusion filter. |
| T-07-29 | Spoofing | Forged `session_id_hash` allows cross-turn correlation attack | accept | 16-hex (64-bit) HMAC truncation. Birthday-collision probability for 4600-member community = ~10^-10. Same disposition as T-07-23 in Plan 07-03 (`session_id_hash` used for correlation, NOT authentication). |
| T-07-30 | Repudiation | A deploy regression causes lifecycle events to STOP emitting silently | mitigate | Task 3 Step 1 (lifecycle event volume + per-leaf breakdown) is the regression sentinel — if ANY of the 12 expected agent names has zero events over 24h with reasonable traffic, surface as a soak failure. Task 2 Part B Step 3 also catches an immediate post-deploy stop (verifies lifecycle events fire within seconds of the first Discord message). |
</threat_model>

<verification>
**Per-task verification:**
- Task 1: User signals `smoke green — OQ1=<YES|NO>` or `BLOCKED — <symptom>`. If green, proceed; if blocked, fix in Plan 07-01/02/03 and retry.
- Task 2: User signals new revision name. `07-04-SOAK-LOG.md` exists with pre-deploy checklist + revision name recorded. Cloud Run shows 100% traffic on the new revision. First Discord message produces lifecycle events in Cloud Logging within 30s.
- Task 3: User signals SHIP / PARTIAL-SHIP / ROLLBACK with SC1-4 verdicts + AGENT-PAR-02 status. `07-04-SOAK-LOG.md` has 3 checkpoint sections (T+1h, T+6h, T+24h) + a Final Decision section with explicit OQ1 verdict.

**Plan-level verification:**
- Full `cd agent && .venv/bin/pytest -q` green BEFORE deploy (Task 2 Part A Step 1).
- `07-04-SOAK-LOG.md` exists, is committed to git, and has both pre-deploy checklist + Final Decision sections populated with concrete numbers (lifecycle event counts, cache hit ratio, per-leaf P95).
- If decision is SHIP and AGENT-PAR-02 closes GREEN: STATE.md "Next Moves #1" entry is removed/marked done in the same commit as the SUMMARY.
- If decision is SHIP/PARTIAL-SHIP with AGENT-PAR-02 still DEFERRED: a NEW STATE.md "Next Moves" entry is added pointing to Phase 8 investigation (do NOT silently drop the deferred gate).
- If decision is ROLLBACK: a Plan 07-05 gap-closure stub is opened per `/gsd-plan-phase --gaps`.
</verification>

<success_criteria>
- Local `adk web` 5-turn smoke run (Task 1) completed; user resumed with `smoke green — OQ1=<YES|NO>` AND the verdict captured in the soak log.
- RESEARCH §14 Open Question 1 (AgentTool execution path callback firing) formally resolved with diagnostic evidence from Turn 5 captured in `07-04-SOAK-LOG.md`. If NO, Phase 7 SUMMARY documents the limitation + the workaround path (manual logging in `lookup_featured_resource`).
- RESEARCH §14 Open Question 3 (callback overhead p99) bounded with a measured number from the 24h soak; recorded in `07-04-SOAK-LOG.md` under "OQ3" line.
- Pre-deploy checklist all green (Task 2 Part A Steps 1-6): full pytest green, docker build clean, callback identity check passes, `.gcloudignore` workaround in place, `USAGE_HASH_SECRET` bound, gcloud version + flag availability confirmed.
- `cwa-assistant-bot` deployed to Cloud Run with the Phase 7 image (Task 2 Part B); traffic on the new revision; healthcheck green; first Discord message produces lifecycle events within 30s.
- 24h soak completed (Task 3) with 3 checkpoint observations (T+1h, T+6h, T+24h) + a Final Decision section in `07-04-SOAK-LOG.md`.
- **Phase 7 SC1-4 gate:** all 4 success criteria from the Phase 7 ROADMAP entry verified GREEN in production via the soak log: (SC1) PII redaction observable, (SC2) tool cache hit ratio measurable, (SC3) lifecycle events for every leaf LlmAgent, (SC4) v6.0 HMAC privacy preserved.
- **AGENT-PAR-02 gate (Phase 6 carryover, STATE.md Next Moves #1):** the new per-leaf `duration_ms` events provide the substrate for re-evaluation. AGENT-PAR-02 marked GREEN (if measured ≥40% drop via per-leaf breakdown — Phase 7 retroactively closes Phase 6's deferred gate) OR DEFERRED with a NEW STATE.md "Next Moves" entry for Phase 8 investigation (e.g., synthesizer streaming, gemini-2.5-flash-lite for non-flagship branches). Bare "DISCUSS — accept the smaller win" without a STATE.md handoff entry is NOT a valid path.
- Privacy invariant preserved end-to-end: 20-sample audit of production `agent.enter` events shows ONLY the 6-field shape, no field contains user-query-shaped content (length-based heuristic in Task 3 Step 5).
- No regression on the HMAC-hashed usage metrics dashboard (Phase 02.1 + Phase 6 layer remains intact).
- `07-04-SOAK-LOG.md` committed to git as the formal soak artifact (atomic commit: `docs(phase-07): record 24h soak — Phase 7 <SHIP|PARTIAL-SHIP>, OQ1=<YES|NO>, AGENT-PAR-02 <closed|still-deferred>`).
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-safety-observability-callbacks/07-04-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. Required frontmatter: `phase`, `plan`, `subsystem: agent`, `tags: [adk, callbacks, deploy, cloud-run, soak, observability, pii, tool-cache, lifecycle, agent-par-02]`, `provides: [phase-7-production-verification, agent-par-02-closure-substrate, per-leaf-p95-dashboard]`, `requires: [07-01-pii-callbacks, 07-02-tool-cache-callbacks, 07-03-lifecycle-callbacks, 06-04-cloud-run-deploy-procedure]`, `affects: [Cloud Run cwa-assistant-bot revision, Cloud Logging dashboards, STATE.md Next Moves (AGENT-PAR-02 entry)]`. Include in body:

(a) The final `adk web` smoke verdict + RESEARCH §14 Open Question 1 resolution (AgentTool callback firing YES/NO with diagnostic evidence).
(b) RESEARCH §14 Open Question 3 resolution — observed callback overhead p99 bound from the 24h soak.
(c) Cloud Run revision name that landed (e.g., `cwa-assistant-bot-00013-phase7`) — for traceability.
(d) Phase 7 SC1-4 verdict — each of the four success criteria marked GREEN or RED with the underlying metric.
(e) **AGENT-PAR-02 closure outcome (Phase 6 carryover):** the per-leaf P95 breakdown from the soak (gh / devto / so / synthesizer), the synthesized `external_knowledge_total_p95`, the comparison to Phase 6-04-SUMMARY's baseline, and the GREEN-or-DEFERRED verdict. If DEFERRED, surface the STATE.md handoff entry path pointing to Phase 8.
(f) Reference back to the 3 prior plan SUMMARYs (Plans 07-01, 07-02, 07-03) — Phase 7's full surface is the union.
(g) Cross-phase contract handoff to Phase 8 (Agentic Orchestra Upgrade phase 3 — Mentor Intake Loop): the post-Phase-7 callback layer is the observability substrate Phase 8's LoopAgent + mentor_intake_clarifier will instrument with the SAME lifecycle callbacks (the LoopAgent's iterations each fire enter/exit per leaf, giving per-turn timing for the clarification loop). The HMAC `session_id_hash` is also reused. Reserved private state namespace `_cb_*` (Plan 07-03) is documented as off-limits to Phase 8 state writes.
(h) Talk-demo angle (RESEARCH §12): structured observability + cache hit ratio + PII safety make a strong showcase narrative for the upcoming conference talk — link to the demo script template.
</output>
