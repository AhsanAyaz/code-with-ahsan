---
phase: 09-marketing-site-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LayoutWrapper.tsx
autonomous: true
requirements: [SPEC-R5]

must_haves:
  truths:
    - "A persistent Sponsor button is visible in the header while scrolling on every page"
    - "The Sponsor button links to /sponsors and is teal (btn-accent), the Sponsor lane"
    - "On small screens the button is icon-only with an accessible aria-label and keeps a ≥44px tap target"
  artifacts:
    - path: "src/components/LayoutWrapper.tsx"
      provides: "Persistent header Sponsor affordance in navbar-end"
      contains: "/sponsors"
  key_links:
    - from: "src/components/LayoutWrapper.tsx"
      to: "/sponsors"
      via: "Link href"
      pattern: 'href="/sponsors"'
---

<objective>
Add the persistent header "Sponsor" button to the navbar (D-05) — the SPEC's nav-level Sponsor affordance and the one allowed side-nav exception. It sits in `navbar-end` before `ProfileMenu`, is always visible because the header is already `sticky top-0`, and uses the teal Sponsor lane.

Purpose: Makes sponsorship a first-class, always-visible entry point (SPEC Req 5) without touching the #263 side-nav mechanics.
Output: One additive nav item in `LayoutWrapper.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-marketing-site-enrichment/09-UI-SPEC.md
@.planning/phases/09-marketing-site-enrichment/09-PATTERNS.md

<interfaces>
From src/components/LayoutWrapper.tsx (lines 1-46) — current navbar-end (lines 35-38):
```tsx
import Link from "./Link";   // local Link (line 3) — reuse this, NOT next/link
// ...
<div className="navbar-end gap-2">
  <ProfileMenu />
  <SideNav />
</div>
```
Header is `sticky top-0` (line 14) → "always visible while scrolling" is satisfied for free.

UI-SPEC header spec (lines 143-145) locks the treatment: `Link` to `/sponsors`, `btn btn-sm btn-accent`, lucide `HandCoins` icon, label "Sponsor" shown ≥ sm, icon-only < sm with `aria-label="Sponsor"`, `min-h-11` so btn-sm keeps ≥44px tap height.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent Sponsor button to navbar-end</name>
  <files>src/components/LayoutWrapper.tsx</files>
  <read_first>src/components/LayoutWrapper.tsx (full, lines 1-46; target navbar-end is lines 35-38); 09-PATTERNS.md "LayoutWrapper MODIFY navbar-end" section (lines ~176-203); 09-UI-SPEC.md header spec (lines 143-145) + spacing exception (line 171)</read_first>
  <action>Insert a Sponsor `Link` as the FIRST child of the `navbar-end` div (before `ProfileMenu`), per UI-SPEC lines 143-145. Use the LOCAL `Link` already imported at line 3 (`./Link`), NOT `next/link`. Markup: `<Link href="/sponsors" aria-label="Sponsor" className="btn btn-sm btn-accent gap-1 min-h-11">` containing a lucide `HandCoins` icon (`className="w-4 h-4"`, `aria-hidden="true"`) and a `<span className="hidden sm:inline">Sponsor</span>` so the label shows ≥ sm and the button is icon-only < sm. Add `import { HandCoins } from "lucide-react";` (new import; HandCoins is confirmed present in lucide-react ^0.562.0). `btn btn-accent` = teal, the Sponsor lane — do NOT use primary/violet here. `min-h-11` guarantees the ≥44px touch floor for the icon-only variant (UI-SPEC line 171). Do NOT change `ProfileMenu`, `SideNav`, or any other nav mechanics from #263 (D-05).</action>
  <verify>
    <automated>grep -q "HandCoins" src/components/LayoutWrapper.tsx && grep -q 'href="/sponsors"' src/components/LayoutWrapper.tsx && grep -q "btn-accent" src/components/LayoutWrapper.tsx && npx tsc --noEmit 2>&1 | grep -i "LayoutWrapper" || echo "LayoutWrapper OK"</automated>
  </verify>
  <acceptance_criteria>
    - `navbar-end` contains a `Link` to `/sponsors` styled `btn btn-sm btn-accent` with `min-h-11`, placed before `ProfileMenu`.
    - `HandCoins` imported from `lucide-react`; icon has `aria-hidden="true"`; the Link has `aria-label="Sponsor"`.
    - Text label `Sponsor` is in a `hidden sm:inline` span (icon-only below sm).
    - `ProfileMenu` and `SideNav` are unchanged; uses local `./Link`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A teal Sponsor button is persistently visible in the header on all pages and routes to /sponsors.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                       |
| ---------- | ----------------------------------------------------------------- |
| none (new) | Static internal nav link; no new inputs, no API/data-model change |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                                   |
| --------- | ---------------------- | ---------------- | ----------- | --------------------------------------------------------------------------------- |
| T-09-02   | Information disclosure | Sponsor nav link | accept      | Links to an existing public page (`/sponsors`); no auth surface, no data exposed. |

</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Grep confirms the /sponsors link, HandCoins import, and btn-accent treatment.
- Manual (folded into 09-05 human-verify): button visible while scrolling, keyboard-focusable, icon-only < sm.
</verification>

<success_criteria>

- Persistent teal Sponsor button in the header routing to /sponsors; #263 nav mechanics untouched.
  </success_criteria>

<output>
Create `.planning/phases/09-marketing-site-enrichment/09-02-SUMMARY.md` when done.
</output>
