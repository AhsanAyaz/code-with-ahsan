---
phase: 09-marketing-site-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/SectionEyebrow.tsx
  - src/components/portfolio/PortfolioBio.tsx
  - src/components/portfolio/TestimonialsSection.tsx
autonomous: true
requirements: [SPEC-R3, SPEC-R7]

must_haves:
  truths:
    - "A single bracketed-mono eyebrow helper exists and renders lowercase self-closing tags (e.g. <work />) in JetBrains Mono"
    - "PortfolioBio can render its name as <h2> instead of <h1> when it is not the page's top heading"
    - "The testimonials section renders nothing (no empty shell) when the testimonials data array is empty"
  artifacts:
    - path: "src/components/home/SectionEyebrow.tsx"
      provides: "Shared bracketed-mono eyebrow helper (the phase signature motif)"
      exports: ["default"]
      min_lines: 8
    - path: "src/components/portfolio/PortfolioBio.tsx"
      provides: "About-Ahsan bio with configurable heading level"
      contains: "as ="
    - path: "src/components/portfolio/TestimonialsSection.tsx"
      provides: "Testimonials with graceful empty-state hide"
      contains: "length"
  key_links:
    - from: "src/components/home/SectionEyebrow.tsx"
      to: "consumers (hero, trusted-by, work group, testimonials, sponsor band)"
      via: "default export, tag prop"
      pattern: "export default"
---

<objective>
Build the shared bracketed-mono eyebrow helper (the phase's signature motif) and apply the two reuse-forced edits to the portfolio components so downstream plans can compose `/about` components onto home and `/sponsors` without re-authoring them.

Purpose: These three small artifacts are prerequisites consumed by every later plan. The eyebrow is the sitewide signature; the PortfolioBio heading-level prop is the single genuine restyle the reuse strategy (D-01) forces; the testimonials empty-state guard satisfies SPEC Req 3's graceful-degradation acceptance.
Output: `SectionEyebrow.tsx` (new), a heading-level prop on `PortfolioBio`, an empty-array guard on `TestimonialsSection`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-marketing-site-enrichment/09-SPEC.md
@.planning/phases/09-marketing-site-enrichment/09-UI-SPEC.md
@.planning/phases/09-marketing-site-enrichment/09-PATTERNS.md

<interfaces>
Existing mono-eyebrow implementations to converge into the shared helper:

From src/app/sponsors/page.tsx (lines 78-84) — the SectionEyebrow to mirror:

```tsx
function SectionEyebrow({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-center text-xs font-mono text-base-content/40 uppercase tracking-widest mb-3">
      {children}
    </p>
  );
}
```

UI-SPEC signature (lines 100-102, 190-198): the new motif is the SAME token vocabulary but LOWERCASE (drop `uppercase`) and framed as a self-closing tag, e.g. `<work />`. Utility register is JetBrains Mono 500, `text-xs`, `tracking-widest`, `text-base-content/40`.

From src/components/portfolio/PortfolioBio.tsx (line 39) — the h1 to make configurable:

```tsx
<h1 className="text-3xl font-bold text-base-content">Muhammad Ahsan Ayaz</h1>
```

From src/components/portfolio/TestimonialsSection.tsx (lines 1, 23) — the unconditional map:

```tsx
import { testimonials } from "@/data/testimonials";
// ...
{testimonials.map((testimonial, index) => ( ... ))}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared bracketed-mono SectionEyebrow helper</name>
  <files>src/components/home/SectionEyebrow.tsx</files>
  <read_first>src/app/sponsors/page.tsx lines 78-84 (SectionEyebrow analog); src/components/home/SocialReachBar.tsx lines 86-88 (inline mono eyebrow); 09-UI-SPEC.md Typography "Utility register" (lines 190-198) and Signature element (lines 99-105)</read_first>
  <action>Create a small presentational component `SectionEyebrow` (default export) that renders the phase's bracketed-mono eyebrow. Copy the token vocabulary VERBATIM from the sponsors analog (`sponsors/page.tsx:78-84`): `text-xs font-mono tracking-widest text-base-content/40`, but render LOWERCASE (no `uppercase` class) per UI-SPEC signature. Accept a `tag` string prop (e.g. "work", "testimonials", "trusted-by", "community-led") and render it framed as a self-closing tag `<{tag} />` using HTML entities so JSX does not parse it as an element. Accept an optional `children`/`suffix` for the descriptive tail (e.g. "— built and shared with the community") rendered in the same mono style. Accept an optional `align` (default centered) since the hero eyebrow is left/center-varying — keep the sponsors default of centered. Do NOT introduce a 5th type size or the 600 weight (per UI-SPEC typography lock). Keep it presentational — no data flow, no client directive needed.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "SectionEyebrow" || echo "no type errors in SectionEyebrow"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/home/SectionEyebrow.tsx` exists with a default export.
    - Rendering with tag="work" produces the literal text `<work />` (via entities), lowercase, in `font-mono text-xs tracking-widest`.
    - No `uppercase` class present; weight is the mono 500 register only (no `font-semibold`/`font-bold`, no 600).
    - `npx tsc --noEmit` reports no errors introduced by this file.
  </acceptance_criteria>
  <done>Shared eyebrow helper compiles and renders a lowercase bracketed tag + optional descriptive suffix.</done>
</task>

<task type="auto">
  <name>Task 2: Add configurable heading level to PortfolioBio (h1→h2 forced edit)</name>
  <files>src/components/portfolio/PortfolioBio.tsx</files>
  <read_first>src/components/portfolio/PortfolioBio.tsx (full, lines 1-149 — the `<h1>` is at line 39); 09-PATTERNS.md "sponsors page INSERT" section (the single genuine restyle, lines ~211-214); 09-UI-SPEC.md restyle call-out (line 149)</read_first>
  <action>Add an optional `as` (or `headingLevel`) prop to `PortfolioBio` defaulting to `"h1"` so existing `/about` usage (`src/app/about/page.tsx:24`) is unaffected (it renders as `<h1>` today, must stay `<h1>`). When `as="h2"` is passed, render the name heading (`PortfolioBio.tsx:39`) as an `<h2>` instead of `<h1>`, keeping the exact same className string (`text-3xl font-bold text-base-content`). This is the single reuse-forced restyle: `/sponsors` already owns an `<h1>` (`sponsors/page.tsx:92`) so the bio must demote to `<h2>` there to preserve one `<h1>` per page. Do NOT change any other markup, copy, avatar, GDE badge, or social links. Implement via a `Heading` local variable (`const Heading = as;`) or a conditional — do not duplicate the whole block.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "PortfolioBio" || echo "PortfolioBio typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `PortfolioBio` accepts an optional heading-level prop defaulting to `h1`.
    - Default (no prop) still renders `<h1>` — `/about` regression-safe.
    - Passing `as="h2"` renders the name as `<h2>` with an unchanged className.
    - No other markup or copy changed; `npx tsc --noEmit` clean for this file.
  </acceptance_criteria>
  <done>PortfolioBio renders h1 by default and h2 on demand; /about unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Guard TestimonialsSection against empty data (graceful hide)</name>
  <files>src/components/portfolio/TestimonialsSection.tsx</files>
  <read_first>src/components/portfolio/TestimonialsSection.tsx (full, lines 1-66); 09-SPEC.md Requirement 3 acceptance ("empty state degrades gracefully — section hidden"); 09-UI-SPEC.md Copywriting "Empty state heading" (section is hidden, no empty shell)</read_first>
  <action>At the top of `TestimonialsSection`, return `null` when `testimonials.length === 0` so no empty section shell renders (SPEC Req 3 acceptance: "section is hidden"). Do not otherwise change the card/rating markup (lines 22-60) or the heading — the bracketed-mono eyebrow for the home context is added by the composing page in a later plan (09-05), not here. Verify the grid still renders correctly at 1 card (no layout break) and 3+ cards — the existing `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` handles all counts; no change needed there.</action>
  <verify>
    <automated>grep -q "testimonials.length" src/components/portfolio/TestimonialsSection.tsx && echo "empty guard present"</automated>
  </verify>
  <acceptance_criteria>
    - `TestimonialsSection` returns `null` when the data array is empty (no `<section>` shell rendered).
    - With ≥1 testimonial, the existing grid renders unchanged.
    - No change to the card markup, ratings, or Topmate link.
  </acceptance_criteria>
  <done>Testimonials section hides gracefully at 0 items and renders unchanged at ≥1.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                            |
| ---------- | ---------------------------------------------------------------------- |
| none (new) | Presentational-only edits; no new inputs, no data-model or API changes |

## STRIDE Threat Register

| Threat ID | Category  | Component                    | Disposition | Mitigation Plan                                                                                                                                             |
| --------- | --------- | ---------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09-01   | Tampering | SectionEyebrow tag rendering | accept      | `tag` is a static string literal from developer code, never user input; rendered as text via entities, not `dangerouslySetInnerHTML`. No injection surface. |

</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors.
- `/about` page still renders PortfolioBio as `<h1>` (default prop) — no regression.
- Grep confirms the testimonials empty guard exists.
</verification>

<success_criteria>

- SectionEyebrow helper exists and renders lowercase bracketed tags in the mono utility register.
- PortfolioBio supports `as="h2"` without affecting default `/about` usage.
- TestimonialsSection hides at 0 items.
  </success_criteria>

<output>
Create `.planning/phases/09-marketing-site-enrichment/09-01-SUMMARY.md` when done.
</output>
