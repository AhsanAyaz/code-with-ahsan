---
phase: 09-marketing-site-enrichment
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/home/CommunityHero.tsx
  - src/components/home/TrustedByStrip.tsx
  - src/components/home/SponsorBand.tsx
autonomous: true
requirements: [SPEC-R1, SPEC-R4, SPEC-R5]

must_haves:
  truths:
    - "The hero renders the community badge, headline, subhead, a monospace proof-manifest block, and three distinct CTAs (Join Discord / Newsletter / Sponsor)"
    - "The hero's live member count still comes from /api/stats with a 5,000 fallback (D-04 integration unbroken)"
    - "A trusted-by strip renders the existing sponsor brand logos in currentColor, theme-aware, with the Cloudways height exception"
    - "A teal Sponsor band renders the locked heading/body/CTA and links to /sponsors"
    - "The Sponsor CTA is the only teal element in the hero; Join-the-community is the only primary/violet CTA"
  artifacts:
    - path: "src/components/home/CommunityHero.tsx"
      provides: "Rebuilt marketing hero with proof manifest + 3-CTA cluster"
      contains: "/api/stats"
    - path: "src/components/home/TrustedByStrip.tsx"
      provides: "Home trusted-by brand-logo strip reusing BRAND_LOGOS"
      contains: "BRAND_LOGOS"
    - path: "src/components/home/SponsorBand.tsx"
      provides: "Teal home Sponsor band CTA → /sponsors"
      contains: "btn-accent"
  key_links:
    - from: "src/components/home/CommunityHero.tsx"
      to: "/api/stats"
      via: "fetch in useEffect"
      pattern: "fetch\\(\"/api/stats\"\\)"
    - from: "src/components/home/TrustedByStrip.tsx"
      to: "src/app/sponsors/logos.ts"
      via: "BRAND_LOGOS import"
      pattern: "BRAND_LOGOS"
    - from: "src/components/home/SponsorBand.tsx"
      to: "/sponsors"
      via: "Link href"
      pattern: "/sponsors"
---

<objective>
Build the three new/rebuilt home components that carry the marketing weight: the rebuilt `CommunityHero` (bracket badge + headline + subhead + monospace proof manifest + 3-CTA cluster), the new `TrustedByStrip` (extraction of the sponsors brands strip, reusing the existing logo data), and the new teal `SponsorBand`. These are composed into `page.tsx` in the final plan (09-05).

Purpose: The hero is where the phase spends its boldness (SPEC Req 1 + UI-SPEC signature "proof manifest"); the strip satisfies Req 4; the band + hero Sponsor CTA satisfy Req 5's prominent home Sponsor entry. All three consume the shared `SectionEyebrow` from 09-01.
Output: `CommunityHero.tsx` (rebuilt), `TrustedByStrip.tsx` (new), `SponsorBand.tsx` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-marketing-site-enrichment/09-UI-SPEC.md
@.planning/phases/09-marketing-site-enrichment/09-PATTERNS.md
@.planning/phases/09-marketing-site-enrichment/09-01-SUMMARY.md

<interfaces>
Shared helper from 09-01:
```tsx
import SectionEyebrow from "@/components/home/SectionEyebrow";
// <SectionEyebrow tag="work"> — built and shared with the community </SectionEyebrow>
```

CommunityHero live-count fetch to KEEP VERBATIM (current CommunityHero.tsx:10-18) — D-04 locked:

```tsx
const [discordCount, setDiscordCount] = useState(5000);
useEffect(() => {
  fetch("/api/stats")
    .then((r) => (r.ok ? r.json() : null))
    .then((data) => {
      const count = data?.social?.discord?.count;
      if (typeof count === "number" && count > 0) setDiscordCount(count);
    })
    .catch(() => {});
}, []);
```

TrustedByStrip data source (reuse — DO NOT re-inline SVGs):

```tsx
import { BRAND_LOGOS } from "@/app/sponsors/logos"; // { name, url, svg }[]
```

Render pattern from sponsors/page.tsx:118-135 (copy verbatim incl. the Cloudways height conditional `logo.name === "Cloudways" ? "h-12 sm:h-14" : "h-7 sm:h-8"` and `dangerouslySetInnerHTML={{ __html: logo.svg }}`).

Locked copy (UI-SPEC Copywriting Contract, lines 226-245):

- Hero eyebrow: tag `community-led` — suffix "open to all developers"
- Hero headline: `Join {liveCount}+ developers learning together` (fallback 5,000)
- Hero subhead: keep the existing paragraph verbatim (CommunityHero.tsx:50-55)
- Proof-manifest rows: `gde : Google Developer Expert` · `books : 4 published` · `installs : 13M+` · `talks : 50+`
- Primary CTA: "Join the community" → https://codewithahsan.dev/discord (btn btn-primary, Users icon)
- Secondary CTA: "Subscribe to the newsletter" → newsletter (btn btn-outline, neutral)
- Tertiary CTA: "Sponsor us" → /sponsors (btn btn-outline, teal/accent)
- Sponsor band heading: "Put your product in front of 180,000+ developers"
- Sponsor band body: "Sponsorships across YouTube, Instagram, LinkedIn, the newsletter, and Discord — content developers already trust."
- Sponsor band CTA: "See sponsorship options" → /sponsors (btn btn-accent)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild CommunityHero (proof manifest + 3-CTA cluster)</name>
  <files>src/components/home/CommunityHero.tsx</files>
  <read_first>src/components/home/CommunityHero.tsx (full, lines 1-90); 09-PATTERNS.md "CommunityHero REBUILD" section (lines ~28-70); 09-UI-SPEC.md Signature element (lines 99-105), Motion Contract (lines 251-258), Copywriting Contract (lines 226-245), Typography (lines 176-198); src/components/portfolio/PortfolioBio.tsx lines 32-34 (credential-line mono vocabulary for the manifest)</read_first>
  <action>In-place REBUILD of `CommunityHero` (stays a "use client" component). KEEP verbatim: the `/api/stats` fetch + 5000 fallback (lines 10-18, D-04 — do NOT alter the fetch), the section shell `min-h-[85vh] flex items-center justify-center overflow-hidden border-b border-base-300` and the grid background (lines 21-24). MOTION: drop the DUAL glow orbs (lines 27-28) to ONE restrained soft blur behind the manifest (UI-SPEC Motion Contract). REPLACE the eyebrow pill (lines 32-38): keep the live pulse-dot pattern, but render the text via the shared `SectionEyebrow` with `tag="community-led"` and suffix "open to all developers" (bracketed-mono). HEADLINE: change to `Join {discordCount.toLocaleString()}+ developers learning together` per locked copy (sentence case; keep the gradient span treatment on the count). SUBHEAD: keep the existing paragraph (lines 50-55) unchanged. NEW proof-manifest block: build a compact monospace `key : value` readout from a local const array `MANIFEST = [{k:"gde",v:"Google Developer Expert"},{k:"books",v:"4 published"},{k:"installs",v:"13M+"},{k:"talks",v:"50+"}]`; each row renders the key in `font-mono text-base-content/50` (matches PortfolioBio:32) and the value in `font-mono text-primary` (Violet per UI-SPEC signature). REBUILD the CTA cluster (replaces lines 58-75) to exactly THREE CTAs per locked copy: (1) "Join the community" → Discord `btn btn-primary btn-lg` with `Users` icon (keep the href verbatim from lines 59-63); (2) "Subscribe to the newsletter" → anchor to `#newsletter` `btn btn-outline btn-lg` (neutral); (3) "Sponsor us" → `/sponsors` `btn btn-outline btn-lg` in the teal accent lane (e.g. `btn-outline` + accent text/border). The Sponsor CTA is the ONLY teal element in the hero; Join-the-community is the ONLY primary/violet CTA (reserved-color contract). Optional: use framer-motion (already a dependency, ^12.34.3) for a staggered fade-up load reveal (eyebrow→headline→manifest→CTAs); if used it MUST respect `prefers-reduced-motion` (collapse to instant, fully visible) — otherwise plain content with no motion is acceptable. Remove the old "Explore Mentorship" CTA and the bottom social-proof strip (lines 77-86) — replaced by the manifest. Stay within the 4-tier prose scale + mono utility register (no 5th size, no 600 weight).</action>
  <verify>
    <automated>grep -q 'fetch("/api/stats")' src/components/home/CommunityHero.tsx && grep -q "SectionEyebrow" src/components/home/CommunityHero.tsx && grep -Eq "Join the community" src/components/home/CommunityHero.tsx && npx tsc --noEmit 2>&1 | grep -i "CommunityHero" || echo "CommunityHero OK"</automated>
  </verify>
  <acceptance_criteria>
    - `/api/stats` fetch + 5000 fallback is byte-identical to the original (D-04 intact).
    - Hero renders: bracketed-mono `community-led` eyebrow, headline with live count, existing subhead, a mono `key : value` proof manifest with all 4 rows (values in `text-primary`), and exactly 3 CTAs.
    - CTA 1 → https://codewithahsan.dev/discord (btn-primary); CTA 2 → newsletter (btn-outline neutral); CTA 3 → /sponsors (teal/accent, the only teal in the hero).
    - Only one glow blur remains (dual orbs removed); any framer-motion reveal is reduced-motion-safe.
    - `npx tsc --noEmit` clean for this file.
  </acceptance_criteria>
  <done>The hero is a dense marketing unit with proof manifest + 3 distinct CTAs and an unbroken live-count integration.</done>
</task>

<task type="auto">
  <name>Task 2: Create TrustedByStrip (reuse BRAND_LOGOS)</name>
  <files>src/components/home/TrustedByStrip.tsx</files>
  <read_first>src/app/sponsors/page.tsx lines 115-136 (brands strip analog); src/app/sponsors/logos.ts lines 1-4 (BrandLogo type + BRAND_LOGOS export); 09-PATTERNS.md "TrustedByStrip NEW" section (lines ~73-104); 09-UI-SPEC.md section-map row 3 (line 126) + Copywriting trusted-by eyebrow (line 236)</read_first>
  <action>Create a new `TrustedByStrip` component (server component is fine — no client state). Import `{ BRAND_LOGOS } from "@/app/sponsors/logos"` — DO NOT duplicate or re-inline the SVG data (logos are `currentColor` mono, theme-aware for free — SPEC Req 4). Copy the render block from `sponsors/page.tsx:118-135` VERBATIM, including the Cloudways height conditional (`logo.name === "Cloudways" ? "h-12 sm:h-14" : "h-7 sm:h-8"` — load-bearing, the stacked mark needs more height) and `dangerouslySetInnerHTML={{ __html: logo.svg }}` (trusted static brand SVGs only — never user input). Above the logo row, render the shared `SectionEyebrow` with `tag="trusted-by"` and suffix "brands we've worked with". Per UI-SPEC row 3 the band is `base-100`, "fused under hero" — do NOT add a `border-t`/band break; wrap in a light `page-padding` section that reads visually continuous with the hero (no heavy divider). Wrap logos in the same `flex flex-wrap items-center justify-center gap-x-10 gap-y-6 text-base-content/70` container.</action>
  <verify>
    <automated>grep -q "BRAND_LOGOS" src/components/home/TrustedByStrip.tsx && grep -q "Cloudways" src/components/home/TrustedByStrip.tsx && grep -q "SectionEyebrow" src/components/home/TrustedByStrip.tsx && npx tsc --noEmit 2>&1 | grep -i "TrustedByStrip" || echo "TrustedByStrip OK"</automated>
  </verify>
  <acceptance_criteria>
    - Imports `BRAND_LOGOS` from `@/app/sponsors/logos` (no duplicated SVG strings).
    - Renders the `trusted-by` bracketed-mono eyebrow + the logo row with the Cloudways height exception preserved.
    - Logos rendered via `dangerouslySetInnerHTML` from the trusted static module only.
    - No heavy band divider (fused-under-hero, base-100); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Trusted-by strip renders the existing brand logos on home, theme-aware, without layout break.</done>
</task>

<task type="auto">
  <name>Task 3: Create SponsorBand (teal home Sponsor band)</name>
  <files>src/components/home/SponsorBand.tsx</files>
  <read_first>src/app/sponsors/page.tsx lines 139-159 (offer band shell analog); src/app/page.tsx lines 66-83 (existing radial-tint newsletter band to adapt from violet → teal); 09-PATTERNS.md "SponsorBand NEW" section (lines ~143-172); 09-UI-SPEC.md section-map row 9 (line 132) + Copywriting Sponsor band rows (lines 239-241)</read_first>
  <action>Create a new `SponsorBand` component (server component). Render a `<section>` with a teal-tinted band background: adapt the existing radial-gradient tint pattern from `page.tsx:67` by swapping the violet `rgba(143,39,224,0.05)` for a teal tint (accent color `#1fb2a6` → e.g. `rgba(31,178,166,0.06)`), OR use a light `bg-accent/5` band. Inside a `page-padding max-w-5xl mx-auto text-center` container render, VERBATIM from locked copy: an `<h2>` "Put your product in front of 180,000+ developers" (`text-3xl md:text-4xl font-bold text-base-content`), a `<p className="text-base-content/70">` "Sponsorships across YouTube, Instagram, LinkedIn, the newsletter, and Discord — content developers already trust.", and a `Link` (from `next/link`) `href="/sponsors"` `className="btn btn-accent"` labelled "See sponsorship options". Optionally add a `SectionEyebrow tag="sponsor"` above the heading. Teal discipline: `btn btn-accent` is the ONLY color treatment — no violet here (this is the Sponsor lane). Follow `py-16` section rhythm. Default `btn` satisfies the ≥44px tap target.</action>
  <verify>
    <automated>grep -q "btn-accent" src/components/home/SponsorBand.tsx && grep -q "/sponsors" src/components/home/SponsorBand.tsx && grep -q "180,000" src/components/home/SponsorBand.tsx && npx tsc --noEmit 2>&1 | grep -i "SponsorBand" || echo "SponsorBand OK"</automated>
  </verify>
  <acceptance_criteria>
    - Renders the locked heading, body, and `See sponsorship options` CTA linking to `/sponsors`.
    - CTA is `btn btn-accent` (teal) — no violet/primary in the band.
    - Teal-tinted band background (adapted from the newsletter radial tint or `bg-accent/5`).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A prominent teal Sponsor band renders the locked copy and routes to /sponsors.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                                                      |
| ------------------- | ---------------------------------------------------------------------------------------------------------------- |
| client → /api/stats | Existing read-only fetch, unchanged (D-04); no new inputs                                                        |
| static module → DOM | Brand SVGs injected via `dangerouslySetInnerHTML` from `src/app/sponsors/logos.ts` (trusted, developer-authored) |

## STRIDE Threat Register

| Threat ID | Category        | Component                                | Disposition | Mitigation Plan                                                                                                                                                              |
| --------- | --------------- | ---------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09-03   | Injection (XSS) | TrustedByStrip `dangerouslySetInnerHTML` | mitigate    | SVG source is the static `BRAND_LOGOS` module (developer-authored), never user input — reuse the existing sponsors-page pattern unchanged; do not accept dynamic/remote SVG. |
| T-09-04   | Tampering       | /api/stats response shape                | accept      | Hero already guards `typeof count === "number" && count > 0` and falls back to 5000 on any failure; no error UI, no console error.                                           |

</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Grep confirms: hero keeps `/api/stats` + uses SectionEyebrow + "Join the community"; strip imports BRAND_LOGOS + Cloudways exception; band uses btn-accent + /sponsors + locked heading.
- Visual verification (375px, both themes, reduced-motion, CTA routing) is performed in 09-05.
</verification>

<success_criteria>

- Hero renders badge + headline + proof manifest + 3 CTAs with unbroken /api/stats.
- Trusted-by strip reuses existing logos, theme-aware.
- Teal Sponsor band links to /sponsors with reserved-color discipline.
  </success_criteria>

<output>
Create `.planning/phases/09-marketing-site-enrichment/09-03-SUMMARY.md` when done.
</output>
