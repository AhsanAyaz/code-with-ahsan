---
phase: 09-marketing-site-enrichment
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-03", "09-04"]
files_modified:
  - src/app/page.tsx
autonomous: false
requirements: [SPEC-R1, SPEC-R2, SPEC-R3, SPEC-R4, SPEC-R5, SPEC-R6, SPEC-R9]

must_haves:
  truths:
    - "The home page renders, in order: banners, rebuilt hero, trusted-by strip, live stats + reach, Ahsan's work showcase (with <work /> group eyebrow), pillars, testimonials, founder/About block, teal Sponsor band, newsletter, FAQ"
    - "Ahsan's work showcase renders Books + Courses + Open Source from the shared data modules on home"
    - "Showcase sections degrade gracefully at 0/1/N items — inherited /about `.map()` behavior on populated data modules; no per-item guard added beyond the existing TestimonialsSection empty-guard (SPEC-R2 acceptance, verified visually)"
    - "The newsletter section is reachable via an id anchor so the hero 'Subscribe to the newsletter' CTA scrolls to it"
    - "Live /api/stats components (CommunityStats, SocialReachBar), HomeFAQ, and FounderCredibility all still render (SPEC Req 6)"
    - "npm run build passes with no serverless maxDuration > 60"
    - "Home and /sponsors have no horizontal scroll at 375px and no console errors in light + dark themes"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Recomposed marketing home page in the locked section order"
      contains: "TrustedByStrip"
  key_links:
    - from: "src/app/page.tsx"
      to: "getCourses"
      via: "await in async Home"
      pattern: "await getCourses"
    - from: "src/app/page.tsx"
      to: "src/components/home/SponsorBand.tsx"
      via: "component import"
      pattern: "SponsorBand"
    - from: "hero newsletter CTA"
      to: "newsletter section"
      via: "#newsletter anchor"
      pattern: "id=.newsletter."
---

<objective>
Recompose `src/app/page.tsx` into the locked marketing section flow, wiring in the new components from 09-03 (rebuilt hero, TrustedByStrip, SponsorBand), the shared `/about` showcase components (Books/Courses/OpenSource) under a `<work />` group eyebrow, and the empty-guarded TestimonialsSection from 09-01 — while retaining live stats, pillars, founder, newsletter, and FAQ (SPEC Req 6). Then run the quality-floor verification and a human visual QA (SPEC Req 9).

Purpose: This is the integrating plan that turns the sparse 8-section page into the jamwithai-shaped marketing landing and proves the whole phase ships without regression.
Output: Rebuilt `page.tsx` + build/quality verification + human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-marketing-site-enrichment/09-UI-SPEC.md
@.planning/phases/09-marketing-site-enrichment/09-PATTERNS.md
@.planning/phases/09-marketing-site-enrichment/09-01-SUMMARY.md
@.planning/phases/09-marketing-site-enrichment/09-03-SUMMARY.md

<interfaces>
Current home composition (src/app/page.tsx:40-88) — already an `async function Home()` (line 40) that awaits `getHomeBanners()`. Existing imports: siteMetadata, HomeFAQ, NewsletterForm, HomeBanners, getBanners, CommunityHero, PillarsGrid, CommunityStats, SocialReachBar, FounderCredibility.

New/updated components to import:

```tsx
import { getCourses } from "@/lib/content/contentProvider";
import TrustedByStrip from "@/components/home/TrustedByStrip"; // 09-03
import SponsorBand from "@/components/home/SponsorBand"; // 09-03
import SectionEyebrow from "@/components/home/SectionEyebrow"; // 09-01
import BooksSection from "@/components/portfolio/BooksSection";
import CoursesSection from "@/components/portfolio/CoursesSection"; // needs courses prop
import OpenSourceSection from "@/components/portfolio/OpenSourceSection";
import TestimonialsSection from "@/components/portfolio/TestimonialsSection"; // empty-guarded (09-01)
```

Locked home section order (09-UI-SPEC.md Section & Component Map, lines 120-134):
1 Banners (HomeBanners, env-gated) · 2 Hero (CommunityHero) · 3 Trusted-by (TrustedByStrip) · 4 Live stats + reach (CommunityStats + SocialReachBar) · 5 Ahsan's work (BooksSection + CoursesSection + OpenSourceSection under a `<work />` group eyebrow) · 6 Pillars (PillarsGrid) · 7 Testimonials (TestimonialsSection) · 8 Founder/About (FounderCredibility — do NOT also add PortfolioBio here, avoids duplicate bio, UI-SPEC row 8) · 9 Sponsor band (SponsorBand) · 10 Newsletter (existing inline block) · 11 FAQ (HomeFAQ).

Showcase group eyebrow copy: tag "work" — suffix "built and shared with the community". Testimonials eyebrow: tag "testimonials" — suffix "what mentees say".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose page.tsx into the locked marketing section order</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx (full, lines 1-89); src/app/about/page.tsx lines 18-33 (showcase composition + getCourses); 09-UI-SPEC.md Section and Component Map (lines 120-134) + restyle call-outs (lines 148-151); 09-PATTERNS.md "Home work showcase" (lines ~107-128) and "Home testimonials" (lines ~131-140); 09-03-SUMMARY.md + 09-01-SUMMARY.md</read_first>
  <action>Rewrite the `Home` component body (keep it `async`) to render the locked 11-section order. Add `const courses = await getCourses();` alongside the existing `const banners = await getHomeBanners();`. Keep Section 1 Banners as-is (lines 45-48). Keep Section 2 `<CommunityHero />` (now the rebuilt version from 09-03). Add Section 3 `<TrustedByStrip />` directly under the hero. Section 4: keep `<CommunityStats />` + `<SocialReachBar />` (untouched, D-04). Section 5 — Ahsan's work group: wrap a `<SectionEyebrow tag="work">built and shared with the community</SectionEyebrow>` group header ABOVE the three reused components `<BooksSection />`, `<CoursesSection courses={courses} />`, `<OpenSourceSection />` (compose exactly as about/page.tsx:27-33). Per UI-SPEC restyle note (lines 149-150) the group eyebrow owns the section title; the individual component `<h2>`s stay as-is unless they double-stack visually — executor's density call, NO data/component edit. Section 6 `<PillarsGrid />`. Section 7 `<TestimonialsSection />` (empty-guarded in 09-01) preceded by a `<SectionEyebrow tag="testimonials">what mentees say</SectionEyebrow>`. Section 8 `<FounderCredibility />` — do NOT add PortfolioBio here (UI-SPEC row 8 — avoids duplicate bio with /sponsors). Section 9 `<SponsorBand />` (from 09-03). Section 10: keep the existing inline newsletter `<section>` (lines 66-83) but add `id="newsletter"` to it so the hero's "Subscribe to the newsletter" CTA anchor (`#newsletter`) scrolls to it. Section 11 `<HomeFAQ />`. Follow the alternating base-100/base-200 band rhythm from the UI-SPEC map (the reused components already carry their own bands; wrap only the new group-eyebrow additions in `page-padding`). Do NOT touch metadata (lines 13-33) or `/api/stats` wiring.</action>
  <verify>
    <automated>grep -q "await getCourses" src/app/page.tsx && grep -q "TrustedByStrip" src/app/page.tsx && grep -q "SponsorBand" src/app/page.tsx && grep -q "TestimonialsSection" src/app/page.tsx && grep -q 'id="newsletter"' src/app/page.tsx && grep -q "CommunityStats" src/app/page.tsx && (npx tsc --noEmit 2>&1 | grep -i "app/page" || echo "page OK")</automated>
  </verify>
  <acceptance_criteria>
    - `Home` awaits both `getHomeBanners()` and `getCourses()`.
    - All 11 sections render in the locked order; TrustedByStrip, SponsorBand, and the Books/Courses/OpenSource showcase are present.
    - The `<work />` and `<testimonials />` bracketed-mono group eyebrows are rendered.
    - CommunityStats, SocialReachBar, FounderCredibility, HomeFAQ all still present (SPEC Req 6); PortfolioBio NOT added to home.
    - Newsletter section has `id="newsletter"` matching the hero CTA anchor.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Home renders the full jamwithai-shaped marketing flow with all retained elements intact.</done>
</task>

<task type="auto">
  <name>Task 2: Quality-floor build verification (SPEC Req 9)</name>
  <files>src/app/page.tsx</files>
  <read_first>09-SPEC.md Requirement 9 + Acceptance Criteria (lines 64-111); 09-UI-SPEC.md Quality Floor (lines 261-266)</read_first>
  <action>Run `npm run build` and confirm it passes. Confirm no serverless route sets `maxDuration > 60` (Vercel hobby ceiling). Confirm `/api/stats` integration is untouched (no edits to the fetch in CommunityHero/SocialReachBar/CommunityStats). Verification-only — fix any build error surfaced (e.g. a missing import or a client/server boundary issue from composing the new components: TrustedByStrip/SponsorBand are server components, CommunityHero stays a client component) and re-run until green. Do NOT add new dependencies or serverless config.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -15; (grep -rEn "maxDuration" src/app 2>/dev/null | grep -vE "maxDuration\s*[:=]\s*([1-9]|[1-5][0-9]|60)\b" && echo "MAXDURATION CHECK: review above" || echo "no maxDuration > 60 found")</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (production build succeeds).
    - No serverless route in `src/app` declares `maxDuration > 60`.
    - `/api/stats` fetch code unchanged across CommunityHero/SocialReachBar/CommunityStats.
  </acceptance_criteria>
  <done>The production build passes within Vercel hobby constraints and the stats integration is intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual QA — responsive, themes, motion, CTA routing</name>
  <what-built>The rebuilt marketing home page (hero with proof manifest + 3 CTAs, trusted-by strip, work showcase, testimonials, teal Sponsor band) and the enriched /sponsors page (About-Ahsan + work showcase), plus the persistent header Sponsor button.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000.
    2. HERO: confirm the bracketed `<community-led />` eyebrow, the headline with a live/`5,000+` count, the monospace proof-manifest (gde / books / installs / talks with values in violet), and three CTAs: "Join the community" (violet), "Subscribe to the newsletter" (neutral), "Sponsor us" (teal). Click each — Discord opens in a new tab, Newsletter scrolls to the newsletter section, Sponsor us goes to /sponsors.
    3. HEADER: confirm the teal "Sponsor" button is visible in the navbar and stays visible while scrolling; Tab to it and confirm a visible focus ring; resize below `sm` and confirm it becomes icon-only (still tappable, ≥44px).
    4. TRUSTED-BY + SHOWCASE + TESTIMONIALS + SPONSOR BAND render in order without overlap; the teal Sponsor band CTA "See sponsorship options" → /sponsors.
    5. /sponsors: confirm the About-Ahsan (PortfolioBio) section appears after the brands strip, the work showcase (books/courses/OSS) appears before the contact form, and there is exactly one page `<h1>` ("Partner with Code with Ahsan").
    6. RESPONSIVE: at 375px width, confirm NO horizontal scroll on home and /sponsors.
    7. THEMES: toggle light and dark — confirm brand logos render (currentColor) in both, no unreadable text, no broken layout.
    8. MOTION: enable OS "reduce motion" and reload — confirm hero content is fully visible with no transforms/animation.
    9. CONSOLE: confirm no errors in the browser console on either page in both themes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (section, theme, viewport, and what looked wrong).</resume-signal>
  <files>src/app/page.tsx, src/app/sponsors/page.tsx, src/components/LayoutWrapper.tsx</files>
  <action>Human verification only — no code changes. Run the dev server and walk the how-to-verify checklist across home + /sponsors in both themes, at 375px, and with reduced motion. If any issue is reported, capture it for a follow-up gap-closure task; do not self-approve.</action>
  <verify>
    <human-check>Reviewer confirms all 9 how-to-verify steps pass and types "approved".</human-check>
  </verify>
  <done>Reviewer has typed "approved"; no horizontal scroll at 375px, both themes render, reduced-motion safe, all CTAs route correctly, /sponsors has one h1, no console errors.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                       |
| ------------------- | ----------------------------------------------------------------- |
| client → /api/stats | Existing read-only fetch, unchanged (D-04)                        |
| build-time → page   | `getCourses()` reads content at build/request time; no user input |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                |
| --------- | ---------------------- | -------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09-06   | Denial of service      | async getCourses on home         | accept      | Home is already async (awaits getBanners); adding getCourses mirrors /about, stays under the 60s hobby ceiling. `npm run build` gate confirms. |
| T-09-07   | Information disclosure | recomposed public marketing page | accept      | All content is public marketing material; no auth surface, no user data rendered.                                                              |

</threat_model>

<verification>
- `npm run build` passes; no `maxDuration > 60`.
- `npx tsc --noEmit` clean.
- Grep confirms all new components wired + `id="newsletter"` anchor present.
- Human checkpoint: 375px no-scroll, light+dark, reduced-motion, CTA routing, single h1 on /sponsors, no console errors.
</verification>

<success_criteria>

- Home renders the full locked marketing flow with all retained elements (SPEC Req 1-6).
- Build passes within hobby constraints; both pages pass the visual quality floor (SPEC Req 9).
  </success_criteria>

<output>
Create `.planning/phases/09-marketing-site-enrichment/09-05-SUMMARY.md` when done.
</output>
