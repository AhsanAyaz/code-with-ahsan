---
phase: 09-marketing-site-enrichment
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/app/sponsors/page.tsx
autonomous: true
requirements: [SPEC-R7, SPEC-R8]

must_haves:
  truths:
    - "/sponsors renders an About-Ahsan section (PortfolioBio) between the hero+brands strip and 'What we offer'"
    - "The About-Ahsan bio renders its name as <h2> so the page keeps exactly one <h1>"
    - "/sponsors renders Ahsan's work showcase (Books + Courses + Open Source) after audience stats, before Contact"
    - "SponsorsPage is an async server component that awaits getCourses() to feed CoursesSection"
  artifacts:
    - path: "src/app/sponsors/page.tsx"
      provides: "Enriched sponsors page with About-Ahsan + shared work showcase"
      contains: "PortfolioBio"
  key_links:
    - from: "src/app/sponsors/page.tsx"
      to: "getCourses"
      via: "await in async server component"
      pattern: "await getCourses"
    - from: "src/app/sponsors/page.tsx"
      to: "PortfolioBio"
      via: "component import, as=h2"
      pattern: "PortfolioBio"
---

<objective>
Enrich `/sponsors` with the two new sections (SPEC Req 7 + 8): an "About Ahsan" block (reusing `PortfolioBio` with the h1→h2 demotion from 09-01) positioned as the person a brand buys access to, and the shared "Ahsan's work" showcase (Books + Courses + Open Source, the same components used on home). This requires converting the currently-SYNC `SponsorsPage` to async to await `getCourses()`.

Purpose: The user's "a bit empty" feedback on `/sponsors` is closed by adding founder authority + proof of output, reusing existing components (D-01) with zero new data layer.
Output: Two inserted sections in `sponsors/page.tsx` + async conversion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-marketing-site-enrichment/09-UI-SPEC.md
@.planning/phases/09-marketing-site-enrichment/09-PATTERNS.md
@.planning/phases/09-marketing-site-enrichment/09-01-SUMMARY.md

<interfaces>
From src/app/about/page.tsx (lines 18-33) — the canonical showcase composition to mirror:
```tsx
export default async function About() {
  const courses = await getCourses();          // from "@/lib/content/contentProvider"
  // ...
  <BooksSection />                              // self-fetches @/data/booksData.js
  <CoursesSection courses={courses} />          // the ONLY section taking a prop
  <OpenSourceSection />                          // self-fetches @/data/openSourceProjects
}
```
`getCourses(): Promise<CourseContent[]>` is exported from `@/lib/content/contentProvider` (contentProvider.ts:129).

From src/app/sponsors/page.tsx (current): `export default function SponsorsPage()` (line 86, SYNC). Hero+brands strip = lines 89-137; "What we offer" starts line 139; audience stats = lines 161-180; contact = lines 183-204. Page already owns an `<h1>` at line 92.

PortfolioBio heading-level prop from 09-01: `<PortfolioBio as="h2" />` renders the name as `<h2>` (default is `<h1>`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert SponsorsPage to async and insert About-Ahsan + work showcase</name>
  <files>src/app/sponsors/page.tsx</files>
  <read_first>src/app/sponsors/page.tsx (full, lines 1-207); src/app/about/page.tsx lines 18-33 (composition analog); 09-PATTERNS.md "sponsors/page.tsx INSERT About + showcase" section (lines ~207-224); 09-UI-SPEC.md Sponsors insertions table (lines 136-141) + restyle call-out (line 149); 09-01-SUMMARY.md (PortfolioBio prop name)</read_first>
  <action>Convert `SponsorsPage` from a sync to an async server component: change `export default function SponsorsPage()` (line 86) to `export default async function SponsorsPage()` and add `const courses = await getCourses();` at the top of the body (mirror about/page.tsx:18-19). Add imports: `getCourses` from `@/lib/content/contentProvider`, and `PortfolioBio`, `BooksSection`, `CoursesSection`, `OpenSourceSection` from `@/components/portfolio/*`. INSERTION 1 — About Ahsan: after the hero+brands strip `</section>` (closes at line 137) and before "What we offer" (line 139), render `<PortfolioBio as="h2" />`. The `as="h2"` is MANDATORY — the page already owns an `<h1>` (line 92), so the bio must demote to `<h2>` to preserve one `<h1>` per page (UI-SPEC line 149, the single reuse-forced restyle; the prop was added in 09-01). INSERTION 2 — Ahsan's work showcase: after the audience-stats `</section>` (closes line 180) and before the contact `<section id="contact">` (line 183), render the three showcase components in order: `<BooksSection />`, `<CoursesSection courses={courses} />`, `<OpenSourceSection />` — same as home Section 5. These render their own `<h2>`/band styling as-is (D-01, reuse). Optionally precede the showcase with the page's existing `SectionEyebrow` (uppercase mono, lines 78-84) for eyebrow consistency within /sponsors, OR omit — the reused components already carry their own headings; do NOT double-stack titles. Do NOT modify the hero, offer cards, audience stats, or contact form. Keep `maxDuration` untouched (no serverless config added — SPEC Req 9 hobby ceiling).</action>
  <verify>
    <automated>grep -q "async function SponsorsPage" src/app/sponsors/page.tsx && grep -q "await getCourses" src/app/sponsors/page.tsx && grep -q 'PortfolioBio as="h2"' src/app/sponsors/page.tsx && grep -q "CoursesSection courses={courses}" src/app/sponsors/page.tsx && npx tsc --noEmit 2>&1 | grep -i "sponsors/page" || echo "sponsors page OK"</automated>
  </verify>
  <acceptance_criteria>
    - `SponsorsPage` is `async` and awaits `getCourses()`.
    - `<PortfolioBio as="h2" />` inserted between the hero+brands strip and "What we offer"; the page still has exactly one `<h1>` (line 92).
    - `<BooksSection />`, `<CoursesSection courses={courses} />`, `<OpenSourceSection />` inserted after audience stats, before contact.
    - Hero, offer cards, audience stats, and contact form unchanged; no `maxDuration` config added.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>/sponsors renders About-Ahsan + the shared work showcase, with one h1 preserved.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                   |
| ----------------- | ----------------------------------------------------------------------------- |
| build-time → page | `getCourses()` reads content at build/request time; no user input, no new API |

## STRIDE Threat Register

| Threat ID | Category          | Component                     | Disposition | Mitigation Plan                                                                                                                                   |
| --------- | ----------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09-05   | Denial of service | async getCourses on /sponsors | accept      | Mirrors the already-shipped `/about` page which awaits the same call; no new serverless duration, stays under the 60s hobby ceiling (SPEC Req 9). |

</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Grep confirms async conversion, awaited getCourses, PortfolioBio as="h2", CoursesSection prop wiring.
- Exactly one `<h1>` on /sponsors (bio demoted to h2).
</verification>

<success_criteria>

- /sponsors gains an About-Ahsan section and the shared work showcase from the same components/data as home.
  </success_criteria>

<output>
Create `.planning/phases/09-marketing-site-enrichment/09-04-SUMMARY.md` when done.
</output>
