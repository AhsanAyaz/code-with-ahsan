---
phase: 02.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/content/blog/search/route.ts
  - src/app/api/content/blog/search/__tests__/route.test.ts
  - agent/community_assistant/platform_client.py
  - agent/community_assistant/sub_agents/content_agent.py
  - agent/community_assistant/agent.py
  - agent/tests/test_content_agent_blog.py
  - agent/tests/fixtures/content_blog.json
autonomous: true
requirements:
  - EXT-CONTENT-BLOG
user_setup:
  - service: ghost
    why: "Ghost Content API key needed for blog.codewithahsan.dev search proxy"
    env_vars:
      - name: GHOST_CONTENT_API_KEY
        source: "blog.codewithahsan.dev → Ghost Admin → Integrations → Custom Integration → Content API Key"
        note: "When piping to `vercel env add` / `gh secret set` / `gcloud secrets versions add`, use `printf '%s' \"$KEY\" | …` — never `echo`. `echo` appends a trailing `\\n` and corrupts the secret; past Vercel/GH Actions incidents documented in memory/feedback_env_vars.md."
must_haves:
  truths:
    - "ADK content_agent.search_blog_posts returns Ghost blog posts whose title or excerpt matches a query"
    - "Each blog result includes a canonical https://blog.codewithahsan.dev/{slug}/ URL"
    - "Repeated calls within 1 hour do NOT hit Ghost Content API upstream (Next.js ISR cache hit)"
    - "Root agent delegates blog questions ('any article on X', 'have you written about Y') to content_agent"
  artifacts:
    - path: "src/app/api/content/blog/search/route.ts"
      provides: "Next.js GET handler that proxies Ghost Content API with ISR cache"
      exports: ["GET"]
    - path: "src/app/api/content/blog/search/__tests__/route.test.ts"
      provides: "Vitest tests mocking upstream fetch and asserting response shape + cache hint"
    - path: "agent/community_assistant/sub_agents/content_agent.py"
      provides: "content_agent LlmAgent with search_blog_posts tool"
      contains: "search_blog_posts"
    - path: "agent/community_assistant/platform_client.py"
      provides: "fetch_blog_posts(query) helper that hits /api/content/blog/search"
      contains: "def fetch_blog_posts"
    - path: "agent/tests/test_content_agent_blog.py"
      provides: "Pytest unit tests for search_blog_posts (mocked _get)"
  key_links:
    - from: "agent/community_assistant/sub_agents/content_agent.py"
      to: "/api/content/blog/search"
      via: "platform_client.fetch_blog_posts"
      pattern: "fetch_blog_posts"
    - from: "src/app/api/content/blog/search/route.ts"
      to: "https://blog.codewithahsan.dev/ghost/api/content/posts/"
      via: "fetch() with revalidate"
      pattern: "revalidate"
    - from: "agent/community_assistant/agent.py"
      to: "content_agent"
      via: "sub_agents=[..., content_agent]"
      pattern: "content_agent"
---

<objective>
Add Ghost blog search to the ADK community assistant via an ISR-cached Next.js proxy route and a new `content_agent` sub-agent that surfaces Ahsan's owned blog content.

Purpose: Community members on Discord can ask "any article on RxJS" / "do you have a blog on signals" and get the actual blog.codewithahsan.dev URL — without burning Ghost API quota on every Discord message (3600s ISR per query). This plan also creates the `content_agent` skeleton that Plan 02 will extend with YouTube.

Output: New `/api/content/blog/search` route with Vitest coverage, new `content_agent` sub-agent wired into the root agent, new `platform_client.fetch_blog_posts` helper, and pytest unit tests for the tool.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-adk-content-external-knowledge-sub-agents/02.1-CONTEXT.md
@agent/community_assistant/agent.py
@agent/community_assistant/platform_client.py
@agent/community_assistant/sub_agents/mentorship_agent.py
@agent/community_assistant/sub_agents/projects_agent.py
@agent/tests/test_mentorship_agent.py
@agent/tests/test_projects_agent.py
@agent/tests/conftest.py
@src/app/api/mentorship/mentors/route.ts
@src/app/api/mentorship/mentors/semantic-search/__tests__/route.test.ts
@next.config.ts

<interfaces>
<!-- Existing patterns the executor must mirror. Read source files for full detail. -->

From `agent/community_assistant/platform_client.py`:
- `BASE_URL` constant (default `https://codewithahsan.dev`, override via `PLATFORM_API_BASE_URL`)
- `_get(path: str, params: dict | None = None) -> dict` — returns `{"ok": True, "data": ...}` on success, `{"ok": False, "error": str, "path": str}` on httpx error
- Module-level `_client = httpx.Client(...)` with 10s timeout
- Each tool helper: `def fetch_X(...) -> dict: return _get("/api/...", {...})`

From `agent/community_assistant/sub_agents/projects_agent.py` (canonical sub-agent shape):
- Imports: `from google.adk.agents import LlmAgent`, `from ..platform_client import BASE_URL, fetch_X`
- Private `_shape_X(raw: dict) -> dict` helper that returns LLM-friendly fields including a `url`
- Tool function with full docstring (Args/Returns) — ADK reads docstrings as tool signatures
- Tool error contract: returns `{"status": "error", "message": ..., "items": []}` on `_get` failure; `{"status": "success", "count": ..., "items": [...]}` on success
- `LlmAgent(name=..., model="gemini-2.5-flash", description=..., instruction=..., tools=[...])`

From `agent/community_assistant/agent.py`:
- Root `LlmAgent` with `sub_agents=[onboarding_agent, mentorship_agent, projects_agent, roadmap_agent]`
- `ROOT_INSTRUCTION` lists each sub-agent with a one-liner of when to delegate

From `src/app/api/mentorship/mentors/route.ts` (canonical Next.js route shape):
- `import { NextRequest, NextResponse } from "next/server"`
- `export async function GET(request: NextRequest)`
- `const { searchParams } = new URL(request.url)` — synchronous
- Returns `NextResponse.json({ ... }, { status: 200 })` / `({ error: ... }, { status: 4xx/500 })`

From `src/app/api/mentorship/mentors/semantic-search/__tests__/route.test.ts` (canonical Vitest pattern):
- `vi.hoisted()` for mocks needed inside `vi.mock(...)` factories
- `vi.mock("@/lib/firebaseAdmin", () => ({ db: { collection: mockCollection } }))`
- `import { GET } from "../route"` AFTER mocks
- `new NextRequest("http://test.local/api/...?q=...")`
- `beforeEach` resets and re-wires chained mocks

Ghost Content API URL pattern (confirm exact param syntax in Ghost docs via Context7 if needed):
`https://blog.codewithahsan.dev/ghost/api/content/posts/?key={GHOST_CONTENT_API_KEY}&limit=5&filter=title:~'{query}'&fields=title,slug,excerpt,published_at,feature_image`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Ghost blog ISR-cached search route + Vitest tests</name>
  <files>src/app/api/content/blog/search/route.ts, src/app/api/content/blog/search/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/mentorship/mentors/route.ts (canonical Next.js GET handler shape — error response, NextResponse.json status codes, new URL(request.url).searchParams pattern)
    - src/app/api/mentorship/mentors/semantic-search/__tests__/route.test.ts (canonical Vitest pattern — vi.hoisted, vi.mock factory ordering, NextRequest construction, mock reset+rewire in beforeEach)
    - next.config.ts (confirms blog.codewithahsan.dev is the Ghost domain via /blog/:path* rewrite — do NOT use the local rewrite as the upstream; call blog.codewithahsan.dev directly to avoid recursive rewrite)
  </read_first>
  <action>
    Create `src/app/api/content/blog/search/route.ts` exporting `async function GET(request: NextRequest)`. Read query param `q` from `new URL(request.url).searchParams`. Return 400 with `{ error: "Missing q param" }` when `q` is empty/missing (mirror semantic-search/route.ts behavior). When `q` is present, call `fetch()` against `https://blog.codewithahsan.dev/ghost/api/content/posts/` with query string `key=${process.env.GHOST_CONTENT_API_KEY}&limit=5&filter=${encodeURIComponent("title:~'" + q + "',excerpt:~'" + q + "'")}&fields=title,slug,excerpt,published_at,feature_image,reading_time` AND pass `{ next: { revalidate: 3600, tags: ["content:blog"] } }` as the fetch options object — this is the Next.js ISR cache directive. Return 502 with `{ error: "Ghost upstream error", status: upstreamRes.status }` on non-2xx upstream. On success, map `posts[]` to `{ title, slug, url: \`https://blog.codewithahsan.dev/${slug}/\`, excerpt, published_at, feature_image, reading_time }` and return `NextResponse.json({ posts: shaped, count: shaped.length }, { status: 200 })`. Return 500 with `{ error: "Failed to search blog" }` on thrown errors and `console.error("Blog search error:", err)`.

    Also handle the case where `GHOST_CONTENT_API_KEY` is unset: return 500 with `{ error: "GHOST_CONTENT_API_KEY not configured" }` BEFORE calling upstream. Do NOT log the key value.

    Create `src/app/api/content/blog/search/__tests__/route.test.ts` mirroring the semantic-search test file. Required test cases:
    1. `"returns 400 when q is missing"` — call GET on `http://test.local/api/content/blog/search` (no q), assert status 400 and body.error contains "Missing q".
    2. `"returns 500 when GHOST_CONTENT_API_KEY is unset"` — `delete process.env.GHOST_CONTENT_API_KEY`, call GET with `?q=angular`, assert status 500 and body.error matches /not configured/i.
    3. `"returns shaped posts on Ghost success"` — set GHOST_CONTENT_API_KEY=test-key, mock global `fetch` via `vi.stubGlobal("fetch", vi.fn().mockResolvedValue({ ok: true, status: 200, json: async () => ({ posts: [{ title: "Signals Deep Dive", slug: "signals-deep-dive", excerpt: "...", published_at: "2025-01-01", feature_image: null, reading_time: 5 }] }) }))`. Call GET with `?q=signals`. Assert status 200, `body.posts[0].url === "https://blog.codewithahsan.dev/signals-deep-dive/"`, `body.count === 1`. Also assert the fetch mock was called with a URL containing `blog.codewithahsan.dev/ghost/api/content/posts/` and an options arg whose `next.revalidate === 3600`.
    4. `"returns 502 on Ghost upstream non-2xx"` — mock fetch to resolve with `{ ok: false, status: 503, json: async () => ({}) }`, assert status 502.
    5. `"returns 500 on fetch throw"` — `vi.stubGlobal("fetch", vi.fn().mockRejectedValue(new Error("network down")))`, assert status 500.

    In `beforeEach`, `vi.unstubAllGlobals(); process.env.GHOST_CONTENT_API_KEY = "test-key";`. Use `vi.hoisted()` only if you need shared mock variables (the simple `vi.stubGlobal("fetch", ...)` approach does not require hoisting).
  </action>
  <verify>
    <automated>cd /Users/amu1o5/personal/code-with-ahsan && npx vitest run src/app/api/content/blog/search/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/api/content/blog/search/route.ts` exists and exports an async function named `GET`.
    - `grep -n "next: { revalidate: 3600" src/app/api/content/blog/search/route.ts` finds exactly one match (ISR directive present and value is 3600).
    - `grep -n "blog.codewithahsan.dev/ghost/api/content/posts" src/app/api/content/blog/search/route.ts` finds at least one match (correct upstream).
    - `npx vitest run src/app/api/content/blog/search/__tests__/route.test.ts` reports 5 passed, 0 failed.
    - The test asserting the shaped URL passes against the actual route handler (not just a stub) — verified by the route test importing `GET` from `../route`.
    - `npm run lint -- src/app/api/content/blog/search/` passes with zero errors.
  </acceptance_criteria>
  <done>Vitest suite for `/api/content/blog/search` is green (5 cases), the route returns ISR-cached shaped Ghost posts, and error paths (missing q, missing key, upstream non-2xx, fetch throw) are covered.</done>
</task>

<task type="auto">
  <name>Task 2: Add fetch_blog_posts to platform_client + create content_agent sub-agent + wire into root agent + pytest tests</name>
  <files>agent/community_assistant/platform_client.py, agent/community_assistant/sub_agents/content_agent.py, agent/community_assistant/agent.py, agent/tests/test_content_agent_blog.py, agent/tests/fixtures/content_blog.json</files>
  <read_first>
    - agent/community_assistant/platform_client.py (existing `_get()` and `fetch_mentors` / `fetch_projects` patterns — must mirror exact signature, docstring style, and return shape)
    - agent/community_assistant/sub_agents/projects_agent.py (canonical sub-agent shape — `_shape_X`, tool docstring format, error contract, `LlmAgent(...)` instantiation)
    - agent/community_assistant/sub_agents/mentorship_agent.py (multi-tool sub-agent pattern — multiple tools registered on one LlmAgent, tool dispatch instructions in `instruction=` block)
    - agent/community_assistant/agent.py (root agent — must add `content_agent` to both the import block AND `sub_agents=[...]` list AND extend `ROOT_INSTRUCTION` delegation rules with a new bullet)
    - agent/tests/test_projects_agent.py (canonical test pattern — `_shape_X` unit tests, tool happy/error path using `mock_platform_client` fixture)
    - agent/tests/conftest.py (the `mock_platform_client` fixture monkeypatches `community_assistant.platform_client._get` keyed by URL prefix — use prefix `/api/content/blog/search`)
  </read_first>
  <action>
    Step 1 — Add a new function to `agent/community_assistant/platform_client.py`, placed immediately after `fetch_roadmaps`. Signature: `def fetch_blog_posts(query: str) -> dict`. Body: a single `return _get("/api/content/blog/search", {"q": query})` call. Docstring: one-line, matching the style of `fetch_projects` ("Fetch Ghost blog posts matching a query from the ISR-cached /api/content/blog/search proxy."). No additional logic — `_get` already handles HTTP errors and returns the `{"ok": bool, ...}` envelope.

    Step 2 — Create `agent/community_assistant/sub_agents/content_agent.py`. Required structure:
    - Module imports: `from google.adk.agents import LlmAgent` and `from ..platform_client import BASE_URL, fetch_blog_posts`
    - Module constant: `_BLOG_LIMIT = 5`
    - Private `_shape_blog_post(raw: dict) -> dict` returning `{ "title", "slug", "url" (the raw["url"] passed through — the Next.js route already produces the canonical https://blog.codewithahsan.dev/{slug}/ URL), "excerpt" (raw["excerpt"] truncated to 300 chars), "published_at", "reading_time" }`. If `url` is missing in raw, fall back to `f"https://blog.codewithahsan.dev/{raw['slug']}/"` when `slug` is present, else `None` (matches the `url=None` pattern in `_shape_mentor`).
    - Tool function `search_blog_posts(query: str) -> dict` with full Args/Returns docstring describing it surfaces Ahsan's owned blog posts from blog.codewithahsan.dev. Body: strip query, return error dict when empty; call `fetch_blog_posts(q)`; on `result["ok"] is False` return `{"status": "error", "message": result["error"], "posts": []}`; on success slice to `_BLOG_LIMIT`, return `{"status": "success", "query": q, "count": len(sliced), "posts": [_shape_blog_post(p) for p in sliced]}`.
    - Module-level `content_agent = LlmAgent(name="content_agent", model="gemini-2.5-flash", description="Surfaces Code with Ahsan's owned content — blog posts from blog.codewithahsan.dev and YouTube videos (added in plan 02).", instruction=<<<see below>>>, tools=[search_blog_posts])`.
    - `instruction=` must include: (1) "When a user asks about a blog post or article Ahsan has written, call search_blog_posts(query) with the topic." (2) "ALWAYS cite the post URL in your reply — community trust is non-negotiable." (3) "If count is 0, say so honestly and suggest https://blog.codewithahsan.dev directly." (4) Single concrete next action at end of every reply.

    Step 3 — Wire into `agent/community_assistant/agent.py`:
    - Add import line `from .sub_agents.content_agent import content_agent` alongside the other sub-agent imports.
    - Append `content_agent` to the `sub_agents=[...]` list (after `roadmap_agent`).
    - Extend `ROOT_INSTRUCTION` delegation bullets with: `- content_agent: blog posts and (later) YouTube videos that Ahsan has published on blog.codewithahsan.dev / youtube.com/codewithahsan`. Place it as the last bullet (just before the "Rules:" block).

    Step 4 — Create `agent/tests/fixtures/content_blog.json` with shape `{ "posts": [...], "count": N }` matching the route response. Include at least 2 posts: one with full fields, one with minimal fields. Use realistic data (titles like "Angular Signals Deep Dive", URLs like "https://blog.codewithahsan.dev/angular-signals-deep-dive/").

    Step 5 — Create `agent/tests/test_content_agent_blog.py`. Required test cases (mirror `test_projects_agent.py` style):
    - `test_shape_blog_post_url_passthrough`: when raw has `url`, _shape returns it as-is.
    - `test_shape_blog_post_url_fallback_from_slug`: raw has no `url` but has `slug` → returned url is `https://blog.codewithahsan.dev/{slug}/`.
    - `test_shape_blog_post_no_slug_no_url`: raw has neither → url is None.
    - `test_search_blog_posts_returns_url(mock_platform_client, request fixture)`: load `content_blog.json` (create local fixture loader in conftest.py only if needed; otherwise inline-load via `json.loads(Path(...).read_text())`), register on path `/api/content/blog/search`, call `search_blog_posts("angular")`, assert status=="success", count>=1, every post has a non-empty url starting with `https://blog.codewithahsan.dev/`.
    - `test_search_blog_posts_empty_query_returns_error`: pass `""`, assert status=="error", posts==[].
    - `test_search_blog_posts_error_path(monkeypatch)`: monkeypatch `community_assistant.platform_client._get` to return `{"ok": False, "error": "boom", "path": "/api/content/blog/search"}`, call `search_blog_posts("anything")`, assert status=="error", posts==[].

    Add a `content_blog_payload` fixture to `agent/tests/conftest.py` that loads `content_blog.json` (mirror existing `mentors_payload` / `projects_payload` fixtures).
  </action>
  <verify>
    <automated>cd /Users/amu1o5/personal/code-with-ahsan/agent && uv run pytest tests/test_content_agent_blog.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `agent/community_assistant/sub_agents/content_agent.py` exists.
    - `grep -n "def search_blog_posts" agent/community_assistant/sub_agents/content_agent.py` finds exactly one match.
    - `grep -n "content_agent = LlmAgent" agent/community_assistant/sub_agents/content_agent.py` finds exactly one match with `name="content_agent"`.
    - `grep -n "def fetch_blog_posts" agent/community_assistant/platform_client.py` finds exactly one match.
    - `grep -n "from .sub_agents.content_agent import content_agent" agent/community_assistant/agent.py` finds exactly one match.
    - `grep -c "content_agent" agent/community_assistant/agent.py` returns >= 3 (import, sub_agents list, ROOT_INSTRUCTION delegation bullet).
    - `grep -v '^#' agent/community_assistant/agent.py | grep -c "content_agent"` returns >= 3 (non-comment matches — guards against header-only counts).
    - `cd agent && uv run pytest tests/test_content_agent_blog.py -x` reports >= 5 passed, 0 failed.
    - `cd agent && uv run pytest -x` (FULL suite — must not break the existing 41+ tests) reports 0 failures.
    - Existing tests `test_mentorship_agent.py`, `test_projects_agent.py`, `test_roadmap_agent.py` still pass unchanged (confirms root agent wiring did not break sibling sub-agents).
  </acceptance_criteria>
  <done>content_agent is registered with the root agent, exposes search_blog_posts(query), and pytest reports both the new blog tests and the full pre-existing suite green.</done>
</task>

</tasks>

<verification>
1. Run `cd /Users/amu1o5/personal/code-with-ahsan && npx vitest run src/app/api/content/blog/search/__tests__/route.test.ts` — 5 passed.
2. Run `cd /Users/amu1o5/personal/code-with-ahsan/agent && uv run pytest -x --tb=short` — full agent suite green including new `test_content_agent_blog.py`.
3. Confirm `grep -n "content_agent" agent/community_assistant/agent.py` shows import + sub_agents list + delegation bullet.
4. Manual smoke (optional, requires `GHOST_CONTENT_API_KEY` in `.env`): start `npm run dev`, `curl 'http://localhost:3000/api/content/blog/search?q=angular'` returns `{"posts":[...],"count":N}` with `url` fields under blog.codewithahsan.dev.
</verification>

<success_criteria>
- Ghost blog search route ships with ISR (`revalidate: 3600`) cache directive and 5 green Vitest cases covering happy/error paths.
- `content_agent` sub-agent is created with `search_blog_posts` tool, registered in root agent, and its instruction mandates URL citation.
- Plan-level pytest suite passes (new blog tests + all pre-existing tests untouched).
- Zero modifications to mentorship/projects/roadmap/onboarding sub-agents (they remain at their current behavior).
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-adk-content-external-knowledge-sub-agents/02.1-01-SUMMARY.md` capturing:
- What was built (route + content_agent skeleton + blog tool + tests + root wiring).
- Decisions made (e.g., how the Ghost filter syntax was constructed; whether feature_image / reading_time were included in the shape).
- Files modified with paths.
- Test counts (Vitest new cases; pytest new + total).
- Pointers for Plan 02 (the executor will EXTEND content_agent with search_youtube_videos and a new tool — the content_agent.py file is already wired into the root agent, so Plan 02 only modifies content_agent.py + adds the YouTube route + tests).
</output>
