---
phase: 02.1
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/api/content/youtube/search/route.ts
  - src/app/api/content/youtube/search/__tests__/route.test.ts
  - agent/community_assistant/platform_client.py
  - agent/community_assistant/sub_agents/content_agent.py
  - agent/tests/test_content_agent_youtube.py
  - agent/tests/fixtures/content_youtube.json
autonomous: true
requirements:
  - EXT-CONTENT-YOUTUBE
user_setup:
  - service: youtube
    why: "YouTube Data API v3 key needed for channel search proxy. v4.0 admin course creator already uses YT_API_KEY — reuse if present, do NOT create a duplicate YOUTUBE_API_KEY env var."
    env_vars:
      - name: YT_API_KEY
        source: "Google Cloud Console → APIs & Services → Credentials → YouTube Data API v3 key (existing key from v4.0 admin course creator)"
        note: "When piping to `vercel env add` / `gh secret set` / `gcloud secrets versions add`, use `printf '%s' \"$KEY\" | …` — never `echo`. `echo` appends a trailing `\\n` and breaks the API key signature; past Vercel/GH Actions incidents documented in memory/feedback_env_vars.md. If the v4.0 key is already in Vercel/Secret Manager, simply alias it for the new route — do not re-pipe."
    dashboard_config:
      - task: "Confirm Code With Ahsan channel ID (UC...) — should be hard-coded as a module constant in the route so the proxy never returns unrelated channels' videos"
        location: "https://youtube.com/codewithahsan → view source / channel about page, or via existing v4.0 admin course creator code"
must_haves:
  truths:
    - "ADK content_agent.search_youtube_videos returns videos from Code With Ahsan's YouTube channel matching a query"
    - "Each YouTube result includes a https://youtube.com/watch?v={id} URL and a thumbnail"
    - "Results are scoped to channelId of Code With Ahsan (not arbitrary YouTube results)"
    - "Repeated calls within 1 hour do NOT hit YouTube Data API v3 upstream (Next.js ISR cache hit, protecting the 10k daily-unit quota)"
    - "content_agent instruction now describes both blog and YouTube tools"
  artifacts:
    - path: "src/app/api/content/youtube/search/route.ts"
      provides: "Next.js GET handler that proxies YouTube Data API v3 search with ISR cache, scoped to Code With Ahsan's channel"
      exports: ["GET"]
    - path: "src/app/api/content/youtube/search/__tests__/route.test.ts"
      provides: "Vitest tests mocking upstream fetch and asserting response shape + cache hint + channelId scoping"
    - path: "agent/community_assistant/sub_agents/content_agent.py"
      provides: "content_agent now exposes both search_blog_posts AND search_youtube_videos"
      contains: "search_youtube_videos"
    - path: "agent/community_assistant/platform_client.py"
      provides: "fetch_youtube_videos(query) helper that hits /api/content/youtube/search"
      contains: "def fetch_youtube_videos"
    - path: "agent/tests/test_content_agent_youtube.py"
      provides: "Pytest unit tests for search_youtube_videos (mocked _get)"
  key_links:
    - from: "agent/community_assistant/sub_agents/content_agent.py"
      to: "/api/content/youtube/search"
      via: "platform_client.fetch_youtube_videos"
      pattern: "fetch_youtube_videos"
    - from: "src/app/api/content/youtube/search/route.ts"
      to: "https://www.googleapis.com/youtube/v3/search"
      via: "fetch() with revalidate"
      pattern: "youtube/v3/search"
    - from: "content_agent.tools"
      to: "[search_blog_posts, search_youtube_videos]"
      via: "LlmAgent tools= list"
      pattern: "search_youtube_videos"
---

<objective>
Extend the `content_agent` with YouTube video search, mirroring the Plan 01 blog pattern: ISR-cached Next.js proxy at `/api/content/youtube/search` plus a `search_youtube_videos(query)` tool on the existing content_agent.

Purpose: Discord community members can ask "do you have a video on Angular signals" / "any YouTube on RxJS" and get clickable youtube.com URLs sourced from Code With Ahsan's channel only — without burning YouTube Data API v3 daily-unit quota (10k/day) on every Discord message (3600s ISR per query, channelId-scoped).

Output: New `/api/content/youtube/search` route with Vitest coverage, extended `content_agent` with the YouTube tool, new `platform_client.fetch_youtube_videos` helper, and pytest unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-adk-content-external-knowledge-sub-agents/02.1-CONTEXT.md
@.planning/phases/02.1-adk-content-external-knowledge-sub-agents/02.1-01-SUMMARY.md
@agent/community_assistant/platform_client.py
@agent/community_assistant/sub_agents/content_agent.py
@src/app/api/content/blog/search/route.ts
@src/app/api/content/blog/search/__tests__/route.test.ts
@src/app/api/admin/courses/youtube/route.ts

<interfaces>
<!-- Critical reference: the v4.0 admin course creator uses `googleapis` SDK + `YT_API_KEY` (NOT `YOUTUBE_API_KEY`). -->
<!-- For this public proxy we will NOT use `googleapis` because:                                                   -->
<!--   1. The v4.0 route is admin-auth-gated, this one is unauthenticated (called by ADK agent through Next.js).   -->
<!--   2. Direct fetch() lets us pass `{ next: { revalidate: 3600 } }` which is the entire point of this layer.   -->
<!--   3. The `googleapis` client does not surface Next.js fetch caching directives.                              -->
<!-- We reuse the env var name `YT_API_KEY` (NOT `YOUTUBE_API_KEY`) to avoid creating a duplicate env var.        -->

From `src/app/api/admin/courses/youtube/route.ts`:
- Existing env var: `process.env.YT_API_KEY`  (reuse — do NOT introduce `YOUTUBE_API_KEY`)
- This route uses `google.youtube({ version: 'v3', auth: ... })`. We will NOT use that — direct fetch enables ISR.

YouTube Data API v3 search endpoint (channel-scoped):
`https://www.googleapis.com/youtube/v3/search?key={YT_API_KEY}&q={query}&type=video&channelId={CWA_CHANNEL_ID}&part=snippet&maxResults=5&order=relevance`

Response shape (relevant fields):
```
{
  items: [
    {
      id: { videoId: "..." },
      snippet: {
        title: "...",
        description: "...",
        publishedAt: "2025-01-01T00:00:00Z",
        thumbnails: { default: {url}, medium: {url}, high: {url} },
        channelTitle: "Code With Ahsan"
      }
    }
  ]
}
```

Code With Ahsan channel ID: Confirm before implementing. Options to locate it:
1. Check existing v4.0 code or env (`grep -rn "UC" agent src --include="*.ts" --include="*.py"` for hard-coded channel IDs starting with UC)
2. Fetch `https://www.youtube.com/codewithahsan` and parse the canonical channel URL
3. Use the YouTube Data API: `GET https://www.googleapis.com/youtube/v3/channels?forHandle=@codewithahsan&part=id&key={YT_API_KEY}`

Hard-code the resolved channel ID as a module constant `const CWA_CHANNEL_ID = "UC..."` in route.ts with a comment explaining it scopes the search to Code With Ahsan's channel only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve channel ID, add YouTube ISR-cached search route + Vitest tests</name>
  <files>src/app/api/content/youtube/search/route.ts, src/app/api/content/youtube/search/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/content/blog/search/route.ts (Plan 01 sibling — same ISR cache directive shape `{ next: { revalidate: 3600, tags: ["content:youtube"] } }`, same error response shape, same missing-key 500)
    - src/app/api/content/blog/search/__tests__/route.test.ts (Plan 01 sibling Vitest pattern with `vi.stubGlobal("fetch", ...)`)
    - src/app/api/admin/courses/youtube/route.ts (existing v4.0 YouTube integration — confirms env var name is `YT_API_KEY` not `YOUTUBE_API_KEY`; do NOT reuse the `googleapis` library — direct fetch enables Next.js ISR)
    - next.config.ts (no rewrites for googleapis.com — direct upstream call works)
  </read_first>
  <action>
    Step 1 — Resolve the Code With Ahsan YouTube channel ID. Run in order until one yields a `UC`-prefixed ID:
    (a) `grep -rn "UC[A-Za-z0-9_-]\{20,\}" src agent --include="*.ts" --include="*.tsx" --include="*.py" 2>/dev/null` — look for an existing hard-coded channel ID from v4.0 work.
    (b) If not found, `curl -sL https://www.youtube.com/codewithahsan | grep -oE '"channelId":"UC[A-Za-z0-9_-]+"' | head -1` (the public channel page embeds the canonical channelId in its JSON-LD).
    (c) If still not found, use the YouTube Data API: `curl -s "https://www.googleapis.com/youtube/v3/channels?forHandle=%40codewithahsan&part=id&key=$YT_API_KEY"` (requires YT_API_KEY in env).
    Record the resolved ID in the SUMMARY at the end.

    Step 2 — Create `src/app/api/content/youtube/search/route.ts`:
    - Top of file: `const CWA_CHANNEL_ID = "<resolved-UC-id>";  // Code With Ahsan channel — scopes all proxy searches to Ahsan's videos only.`
    - `import { NextRequest, NextResponse } from "next/server";`
    - `export async function GET(request: NextRequest)`:
      - Read `q` from `new URL(request.url).searchParams`. Return 400 `{ error: "Missing q param" }` if empty.
      - If `!process.env.YT_API_KEY` return 500 `{ error: "YT_API_KEY not configured" }`.
      - Build URL `https://www.googleapis.com/youtube/v3/search?key=${process.env.YT_API_KEY}&q=${encodeURIComponent(q)}&type=video&channelId=${CWA_CHANNEL_ID}&part=snippet&maxResults=5&order=relevance`.
      - `await fetch(url, { next: { revalidate: 3600, tags: ["content:youtube"] } })`.
      - Non-2xx: return 502 `{ error: "YouTube upstream error", status: upstreamRes.status }`.
      - On success, map `data.items[]` to `{ videoId: item.id.videoId, title: item.snippet.title, description: (item.snippet.description || "").slice(0, 300), url: \`https://youtube.com/watch?v=${item.id.videoId}\`, thumbnail: item.snippet.thumbnails?.high?.url || item.snippet.thumbnails?.medium?.url || item.snippet.thumbnails?.default?.url || null, published_at: item.snippet.publishedAt, channel_title: item.snippet.channelTitle }`. Return `NextResponse.json({ videos: shaped, count: shaped.length }, { status: 200 })`.
      - Wrap in try/catch returning 500 `{ error: "Failed to search YouTube" }` and `console.error("YouTube search error:", err)`.
      - Do NOT log `YT_API_KEY` or the full upstream URL containing the key.

    Step 3 — Create `src/app/api/content/youtube/search/__tests__/route.test.ts`. Required cases (mirror the blog test file structure):
    1. `"returns 400 when q is missing"` — assert status 400, body.error matches /missing q/i.
    2. `"returns 500 when YT_API_KEY is unset"` — `delete process.env.YT_API_KEY`, assert status 500, body.error matches /not configured/i.
    3. `"returns shaped videos and scopes search to CWA channelId"` — set `YT_API_KEY=test-key`, mock fetch returning `{ ok:true, status:200, json: async () => ({ items: [{ id: { videoId: "abc123" }, snippet: { title: "Angular Signals Deep Dive", description: "...", publishedAt: "2025-01-01T00:00:00Z", thumbnails: { high: { url: "https://i.ytimg.com/vi/abc123/hqdefault.jpg" } }, channelTitle: "Code With Ahsan" } }] }) }`. Call GET `?q=signals`. Assert status 200, `body.videos[0].url === "https://youtube.com/watch?v=abc123"`, `body.count === 1`. Then assert the fetch mock was called with a URL containing both `channelId=UC` and `youtube/v3/search`, AND with options arg `next.revalidate === 3600`.
    4. `"returns 502 on YouTube upstream non-2xx"`.
    5. `"returns 500 on fetch throw"`.

    Use `vi.stubGlobal("fetch", ...)` and `vi.unstubAllGlobals()` in `beforeEach` exactly as Plan 01 does.
  </action>
  <verify>
    <automated>cd /Users/amu1o5/personal/code-with-ahsan && npx vitest run src/app/api/content/youtube/search/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/api/content/youtube/search/route.ts` exists and exports an async `GET`.
    - `grep -n 'const CWA_CHANNEL_ID = "UC' src/app/api/content/youtube/search/route.ts` finds exactly one match (channel ID hard-coded as a UC-prefixed constant).
    - `grep -n 'process.env.YT_API_KEY' src/app/api/content/youtube/search/route.ts` finds at least one match — and `grep -n 'YOUTUBE_API_KEY' src/app/api/content/youtube/search/route.ts` finds ZERO matches (reuse of v4.0 env var name confirmed).
    - `grep -n 'next: { revalidate: 3600' src/app/api/content/youtube/search/route.ts` finds exactly one match.
    - `grep -n 'channelId=' src/app/api/content/youtube/search/route.ts` finds at least one match (channel-scoped query).
    - `npx vitest run src/app/api/content/youtube/search/__tests__/route.test.ts` reports 5 passed, 0 failed.
    - The "scopes to CWA channelId" test asserts the fetch mock was called with a URL substring containing `channelId=UC` — verifying scoping is wired and not just present in source.
    - `npm run lint -- src/app/api/content/youtube/` passes.
  </acceptance_criteria>
  <done>YouTube proxy route ships with a CWA-scoped channelId, ISR revalidate=3600, no `googleapis` dependency, and 5 green Vitest cases. The route does not log the API key.</done>
</task>

<task type="auto">
  <name>Task 2: Extend content_agent with search_youtube_videos tool + platform_client helper + pytest tests</name>
  <files>agent/community_assistant/platform_client.py, agent/community_assistant/sub_agents/content_agent.py, agent/tests/test_content_agent_youtube.py, agent/tests/fixtures/content_youtube.json</files>
  <read_first>
    - agent/community_assistant/platform_client.py (after Plan 01 it now contains `fetch_blog_posts` — add `fetch_youtube_videos` directly after it, same shape)
    - agent/community_assistant/sub_agents/content_agent.py (after Plan 01 it contains `search_blog_posts` + `content_agent` LlmAgent — extend with `search_youtube_videos`, do NOT touch root agent registration)
    - agent/community_assistant/sub_agents/mentorship_agent.py (multi-tool sub-agent reference — instruction block describes BOTH tools with a search strategy)
    - agent/tests/test_content_agent_blog.py (Plan 01 sibling test pattern — mirror it for YouTube)
    - agent/tests/conftest.py (add `content_youtube_payload` fixture mirroring `content_blog_payload`)
  </read_first>
  <action>
    Step 1 — Add a new function `fetch_youtube_videos(query: str) -> dict` to `agent/community_assistant/platform_client.py`, placed directly after `fetch_blog_posts` (introduced in Plan 01). Body: a single `return _get("/api/content/youtube/search", {"q": query})` call. Docstring: one-line, matching `fetch_blog_posts` style ("Fetch YouTube videos from Code With Ahsan's channel via the ISR-cached /api/content/youtube/search proxy."). No additional logic — `_get` handles HTTP errors.

    Step 2 — Extend `agent/community_assistant/sub_agents/content_agent.py`:
    - Add to module imports: `from ..platform_client import BASE_URL, fetch_blog_posts, fetch_youtube_videos` (the import line already exists from Plan 01 — extend, don't duplicate).
    - Add constant: `_YOUTUBE_LIMIT = 5`.
    - Add `_shape_youtube_video(raw: dict) -> dict` returning `{ "video_id": raw.get("videoId"), "title": raw.get("title"), "url": raw.get("url") if raw.get("url") else (f"https://youtube.com/watch?v={raw['videoId']}" if raw.get("videoId") else None), "description": (raw.get("description") or "")[:300], "thumbnail": raw.get("thumbnail"), "published_at": raw.get("published_at") }`.
    - Add tool function `search_youtube_videos(query: str) -> dict` with full Args/Returns docstring stating it surfaces videos from Code With Ahsan's YouTube channel only (not arbitrary YouTube). Body: strip query → empty error guard → `fetch_youtube_videos(q)` → error guard → slice to `_YOUTUBE_LIMIT` → return `{"status": "success", "query": q, "count": len(sliced), "videos": [_shape_youtube_video(v) for v in sliced]}`. Error path returns `{"status": "error", "message": ..., "videos": []}`.
    - Register on the agent: change `tools=[search_blog_posts]` to `tools=[search_blog_posts, search_youtube_videos]`.
    - Update the agent's `description=` to mention YouTube alongside blog: `"Surfaces Code with Ahsan's owned content — blog posts from blog.codewithahsan.dev and YouTube videos from youtube.com/codewithahsan."`.
    - Update the `instruction=` block to include YouTube guidance:
      - TOOLS section lists both `search_blog_posts(query)` and `search_youtube_videos(query)` with one-line summaries.
      - SEARCH STRATEGY: "If the user asks 'do you have a VIDEO on X' / 'YouTube on Y' → call search_youtube_videos. If they ask 'BLOG / ARTICLE / WRITTEN ON X' → call search_blog_posts. If ambiguous ('do you have anything on X') → call BOTH in parallel and merge the top results."
      - GUIDELINES preserve the URL-citation mandate (community trust is non-negotiable) and the "end with a single concrete next action" rule.

    Step 3 — Create `agent/tests/fixtures/content_youtube.json` with 2-3 realistic videos matching the route response shape (`videoId`, `title`, `description`, `url`, `thumbnail`, `published_at`, `channel_title`). Use the resolved CWA channel name "Code With Ahsan" in `channel_title`. Wrap as `{"videos": [...], "count": N}`.

    Step 4 — Add a `content_youtube_payload` pytest fixture to `agent/tests/conftest.py` (mirror existing fixtures).

    Step 5 — Create `agent/tests/test_content_agent_youtube.py`:
    - `test_shape_youtube_video_url_passthrough`: raw has `url` → returned as-is.
    - `test_shape_youtube_video_url_fallback_from_video_id`: raw has `videoId` but no `url` → returned url is `https://youtube.com/watch?v={videoId}`.
    - `test_shape_youtube_video_no_id_no_url`: neither → url is None.
    - `test_shape_youtube_video_truncates_description`: description >300 chars → truncated to 300.
    - `test_search_youtube_videos_returns_url(mock_platform_client, content_youtube_payload)`: register on `/api/content/youtube/search`, call `search_youtube_videos("angular")`, assert every video.url starts with `https://youtube.com/watch?v=`.
    - `test_search_youtube_videos_empty_query_returns_error`: empty query → status=="error".
    - `test_search_youtube_videos_error_path(monkeypatch)`: monkeypatch `_get` to return ok=False, assert status=="error", videos==[].
  </action>
  <verify>
    <automated>cd /Users/amu1o5/personal/code-with-ahsan/agent && uv run pytest tests/test_content_agent_youtube.py tests/test_content_agent_blog.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def search_youtube_videos" agent/community_assistant/sub_agents/content_agent.py` finds exactly one match.
    - `grep -n "def fetch_youtube_videos" agent/community_assistant/platform_client.py` finds exactly one match.
    - `grep -n "tools=\[search_blog_posts, search_youtube_videos\]" agent/community_assistant/sub_agents/content_agent.py` finds exactly one match (BOTH tools registered).
    - `grep -v '^#' agent/community_assistant/sub_agents/content_agent.py | grep -c "search_youtube_videos"` returns >= 4 (def, registration, instruction TOOLS bullet, instruction strategy reference).
    - `cd agent && uv run pytest tests/test_content_agent_youtube.py -x` reports >= 6 passed.
    - `cd agent && uv run pytest tests/test_content_agent_blog.py -x` STILL passes (Plan 01 tests not broken by extension).
    - `cd agent && uv run pytest -x` (full suite) reports 0 failures (no regression to mentorship/projects/roadmap tests).
  </acceptance_criteria>
  <done>content_agent now exposes both `search_blog_posts` and `search_youtube_videos` with citation-mandatory instruction; YouTube tool returns CWA-channel URLs; pytest suite (including Plan 01 tests) all green.</done>
</task>

</tasks>

<verification>
1. `cd /Users/amu1o5/personal/code-with-ahsan && npx vitest run src/app/api/content/youtube/search/__tests__/route.test.ts` — 5 passed.
2. `cd /Users/amu1o5/personal/code-with-ahsan/agent && uv run pytest -x --tb=short` — full suite green including new YouTube tests and unaffected blog/mentorship/projects/roadmap tests.
3. Confirm channel scoping: `grep -c "channelId=" src/app/api/content/youtube/search/route.ts` >= 1.
4. Confirm no duplicate env var introduction: `grep -rn "YOUTUBE_API_KEY" src/app/api/content/youtube/` returns ZERO matches (must reuse `YT_API_KEY`).
5. Manual smoke (optional, requires `YT_API_KEY` and `npm run dev`): `curl 'http://localhost:3000/api/content/youtube/search?q=signals'` returns videos under `youtube.com/watch?v=`.
</verification>

<success_criteria>
- YouTube proxy route ships with channelId scoping to Code With Ahsan, ISR revalidate=3600, env var name `YT_API_KEY` (not `YOUTUBE_API_KEY`).
- `content_agent` now exposes both `search_blog_posts` AND `search_youtube_videos` and its instruction routes between them by intent (VIDEO/YouTube → YT tool, ARTICLE/BLOG → blog tool, ambiguous → both).
- Full pytest suite green: Plan 01 blog tests + new YouTube tests + all 41 pre-existing tests.
- No new runtime dependency added (uses direct `fetch`, not `googleapis`).
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-adk-content-external-knowledge-sub-agents/02.1-02-SUMMARY.md` capturing:
- Resolved CWA channel ID (the UC… value hard-coded in route.ts).
- Whether `YT_API_KEY` was already present in `.env` or had to be added.
- Decisions made (e.g., direct `fetch` chosen over `googleapis` because ISR caching is the entire point).
- Files modified.
- Test counts (Vitest cases added; pytest new + total).
- Pointer for Plan 03 (the executor will add a SEPARATE `external_knowledge_agent` for GH/dev.to/SO, and at the end perform the ADK Cloud Run redeploy — Plan 03 will not modify content_agent.py).
</output>
