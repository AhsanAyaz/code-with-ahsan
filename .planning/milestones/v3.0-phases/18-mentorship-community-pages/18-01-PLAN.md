---
phase: 18-mentorship-community-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/mentorship/page.tsx
  - src/components/mentorship/MentorshipHero.tsx
  - src/components/mentorship/HowItWorks.tsx
  - src/components/mentorship/MentorshipStats.tsx
autonomous: true
requirements: [MENT-01, MENT-02, MENT-03, MENT-04]

must_haves:
  truths:
    - "Mentorship page hero says 'find a mentor' or 'become a mentor' — no community entry language"
    - "Visitor can see a step-by-step process of how the mentorship program works"
    - "Visitor can browse and search mentors from the mentorship landing page"
    - "Visitor sees mentorship stats (mentors, active mentorships, completed, avg rating) pulled from /api/stats"
  artifacts:
    - path: "src/components/mentorship/MentorshipHero.tsx"
      provides: "Mentorship-focused hero with find/become CTAs"
      contains: "Find a Mentor"
    - path: "src/components/mentorship/HowItWorks.tsx"
      provides: "Step-by-step process overview"
      contains: "how"
    - path: "src/components/mentorship/MentorshipStats.tsx"
      provides: "Mentorship stats from /api/stats"
      contains: "api/stats"
    - path: "src/app/mentorship/page.tsx"
      provides: "Assembled mentorship landing page"
  key_links:
    - from: "src/components/mentorship/MentorshipStats.tsx"
      to: "/api/stats"
      via: "fetch in useEffect"
      pattern: "fetch.*api/stats"
    - from: "src/app/mentorship/page.tsx"
      to: "src/components/mentorship/MentorshipHero.tsx"
      via: "import"
      pattern: "import.*MentorshipHero"
---

<objective>
Refocus the mentorship landing page from a generic "Join Our Mentorship Community" entry point to a mentorship-specific page that converts visitors into mentors or mentees.

Purpose: The current mentorship page hero uses community language and lacks a process overview. Visitors need to understand what the mentorship program is, how it works, and how to get started — either as a mentor or mentee.

Output: Refactored `/mentorship` page with new hero, "How It Works" section, stats from API, and retained mentor browsing grid.
</objective>

<execution_context>
@/home/ahsan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/api/stats/route.ts — the /api/stats response shape:
```typescript
type CachedStats = {
  community: {
    mentors: number;
    mentees: number;
    activeMentorships: number;
    completedMentorships: number;
    averageRating: number;
  };
  social: Record<string, { label: string; count: number; url: string }>;
  cachedAt: string;
};
```

From src/components/home/CommunityStats.tsx — established fetch+useEffect pattern:
```typescript
// Use the same pattern: fetch /api/stats, cancelled flag, return null on error, skeleton on loading
```

From src/app/mentorship/page.tsx — existing page is "use client", uses:
```typescript
import { AuthContext } from "@/contexts/AuthContext";
import { useMentorship } from "@/contexts/MentorshipContext";
import type { PublicMentor } from "@/types/mentorship";
import ProfileAvatar from "@/components/ProfileAvatar";
import DiscordValidationBanner from "@/components/mentorship/DiscordValidationBanner";
```

Existing page has: hero, stats banner, mentor browsing grid with search filter.
Keep: AuthContext/MentorshipContext usage, mentor fetching, filtering, MentorCard grid, DiscordValidationBanner.
Replace: hero section, stats banner. Add: HowItWorks section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MentorshipHero, HowItWorks, and MentorshipStats components</name>
  <files>src/components/mentorship/MentorshipHero.tsx, src/components/mentorship/HowItWorks.tsx, src/components/mentorship/MentorshipStats.tsx</files>
  <action>
Create three new components in src/components/mentorship/:

**MentorshipHero.tsx** ("use client"):
- Accepts props: `{ user, profile, loading, onRoleClick }` where onRoleClick is `(role: "mentor" | "mentee") => void`
- Same gradient card style as existing hero (bg-gradient-to-r from-primary to-secondary text-primary-content)
- Heading: "Find a Mentor or Share Your Expertise" (NOT "Join Our Mentorship Community")
- Subtext: focus on 1-on-1 guidance, career acceleration, structured mentorship
- CTAs: If loading, show spinner. If user has profile, show "Go to Dashboard" link to /mentorship/dashboard. Otherwise show two buttons: "Find a Mentor" (calls onRoleClick("mentee")) and "Become a Mentor" (calls onRoleClick("mentor")). Same button styling as existing (btn-lg bg-white/20 hover:bg-white/30 border-none)
- Keep DiscordValidationBanner rendering above or below hero if profile exists (pass through)

**HowItWorks.tsx** (server component, no "use client"):
- Section heading: "How It Works"
- 3 steps displayed in a horizontal card row (grid-cols-1 md:grid-cols-3):
  1. "Sign Up" — Register as a mentor or mentee. Describe your expertise or learning goals. (icon: UserPlus from lucide-react)
  2. "Get Matched" — Browse mentor profiles, find the right fit, and request a match. (icon: Users from lucide-react)
  3. "Grow Together" — Meet regularly, set goals, track progress, and level up your career. (icon: TrendingUp from lucide-react)
- Each step card: DaisyUI card bg-base-200 with step number badge (badge badge-primary), icon, title, description
- Use DaisyUI + Tailwind styling consistent with existing codebase (no custom CSS)

**MentorshipStats.tsx** ("use client"):
- Fetches from /api/stats using same pattern as CommunityStats (fetch+useEffect, cancelled flag, return null on error, skeleton loading)
- Displays 4 mentorship-specific stats: Active Mentors, Active Mentorships, Completed Mentorships, Avg Rating
- Use DaisyUI stats component (like the existing stats banner on the page but pulling from API instead of computing from mentor array)
- Stats layout: grid or DaisyUI stats bar, responsive
- Icons from lucide-react: UserCheck, BookOpen, CheckCircle, Star (same as CommunityStats uses)
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Three component files exist, TypeScript compiles without errors, MentorshipHero accepts user/profile/loading/onRoleClick props, HowItWorks shows 3 steps, MentorshipStats fetches from /api/stats</done>
</task>

<task type="auto">
  <name>Task 2: Rewire mentorship page to use new components</name>
  <files>src/app/mentorship/page.tsx</files>
  <action>
Refactor src/app/mentorship/page.tsx to replace the inline hero and stats banner with the new components while keeping all existing mentor browsing functionality.

Page structure (top to bottom):
1. DiscordValidationBanner (if profile exists) — keep as-is
2. MentorshipHero — pass user, profile, loading, handleRoleClick
3. MentorshipStats — self-contained, just render it
4. HowItWorks — self-contained, just render it
5. Mentor browsing section — keep the entire existing "Community Mentors" section as-is (search input, mentors grid, empty state, MentorCard rendering). Only change the heading from "Community Mentors" to "Browse Mentors"

Remove:
- The inline hero card (lines 84-125 of current page)
- The inline stats banner (lines 128-148 of current page) — replaced by MentorshipStats

Keep unchanged:
- All useState/useEffect hooks for mentors, filter, pendingRedirect
- The handleRoleClick function
- The filteredMentors logic
- The entire mentor card grid rendering
- All imports for AuthContext, useMentorship, PublicMentor, ProfileAvatar

Add imports:
- MentorshipHero from "@/components/mentorship/MentorshipHero"
- HowItWorks from "@/components/mentorship/HowItWorks"
- MentorshipStats from "@/components/mentorship/MentorshipStats"
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit --pretty 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Mentorship page renders MentorshipHero (with "Find a Mentor" language), HowItWorks (3 steps), MentorshipStats (from API), and mentor browsing grid. No inline hero or stats banner remains. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Mentorship page at /mentorship shows:
  - Hero with "Find a Mentor" / "Become a Mentor" focus (not "Join Our Mentorship Community")
  - "How It Works" section with 3 steps
  - Stats from /api/stats (mentors, active, completed, rating)
  - Mentor browsing grid with search (existing functionality preserved)
- No regression in authenticated user flows (dashboard redirect, onboarding redirect)
</verification>

<success_criteria>
- MENT-01: Hero says "Find a Mentor" not "Join Our Community"
- MENT-02: HowItWorks section shows 3-step process
- MENT-03: Mentor browsing with search preserved
- MENT-04: Stats pulled from /api/stats instead of computed from mentor array
</success_criteria>

<output>
After completion, create `.planning/phases/18-mentorship-community-pages/18-01-SUMMARY.md`
</output>
