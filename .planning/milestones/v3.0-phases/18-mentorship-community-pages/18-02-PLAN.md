---
phase: 18-mentorship-community-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/community/page.tsx
  - src/components/community/CommunityGetInvolved.tsx
  - src/components/community/CommunityStatsBar.tsx
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03]

must_haves:
  truths:
    - "Community page presents clear onramps to Discord, mentorship, projects, and roadmaps"
    - "Community page shows live community stats and social proof"
    - "Discord channel directory is accessible as secondary content, not the hero"
  artifacts:
    - path: "src/components/community/CommunityGetInvolved.tsx"
      provides: "Get Involved hero with onramp cards"
      contains: "Get Involved"
    - path: "src/components/community/CommunityStatsBar.tsx"
      provides: "Community stats from /api/stats"
      contains: "api/stats"
    - path: "src/app/community/page.tsx"
      provides: "Assembled community page"
  key_links:
    - from: "src/components/community/CommunityStatsBar.tsx"
      to: "/api/stats"
      via: "fetch in useEffect"
      pattern: "fetch.*api/stats"
    - from: "src/app/community/page.tsx"
      to: "src/components/community/CommunityGetInvolved.tsx"
      via: "import"
      pattern: "import.*CommunityGetInvolved"
---

<objective>
Transform the `/community` page from a Discord channel directory into a "Get Involved" hub with clear onramps to all community activities, live stats, and the Discord directory as secondary content.

Purpose: The current community page is entirely a Discord channel listing. Visitors need a clear picture of all the ways they can get involved (Discord, mentorship, projects, roadmaps) with community stats as social proof, before diving into Discord details.

Output: Redesigned `/community` page with "Get Involved" hero, onramp cards, community stats, and Discord directory pushed below.
</objective>

<execution_context>
@/home/ahsan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/api/stats/route.ts — the /api/stats response shape:
```typescript
type CachedStats = {
  community: {
    mentors: number;
    mentees: number;
    activeMentorships: number;
    completedMentorships: number;
    averageRating: number;
  };
  social: Record<string, { label: string; count: number; url: string }>;
  cachedAt: string;
};
```

From src/components/home/CommunityStats.tsx — established fetch+useEffect pattern:
```typescript
// Pattern: fetch /api/stats, cancelled flag, return null on error, skeleton on loading
```

From src/app/community/page.tsx — existing page exports:
```typescript
// Server component with metadata export
// Contains: channelCategories array, faqs array, GUILD_ID, DISCORD_INVITE constants
// All Discord channel data and FAQ data are inline — keep them in the file
```

Existing page structure: Hero -> Discord Channels Grid -> FAQ.
New structure: Get Involved Hero -> Stats -> Onramp Cards -> Discord Channels Grid -> FAQ.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CommunityGetInvolved and CommunityStatsBar components</name>
  <files>src/components/community/CommunityGetInvolved.tsx, src/components/community/CommunityStatsBar.tsx</files>
  <action>
Create src/components/community/ directory and two new components:

**CommunityGetInvolved.tsx** (server component, no "use client"):
- Section with heading: "Get Involved" and subtext about the community
- 4 onramp cards in a grid (grid-cols-1 md:grid-cols-2 lg:grid-cols-4):
  1. "Join Discord" — Connect with 4,300+ developers, get help, share knowledge. CTA links to https://discord.gg/KSPpuxD8SG (external link, target _blank). Icon: MessageCircle from lucide-react. Color accent: text-info.
  2. "Find a Mentor" — Get matched with an experienced developer for 1-on-1 guidance. CTA links to /mentorship. Icon: Users from lucide-react. Color accent: text-primary.
  3. "Join a Project" — Collaborate on real-world projects with structured team support. CTA links to /projects. Icon: FolderGit2 from lucide-react. Color accent: text-secondary.
  4. "Follow a Roadmap" — Structured learning paths from beginner to advanced. CTA links to /roadmaps. Icon: Map from lucide-react. Color accent: text-accent.
- Each card: DaisyUI card bg-base-200 border border-base-300, icon in colored circle, title, description, "Explore" or "Join" link button (btn btn-sm btn-outline)
- Use Next.js Link for internal links, anchor tag for Discord

**CommunityStatsBar.tsx** ("use client"):
- Fetches from /api/stats using same pattern as CommunityStats (fetch+useEffect, cancelled flag, return null on error, skeleton loading)
- Displays a horizontal stats bar with: Discord Members (from social.discord.count), Active Mentors, Active Mentorships, Avg Rating
- Compact layout: DaisyUI stats component or grid, fits in a single row on desktop
- Same graceful error handling: return null on error (established project pattern)
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Two component files exist in src/components/community/, TypeScript compiles without errors, CommunityGetInvolved has 4 onramp cards, CommunityStatsBar fetches from /api/stats</done>
</task>

<task type="auto">
  <name>Task 2: Redesign community page layout with new components</name>
  <files>src/app/community/page.tsx</files>
  <action>
Refactor src/app/community/page.tsx to use the new components while keeping all existing Discord channel data and FAQ content.

New page structure (top to bottom):
1. **Hero section** — Replace current Discord-only hero with a broader "Get Involved" hero:
   - Heading: "Get Involved with Code With Ahsan" (not "Join the CodeWithAhsan Community")
   - Subtext: Broaden to mention mentorship, projects, roadmaps, Discord — not just Discord
   - Keep a primary "Join Discord" CTA button and add a secondary "Explore Mentorship" button linking to /mentorship
2. **CommunityStatsBar** — render directly after hero
3. **CommunityGetInvolved** — onramp cards section
4. **Discord Channels section** — Keep the existing channelCategories grid code as-is (move it below the onramps). Keep the section heading "Explore Our Discord Channels" (renamed from "Explore Our Channels" to clarify it is Discord-specific)
5. **FAQ section** — Keep entirely as-is

Keep unchanged:
- All channelCategories data, ChannelItem/ChannelCategory types, channelUrl function
- All faqs data, FaqItem type
- GUILD_ID, DISCORD_INVITE constants
- Metadata export

Update metadata:
- title: "Get Involved - {siteMetadata.title}"
- description: Update to mention mentorship, projects, roadmaps alongside Discord

Add imports:
- CommunityGetInvolved from "@/components/community/CommunityGetInvolved"
- CommunityStatsBar from "@/components/community/CommunityStatsBar"
- Link from "next/link" (for internal Mentorship link in hero)
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx tsc --noEmit --pretty 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Community page renders: Get Involved hero, stats bar, onramp cards, Discord channel grid (secondary), FAQ. Build succeeds. Discord directory is present but not the hero content.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Community page at /community shows:
  - "Get Involved" hero (not just "Join Discord")
  - Live community stats from /api/stats
  - 4 onramp cards (Discord, Mentorship, Projects, Roadmaps)
  - Discord channel directory below onramps (secondary position)
  - FAQ section preserved
- All Discord channel links still work
</verification>

<success_criteria>
- COMM-01: Clear onramps to Discord, mentorship, projects, roadmaps visible on /community
- COMM-02: Community stats from /api/stats displayed on page
- COMM-03: Discord channel directory retained but positioned as secondary content below onramps
</success_criteria>

<output>
After completion, create `.planning/phases/18-mentorship-community-pages/18-02-SUMMARY.md`
</output>
