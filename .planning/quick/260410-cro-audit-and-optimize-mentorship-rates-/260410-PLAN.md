---
phase: quick
plan: 260410
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/rates/RatesClient.tsx
autonomous: true
requirements: [CRO-01, CRO-02, CRO-03]
must_haves:
  truths:
    - "Every featured package card has a visible CTA button linking to Book a call"
    - "A mid-page CTA appears between featured packages and a la carte section"
    - "A la carte content is organized in collapsible accordion sections, not a text wall"
    - "A social proof / trusted-by strip is visible below the hero"
    - "The Growth (Most popular) card has stronger visual differentiation than the other two"
    - "An availability note communicates limited slots near the packages section"
  artifacts:
    - path: "src/app/rates/RatesClient.tsx"
      provides: "CRO-optimized rates page with CTAs, social proof, collapsible a la carte"
      min_lines: 250
  key_links:
    - from: "Package card CTA buttons"
      to: "https://calendar.app.google/Z6g5dMyczq25hmjYA"
      via: "anchor href"
      pattern: "calendar\\.app\\.google"
    - from: "Mid-page CTA"
      to: "mailto:ahsan.ubitian@gmail.com"
      via: "anchor href"
      pattern: "ahsan\\.ubitian"
---

<objective>
CRO-optimize the sponsorship rates page (codewithahsan.dev/rates) by adding strategic CTAs to package cards, a mid-page conversion point, social proof, collapsible a la carte sections, and visual hierarchy improvements to the featured packages.

Purpose: The current page has zero CTAs on package cards (the highest-intent moment), a massive text wall for a la carte pricing, no social proof, and weak visual differentiation on the recommended package. These are standard CRO failures that directly reduce conversion.

Output: Updated RatesClient.tsx with all conversion optimizations applied.
</objective>

<execution_context>
@/home/ahsan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/rates/RatesClient.tsx

Key constants to preserve:
- Book a call URL: https://calendar.app.google/Z6g5dMyczq25hmjYA
- Email: ahsan.ubitian@gmail.com?subject=Collaboration%20Inquiry%20%E2%80%94%20%5BYour%20Brand%5D
- Brand color: #9B00D9 (neon-purple)
- Past collaborations: Airia (airia.com), Kimi/Moonshot AI (kimi.com), Cloudways (cloudways.com)

<interfaces>
From src/app/rates/RatesClient.tsx:
```typescript
// Component receives a `post` prop with markdown article and resources
export default function RatesClient({ post }: { post: any })

// post.article is a markdown string rendered via LegitMarkdown
// post.resources is an array of resource links

// Existing imports used:
import LegitMarkdown from "@/components/LegitMarkdown";
import ResourcesLinks from "@/components/ResourcesLinks";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CTAs to package cards, mid-page CTA, and availability note</name>
  <files>src/app/rates/RatesClient.tsx</files>
  <action>
Modify the FEATURED_PACKAGES data and rendering in RatesClient.tsx:

1. **Add CTA button to each package card.** After the deliverables `<ul>`, add a "Book a call" anchor button styled consistently with the closing CTA buttons. Use the existing calendar URL (https://calendar.app.google/Z6g5dMyczq25hmjYA). Style: full-width, bg-[#9B00D9] for the Growth/popular card, border border-neon-purple outline style for the other two. Add `mt-6` spacing.

2. **Enhance Growth card visual weight.** The Growth card already has a badge and purple border. Add: `shadow-lg shadow-neon-purple/20` to the card, `scale-[1.02]` transform on md+ screens (use `md:scale-[1.02]`), and make its CTA button filled (bg-[#9B00D9]) while the other two cards get outline-style CTAs.

3. **Add a la carte equivalent anchoring to packages.** Add an `alaCarteValue` field to each FEATURED_PACKAGES object:
   - Awareness: "$1,950" (reel $700 + story set $350x3 = $1,050... actually just use reasonable values that make the savings line make sense with the existing "Save $X" text)
   - Growth: "$4,400"
   - Authority: "$6,600"
   Display as a struck-through price above the actual price: `<span className="text-sm text-gray-500 line-through">{pkg.alaCarteValue}</span>` next to or above the main price.

4. **Add availability note.** Below the stats grid in the hero (after the closing `</div>` of the grid), add a subtle availability line:
   ```
   <p className="mt-4 text-sm text-gray-400">
     <span className="inline-block w-2 h-2 rounded-full bg-green-400 mr-2 animate-pulse"></span>
     Currently accepting 4 brand partnerships per month
   </p>
   ```

5. **Add mid-page CTA between packages and a la carte.** After the featured packages `</section>` and before the a la carte markdown `{post.article && ...}` section, insert:
   ```jsx
   <section className="mb-12 text-center py-8 border-y border-white/10">
     <p className="text-xl font-semibold mb-2">Not sure which package fits?</p>
     <p className="text-gray-400 mb-6">Tell us about your product and goals — we will recommend the right format.</p>
     <div className="flex flex-col sm:flex-row gap-3 justify-center">
       <a href="mailto:ahsan.ubitian@gmail.com?subject=Collaboration%20Inquiry%20%E2%80%94%20%5BYour%20Brand%5D"
          className="inline-block px-6 py-2.5 bg-[#9B00D9] text-white font-bold rounded-lg hover:opacity-90 transition-opacity">
         Send a brief
       </a>
       <a href="https://calendar.app.google/Z6g5dMyczq25hmjYA" target="_blank" rel="noreferrer"
          className="inline-block px-6 py-2.5 border border-neon-purple text-neon-purple font-bold rounded-lg hover:bg-neon-purple/10 transition-colors">
         Book a quick call
       </a>
     </div>
   </section>
   ```
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Every package card has a CTA button. Growth card has enhanced visual treatment. A la carte value comparison shown on each card. Availability note visible below hero stats. Mid-page CTA section exists between packages and a la carte content.</done>
</task>

<task type="auto">
  <name>Task 2: Add social proof strip and collapsible a la carte sections</name>
  <files>src/app/rates/RatesClient.tsx</files>
  <action>
Continue modifying RatesClient.tsx:

1. **Add social proof / trusted-by strip below the hero.** After the `</header>` tag and before the featured packages section, add a "Trusted by" strip. Since we do not have actual logo image files, use a clean text-based treatment:
   ```jsx
   <section className="mb-12 text-center">
     <p className="text-xs uppercase tracking-widest text-gray-500 mb-4">Past brand collaborations</p>
     <div className="flex flex-wrap justify-center items-center gap-6 sm:gap-10">
       {[
         { name: "Airia", url: "https://airia.com" },
         { name: "Kimi (Moonshot AI)", url: "https://kimi.com" },
         { name: "Cloudways", url: "https://cloudways.com" },
       ].map((brand) => (
         <a key={brand.name} href={brand.url} target="_blank" rel="noopener noreferrer"
            className="text-lg sm:text-xl font-bold text-gray-400 hover:text-neon-purple transition-colors">
           {brand.name}
         </a>
       ))}
     </div>
   </section>
   ```

2. **Wrap a la carte markdown in collapsible accordion sections.** The `post.article` markdown is a single blob we cannot easily split by platform. Instead, wrap the entire a la carte section in a DaisyUI collapse component so it is not a wall of text by default:
   ```jsx
   {post.article && (
     <section className="mb-8">
       <h2 className="text-2xl font-bold mb-4 text-center">A la carte options</h2>
       <p className="text-center text-gray-400 mb-6 text-sm">Individual placements if you prefer to build your own package.</p>
       <div className="collapse collapse-arrow border border-white/10 bg-white/5 rounded-2xl">
         <input type="checkbox" />
         <div className="collapse-title text-lg font-semibold">
           View all platform pricing
         </div>
         <div className="collapse-content">
           <LegitMarkdown
             components={{
               a: (props: any) => (
                 <a className="text-neon-purple" target="_blank" rel="noreferrer" {...props}>
                   {props.children}
                 </a>
               ),
             }}
           >
             {post.article}
           </LegitMarkdown>
         </div>
       </div>
     </section>
   )}
   ```
   This replaces the existing bare `<LegitMarkdown>` rendering. The content is still fully accessible but hidden behind a single click, dramatically reducing visual noise and decision paralysis.

3. **Simplify Usage Rights reference.** Add a brief note inside the mid-page CTA or below the collapse:
   After the collapse `</div>`, add:
   ```jsx
   <p className="text-center text-sm text-gray-500 mt-4">
     Usage rights, whitelisting, and add-ons are available — details discussed on call.
   </p>
   ```

4. **Keep ResourcesLinks rendering intact** — the `{post.resources?.length > 0 && ...}` block remains unchanged after the a la carte section.
  </action>
  <verify>
    <automated>cd /home/ahsan/projects/code-with-ahsan && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Social proof strip with brand names visible below hero. A la carte markdown wrapped in DaisyUI collapse accordion (collapsed by default). Usage rights simplified to a one-liner with "details on call" approach. Page builds without errors.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. Visual check: rates page shows CTA buttons on all 3 package cards
3. Visual check: Growth card has stronger visual presence (shadow, scale)
4. Visual check: Mid-page CTA visible between packages and a la carte
5. Visual check: Brand collaboration names visible below hero
6. Visual check: A la carte section is collapsed by default, expandable on click
7. All CTA links point to the correct calendar URL or email
</verification>

<success_criteria>
- Every package card has a clickable CTA button
- Growth card is visually differentiated as the recommended option
- Mid-page CTA provides a conversion point before the a la carte section
- A la carte pricing is hidden behind a collapsible section (not a text wall)
- Social proof strip shows past brand collaborations
- Availability note communicates limited slots
- Page builds and renders without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260410-cro-audit-and-optimize-mentorship-rates-/260410-SUMMARY.md`
</output>
