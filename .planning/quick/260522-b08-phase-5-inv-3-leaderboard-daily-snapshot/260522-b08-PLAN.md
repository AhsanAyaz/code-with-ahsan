---
quick_id: 260522-b08
type: quick-full
created: 2026-05-22
phase_target: 05-dashboard-leaderboard-offboarding-alumni
closes_invariant: INV-3 (leaderboard FAIL)
verified_against: .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-VERIFICATION.md
architectural_decision:
  locked: 2026-05-22
  decision: "Daily leaderboard snapshot (07:00 UTC) — NOT hourly per original DASH-07 spec"
  rationale: "Small ambassador cohort; daily cadence is sufficient. Hourly was over-engineered for current scale."
  chosen_by: user
autonomous: true
tdd: true
files_modified:
  - src/lib/ambassador/leaderboard.ts
  - src/lib/ambassador/leaderboard.test.ts
  - src/app/api/ambassador/dashboard/leaderboard/route.ts
  - src/app/api/ambassador/dashboard/leaderboard/route.test.ts
  - .github/workflows/ambassador-activity-checks.yml
  - scripts/ambassador-leaderboard-snapshot.ts
  - src/app/ambassadors/dashboard/LeaderboardPanel.tsx
  - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-02-SUMMARY.md
  - .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-03-SUMMARY.md
files_off_limits:
  - src/app/api/ambassador/dashboard/me/route.ts
  - src/app/api/ambassador/profile/route.ts
  - src/app/api/ambassador/members/[uid]/route.ts
  - src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx
  - src/app/api/ambassador/members/[uid]/offboard/route.ts
  - src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx

must_haves:
  truths:
    - "GET /api/ambassador/dashboard/leaderboard returns a boolean `graceActive` field computed server-side"
    - "Each top-3 entry carries a numeric `rank` field (1, 2, or 3 — standard competition ranking)"
    - "`ownRank` payload uses keys { referrals, events, reportsOnTime } — NOT { referralsRank, eventsRank, reportsRank }"
    - "Route reads `leaderboard_snapshots/{cohortId}` doc first; falls back to live `buildLeaderboardSnapshot` only if doc missing"
    - "Snapshot script runs daily at 07:00 UTC via GitHub Actions cron"
    - "LeaderboardPanel renders 'Updated N min/h/d ago' depending on age of snapshot (handles 24h+ staleness gracefully)"
    - "SUMMARYs 05-02 and 05-03 reflect daily cadence + snapshot-read truth (no longer claim hourly / no longer claim live-compute)"
  artifacts:
    - path: "src/lib/ambassador/leaderboard.ts"
      provides: "LeaderboardEntry with rank field; LeaderboardCategoryRanks with {referrals,events,reportsOnTime} keys"
      contains: "rank: number"
    - path: "src/lib/ambassador/leaderboard.test.ts"
      provides: "Updated assertions for new rank field + renamed ranks-shape keys"
      contains: "rank"
    - path: "src/app/api/ambassador/dashboard/leaderboard/route.ts"
      provides: "Snapshot-read with live fallback; graceActive boolean; ownRank with renamed keys"
      contains: "graceActive"
    - path: "src/app/api/ambassador/dashboard/leaderboard/route.test.ts"
      provides: "Coverage for snapshot-read, fallback path, graceActive computation, ownRank shape"
      exports: ["GET"]
    - path: ".github/workflows/ambassador-activity-checks.yml"
      provides: "Daily 07:00 UTC cron job for leaderboard-snapshot"
      contains: "0 7 * * *"
    - path: "scripts/ambassador-leaderboard-snapshot.ts"
      provides: "Daily leaderboard snapshot writer (header comment updated)"
      contains: "Daily leaderboard snapshot writer"
    - path: "src/app/ambassadors/dashboard/LeaderboardPanel.tsx"
      provides: "Human-readable Updated N min/h/d ago label"
      contains: "min ago"
  key_links:
    - from: "scripts/ambassador-leaderboard-snapshot.ts"
      to: "leaderboard_snapshots/{cohortId} Firestore doc"
      via: "FieldValue.serverTimestamp() + .set({...snapshot, updatedAt})"
      pattern: "LEADERBOARD_SNAPSHOTS_COLLECTION"
    - from: ".github/workflows/ambassador-activity-checks.yml"
      to: "scripts/ambassador-leaderboard-snapshot.ts"
      via: "npx tsx invocation in ambassador-leaderboard-snapshot job"
      pattern: "ambassador-leaderboard-snapshot.ts"
    - from: "src/app/api/ambassador/dashboard/leaderboard/route.ts"
      to: "leaderboard_snapshots/{cohortId}"
      via: "db.collection(LEADERBOARD_SNAPSHOTS_COLLECTION).doc(cohortId).get()"
      pattern: "LEADERBOARD_SNAPSHOTS_COLLECTION"
    - from: "src/app/ambassadors/dashboard/LeaderboardPanel.tsx"
      to: "GET /api/ambassador/dashboard/leaderboard"
      via: "authFetch — reads graceActive, top3[].rank, ownRank.{referrals,events,reportsOnTime}"
      pattern: "graceActive"
---

<objective>
Close Phase 5 INV-3 (leaderboard FAIL) by:
1. Fixing the UI↔API contract drift (add `rank` to entries, rename `ownRank` keys, emit `graceActive`).
2. Restoring the snapshot pipeline at DAILY cadence (07:00 UTC) per user-locked architectural decision (2026-05-22) — NOT hourly per the original DASH-07 spec.
3. Switching the route from in-memory 5-min cache → Firestore snapshot doc with live-compute fallback for cold-start resilience.
4. Polishing the UI staleness label to handle 24h+ gaps (min/h/d).
5. Correcting the false claims in SUMMARYs 05-02 / 05-03.

Purpose: Phase 5 is at 4-of-5 invariants verified; INV-3 is the last gate to FULLY-VERIFIED status. The 5-min in-memory cache that replaced the snapshot pipeline (commit 76ad6f7) is architecturally wrong for serverless (each cold start re-computes; cache doesn't survive deploys) and stale SUMMARYs misrepresent the wiring.

Output: Working leaderboard end-to-end (UI renders ranks + grace banner + own rank correctly, snapshot doc written daily, route reads doc with fallback, docs reflect reality).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-VERIFICATION.md

# Source files — read these once at the start; do NOT re-read during execution
@src/lib/ambassador/leaderboard.ts
@src/lib/ambassador/leaderboard.test.ts
@src/app/api/ambassador/dashboard/leaderboard/route.ts
@src/app/ambassadors/dashboard/LeaderboardPanel.tsx
@scripts/ambassador-leaderboard-snapshot.ts
@.github/workflows/ambassador-activity-checks.yml
@src/lib/ambassador/constants.ts
@src/app/api/ambassador/dashboard/me/route.test.ts

<interfaces>
<!-- Contracts the executor must honor. Extracted from the codebase above. -->

From `src/lib/ambassador/leaderboard.ts` (CURRENT — to be changed in Task 1):
```typescript
export interface LeaderboardEntry {
  uid: string;
  displayName: string;
  photoURL: string;
  count: number;
  // MISSING: rank — to be added in Task 1
}

export interface LeaderboardCategoryRanks {
  referralsRank: number;   // → rename to `referrals`
  eventsRank: number;      // → rename to `events`
  reportsRank: number;     // → rename to `reportsOnTime`
}

export interface LeaderboardWindow {
  referrals: LeaderboardEntry[];      // top-3 only
  events: LeaderboardEntry[];
  reportsOnTime: LeaderboardEntry[];
  ambassadorRanks: Record<string, LeaderboardCategoryRanks>;
}

export interface LeaderboardSnapshot {
  cohortId: string;
  updatedAt: string;       // ISO string from builder; cron overwrites with serverTimestamp
  graceEndDate: string;
  cumulative: LeaderboardWindow;
  thisMonth: LeaderboardWindow & { month: string };
}
```

From `src/app/ambassadors/dashboard/LeaderboardPanel.tsx` (CONSUMER — DO NOT change the shape it reads; the API must match this):
```typescript
type LeaderboardEntry = {
  uid: string;
  displayName: string;
  photoURL: string;
  count: number;
  rank: number;                     // line 11
};

type LeaderboardData = {
  view: "cumulative" | "this_month";
  cohortId: string | null;
  graceActive: boolean;             // line 17 — API MUST emit this
  graceEndDate: string | null;
  month: string | null;
  top3: {
    referrals: LeaderboardEntry[];
    events: LeaderboardEntry[];
    reportsOnTime: LeaderboardEntry[];
  };
  ownRank: {
    referrals: number | null;       // lines 26-28 — NOT *Rank
    events: number | null;
    reportsOnTime: number | null;
  };
  updatedAt: string | null;
};
```

From `src/lib/ambassador/constants.ts`:
```typescript
export const LEADERBOARD_SNAPSHOTS_COLLECTION = "leaderboard_snapshots" as const;
export const LEADERBOARD_GRACE_PERIOD_MS = 28 * 24 * 60 * 60 * 1000;  // 28 days
```

From `.github/workflows/ambassador-activity-checks.yml` (CURRENT — to be extended in Task 3):
- Schedules: `'0 8 * * *'` (report-flag), `'0 9 * * 1'` (discord-reconciliation)
- workflow_dispatch.inputs.job options: `report-flag`, `discord-reconciliation`, `both`
- Jobs: `ambassador-report-flag`, `ambassador-discord-reconciliation`
- Pattern to mirror: env block, `dry_run` handling, ubuntu-latest + node 22.x + npm ci + npx tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Library — add `rank` on LeaderboardEntry, rename LeaderboardCategoryRanks keys</name>
  <files>src/lib/ambassador/leaderboard.ts, src/lib/ambassador/leaderboard.test.ts</files>
  <behavior>
    Library-layer contract changes (RED-GREEN TDD — write tests first, watch them fail, then implement).

    Test cases to add to `src/lib/ambassador/leaderboard.test.ts`:
    - Test A (rank on entry): After buildWindowCategory-style synthesis, the top-1 entry has `rank: 1`, top-2 has `rank: 2`, top-3 has `rank: 3`. With a tie (e.g., counts [10,8,8,5]), entries [0]=rank1, [1]=rank2, [2]=rank2.
    - Test B (ranks-shape rename — type-level): the `LeaderboardCategoryRanks` interface has keys `referrals`, `events`, `reportsOnTime` (all `number`). NO `referralsRank` / `eventsRank` / `reportsRank` keys remain.
    - Test C (existing rankByCount + grace + month tests continue to pass — these are pure-function tests and unaffected; just verify they still pass).

    Because Tests A and B exercise `buildWindowCategory` (currently NOT exported) and the interface directly, the simplest approach is:
    - Export `buildWindowCategory` from leaderboard.ts so the test can call it. (It's a pure helper — exporting it is a small surface widening, no security implication; it does no I/O.)
    - Test A: call `buildWindowCategory([{uid:"a",displayName:"A",photoURL:"",count:10},{uid:"b",displayName:"B",photoURL:"",count:8},{uid:"c",displayName:"C",photoURL:"",count:8},{uid:"d",displayName:"D",photoURL:"",count:5}])`. Assert `result.top3[0].rank === 1`, `result.top3[1].rank === 2`, `result.top3[2].rank === 2`.
    - Test B: a TypeScript-level assertion via a typed const — declare `const r: LeaderboardCategoryRanks = { referrals: 1, events: 2, reportsOnTime: 3 }` then assert `r.referrals === 1`. If `referralsRank` were still in the interface as required, this would type-error at tsc; vitest doesn't run tsc but the next `npx tsc --noEmit` gate catches it.
  </behavior>
  <action>
    RED step — extend `src/lib/ambassador/leaderboard.test.ts`:
    - Add test A asserting `rank: number` on each top3 entry returned by `buildWindowCategory` (1,2,2 for the four-entry tie example).
    - Add test B asserting that `LeaderboardCategoryRanks` is `{ referrals: number; events: number; reportsOnTime: number }` (assignment with the new keys compiles and values round-trip).
    - Run `npm test -- --run src/lib/ambassador/leaderboard.test.ts` — confirm Tests A and B FAIL (Test A because `buildWindowCategory` is not exported AND entries lack `rank`; Test B because the type still has *Rank keys). Commit the failing tests: `test(05-b08): add failing rank + ranks-shape assertions to leaderboard tests`.

    GREEN step — modify `src/lib/ambassador/leaderboard.ts`:
    1. Extend `LeaderboardEntry` interface: add `rank: number` after `count: number`.
    2. Rename `LeaderboardCategoryRanks` keys: `referralsRank` → `referrals`, `eventsRank` → `events`, `reportsRank` → `reportsOnTime`.
    3. Export `buildWindowCategory` (add the `export` keyword to its declaration). Its callers inside `buildLeaderboardSnapshot` are unaffected.
    4. Inside `buildWindowCategory`, after computing `top3`, attach `rank: ranks.get(uid) ?? 0` to each top3 entry. The function signature changes return type to top3 entries having `rank`. Update its return-type inference (it already returns plain objects; just spread or add the rank field). Recommended implementation:
       ```
       const top3 = [...entries]
         .sort((a, b) => b.count - a.count)
         .filter((e) => e.count > 0)
         .slice(0, 3)
         .map((e) => ({ ...e, rank: ranks.get(e.uid) ?? 0 }));
       ```
       This preserves the existing zero-count filter (per Plan 02 SUMMARY decision).
    5. Update `cumulativeRanks` and `thisMonthRanks` population in `buildLeaderboardSnapshot` (lines 267-280) to use the NEW key names:
       ```
       cumulativeRanks[a.uid] = {
         referrals: cumRefBuilt.ranks.get(a.uid) ?? 0,
         events: cumEvtBuilt.ranks.get(a.uid) ?? 0,
         reportsOnTime: cumRptBuilt.ranks.get(a.uid) ?? 0,
       };
       // same shape for thisMonthRanks
       ```
    6. Run `npm test -- --run src/lib/ambassador/leaderboard.test.ts` — all tests must now PASS.
    7. Run `npx tsc --noEmit` — confirm no NEW type errors (pre-existing social-icons SVG errors are unrelated; ignore those). The route handler at `src/app/api/ambassador/dashboard/leaderboard/route.ts` will type-error against the renamed keys — that is EXPECTED and will be fixed in Task 2; do not patch the route in this task.
    8. Commit: `feat(05-b08): add rank on LeaderboardEntry + rename LeaderboardCategoryRanks keys`.

    DO NOT change leaderboard semantics: no on-time filter on `reportsOnTime` (already out of scope per planning brief); the reports category still counts all monthly_reports docs.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/ambassador/leaderboard.test.ts</automated>
  </verify>
  <done>
    Tests pass (existing 7 plus new rank + ranks-shape assertions). `LeaderboardEntry` has `rank: number`. `LeaderboardCategoryRanks` has `{ referrals, events, reportsOnTime }`. `buildWindowCategory` is exported and attaches `rank` to each top3 entry. `buildLeaderboardSnapshot` populates both rank maps with renamed keys. Two commits exist (RED, GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route — snapshot-read with live fallback, graceActive, renamed ownRank shape</name>
  <files>src/app/api/ambassador/dashboard/leaderboard/route.ts, src/app/api/ambassador/dashboard/leaderboard/route.test.ts</files>
  <behavior>
    Route-layer contract alignment (RED-GREEN TDD).

    Test cases for new `src/app/api/ambassador/dashboard/leaderboard/route.test.ts` (mirror the mocking pattern from `me/route.test.ts`):
    - Test A (auth gate — keep existing semantics): feature off → 404; no auth → 401; non-ambassador → 403.
    - Test B (snapshot present, grace active): mock `leaderboard_snapshots/{cohortId}` doc with `graceEndDate` 7 days in the future, `updatedAt` Timestamp. Assert response: `graceActive === true`, `graceEndDate` is ISO string, `top3.referrals[0].rank` is a number, `ownRank` has `{ referrals, events, reportsOnTime }` keys (NOT *Rank).
    - Test C (snapshot present, grace expired): graceEndDate in the past → `graceActive === false`.
    - Test D (snapshot missing — fallback path): doc `.exists === false` → route falls back to `buildLeaderboardSnapshot(cohortId)` (mock this fn) and returns the live snapshot with same shape (graceActive computed from the fallback's graceEndDate).
    - Test E (no cohort): `getCurrentCohortId` returns null → `{ snapshot: null }` shape (preserve existing behavior for UI compatibility — LeaderboardPanel handles this).
    - Test F (own rank not in map): ambassador not in `ambassadorRanks` → `ownRank = { referrals: null, events: null, reportsOnTime: null }` (NOT a thrown error, NOT all-undefined).
    - Test G (anti-pattern guard): response JSON does NOT contain the full `ambassadorRanks` map at any nesting level — only `top3` and `ownRank`.
  </behavior>
  <action>
    RED step — create `src/app/api/ambassador/dashboard/leaderboard/route.test.ts`:
    - Use the mocking pattern from `src/app/api/ambassador/dashboard/me/route.test.ts` (vi.mock for `@/lib/features`, `@/lib/auth`, `@/lib/permissions`, `@/lib/firebaseAdmin`).
    - Also vi.mock `@/lib/ambassador/currentCohort` (return `"cohort1"` by default; `null` for Test E) and `@/lib/ambassador/leaderboard` (mock `buildLeaderboardSnapshot` for Test D fallback only).
    - Build a fixture LeaderboardSnapshot conforming to the NEW shape from Task 1 (entries have `rank`, ranks use `{ referrals, events, reportsOnTime }`).
    - For Tests B/C/F/G: mock `db.collection("leaderboard_snapshots").doc(cohortId).get()` to return a snapshot with `.exists: true, .data: () => fixture` where `fixture.updatedAt = { toDate: () => new Date(...) }` (Firestore Timestamp shape) and `fixture.graceEndDate` is a Timestamp (also `toDate()`-able). Or use ISO strings if the route normalizes either input — the route MUST handle Timestamp→ISO normalization for both `updatedAt` and `graceEndDate` per the LeaderboardSnapshot type comment on lines 47-48.
    - Write all 7 tests; confirm RED. Commit: `test(05-b08): add failing route tests for snapshot-read + graceActive + ownRank shape`.

    GREEN step — rewrite `src/app/api/ambassador/dashboard/leaderboard/route.ts`:
    1. Drop the `cache` Map and `CACHE_TTL_MS` constant — daily Firestore doc is now the cache layer.
    2. Add import: `LEADERBOARD_SNAPSHOTS_COLLECTION` from `@/lib/ambassador/constants`.
    3. Add import: `db` from `@/lib/firebaseAdmin` (or keep existing — currently absent; the live-fallback path needs it).
    4. Preserve the three-gate (404 feature off → 401 unauth → 403 non-ambassador) at the top of GET.
    5. Preserve `view` param normalization (`"this_month"` else `"cumulative"`).
    6. Resolve `cohortId = await getCurrentCohortId()`; preserve the `{ snapshot: null }` early return when `cohortId == null`.
    7. Read snapshot doc:
       ```
       const docSnap = await db.collection(LEADERBOARD_SNAPSHOTS_COLLECTION).doc(cohortId).get();
       let data: LeaderboardSnapshot;
       if (docSnap.exists) {
         const raw = docSnap.data() as any;
         // Normalize Firestore Timestamps → ISO strings for updatedAt + graceEndDate
         const updatedAtIso = raw.updatedAt?.toDate
           ? raw.updatedAt.toDate().toISOString()
           : (typeof raw.updatedAt === "string" ? raw.updatedAt : new Date().toISOString());
         const graceEndIso = raw.graceEndDate?.toDate
           ? raw.graceEndDate.toDate().toISOString()
           : raw.graceEndDate;
         data = { ...raw, updatedAt: updatedAtIso, graceEndDate: graceEndIso } as LeaderboardSnapshot;
       } else {
         // Fallback: live compute (cohort just started, before first cron tick)
         data = await buildLeaderboardSnapshot(cohortId);
       }
       ```
    8. Compute `graceActive` server-side: `const graceActive = data.graceEndDate ? Date.now() < Date.parse(data.graceEndDate) : false;`
    9. Select window: `const window = view === "this_month" ? data.thisMonth : data.cumulative;`
    10. Extract ownRank with the NEW shape (renamed from Task 1):
        ```
        const ownRanks = window.ambassadorRanks?.[ctx.uid];
        const ownRank = ownRanks
          ? { referrals: ownRanks.referrals, events: ownRanks.events, reportsOnTime: ownRanks.reportsOnTime }
          : { referrals: null, events: null, reportsOnTime: null };
        ```
    11. Return the response WITHOUT spreading `ambassadorRanks` (anti-pattern guard):
        ```
        return NextResponse.json({
          view,
          cohortId,
          graceActive,
          graceEndDate: data.graceEndDate ?? null,
          month: view === "this_month" ? data.thisMonth.month : null,
          top3: {
            referrals: window.referrals,    // each entry now carries `rank` from Task 1
            events: window.events,
            reportsOnTime: window.reportsOnTime,
          },
          ownRank,
          updatedAt: data.updatedAt,
        });
        ```
    12. Run `npm test -- --run src/app/api/ambassador/dashboard/leaderboard/route.test.ts` — all 7 tests pass.
    13. Run `npx tsc --noEmit` — no new errors.
    14. Commit: `feat(05-b08): leaderboard route reads daily snapshot doc + emits graceActive + renames ownRank shape`.

    Preserve: 404 (feature off), 401 (unauth), 403 (non-ambassador), `view=cumulative|this_month` query, anti-pattern guard (no full ambassadorRanks leak).
  </action>
  <verify>
    <automated>npm test -- --run src/app/api/ambassador/dashboard/leaderboard/route.test.ts</automated>
  </verify>
  <done>
    7 new route tests pass. Route reads `leaderboard_snapshots/{cohortId}` doc first, falls back to `buildLeaderboardSnapshot` if missing. Response includes `graceActive: boolean`, top3 entries carry `rank`, `ownRank` uses `{ referrals, events, reportsOnTime }` shape (null when uid absent from map). 5-min in-memory cache removed. Two commits exist (RED, GREEN). `npx tsc --noEmit` reports no NEW errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cron — daily 07:00 UTC GitHub Actions wiring + script header update</name>
  <files>.github/workflows/ambassador-activity-checks.yml, scripts/ambassador-leaderboard-snapshot.ts</files>
  <action>
    Wire the orphaned snapshot script to a daily cron. No TDD here (YAML + comment edits).

    Step A — edit `.github/workflows/ambassador-activity-checks.yml`:

    1. Under `on.schedule`, add a third entry BEFORE the existing two:
       ```
       # Daily at 07:00 UTC — leaderboard snapshot writer (DASH-07, daily cadence per architectural decision 2026-05-22)
       - cron: '0 7 * * *'
       ```
       Result: three schedules — `0 7 * * *` (leaderboard), `0 8 * * *` (report-flag), `0 9 * * 1` (discord).

    2. Under `workflow_dispatch.inputs.job.options`, ADD `leaderboard-snapshot` as a new choice. Keep `both` semantics unchanged (still means report-flag + discord-reconciliation — leaderboard-snapshot is a standalone manual choice). New options list:
       ```
       options:
         - report-flag
         - discord-reconciliation
         - leaderboard-snapshot
         - both
       ```

    3. Add a NEW job `ambassador-leaderboard-snapshot` after `ambassador-discord-reconciliation`. Mirror the structure of `ambassador-report-flag` (line 30-56), with these differences:
       - Name: `Daily leaderboard snapshot (DASH-07)`
       - `if:` trigger condition:
         ```
         if: |
           (github.event_name == 'schedule' && github.event.schedule == '0 7 * * *') ||
           (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'leaderboard-snapshot')
         ```
         (Note: do NOT include `|| ... == 'both'` — `both` retains its current report-flag+discord semantics.)
       - Env block: ONLY `NODE_ENV`, `FIREBASE_SERVICE_ACCOUNT_KEY`, `NEXT_PUBLIC_FIREBASE_PROJECT_ID` (no Discord secrets — script only writes Firestore, per 05-02 SUMMARY decision "leaderboard job uses only Firebase secrets").
       - Run step:
         ```
         run: |
           if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
             npx tsx scripts/ambassador-leaderboard-snapshot.ts --dry-run
           else
             npx tsx scripts/ambassador-leaderboard-snapshot.ts
           fi
         ```

    Step B — edit `scripts/ambassador-leaderboard-snapshot.ts` header comment ONLY (no logic changes — script is already correct):

    1. Line 3 currently reads `* Phase 5 (DASH-07): Hourly leaderboard snapshot writer.` — change to:
       `* Phase 5 (DASH-07): Daily leaderboard snapshot writer.`
    2. Line 6 currently reads `* (cron '0 * * * *' or workflow_dispatch).` — change to:
       `* (cron '0 7 * * *' daily or workflow_dispatch).`
    3. Leave all other code untouched (the `--dry-run` flag, `getCurrentCohortId`, `buildLeaderboardSnapshot`, write to `LEADERBOARD_SNAPSHOTS_COLLECTION` all remain identical).

    Step C — verify the YAML parses:
    - Run `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ambassador-activity-checks.yml'))" 2>&1` (or `npx js-yaml` if python yaml unavailable).
    - Verify with grep that the cron line and job name are present.

    Commit: `feat(05-b08): wire daily leaderboard snapshot cron at 07:00 UTC + update script header`.
  </action>
  <verify>
    <automated>grep -q "cron: '0 7 \* \* \*'" .github/workflows/ambassador-activity-checks.yml && grep -q "ambassador-leaderboard-snapshot:" .github/workflows/ambassador-activity-checks.yml && grep -q "Daily leaderboard snapshot writer" scripts/ambassador-leaderboard-snapshot.ts && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ambassador-activity-checks.yml'))"</automated>
  </verify>
  <done>
    Workflow file has `cron: '0 7 * * *'` schedule, `leaderboard-snapshot` workflow_dispatch option, and a new `ambassador-leaderboard-snapshot` job that runs the script (with `--dry-run` honoring the existing input). Script header reads "Daily leaderboard snapshot writer" and "'0 7 * * *' daily". YAML parses cleanly. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 4: UI staleness label + SUMMARY corrections</name>
  <files>src/app/ambassadors/dashboard/LeaderboardPanel.tsx, .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-02-SUMMARY.md, .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-03-SUMMARY.md</files>
  <action>
    Final polish + docs sync. No code TDD (UI text helper is trivial; SUMMARY edits are prose).

    Step A — edit `src/app/ambassadors/dashboard/LeaderboardPanel.tsx`:

    Currently lines 171-173 + 277-279:
    ```
    const minutesAgo = updatedAt
      ? Math.max(0, Math.round((Date.now() - Date.parse(updatedAt)) / 60000))
      : 0;
    ...
    <p aria-live="polite" className="text-sm text-base-content/60">
      Updated {minutesAgo} minutes ago
    </p>
    ```

    Replace with an in-file helper + new render:
    ```typescript
    function formatUpdatedAgo(updatedAt: string | null): string {
      if (!updatedAt) return "Updated just now";
      const parsed = Date.parse(updatedAt);
      if (Number.isNaN(parsed)) return "Updated just now";
      const diffMs = Math.max(0, Date.now() - parsed);
      const mins = Math.round(diffMs / 60_000);
      if (mins < 1) return "Updated just now";
      if (mins < 60) return `Updated ${mins} min ago`;
      const hours = Math.round(mins / 60);
      if (hours < 24) return `Updated ${hours} h ago`;
      const days = Math.round(hours / 24);
      return `Updated ${days} d ago`;
    }
    ```
    Place this helper above the `LeaderboardPanel` export (module scope, not inside the component, to avoid re-allocation each render). Replace the `minutesAgo` derivation in the component body with `const updatedLabel = formatUpdatedAgo(updatedAt);`. Replace the `<p>` body with `{updatedLabel}`.

    No tests required for this helper (trivial branching, behavioral check is the human-verification step from VERIFICATION.md). If you want a cheap test, add `src/app/ambassadors/dashboard/__tests__/formatUpdatedAgo.test.ts` covering each branch — but this is OPTIONAL; skip if it slows the task.

    Step B — edit `.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-02-SUMMARY.md`:

    Find any text claiming hourly cron / `'0 * * * *'`. Specifically:
    - Line ~97 ("Added third schedule entry: `cron: '0 * * * *'` (hourly)") — REPLACE with: `Added third schedule entry: \`cron: '0 7 * * *'\` (daily 07:00 UTC — original hourly cadence orphaned by commit 76ad6f7 (commit was actually a refactor TO live cache, not from hourly; SUMMARY was historically inaccurate). Restored at daily cadence per architectural decision 2026-05-22 ("we don't have many ambassadors at the moment") — closed by quick 260522-b08.`

    Add at the bottom of the file (before `## Self-Check: PASSED`) a new block:
    ```markdown

    ## Post-Hoc Correction (2026-05-22, quick 260522-b08)

    This SUMMARY originally claimed an hourly cron `'0 * * * *'` was wired. That was inaccurate even at the time of writing — commit `76ad6f7` (post-Plan-02) refactored the route to live computation with a 5-min in-memory cache, orphaning `scripts/ambassador-leaderboard-snapshot.ts`. Phase 5 verification (`05-VERIFICATION.md`, INV-3 FAIL) caught this. Quick 260522-b08 closed the gap with a **daily** snapshot at `'0 7 * * *'` (07:00 UTC, before the 08:00 report-flag job) — a deliberate cadence change (hourly → daily) given current ambassador count. The script's logic remains unchanged; only the GitHub Actions wiring + the script's header comment were updated to reflect the daily cadence.
    ```

    Step C — edit `.planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-03-SUMMARY.md`:

    The line ~77 claims `Reads single `leaderboard_snapshots/{cohortId}` doc (written hourly by Plan 02 cron)`. That claim was correct at write-time but was invalidated by commit 76ad6f7. Add the same post-hoc correction block at the bottom (before `## Self-Check: PASSED`):
    ```markdown

    ## Post-Hoc Correction (2026-05-22, quick 260522-b08)

    This SUMMARY described the route as reading `leaderboard_snapshots/{cohortId}` (the as-designed contract). Commit `76ad6f7` (subsequent to Plan 03) refactored the route to live computation + a 5-min in-memory cache, orphaning the snapshot pipeline and introducing UI↔API contract drift (UI consumed `graceActive`, `entry.rank`, `ownRank.{referrals,events,reportsOnTime}` — API emitted none). Phase 5 verification flagged this as INV-3 FAIL. Quick 260522-b08 restored the snapshot read with a **daily** cron (07:00 UTC) and added a live-compute fallback for cold-start days (snapshot doc missing). The route now also emits `graceActive` server-side and uses the renamed `LeaderboardCategoryRanks` shape.
    ```

    Run `npx tsc --noEmit` to catch any new type errors from the UI edit. Run `npm test -- --run src/app/ambassadors/dashboard` if any UI tests exist (probably none — skip cleanly if no test files match).

    Commit: `feat(05-b08): humanize Updated N ago label + correct phase 5 SUMMARYs to daily cadence`.
  </action>
  <verify>
    <automated>grep -q "Updated.*min ago" src/app/ambassadors/dashboard/LeaderboardPanel.tsx && grep -q "Post-Hoc Correction (2026-05-22, quick 260522-b08)" .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-02-SUMMARY.md && grep -q "Post-Hoc Correction (2026-05-22, quick 260522-b08)" .planning/phases/05-dashboard-leaderboard-offboarding-alumni/05-03-SUMMARY.md && npx tsc --noEmit 2>&1 | grep -v "social-icons" | (! grep -E "(error TS|Error:)")</automated>
  </verify>
  <done>
    LeaderboardPanel.tsx renders `Updated N min ago` / `Updated N h ago` / `Updated N d ago` / `Updated just now` based on snapshot age (helper at module scope). SUMMARY 05-02 reflects daily cadence + post-hoc correction block. SUMMARY 05-03 reflects snapshot-read truth + post-hoc correction block. `npx tsc --noEmit` reports no NEW errors. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 5: Full-suite verification gate</name>
  <files>(read-only verification)</files>
  <action>
    No file edits. Run the full check matrix to confirm INV-3 is closed AND nothing else regressed.

    1. Run `npm test -- --run src/lib/ambassador` — expect ambassador library tests pass (was 35/35 after quick 260522-a2f; new tests from Task 1 should push this higher).
    2. Run `npm test -- --run src/app/api/ambassador` — expect all ambassador API route tests pass (including the new leaderboard route tests from Task 2 and existing /me, /profile, /members tests untouched by this quick).
    3. Run `npx tsc --noEmit` — capture output. Filter known pre-existing social-icons SVG module noise. Confirm zero NEW errors.
    4. Manual grep audit (no new edits — just confirm):
       - `grep -n "referralsRank\|eventsRank\|reportsRank" src/` (exclude test fixtures if any from prior phases reference these — should be empty in src/app, src/lib).
       - `grep -n "rank:" src/lib/ambassador/leaderboard.ts` — confirm `rank` is added to top3 entries.
       - `grep -n "graceActive" src/app/api/ambassador/dashboard/leaderboard/route.ts` — confirm route emits it.
       - `grep -n "leaderboard_snapshots\|LEADERBOARD_SNAPSHOTS_COLLECTION" .github/workflows/ambassador-activity-checks.yml scripts/ambassador-leaderboard-snapshot.ts src/app/api/ambassador/dashboard/leaderboard/route.ts` — confirm pipeline is wired end-to-end.
       - `grep -n "5 \* 60 \* 1000\|CACHE_TTL_MS\|new Map" src/app/api/ambassador/dashboard/leaderboard/route.ts` — confirm the in-memory cache was REMOVED.
    5. Off-limits audit — confirm no edits to the INV-1/2/4/5 files:
       ```
       git diff --name-only main HEAD
       ```
       Expect ONLY the 9 files in `files_modified` frontmatter. NO appearance of:
       - `src/app/api/ambassador/dashboard/me/route.ts`
       - `src/app/api/ambassador/profile/route.ts`
       - `src/app/api/ambassador/members/[uid]/route.ts`
       - `src/app/admin/ambassadors/members/[uid]/MemberDetailClient.tsx`
       - `src/app/api/ambassador/members/[uid]/offboard/route.ts`
       - `src/app/admin/ambassadors/members/[uid]/OffboardConfirmModal.tsx`

    If any of steps 1-5 fail, fix in-task before declaring done. If a regression is found that requires editing an off-limits file, STOP and surface the conflict — do not silently expand scope.

    No commit (read-only gate). If everything passes, the next step is `/gsd:quick verify 260522-b08` or analogous verification command.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/ambassador && npm test -- --run src/app/api/ambassador/dashboard/leaderboard && npx tsc --noEmit 2>&1 | grep -v "social-icons" | (! grep -E "error TS")</automated>
  </verify>
  <done>
    All ambassador library tests pass. All ambassador dashboard API tests pass. `npx tsc --noEmit` reports no NEW type errors. Grep audits confirm: no `*Rank` keys remain in app/lib source; `rank` is on top3 entries; `graceActive` is emitted by the route; in-memory cache removed; cron wiring present in workflow YAML + script; no off-limits files touched in this branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/ambassador/dashboard/leaderboard | Authenticated ambassador requests rank data; server must not leak full ambassadorRanks map |
| GitHub Actions runner → Firestore | Admin SDK writes `leaderboard_snapshots/{cohortId}` doc; Firestore rules deny client read/write (already in place, `firestore.rules:263-265`) |
| Cron script ↔ Firebase Admin | Service account key from GH secret; no Discord API calls in this job (Firebase-only env) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b08-01 | I (Information disclosure) | leaderboard route response | mitigate | Anti-pattern guard preserved from Plan 03 — never spread `ambassadorRanks` map; extract only `ownRank` for `ctx.uid` (Task 2 step 10-11) |
| T-b08-02 | T (Tampering) | leaderboard_snapshots collection | mitigate | `firestore.rules:263-265` already denies all client read/write (verified by Plan 03); only Admin SDK (cron + route fallback) can read/write |
| T-b08-03 | S (Spoofing) | snapshot route auth | mitigate | Three-gate preserved: feature flag → verifyAuth → hasRoleClaim("ambassador"); same as existing route (Task 2 step 4) |
| T-b08-04 | D (DoS) | live fallback in route | accept | Fallback path calls `buildLeaderboardSnapshot` (live compute) only when snapshot doc missing — bounded to cohort startup window (≤24h before first cron tick); cost is identical to the previous in-memory-cache path's cold start. Once daily cron is running, the fast path (Firestore doc read) dominates |
| T-b08-05 | I | GitHub Actions cron logs | accept | Snapshot script logs ambassador counts + top3 lengths (no PII beyond what is already public in `public_ambassadors`); existing pattern from report-flag + discord-reconciliation jobs |
| T-b08-06 | T | UI staleness label | accept | `Updated N ago` is informational; client-side computation from server-provided `updatedAt`; no auth surface |
</threat_model>

<verification>
Final checks before declaring this quick task complete (run from project root):

1. **Library tests:** `npm test -- --run src/lib/ambassador/leaderboard.test.ts` — all pass.
2. **Route tests:** `npm test -- --run src/app/api/ambassador/dashboard/leaderboard/route.test.ts` — all 7 new tests pass.
3. **Full ambassador suite:** `npm test -- --run src/lib/ambassador && npm test -- --run src/app/api/ambassador` — no regressions vs the 35/35 baseline from quick 260522-a2f.
4. **TypeScript:** `npx tsc --noEmit` — zero NEW errors (pre-existing social-icons SVG noise is ignored).
5. **YAML parse:** `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ambassador-activity-checks.yml'))"` — no exceptions.
6. **Off-limits audit:** `git diff --name-only main HEAD` — modifies ONLY the 9 files in `files_modified`. None of the INV-1/2/4/5 files appear.
7. **Manual grep audit:**
   - `grep -rn "referralsRank\|eventsRank\|reportsRank" src/app src/lib` → empty (renamed everywhere).
   - `grep -n "rank" src/lib/ambassador/leaderboard.ts | grep -c "rank: "` → ≥2 occurrences (interface field + entry map).
   - `grep -n "graceActive" src/app/api/ambassador/dashboard/leaderboard/route.ts` → ≥1 occurrence (server computes + emits).
   - `grep -n "CACHE_TTL_MS\|new Map" src/app/api/ambassador/dashboard/leaderboard/route.ts` → empty (in-memory cache removed).
8. **Workflow shape:** `grep -A 30 "ambassador-leaderboard-snapshot:" .github/workflows/ambassador-activity-checks.yml` shows env block has FIREBASE_SERVICE_ACCOUNT_KEY + NEXT_PUBLIC_FIREBASE_PROJECT_ID + NODE_ENV (no DISCORD_* secrets).
</verification>

<success_criteria>
Quick 260522-b08 is complete when:

- INV-3 from `05-VERIFICATION.md` is fully addressable by re-verifying: every "missing" bullet under invariant 1 has been delivered (graceActive emitted, rank on entries, ownRank keys renamed, cron + snapshot pipeline restored at daily cadence).
- `curl GET /api/ambassador/dashboard/leaderboard` (when authed) returns the shape: `{ view, cohortId, graceActive: boolean, graceEndDate, month, top3: { referrals: [{...,rank:number}], events: [...], reportsOnTime: [...] }, ownRank: { referrals: number|null, events: number|null, reportsOnTime: number|null }, updatedAt }`.
- LeaderboardPanel renders ranks (`#1 Alice (12)`), the grace banner displays when `graceActive=true`, and the "Updated N ago" label adapts to min/h/d.
- GitHub Actions YAML has `cron: '0 7 * * *'` and an `ambassador-leaderboard-snapshot` job; running `gh workflow run ambassador-activity-checks.yml -f job=leaderboard-snapshot -f dry_run=true` would dispatch successfully (NOT executed by this quick task — left to user / Phase 5 final verification).
- `scripts/ambassador-leaderboard-snapshot.ts` header reads "Daily leaderboard snapshot writer" (script body unchanged).
- SUMMARYs 05-02 and 05-03 contain a "Post-Hoc Correction" block acknowledging the historical wiring drift and pointing to this quick.
- `npx tsc --noEmit` clean (no NEW errors).
- INV-1/2/4 already-closed files NOT touched. INV-5 files NOT touched. Phase 1 invariants (no legacy `role` field reintroduction) preserved.
- Five commits total (RED + GREEN for Task 1, RED + GREEN for Task 2, single commit each for Tasks 3 + 4; Task 5 is a verification gate with no commit). Tally: 6 commits if both TDD pairs are followed strictly.
</success_criteria>

<output>
After completion, create `.planning/quick/260522-b08-phase-5-inv-3-leaderboard-daily-snapshot/260522-b08-SUMMARY.md` following the standard quick-task summary template, including:
- All 5 task outcomes
- Commit list with SHAs
- Test results (pass counts before/after)
- Architectural decision record entry for the hourly → daily cadence change
- Confirmation that Phase 5 INV-3 is now ready for re-verification by the next `/gsd-verify-phase 05` run

Then update `.planning/STATE.md`:
- Move INV-3 from "Next Moves" item 1 to "Quick Tasks Completed" table
- Update Phase 5 status from `verifying (INV-3 open)` to `verifying (all invariants closed — awaiting re-verification)` or similar.
</output>
